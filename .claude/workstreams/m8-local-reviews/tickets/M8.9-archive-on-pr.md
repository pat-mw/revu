# M8.9 — Archive when a PR appears

| field | value |
| --- | --- |
| **State** | In Review |
| **Surface** | revu:revud, revu:app |
| **Kind** | Feature |
| **Depends** | M8.4, M8.5, M8.6, M8.7 |
| **Blocks** | M8.11 |
| **Branch** | `m8.9/archive-on-pr` |
| **PR** | [#82](https://github.com/pat-mw/revu/pull/82) (base `m8.11/conformance-e2e-docs`) |
| **Assignee** | session 2026-09-02 (M8.9, orchestrator) |

## Goal

Implement **D1** (`LOCAL_REVIEWS.md §3`): when a pull request appears for the same repo + head ref + base
ref that a local review covers, that local review goes **read-only and links to the PR**, and nothing is ever
copied across. D1 is the decision that makes the whole feature safe — a publish path would reintroduce
exactly the leak the feature exists to prevent — so this ticket is **detection + read-only archival + a
link, and nothing else**. Three properties carry the weight: detection compares `head.repo.full_name` as
well as the two ref names, because a fork can have an identically named branch (`§8` case 20); archiving is
**non-destructive** — threads, drafts, submitted reviews and history all stay readable, and the only
`DELETE` in this store remains `deleteDraft` (`§8` case 22, and `hard-constraints.md`'s "drafts survive
everything"); and a PR **closed without merging does not un-archive**, so `archived_pr` is sticky once set.
The practical obstacle this ticket has to solve first is `§8` case 21: direct mode's `listPulls` is a 501
today, and M8.5's local list is a *local* listing, not the **GitHub** pull listing detection needs — so the
detection input does not exist yet and has to be built.

## Units

Default execution order. **M8.9.1, M8.9.2 and M8.9.3 are mutually independent** and touch disjoint files —
a pure predicate module, the store's local slice, and a new GitHub listing seam — so all three can run in
parallel from the start. **M8.9.4 needs all three.** **M8.9.5 needs M8.9.2** for the stored state and
M8.9.4 for it to ever be set. **M8.9.6 needs M8.9.5** — it renders whatever shape M8.9.5 puts on the wire,
so do not start it until that shape is decided (see Open question 4).

**M8.9.6 is also why this ticket depends on M8.6 and M8.7, not on M8.5 alone.** It edits `inbox.tsx`,
`pr-layout.tsx` and `review-bar.tsx` — three files M8.6 and M8.7 substantially rewrite (the local inbox
section and row variant; the mode threading, header identity, 404 replacement and review-bar variant) — and
its superseded chrome sits *inside* surfaces those tickets create: a superseded badge belongs in M8.6.4's
local row, and suppressing the composer means suppressing it beside M8.7.5's already-gated lock popover.
M8.6's open question 3 says "coordinate with M8.9" about exactly this, and without the dependency the graph
permitted M8.9.6 to land before or beside the tickets whose output it edits. Units M8.9.1–M8.9.5 and M8.9.7
are daemon-side and need only M8.5. **M8.9.7 needs M8.9.2 and M8.9.4** — it pins what `syncPull` does to an
already-archived review, so the archived state has to exist first.

**Test-first ordering inside the units.** Three assertions land before the code they constrain, and the
ticket is not startable in any other order. M8.9.1's module-purity scan is written against a
`local-archive.ts` that does not exist yet — it goes red on the missing import, then stays green through
M8.9.3's seam and M8.9.4's store port, which is what makes the module's purity structural rather than a
snapshot of one afternoon. M8.9.2's untouched-rows case is written before `markLocalReviewArchived` exists.
M8.9.3's "no listing source ⇒ zero requests" case is written before the seam is wired, because a seam built
first gets built with a default source and that case can then never have been red. M8.9.4's addition to
M8.4.6's banned-specifier list is the one guard that lands **green** — nothing imports the detector yet — so
its negative control is not optional: it is the only evidence the new specifier is actually being scanned.

### M8.9.1 — The supersede predicate, pure and ref-shape-aware
**Do:** A standalone, I/O-free module exporting one predicate: given a local review's
`{ repo, baseRef, headRef }` and a `PullSummary`, answer whether that PR supersedes that review. Three
comparisons, all required: `pull.head.repo.full_name` equals the local review's repo identity (`§8` case 20
— a fork's `feature/x` is not this workspace's `feature/x`), `pull.head.ref` equals the local head ref, and
`pull.base.ref` equals the local base ref. Every comparison runs over **normalized** ref names, because the
two sides spell refs differently: M8.3 normalizes local refs to `refs/heads/…`, a base may legitimately be
a remote-tracking ref like `origin/main` (`§8` case 3), and GitHub reports bare `feature/x` / `main` on
`GhRef.ref` (`shared/src/api/types.ts:60-65`). Normalize both sides to the bare short name before
comparing. **Never compare SHAs** — under D8 a local review's merge base is computed against the live base
tip while GitHub's `pull.base.sha` only refreshes on a `synchronize` event, so the two legitimately
disagree about the same branch pair. Reject empty-string repo identities explicitly: the summary mapper
turns an absent `head.repo` (a deleted fork) into `full_name: ''` (`github-client.ts:862-875`), and an
empty local repo identity would otherwise match it. **Write `local-archive.test.ts` first** and observe it
red on the missing module — it is also the file M8.9.3, M8.9.4 and M8.9.7 extend, so it exists before any of
them.
**Files:** `packages/revud/src/direct/local-archive.ts` (new), `packages/revud/src/direct/local-archive.test.ts` (new)
**Check:** `bun test packages/revud/src/direct/local-archive.test.ts`, table-driven, one row per case, each
asserting exactly one comparison is load-bearing: same repo + same head + same base → supersedes; **fork
with the identical head ref** (`head.repo.full_name` differs, `head.ref` identical) → does **not**;
different base ref, same head → does **not**; local base `origin/main` vs PR base `main` → **does**; local
head `refs/heads/feature/x` vs PR head `feature/x` → **does**; `head.repo.full_name: ''` against a local
repo identity of `''` → does **not** (both blank never matches).

Plus the **module-purity assertion**, in the same file and built so it cannot go vacuous: read
`local-archive.ts`'s own source, extract its import specifiers, and assert the set contains neither
`./github-client` nor `./store` at any depth. The listing seam M8.9.3 adds and the store port M8.9.4 needs
are **locally declared interfaces**, never imports, so this assertion stays green through both units — that
is what makes the purity structural rather than a snapshot of this unit. Two self-assertions keep it honest,
in the shape M8.4.6 establishes: the source read is asserted non-empty (a renamed or moved file would
otherwise pass by scanning nothing), and the specifier extractor is run over an **inline fixture source**
importing `./github-client` and asserted to report that specifier, so a regex that stops matching fails here
instead of silently permitting everything.
**Negative control, recorded:** add `import type { GithubClient } from './github-client'` to
`local-archive.ts`, observe a **red** run naming the banned specifier, revert, observe green. Both
observations go in the `## Log`.

### M8.9.2 — Sticky `archived_pr` on the store's local slice
**Do:** Add the archive read/write to M8.2's local-review store slice over the existing
`archived_pr INTEGER` column (`§4.2`): a `markLocalReviewArchived(localId, prNumber)` that is **write-once**
(`… SET archived_pr = ? WHERE id = ? AND archived_pr IS NULL`, following the first-write-wins shape
`recordPrAuthor` already uses at `store.ts:561`) and a read that surfaces it on the local review row. There
is **no un-archive method** — that is `§8` case 22 stated as an absent capability rather than a guarded one.
Archiving writes exactly one column: no row in `local_threads`, `local_drafts`, `local_reviews_submitted`
or `local_viewed` is touched, and `immutables` / `blobs` are untouched. Write failures surface as
`StoreWriteError` like every other durable write (`store.ts:91-100`). **The store cases are written before
`markLocalReviewArchived` exists** and observed red.
**Files:** `packages/revud/src/direct/store.ts`, `packages/revud/src/direct/store.test.ts`
**Check:** `bun test packages/revud/src/direct/store.test.ts`. New cases: marking twice with two different
PR numbers leaves the **first** number standing; marking a review that already carries drafts, threads and a
submitted review leaves `SELECT count(*)` on `local_drafts`, `local_threads` and `local_reviews_submitted`
**unchanged** *and* the draft read back byte-identical to the seeded one
(`JSON.stringify(after) === JSON.stringify(seeded)`, so a silently rewritten draft fails as loudly as a
deleted one — "drafts survive everything" is the product); `expect(STORE_VERSION).toBe(4)` — M8.2's value,
pinned as an assertion rather than left to review, because the column already exists and this ticket adds no
migration step (`store.ts:65`); and a durable write failure surfaces as `StoreWriteError`, driven the way
`store.test.ts` already drives its write-failure cases.

The "no second `DELETE`" property becomes a **durable allowlist** rather than a `grep`: in the same file,
read `store.ts`'s source, extract every `DELETE FROM <table>` occurrence, and assert the sorted table list
equals an explicit literal — `drafts` today, plus whatever M8.2/M8.10 have already added by the time this
lands. A `DELETE` introduced later then fails in-gate until someone names it, which is the only form of
"archiving added no delete" that survives past this branch. Same two anti-vacuity self-assertions as M8.9.1:
the source read is asserted non-empty, and the extractor is proven on an inline fixture containing a
`DELETE FROM x`.
**Negative control, recorded:** make `markLocalReviewArchived` also call `deleteDraft`, observe **both** the
untouched-rows case and the allowlist go red, revert, observe green. Log both.

### M8.9.3 — A GitHub pull listing direct mode does not have
**Do:** `§8` case 21 in one unit. `createGithubClient` already returns `GithubClient & PullListClient`
(`github-client.ts:924`), but `DirectContext.github` is typed as the narrower `GithubClient`
(`context.ts:25-34`), and `mainDirect` builds no poll loop at all (`index.ts:172-181`) — so no direct-mode
code path can list pull requests today. Introduce a **narrow one-method read seam** for archive detection,
shaped exactly like `PullListSource` (`direct-api.ts:191-193`): the detector depends on the read, never on
a client or a loop lifecycle. Decide and document which GitHub call backs it (see Open question 1) and wire
it in both mains — direct from `context`, broker from the already-built `pollClient` (`index.ts:284-292`)
rather than a second client. The seam must be **optional and absent-tolerant**: a workspace with no origin
and no token has no PR listing and must keep working, which is the milestone's headline exit criterion.
Declare the seam **locally in `local-archive.ts`** — a one-method interface, not an import of
`PullListClient` — so M8.9.1's purity assertion stays green. **Write the absent-source case first**, red,
before the seam is wired: a seam built first gets a default source and that case can never have been red.
**Files:** `packages/revud/src/direct/local-archive.ts`, `packages/revud/src/direct/context.ts`,
`packages/revud/src/index.ts`, `packages/revud/src/direct/local-archive.test.ts`
**Check:** `bun test packages/revud/src/direct/local-archive.test.ts` — a fake source drives the detector
and a `throwingGithubClient()` (`direct/github-write-stubs.ts:50`) proves the detector never reaches a
GitHub client of its own. A detector constructed with **no** listing source resolves to "nothing archived"
and issues zero requests — assert with a `fetch` double that throws on any call, and carry its **positive
control in the same test**: a sibling case with the same double installed makes one deliberate
`fetch('https://api.github.com/…')` and asserts it throws, so "zero requests" cannot pass because the double
was never installed or was torn down early. M8.9.1's purity assertion re-runs over the post-seam
`local-archive.ts` and must still be green — that is the durable proof the seam is a local interface rather
than a client import. `bun test packages/revud/src/direct/context.test.ts` green **unamended** under the
default (widening a type must not change boot behavior); it runs on every gate, so a boot behavior that
shifts with the wider type fails there rather than in review.

### M8.9.4 — The sweep: detect → archive → refuse writes
**Do:** Join M8.9.1–M8.9.3. For each non-archived local review in the repo, ask the listing seam, run the
predicate, and on a match call `markLocalReviewArchived`. Then make the archived review **read-only at the
daemon**: all four local write verbs refuse before mutating anything. `submitReview` answers the contract's
200-level `{ status: 'forbidden', reason }` (`shared/src/api/types.ts:495-498`); `replyToThread`,
`resolveThread` and `addReaction` throw a typed `ApiError('forbidden', …)`, which
`envelopeForError` already serializes to its own status (`direct-router.ts:192-195`). The `reason` string
names the PR and says the review is archived — it must not read like the self-approval refusal (see
Landmines). **Reads stay fully open**: snapshot, threads, drafts, viewed, reconcile and blobs all continue
to serve. The archived check the write sink consults is a **plain store read of `archived_pr`** — the
detector, which holds a GitHub listing seam, must not enter the write sink's import graph (D7).
Decide and document what triggers the sweep (Open question 2), and assert the chosen trigger — a sweep
nothing calls is the failure mode the Landmines already name for direct mode.
**Files:** `packages/revud/src/direct/local-archive.ts`, the local write sink module M8.4 created
(`packages/revud/src/direct/local-writes.ts`), `packages/revud/src/direct/local-archive.test.ts`,
`packages/revud/src/direct/local-write-isolation.test.ts`
**Check:** `bun test packages/revud/src/direct/local-archive.test.ts`, over M8.4.8's
`createFakeLocalStore()` harness:

- **Detection.** A fake listing carrying one matching PR and one fork PR with the same head ref archives
  **exactly one** review and leaves the other's `archived_pr` null.
- **The refusals refuse before mutating.** After archiving, each of the four writes refuses (submit → a 200
  `forbidden` value, never a throw; the other three → an `ApiError('forbidden', …)`), and after each one the
  whole persisted local state is **byte-identical** to a `JSON.stringify` snapshot taken before the call —
  so "refuses before mutating anything" is asserted rather than described. The `reason` string names the PR
  number and matches none of the self-approval vocabulary (see Landmines).
- **Reads stay open.** `getSnapshot`, `getDraft`, `getFileViewed` and the thread read all answer with the
  same values they answered before the archive, asserted with `toEqual` against the pre-archive results.
- **Closed-without-merge is a no-op, with a positive control in the same test.** Re-run the sweep with the
  PR now `closed` and `merged: false` → `archived_pr` unchanged and no write becomes possible again; in that
  same sweep a **second**, not-yet-archived review whose PR is open **does** archive, so the "unchanged"
  assertion cannot pass because the sweep did nothing at all.
- **The trigger fires.** Whatever Open question 2 settles on, one assertion drives that trigger end-to-end
  and observes the archive land — if it is on `syncPull` or `GET /api/pulls`, the case lives in
  `packages/revud/src/direct/direct-api.test.ts` or `packages/revud/src/direct-router.test.ts` respectively
  and asserts `archived_pr` set afterwards. Its companion: with the listing seam **absent**, the same trigger
  fires, archives nothing, and issues zero requests under M8.9.3's `fetch` double.

And **D7 as a gate, not a courtesy**: `bun test packages/revud/src/direct/local-write-isolation.test.ts`
green with `./local-archive` **added to its banned-specifier list** — the detector legitimately holds a
listing seam, so the write sink must not import it either, and the archived guard is a plain store read.
That specifier lands green (nothing imports the detector yet), so its **negative control is the only
evidence it is being scanned**: import `./local-archive` from `local-writes.ts`, observe a red run naming
the specifier, revert, observe green. Record both in the `## Log`.

### M8.9.5 — Serving the archived state, with an ETag that actually moves
**Do:** Put the archived state on the wire, and make the list surface **reachable**. This is the unit with
the trap: `brokerEtag` composes the upstream list ETag with a hash of a fixed annotation projection
(`poll-loop.ts:141-155`, `:168-170`) that does not include `pull.state`, and `§4.1` specifies the *local*
list's ETag as a hash of the local reviews' `compareKey`s — neither of which moves when a review is
archived. Archiving changes no compare key and no annotation, so a client holding the prior ETag would 304
forever and **never learn the review was superseded**. Fold the archived state into whatever the list ETag
hashes, exactly as the CI rollup was folded in for the same reason (`poll-loop.ts:132-139` states the
argument). Then decide and implement how the archived PR number reaches the app (Open question 3) and what
`pull.state` reads as on an archived local review (Open question 4). Mirror the whole thing in the mock —
D6 makes the mock the spec, so the archived shape is defined there and revud conforms.
**Test first:** the ETag-composition case is written against today's ETag and observed **red** — archiving
leaves it byte-identical — before the fold is implemented. That red run is the landmine reproduced, and it
is what distinguishes this suite from one that encodes whatever the code ended up doing.
**Files:** `packages/revud/src/direct/direct-api.ts`, `packages/revud/src/direct/local-archive.ts`,
`packages/app/src/api/mock/adapter.ts`, `packages/app/src/api/mock/store.ts`,
`packages/revud/src/direct/direct-api.test.ts` (M8.5.1's file),
`packages/revud/src/direct-router.test.ts`, `packages/app/src/api/mock/store.test.ts`
Note what is **not** open here: the archived PR number's wire path is settled by M8.1's decision **D-c** — it
rides `LocalReviewSummary.archivedPr` through `listLocalReviews`, which M8.5.2 already serves and M8.6.2
already queries for the dirty badge, so no field is added to the frozen `BrokerPullMeta`. What this unit still
owes is the **ETag**, because the app's only *live* surface is `listPulls`: if archiving moves nothing the
client polls, the annotation query is the only thing that can notice, and it must not be given a long
`staleTime` (M8.6.2 says so). Fold the archived state into the list ETag anyway — a client that keeps writing
into a read-only review because its 304 never broke is the failure this unit exists to prevent, and the
annotation query is a second chance, not the mechanism.
**Check:** three legs, modelled on `checks-rollup.test.ts:98-136` and `:182-220` — the suite that exists
precisely to prove a non-list-derived annotation reaches the served ETag.

- **ETag composition**, in `packages/revud/src/direct/direct-api.test.ts`, where the local list's ETag is
  computed: archiving one local review with **every other input held constant** flips the served ETag, and
  an unchanged list leaves it **byte-identical** across two calls (the other half of the contract — an ETag
  that churns makes every poll a 200 and the conditional read stops being free). There is no un-archive
  case, because there is no un-archive method.
- **The conditional GET**, in `packages/revud/src/direct-router.test.ts`: `GET /api/pulls` → 200 with an
  `etag` header; replaying it → **304** with an asserted-empty body (`expect(await res.text()).toBe('')`,
  the shape M8.5.3 pins); then archive; then replay the **same** pre-archive ETag → **200**, not 304. In
  that 200's body, the archived review is **still present** as an item — `usePullItem` (`queries.ts:175-181`)
  selects out of this list, and a row that vanishes here is the `pr-layout.tsx:337-360` installation-404
  landmine, asserted rather than walked.
- **The mock, as the spec (D6).** `bun test packages/app/src/api/mock/conformance.test.ts` green with the
  archived shape in place, plus a case in `packages/app/src/api/mock/store.test.ts` (with `mockDev.reset()`
  in `beforeAll` — the shared-`localStorage`-shim rule) asserting the mock's `listLocalReviews` carries
  `archivedPr` and that the mock's `listPulls` ETag **also** moves on archive. The mock hashes the whole
  item (`adapter.ts:224-257`) so it moves "for free" while revud's does not — pinning both means the two
  transports cannot drift apart on the one property this unit exists to guarantee.

### M8.9.6 — App: the superseded chrome and the link out
**Do:** Render the archived state. Three surfaces. **Inbox row**: a superseded badge alongside the existing
draft / approvable badges (`inbox.tsx:567-578`), and — if Open question 4 lands on `state: 'closed'` — the
section and tree filters at `inbox.tsx:122` and `:175` must stop dropping it, or the review vanishes from
the inbox entirely and `§8` case 22's "stays visible" is violated at the UI while the data is intact.
**PR chrome**: a superseded banner in the slot the author banner already owns
(`pr-layout.tsx:440-443` — `empty:hidden` around a component that decides its own visibility, exactly the
`author-banner.tsx:38-39` pattern), carrying the PR number and an external link to it. Author it
**portal-free and prop-driven** like `AuthorBanner` — it takes the PR number and the resolved URL as props
and owns no query — so `renderToStaticMarkup` renders it with no DOM and no new dependency and its markup is
directly assertable (`components/ui/markdown.test.ts:1-21` is the established shape). **Review bar**: on an
archived review there is nothing to submit, so the composer and verdict picker are not rendered at all —
`§7`'s established precedent is *omit the group entirely rather than render it empty*, and it matters more
here because the `forbidden` submit branch reroutes the verdict to COMMENT (`review-bar.tsx:121-123`), which
is the wrong story. Threads stay readable and existing drafts stay visible and non-destroyed.
**Files:** `packages/app/src/pages/inbox.tsx`, `packages/app/src/pages/pr-layout.tsx`,
`packages/app/src/components/review/review-bar.tsx`, `packages/app/src/lib/mode-copy.ts` (M8.7's copy module),
`packages/app/src/lib/mode-copy.test.ts`, `packages/app/src/lib/inbox-sections.test.ts` (M8.6.4's pure
module), plus `packages/app/src/components/review/superseded-banner.tsx` and
`packages/app/src/components/review/superseded-banner.test.ts` (both new)
**Check:** `packages/app` has no RTL and no jsdom, and no `.test.tsx` exists by convention — but
`renderToStaticMarkup` needs neither, so the banner is asserted on **real HTML**, not walked. Four durable
legs in `bun test`, then a walk that corroborates and never substitutes.

1. **The banner, on real HTML.** `bun test packages/app/src/components/review/superseded-banner.test.ts` —
   a `.ts` file rendering through `createElement` + `renderToStaticMarkup`, exactly as
   `components/ui/markdown.test.ts:1-21` does. Archived → the markup contains the PR number and, when a URL
   is derivable, an `<a href="…/pull/<n>">` carrying `rel="noreferrer"`. Not archived → the render is
   exactly `''` (the `author-banner.tsx:38-39` self-gating contract, and what `empty:hidden` at
   `pr-layout.tsx:440-443` depends on). That absence carries its **positive control in the same test**: the
   archived render is asserted non-empty, so `''` cannot pass because the component throws or renders
   nothing unconditionally.
2. **The link is a derivation with a failure case.** A pure `archivedPrUrl(repoIdentity, prNumber)
   → string | null`, table-tested beside the banner: an `owner/name` identity → the constructed URL; a
   **path-shaped** identity (`/Users/…/repo`, the no-origin case Open question 7 settles) → `null`; and the
   banner rendered with `null` shows the PR number as text with **no** `<a` in its markup. Without that row
   the offline workspace this milestone exists to serve renders `https://github.com//Users/…/pull/7`, which
   is the guess Open question 5 warns about, shipped.
3. **The three predicates, beside M8.7's copy module.** `bun test packages/app/src/lib/mode-copy.test.ts`
   gains: the **badge predicate** (a local item with `archivedPr` set → superseded; without → not; a GitHub
   item → never), the **banner copy** (names the PR number, matches none of `/installation/i`,
   `/approves on github\.com/i` or the self-approval vocabulary — asserted against the real exported
   function, never a pasted literal, in the shape `components/review/error-copy.ts:20-29` establishes), and
   the **composer-suppression predicate** (archived → no composer and no verdict picker, independent of
   `canApprove`, which `review-bar.tsx:61` defaults to `false` during the list's first load anyway).
4. **The inbox does not drop it** — the assertion that makes Open question 4 safe whichever way it lands.
   `bun test packages/app/src/lib/inbox-sections.test.ts` gains a case over M8.6.4's pure
   `buildInboxSections`: an archived local item **whose `pull.state` is `'closed'`** still appears, in
   exactly one section, and that section is `local`. `inbox.tsx:122`/`:175` filter `state === 'open'` before
   any section or tree is built, so this is `§8` case 22's "stays visible" as a durable assertion instead of
   a walk. Its positive control is the file's closed-GitHub-item-dropped case (M8.6.4 lands it as a named
   assertion) — the filter must lose exactly one of the two.

Rendered, in the `?mock=1` walk recorded in the Log — corroboration for the wiring the four legs above
deliberately do not cover (see `## Testing exceptions`): the inbox lists the archived review with the badge;
`/pr/<localId>` renders the banner and the PR link and **not** the "isn't in this installation" empty state
(`pr-layout.tsx:338-360`); the review bar shows no submit button and no verdict picker; the review's threads
and its saved draft both still render; and a real fixture PR's inbox row and chrome are unchanged from
`main`. M8.11's e2e is the automated backstop for the walk, not this unit.

### M8.9.7 — Whether an archived review still re-syncs, decided and pinned
**Do:** Open question 9 is the one behavior this ticket creates that no unit asserts. M8.9.4 makes the four
**writes** refuse and is deliberately silent about `syncPull`, so today an archived review syncs or does not
sync purely as a side effect of M8.5.1's dispatch — and a later agent can flip it in either direction with
nothing going red. Decide it (freeze at the last synced `compareKey`, or keep syncing), record the decision
and its reason in Open question 9, then pin it. What does **not** reopen here: the archived review's
existing threads, submitted reviews and drafts stay readable and untouched either way — that is M8.9.2's
constraint. Nor does the decision reach the write refusals: a review that keeps syncing is still read-only
to the four write verbs.
**Files:** `packages/revud/src/direct/local-archive.ts`, `packages/revud/src/direct/local-archive.test.ts`,
`packages/revud/src/direct/direct-api.test.ts`
**Check:** `bun test packages/revud/src/direct/local-archive.test.ts` and
`bun test packages/revud/src/direct/direct-api.test.ts`, over M8.4.8's fake store, with the branch that was
chosen asserted and the other one impossible to reintroduce silently:
- **If archived reviews freeze:** with the fixture branch's head moved after the archive, `syncPull` on the
  archived local id resolves to the stored snapshot, its `compareKey` is **unchanged**, and the fake store's
  immutable-half count is unchanged — no new half is minted.
- **If they keep syncing:** `syncPull` produces a **new** `compareKey` after the head moves, and the
  archived review's threads and its draft are still readable afterwards and byte-identical
  (`JSON.stringify`) to what they were before the sync.

Either way the four write verbs are re-asserted `forbidden` **after** that sync, so the sync decision cannot
quietly restore writability — that is the one crossing between the two behaviors and it is the one worth an
assertion. **Negative control, recorded:** implement the branch that was *not* chosen, observe the case go
red, revert.

### M8.9.8 — The archived semantics, pinned once and run on every transport
*(Appended 2026-09-02 at integration. Archive changes what `syncPull`, the four write verbs, `listPulls` and
`listLocalReviews` mean, and M8.10's ruling 1 went unimplemented in both transports precisely because
`deleteLocalReview` had no parity leg — the same class, prevented rather than repaired.)*
**Do:** A shared conformance block `runLocalReviewArchiveConformance(config)` in
`packages/shared/conformance/local-archive.ts`, taking the local suite's `makeApi` / `humanId` / `restart`
plus `superseded: Lazy<{ pair, prNumber, appear() }>` and a `listPulls(etag)` hook (the wire and the direct
engine spell that method differently — a finding, bridged by the router). Seven steps: live before the PR,
the sync that finds it, frozen, the four refusals, reads and drafts open, a restart, sticky. Registered in all
three runners; the direct leg assembles a recording `SupersedingPullSource` into the archive block only, on a
second base branch of the same fixture.
**Files:** `packages/shared/conformance/local-archive.ts` (new), `packages/shared/conformance/index.ts`,
`packages/app/src/api/mock/conformance-local.test.ts`, `packages/app/src/api/http/conformance-local.test.ts`,
`packages/revud/src/direct/conformance-local.test.ts`
**Check:** the three runner files green with the block's 17 tests each; each leg observed red before its
producer was wired (an uncovered pair on E/F, an empty source on G); `bun run conformance:matrix` with
`[E] [F] [G] PASS`.

## Verify

Additional to `bun run check`:

- `bun test packages/revud/src/direct/local-archive.test.ts` — the predicate table (fork, base mismatch,
  ref-shape normalization, blank repo identity), the module-purity scan and its anti-vacuity self-checks,
  the sweep and its chosen trigger, the four write refusals **with the persisted state byte-identical
  after each**, the closed-without-merge no-op with its positive control, and M8.9.7's sync decision.
- `bun test packages/revud/src/direct/store.test.ts` — write-once `archived_pr`; drafts, threads and
  submitted reviews provably untouched by an archive; the `DELETE FROM` allowlist unchanged; `STORE_VERSION`
  still M8.2's 4.
- `bun test packages/revud/src/direct/local-write-isolation.test.ts` — M8.4's D7 guard, green with
  `./local-archive` added to its banned specifiers, so the archived guard in the write sink is provably a
  store read and the detector is provably outside that import graph.
- `bun test packages/revud/src/direct/direct-api.test.ts` — the local list's ETag moves on archive with
  every other input held constant, and is byte-identical across an unchanged list.
- `bun test packages/revud/src/direct-router.test.ts` — a client replaying its pre-archive ETag gets a
  **200** rather than a 304, and the archived review is still an item in the list it gets back.
- `bun test packages/app/src/api/mock/conformance.test.ts` and `packages/app/src/api/mock/store.test.ts` —
  the mock's archived shape is conformant, carries `archivedPr` on `listLocalReviews`, and moves its own
  `listPulls` ETag on archive.
- `bun test packages/app/src/components/review/superseded-banner.test.ts`,
  `packages/app/src/lib/mode-copy.test.ts`, `packages/app/src/lib/inbox-sections.test.ts` — the banner's
  real HTML including the empty render and its positive control, the URL derivation with its path-shaped
  identity → `null` row, the three predicates, and an archived local review surviving the inbox's
  `state === 'open'` filter.
- **The fork case is in-gate, not only on a fixture walk:** M8.9.1's table row (`head.repo.full_name`
  differs, everything else matches → no archive) and M8.9.4's two-review sweep (one matching PR, one fork PR
  with the identical head ref → **exactly one** archived) both run on every gate. The scratch-repo fork PR
  below corroborates them against real GitHub payloads.
- **The end-to-end proof, both halves** — corroboration recorded in the `## Log`, with M8.11's local e2e leg
  as the automated backstop: in a seeded scratch repo, create a local review, comment, submit, then open a
  real PR for the same branch pair. Observe: exactly that review archives; a **second** local review on a
  *different* base ref does not; a fork PR whose head branch has the identical name does not; the archived
  review still renders every thread, its submitted review and its draft; and the PR on github.com carries
  **zero** comments, zero reviews and zero reactions. That last observation is D1 — record the PR URL and
  the observation, and note which in-gate assertions it corroborates (`## Testing exceptions`), because a
  one-time observation of an absence guards nothing on its own.
- `git diff --stat` touches **no file** under `packages/shared/src/api/` unless Open question 3 is resolved
  in favor of a contract field — in which case the change is coordinated with M8.1's owner first
  (AGENTS.md ownership), never made unilaterally.
- `bun test packages/app/src/api/mock/conformance-local.test.ts packages/app/src/api/http/conformance-local.test.ts
  packages/revud/src/direct/conformance-local.test.ts` and `bun run conformance:matrix` — M8.9.8's block green
  on E, F and G (**run 2026-09-02: 35 · 35 · 38, matrix A/B/E/F/G PASS**).
- **Run 2026-09-02, all of the above:** `env -u GH_TOKEN -u GITHUB_TOKEN TZ=UTC bun run check` →
  **3248 pass · 1 skip · 0 fail · 116 files**; `packages/shared/src/api/` untouched (`git diff --stat` on the
  branch shows only `packages/shared/src/lib/`, `conformance/` and `index.ts`); the `?mock=1` walk recorded in
  the Log with the defect it found and the fix. The scratch-repo both-halves proof (a real PR on github.com
  carrying zero comments) was **not** run in that session (no token) — **it ran later the same day, next
  bullet**; the direct leg's recording source plus the daemon's zero-request case under the `fetch` double
  remain the in-gate half. `bun run test:e2e` (2026-09-02): both drivers `ALL CHECKS PASSED`
  — the local-review driver runs the changed sync path under the netlog guard with no seam wired.
- **Run 2026-09-02 (later session) — the scratch-repo both-halves proof, ✅ run.** Target `pat-mw/revu-sandbox`;
  revud `--direct` booted from `tmp/revu-sandbox` (viewer `pat-mw`, credential via `gh auth token`, a temp
  `REVU_DATA_DIR`, a `build:e2e` dist), the flow walked in the served app. Real PR:
  <https://github.com/pat-mw/revu-sandbox/pull/6> (`main ← proof/archive-2026-09-02`, head `ce1dab4`, head repo
  `pat-mw/revu-sandbox`). Observed: **exactly** the `main ← proof/…` review archived on its next sync
  (`archivedPr: 6`; banner `Archived — superseded by pull request #6` linking to the PR; chip `archived`; inbox
  chip `archived · superseded by #6`); the **second** review of the same head against
  `fixture/base-advances-target` stayed live across three syncs (OQ8); the archived review kept its thread, its
  submitted review (`local_reviews_submitted`, state `COMMENTED`) and its pending draft, and the review bar
  rendered **no submit and no verdict picker**; the PR on github.com carried **zero** review comments, zero
  reviews, zero issue comments and zero reactions (`pulls/6/comments`, `pulls/6/reviews`, `issues/6/comments`,
  `issues/6/reactions` all `[]`); `gh pr close 6` → re-sync → **still archived**, frozen at its `syncedAt`; the
  head branch then deleted locally and remotely → sync still answers 200 with the frozen snapshot, zero requests.
  The four writes refused (`submitReview` → 200 `{ status: 'forbidden' }`; reply / resolve / reaction → 403
  `forbidden`, every reason naming #6) with threads, resolution and reactions byte-identical after. **The fork
  case was not produced** (no fork exists) — it stays in-gate, as planned. **Corroborates:**
  `local-write-isolation.test.ts` (the write sink cannot import a GitHub client), `no-network-preload.ts` + the
  e2e netlog guard (the local flow issues no outbound request), `local-archive.test.ts` (the sweep, the four
  refusals, the closed-without-merge no-op, M8.9.7's freeze), `store.test.ts` (write-once `archived_pr`;
  drafts, threads and submitted reviews untouched by an archive), and the conformance block on E/F/G. Full
  record, including two non-defect observations (OQ10) and the operational notes, in the `## Log`.

## Context — verified seams

| anchor | what is there, and why it binds |
| --- | --- |
| `packages/shared/src/api/types.ts:60-65` | `GhRef` — `{ ref, sha, label, repo: { full_name, default_branch } }`. `head.repo.full_name` is the fork discriminator `§8` case 20 requires, and `ref` is a **bare** short name (`feature/x`), never `refs/heads/…`. |
| `packages/shared/src/api/types.ts:68-84` | `PullSummary` — the whole detection input. Note what is **absent**: no `html_url`, so the "link to the PR" has to be constructed, not read (Open question 5). |
| `packages/shared/src/api/types.ts:278-301` | `BrokerPullMeta` — every field the list carries per pull. There is no archived/superseded field, and this file is a read-only input owned by M8.1. |
| `packages/shared/src/api/types.ts:495-498` | `SubmitResult` — `ok` \| `head_moved` \| `{ status: 'forbidden'; reason }`. The archived refusal is a 200-level **value**, never a throw. |
| `packages/shared/src/api/types.ts:538-554` | `ApiErrorCode` — `forbidden` exists; `not_implemented` does **not** (the router emits it as a raw 501 body). The three non-submit writes refuse with `forbidden`. |
| `packages/shared/src/http.ts:92` + `:113-114` | `CONDITIONAL_LIST_ROUTE` / `CONDITIONAL_LIST_304_RULE` — `listPulls` is the **only** conditional surface, and a 304 carries no body. Any archived state the app must see has to be reachable through that ETag. |
| `packages/revud/src/broker/poll-loop.ts:141-155` | `serializeAnnotations` — the ETag's meta projection is exactly `{pr, authorHumanId, canApprove, assignedReviewerHumanIds, checks}`. **`pull.state` is not in it.** This is the single most important anchor in this ticket. |
| `packages/revud/src/broker/poll-loop.ts:132-139` | The comment explaining *why* the CI rollup had to be folded into the ETag hash — "the one served annotation that can move while every other input stands still". The archived flag is the second such annotation, and the argument transfers verbatim. |
| `packages/revud/src/broker/poll-loop.ts:168-170` | `brokerEtag(listEtag, items)` — `W/"pulls:<djb2(listEtag)>:<djb2(annotations)>"`. The composition point M8.9.5 extends. |
| `packages/revud/src/broker/poll-loop.ts:535-550` | `changedNumbers` — a pull is "changed" on head SHA, `updated_at`, or **base SHA**. Nothing here reacts to a *local review* changing, because local reviews are not in this loop at all. |
| `packages/revud/src/broker/poll-loop.ts:765-788` | `listPulls(ifNoneMatch)` — the exact 304 semantics (`items: []`, echoed etag, `notModified: true`) any merged list must reproduce. |
| `packages/revud/src/broker/poll-loop.ts:642-663` | `pollOnce` and its single `deps.client.listOpenPulls(...)` call at `:659` — the only place in the daemon that lists pull requests today, and it exists in broker mode only. |
| `packages/revud/src/direct/github-client.ts:145-172` | `PullListClient` — deliberately separate from `GithubClient` so the many `GithubClient` fakes need not grow list methods. The archive listing seam should follow the same narrowing discipline. |
| `packages/revud/src/direct/github-client.ts:1063-1096` | `listOpenPulls` — `?state=open&per_page=100`, **one page, no pagination follow-up**. A closed PR is invisible to it and the 101st open PR is truncated away; both are detection gaps if this is the chosen input (Open question 1). |
| `packages/revud/src/direct/github-client.ts:862-875` | The summary ref mapper: an absent or null `repo` on a side maps to `full_name: ''`. A deleted fork therefore arrives as the empty string, which must never match a blank local repo identity. |
| `packages/revud/src/direct/github-client.ts:924` | `createGithubClient(...): GithubClient & PullListClient` — the listing capability already exists on the concrete client; only the **type** at the context boundary hides it. |
| `packages/revud/src/direct/mappers.ts:76-91` | `mapPullDetail`'s `ref()` helper — the same `full_name: ''` fallback on the detail path, so a synced snapshot's `pull.head.repo.full_name` has the identical hazard. |
| `packages/revud/src/direct/context.ts:25-34` | `DirectContext.github: GithubClient` — narrower than what `createGithubClient` returns. This is why direct mode "has no PR listing" even though the client can do it. |
| `packages/revud/src/index.ts:172-181` | `mainDirect`'s `createDirectApi` call — **no `pullList`**, no poll loop, no tick. Direct mode has no periodic anything to hang a sweep on (Open question 2). |
| `packages/revud/src/index.ts:284-292` | `mainBroker` builds a dedicated `pollClient` + `createPollLoop`. Broker mode already has both a GitHub listing and a ~30s cadence; the archive sweep should reuse them rather than add a second poller. |
| `packages/revud/src/direct/direct-api.ts:159-193` | `DirectApiDeps` + the one-method `PullListSource`. The exact precedent for M8.9.3's seam: the api depends on the read, never on the lifecycle. |
| `packages/revud/src/direct/direct-api.ts:218-233` | `listPulls` — **synchronous**, and a typed `not_found` when no source is wired. Anything the archive state rides on this method must also be synchronous. |
| `packages/revud/src/direct-router.ts:295-314` | The `listPulls` branch: emits the ETag on the 200 and a **bodiless** 304 on a match. M8.5.3 changed its gate; this ticket changes only what the ETag is computed over. |
| `packages/revud/src/direct-router.ts:186-234` | `envelopeForError` — an `ApiError` is serialized to its own status at `:192-195`; everything unrecognized falls to a generic `broker_unreachable` 500 at `:232-233`. An archived-write refusal must be an `ApiError`, or it renders as an unreachable broker. |
| `packages/revud/src/direct-router.ts:147-151` | `prNumberOf` — `Number.isInteger(n) && n > 0`. Unchanged by this ticket; the archived review keeps its local id and its `/pr/:n` route. |
| `packages/revud/src/direct/store.ts:65` | `STORE_VERSION` (3 today, 4 after M8.2). This ticket adds **no** migration step — `archived_pr` is already in M8.2's `local_reviews` DDL. |
| `packages/revud/src/direct/store.ts:463-467` | `deleteDraft` — the **only** `DELETE` in the store. Archiving must not add a second one. |
| `packages/revud/src/direct/store.ts:561-574` | `recordPrAuthor` — first-write-wins via `ON CONFLICT … DO NOTHING`. The shape `markLocalReviewArchived` copies for its write-once semantics. |
| `packages/revud/src/direct/store.ts:236-245` + `:212-219` | `recordPrAuthor` / `getPrAuthor` and `appendAudit` / `listAudit` on `DirectStore` — the two PR-keyed surfaces D4 forbids the local path from touching. The **real** PR number discovered by detection lives only in `local_reviews.archived_pr`. |
| `packages/revud/src/direct/conformance-fakes.ts:518-519` | The fake pull's `head`/`base`, both `repo: { full_name: 'o/r' }`. A fork fixture needs a *different* `head.repo.full_name`, which no existing fake produces — build one. |
| `packages/revud/src/direct/conformance-fakes.ts:567-569` | `fakePollSources` — a `PullListClient` + `PollFactsSource` over mutable state with real conditional-ETag semantics. The ready-made harness for an archive-detection test. |
| `packages/revud/src/broker/checks-rollup.test.ts:98-136` | "the broker ETag reaches the CI rollup" — four tests that are the exact model for M8.9.5's ETag suite, including "an unchanged rollup leaves the ETag byte-identical". |
| `packages/revud/src/broker/checks-rollup.test.ts:182-220` | "a finished build reaches a client holding the prior ETag" — the same conditional-GET-becomes-a-200 proof the archived flag needs. |
| `packages/revud/src/direct-router.test.ts:361-371` | The 501 case for `GET /api/pulls` that M8.5.3 rewrote. Read it before touching the list path, so this branch does not re-break it. |
| `packages/app/src/state/queries.ts:52-75` | `usePullList` — polls every 15s, replays its ETag, and on a 304 **keeps the cached items** and takes only the fresh rate limit. A stale ETag therefore pins stale items indefinitely. |
| `packages/app/src/state/queries.ts:175-181` | `usePullItem` — one row selected out of the list query. Everything downstream (`PrLayout`, `ReviewBar`, `AuthorBanner`) reads through it, so a review missing from the list is a missing review everywhere. |
| `packages/app/src/state/queries.ts:147-162` | `useStaleness` — compares `item.broker.compareKey` to the snapshot's. Archiving changes no compare key, which is exactly why the ETag does not move on its own. |
| `packages/app/src/pages/inbox.tsx:121-157` | The section builder, opening with `items.filter((it) => it.pull.state === 'open')` at `:122`. A `closed` archived review is dropped from **every** section here. |
| `packages/app/src/pages/inbox.tsx:174-178` | `treeRoots` — the same `state === 'open'` filter at `:175`, so the tree view drops it too. |
| `packages/app/src/pages/inbox.tsx:567-578` | The row's right-hand badge cluster (draft badge, "org PR — approvable"). Where a superseded badge belongs. |
| `packages/app/src/pages/pr-layout.tsx:279-282` | `item` resolved by finding `prNumber` in `list.data.items`. If the archived review leaves the list, this is `undefined`. |
| `packages/app/src/pages/pr-layout.tsx:337-360` | The `!item` branch: the "PR #N isn't in this installation" empty state, whose hint names a GitHub App installation. `§7` calls this out as the screen a mis-keyed local review lands on — an archived one must never reach it. |
| `packages/app/src/pages/pr-layout.tsx:377-381` | `stateBadge` — `merged_at` → merged, `state === 'open'` → open, else **closed / danger-red**. An archived local review rendering danger-red "closed" is the wrong signal. |
| `packages/app/src/pages/pr-layout.tsx:440-443` | The author-banner slot: `<div className="pt-2 empty:hidden">` wrapping a component that decides its own visibility. The exact slot and pattern for the superseded banner. |
| `packages/app/src/components/author/author-banner.tsx:38-39` | `if (!item) return null` / `if (… || item.pull.state !== 'open') return null` — the self-gating precedent, and a component that already goes silent on a non-open pull. |
| `packages/app/src/components/review/review-bar.tsx:61` | `const canApprove = item?.broker.canApprove ?? false` — the review bar's only list-derived gate today. There is no read-only concept in it yet. |
| `packages/app/src/components/review/review-bar.tsx:121-123` | The `forbidden` submit branch: `actions.setEvent('COMMENT')` then a toast of `result.reason`. Written for the self-approval refusal, and actively wrong for an archived review. |
| `packages/app/src/components/review/review-bar.tsx:412-418` | The lock popover — "an org member (e.g. dkozlov) approves on github.com". `§6` and `§7` both forbid this rendering on a local review; M8.7 owns suppressing it, and an archived review must not resurrect it. |
| `packages/app/src/lib/pull-tooltip.ts:156-163` | `branchPair` — the existing `pull.head.repo.full_name !== pull.base.repo.full_name` fork check, and the app's only precedent for reasoning about fork identity. |
| `packages/app/src/api/mock/adapter.ts:224-257` | The mock's `listPulls`, including its own ETag over `JSON.stringify(items)`. D6 makes this the spec for the archived shape, and note it hashes the whole item — so the mock's ETag moves on archive "for free" while revud's does **not**. |

## Constraints that bind this ticket

- **D1 is the whole ticket, and it is a prohibition.** No code path may copy a local comment, thread, review
  summary or reaction to a pull request. Detection is a **read** of GitHub; archival is a **write to the
  local store**. If a unit starts needing a GitHub write client, the design has been reversed.
- **D7 — no GitHub client in the local write path, structurally.** The archive **detector** legitimately
  holds a GitHub listing seam; the local **write sink** must not. The archived guard M8.9.4 adds to the
  write path is a plain store read of `archived_pr`, so the detector never enters that module's import
  graph. M8.4's "the local write module imports no GitHub client" assertion must still pass after this
  branch — re-run it as a gate, not as a courtesy.
- **D4 — the real PR number stays out of the PR-keyed tables.** `archived_pr` holds a genuine GitHub PR
  number, and it lives **only** in `local_reviews`. Nothing here writes `snapshots.pr_number`,
  `audit_log.pr` (`store.ts:212-219`) or `pr_author.pr` (`store.ts:236-245`) — the collector's and poll
  loop's reading of "PR #N" stays truthful, and a local review remains invisible to the audit trail by
  design (`§8` case 25).
- **D8 — compare refs, never SHAs.** A local review's merge base comes from the live base tip; a PR's
  `base.sha` is refreshed only on a `synchronize` event. The two legitimately disagree for the same branch
  pair, so a SHA comparison would both false-negative and false-positive.
- **Archiving is non-destructive** (`§8` case 22, `hard-constraints.md`): threads, submitted reviews,
  history **and drafts** stay. "Drafts survive everything — that is the product." An archive that clears a
  draft is a product-level defect regardless of what the tests say.
- **The frozen contract is a read-only input.** `packages/shared/src/api/client.ts` and `types.ts` belong to
  M8.1 (AGENTS.md ownership). If surfacing the archived state seems to require a new field on
  `BrokerPullMeta`, that is a boundary question to raise (Open question 3), not an edit to make.
- **`CONDITIONAL_LIST_304_RULE`** (`http.ts:113-114`): `listPulls` stays the only conditional surface and a
  304 carries no body. Any state the app must observe rides through that ETag or it is unobservable.
- **The three transport semantics survive** (`hard-constraints.md`): an archived review's `getSnapshot`
  still returns `null`-not-404 for a never-synced review, and `submitReview` still returns its refusal as a
  **200-level value**, never an HTTP error.
- **D6 — the mock is the spec.** The archived shape is defined in the mock first and revud conforms; the
  conformance suite runs against the mock, and M8.11's local leg will hold both to it.
- **Identity**: the synthesized local `GhUser` keeps the git-config **name**, never the email (`§4.1`,
  `§8` case 26). Archival adds no identity field and must not smuggle one into the banner copy.
- **AGENTS.md code style**: no M-ID, phase, ticket or tracking reference in any comment or docstring;
  `import type` for types; no unused bindings; no raw hex in components (`globals.css` tokens only); every
  interactive element keyboard-reachable with a visible focus ring.

## Landmines

- **The served ETag does not move when a review is archived, so the app never finds out.** `brokerEtag`
  hashes the list ETag plus a fixed annotation projection (`poll-loop.ts:141-155`) that excludes
  `pull.state`; `§4.1` defines the local list's ETag as a hash of the **compareKeys**, and archiving changes
  neither. `usePullList` (`queries.ts:52-75`) keeps its cached items on a 304 — so the inbox shows the
  review as live indefinitely and the reviewer keeps writing comments into a review that is supposed to be
  read-only. This is the highest-value trap in the ticket, and `checks-rollup.test.ts:98-136` exists because
  the identical bug was already found once for the CI rollup.
- **`§4.2`'s `UNIQUE(repo, base_ref, head_ref)` contradicts `§8` case 22's "a new local review can be
  created".** Creating the same triple returns the existing (now archived) row (`§3.1`), so after an archive
  the user cannot start a fresh local review on the same branch pair at all. One of the two statements has to
  give, and **M8.2's open question 1 owns the answer** because it shapes the unique key — read what M8.2
  settled before assuming this ticket is a one-way door on that branch pair.
- **`pull.state: 'closed'` makes the review disappear from the inbox.** `inbox.tsx:122` and `:175` both
  filter `state === 'open'` before any section or tree is built, and `pr-layout.tsx:377-381` paints a closed
  pull's badge **danger-red**. `§4.1` says the archived local review's state is `'closed'`; `§8` case 22
  says it stays visible. Both cannot be true without a change at one end.
- **A missing list row lands the archived review on the GitHub-installation 404.** `usePullItem`
  (`queries.ts:175-181`) → `PrLayout`'s `!item` branch (`pr-layout.tsx:337-360`) renders "isn't in this
  installation", the exact screen `§7` names as the worst possible outcome for a local review. If archiving
  drops the review from `listPulls` for any reason, that is the screen the user gets.
- **The fork case is not hypothetical and the naive check misses it.** Comparing only `head.ref` + `base.ref`
  archives a local review because *someone else's fork* opened a PR from their `feature/x`. Compare
  `head.repo.full_name`. The related trap: a same-repo PR has `head.repo.full_name === base.repo.full_name`,
  so a "heads differ from base ⇒ fork" test (the `branchPair` shape at `pull-tooltip.ts:157`) is the **wrong**
  predicate here — the comparison is head-repo against the **local review's** repo identity.
- **A blank repo identity matches a deleted fork.** `github-client.ts:862-875` maps an absent `head.repo` to
  `full_name: ''`. M8.2's recorded rule forbids a blank local identity for exactly this reason — but the
  predicate must still reject the pair explicitly (M8.9.1's last table row), because the rule is enforced by a
  different ticket in a different package and a future no-origin path that produced `''` would otherwise
  archive every local review at once.
- **Ref spellings differ on the two sides.** M8.3 normalizes local refs to `refs/heads/…`; `§8` case 3 allows
  a base of `origin/main`; GitHub reports bare `main` / `feature/x`. A raw string compare silently never
  matches, and the failure mode is the *absence* of an archive — no error, no log, just a local review that
  quietly stays writable after the PR is open.
- **`listOpenPulls` is `state=open`, one page of 100** (`github-client.ts:1063-1096`). A PR opened and merged
  between two ticks is never observed, and a repo with more than 100 open pulls truncates. If detection is
  built on this list, both gaps are real and must be stated, not assumed away.
- **Direct mode has no tick at all.** `mainDirect` (`index.ts:172-181`) wires no poll loop, so "detection
  runs on the poll" quietly means "detection never runs in direct mode" — which is the mode the feature's
  primary user runs. Decide the trigger explicitly (Open question 2).
- **Detection spends the shared GitHub bucket, on a feature whose premise is "no token, no network".** A
  sweep that fires unconditionally either costs requests on every tick or hard-fails in the offline
  workspace the milestone exists to serve. The listing seam must be optional and its absence must be a
  no-op, never an error.
- **The `forbidden` submit branch tells the wrong story.** `review-bar.tsx:121-123` reacts to
  `{ status: 'forbidden' }` by silently switching the verdict to COMMENT and toasting the reason — copy
  written for the self-approval refusal. An archived review that surfaces only through that path reads as
  "you can't approve, use comment". Suppress the composer instead, and keep the server refusal as a
  backstop.
- **Archiving must not become the store's second `DELETE`.** `deleteDraft` (`store.ts:464`) is the only one
  today. "Read-only" means writes are refused, not that anything is removed; retention and pruning are
  M8.10's, and even there the rule is "prune unreferenced immutables", never "drop an archived review's
  threads".
- **`archived_pr` must be write-once in SQL, not in a read-then-write.** Two concurrent sweeps, or a sweep
  racing a manual archive, would otherwise last-writer-wins onto a different PR number. `recordPrAuthor`
  (`store.ts:561`) already shows the conditional-write shape.

## Open questions

1. ~~**What GitHub call feeds detection?**~~ **Decided 2026-09-02 (session, from the repo): the targeted
   query, `state=open` only** — `GET /repos/{owner}/{repo}/pulls?state=open&head={owner}:{head}&base={base}`,
   one request per sync of that review, behind an optional `SupersedingPullSource` seam that is absent with no
   origin/token. `state=all` (the roadmap's recommendation) was rejected because a branch name reused months
   later would match an old closed PR and archive the new review through a **one-way door** (M8.2 R-A) — a
   false positive with no recovery. Accepted gap, stated in the guide: a PR opened and closed or merged between
   two syncs is never observed and the review stays live; that is harmless (nothing local reaches GitHub either
   way) and archive is sticky once seen. The `head=` filter already excludes fork PRs; the predicate still
   compares `head.repo.full_name` (belt and braces, and the in-gate fork row). Several open matches → the
   lowest number. The predicate is **shared** (`packages/shared/src/lib/local-archive.ts`) so the mock — the
   specification — runs the same code over its own fixture pulls and revud cannot drift from it.
   Original text, for the record: Two candidates with different costs. (a) The existing
   `listOpenPulls` (`github-client.ts:1063-1096`) — one request per tick for the whole repo, but `state=open`
   only and capped at 100, so a PR opened-and-closed between ticks is never seen. (b) A **targeted** query
   per non-archived local review — `GET /pulls?head=<owner>:<branch>&base=<base>&state=all` — which is exact,
   catches closed and merged PRs, and costs one request per live local review. A workspace typically has few
   local reviews, which argues for (b); (a) reuses machinery that already exists in broker mode. `§8` case 21
   names the gap but not the fix. **Decide before M8.9.3** — it is the shape of the seam.
2. ~~**What triggers the sweep?**~~ **Answered by the owner, 2026-08-19: on each sync of that review.**
   Detection runs inside the local `syncPull` path — the daemon is already talking to git, the user is
   looking at that review, and the cost is bounded by user action rather than by a clock. The rejected
   candidates and why: `GET /api/pulls` makes an inbox poll do GitHub work on every tick; a direct-mode timer
   is a second scheduler to own and to shut down cleanly; broker-mode-only leaves the milestone's exit
   criterion reading as if direct mode archives when it does not.
   **The accepted cost, and it must be carried into the UI copy rather than hidden:** a branch nobody
   re-syncs stays un-archived indefinitely, so the user can keep writing local comments while a PR is already
   open. That is the confusion D1 exists to prevent, now bounded to the window before the next sync instead
   of eliminated. It is not a licence to add a background tick — it is a copy problem for M8.9.6's banner.
   Per this ticket's own rule, M8.9.4's Check must drive the sweep **through a sync**, end to end: a sweep
   nothing calls fails in the field, not in the gate.
3. ~~**How does the archived PR number reach the app?**~~ **Settled by M8.1's decision D-c**: it rides
   `LocalReviewSummary.archivedPr` through `listLocalReviews`, alongside `dirty`, queried under its own key by
   M8.6.2 and read only for badges and banners. `BrokerPullMeta` (`types.ts:278-301`) gains no field, the
   frozen contract is untouched, and `listPulls` remains the row source (D-a). If this ticket's owner wants
   the number on the list item instead, that is a contract change and it goes back to M8.1 — not a unilateral
   edit here, and not a `PullSummary` field smuggled in the `body` (`§7` warns against exactly that).
4. ~~**Does an archived local review's `pull.state` read `'closed'`?**~~ **Settled by the repo, 2026-09-02:
   yes, `'closed'`** — the mock (`local.ts:179`), the daemon list row (`direct-api.ts:508`) and the snapshot
   (`local-sync.ts:1336`) all derived it that way before this ticket started, and `stateChipCopy` already reads
   `'archived'` for it, so flipping it would be a spec change to something three transports pin. The inbox
   therefore learns about it: `showsInInbox` keeps every local item regardless of state (closed GitHub items
   are still dropped), the zero-state count uses the same predicate, and the header chip stops painting a
   local closed review danger-red. Original text: `§4.1` says yes; `inbox.tsx:122`/`:175`
   filter closed pulls out of every section and every tree, and `pr-layout.tsx:377-381` paints them
   danger-red. Either `§4.1` is amended for local reviews (state stays `'open'`, archival is carried
   elsewhere) or the inbox filters and the badge learn about the archived state. Both are real changes; the
   design does not pick one.
5. ~~**What exactly is the link, and where does its host come from?**~~ **Decided 2026-09-02: derived, never
   stored** — `archivedPrUrl(repo, n)` builds `https://github.com/<owner>/<name>/pull/<n>` only for an exact
   `owner/name` identity and answers `null` for anything else (a path-shaped identity renders the number as
   text). github.com is assumed because direct mode already refuses a non-github.com origin
   (`context.ts` "Direct mode only supports github.com"); a stored URL column is a follow-up for the day an
   Enterprise host is supported, recorded on the board, not a v5 migration now. Original text: `PullSummary`
   carries no `html_url`
   (`types.ts:68-84`), the store keeps only `archived_pr INTEGER` (`§4.2`), and the app has no
   PR-link construction anywhere today. Constructing `https://github.com/<owner>/<name>/pull/<n>` needs the
   repo identity **and** assumes github.com — wrong on a GitHub Enterprise host. Consider storing the
   detected PR's URL alongside the number, which makes the link a fact rather than a guess. Whichever way it
   lands, M8.9.6's `archivedPrUrl` returns `null` rather than a guess when no URL is derivable, and the
   banner renders the PR number as text in that case — a path-shaped repo identity (Open question 7) must
   never produce `https://github.com//Users/…/pull/7`.
6. ~~**What happens when the user wants a fresh local review on an archived branch pair?**~~ **Owned by
   M8.2** (its open question 1), because the resolution shapes `UNIQUE(repo, base_ref, head_ref)` and
   changing that after v4 ships is a migration. This ticket consumes whatever M8.2 settled; it does not
   re-open it. The three candidates remain as M8.2 records them: widen the key with a generation or archived
   discriminator, let a create on an archived triple mint a successor and re-key the old row, or amend `§8`
   case 22. If M8.2 shipped without answering it, that is a defect in M8.2, and it is a migration to fix.
7. ~~**What is the repo identity string, and can it ever equal `head.repo.full_name`?**~~ **Settled by M8.2**:
   `repo` is `owner/name` whenever an origin parses, and the absolute toplevel path otherwise — never a hash,
   never blank. So detection is live in exactly the workspaces that had an origin when the review was created,
   and inert (correctly, not silently) in the offline workspaces the milestone exists to serve. Two
   consequences to carry into M8.9.1's table: a path-shaped identity can never equal a GitHub `owner/name`, so
   those reviews never archive and that is the intended behaviour, not a bug to work around; and a workspace
   that gains an `origin` *after* its first local review keeps the path-shaped identity on that row.
8. ~~**A PR opened against a *different* base than the local review.**~~ **Decided 2026-09-02: no archive,
   said in the guide rather than in the UI.** The daemon queries with the base filter, so it never learns about
   a PR on another base and the UI has nothing to say; the guide's §7 states that a PR against a different base
   leaves the review live because the diffs genuinely differ. Original text: Local review is `main ← feature/x`; the
   PR is `develop ← feature/x`. `§8` case 20 requires the base to match, so no archive — the local review
   stays live and writable while the client repo already has a PR carrying that branch. That may be the
   intended behavior (the diffs genuinely differ), but it is the one shape where D1's guarantee and the
   user's mental model come apart, and it deserves a decision and a line of UI copy rather than silence.
9. ~~**Does an archived local review still re-sync?**~~ **Decided 2026-09-02: it freezes.** The order inside
   the local `syncPull` path is **freeze check → detection → sync**: an archived review with a stored snapshot
   returns it untouched (no git, no new compare key, no pin, no prune, no detection); a live review first asks
   the seam, is marked if a PR covers it, and then syncs one last time — so the snapshot it keeps was
   synthesized from the freshly marked row and already reads `pull.state: 'closed'`, agreeing with the list. Reasons: an archive is a record of what was reviewed and
   should not shift under its own threads; a review nobody can write to should not keep minting immutable
   halves for retention to chase; and "read-only" then means one thing on both the write and the sync axis.
   The four write verbs are re-asserted `forbidden` after a frozen sync (M8.9.7). Defensive branch: an archived
   review with no stored snapshot (unreachable by construction, reachable by a raw store edit) syncs normally
   once so it has something to serve. Original text: The branch can keep moving after the PR opens. Freezing
   at the last synced `compareKey` keeps the archived artifact stable and matches "read-only"; continuing to
   sync keeps the diff honest but means an archived review keeps changing under its own threads. The design
   says archival is read-only about **writes** and is silent about **syncs**. **M8.9.7 owns deciding this
   and pinning whichever answer lands**, because otherwise the behavior is a side effect of M8.5.1's
   dispatch that nothing asserts and anyone can flip.

10. **Should a draft stay writable on an archived review?** Open, unasked — the owner's call. The scratch-repo
    walk (2026-09-02) found that on an archived review the review bar withholds the submit composer and the
    verdict picker exactly as M8.9.6 promised, but the **inline gutter composer still opens** (`Comment on line
    1…` with `Add to review`) and `PUT /api/pulls/<id>/draft` answers **200** — the draft's `updatedAt` moved.
    That is consistent with the design as written: M8.9.4 lists drafts under "reads stay open", the four
    refused verbs are submit / reply / resolve / reaction, and "every thread and draft in it is kept" plus the
    bar's `Discard` say a draft is the reader's own private notes. The cost is a composer that accepts text
    which can never be submitted. Either answer is one unit: refuse the draft write in the write sink (both
    transports, a conformance case) and hide the gutter affordance behind the same predicate that hides the bar's
    composer; or keep it and say so in the guide. Nothing changed; recorded so it is a decision, not a drift.

## Testing exceptions

Two things in this ticket carry no automated assertion, and both are exceptions rather than oversights.

- **"The PR on github.com carries zero comments, zero reviews and zero reactions."** D1's headline
  observation needs a real PR on a real host with a real token — none of which the gate has, and none of
  which it may acquire. **Compensating assertions, both in-gate:** M8.4.6's
  `local-write-isolation.test.ts`, extended by M8.9.4 with `./local-archive`, proves the local write path
  cannot reach a GitHub client at any import depth; and M8.5.7's `no-network-preload.ts` fetch tripwire
  proves the local flow issues no outbound request at all. The github.com observation is recorded in the
  `## Log` as corroboration of those two — it is one observation of an absence, and on its own it guards
  nothing.
- **The rendered walk over `inbox.tsx`, `pr-layout.tsx` and `review-bar.tsx`.** All three read through hooks
  and providers (`usePullList`, `usePullItem`, the session context), so `renderToStaticMarkup` of them
  yields a throw or an empty shell; there is no RTL and no jsdom, and introducing one is not this ticket's
  call. **Compensating assertions:** the four legs of M8.9.6's Check — the banner as real HTML, the URL
  derivation, the three predicates, and `buildInboxSections` keeping an archived `closed` item in the
  `local` section — cover every *decision* those three files make about the archived state. What remains
  unasserted is wiring that reads a prop and passes it down, kept thin enough to read at a glance, with
  M8.11's e2e as the backstop.

## Log

_Append `**YYYY-MM-DD** — <what changed, files, gate result>` entries as work lands. Newest last._

**2026-09-02** — Picked up on `m8.9/archive-on-pr` (branched from the `m8.11` tip; orient gate 2998 · 1 · 0 · 111).
OQ1/OQ4/OQ5/OQ8/OQ9 decided from the repo and recorded above. The docs flipped to present tense
(`guides/local-review.mdx` §7 + its callout, `run-modes/direct.mdx`'s "Nothing is ever published" bullet,
`LOCAL_REVIEWS.md` §8 cases 21–22) — `bun run docs:build` exit 0. Plan deviations, with reasons: the predicate
lives in **`packages/shared/src/lib/local-archive.ts`** (not only revud) so the mock — the specification —
runs the same code over its own fixture pulls; **M8.9.5 shrinks to pins** because M8.5 already hashes the
whole serialized local row into the list ETag (`direct-api.ts` `reviewListEtag`), so "archiving leaves it
byte-identical" cannot be red — the negative control is the §4.1 compareKey-only hash instead; **M8.9.8
appended**: a three-transport conformance block, because archive changes the meaning of `syncPull`, the four
writes, `listPulls` and `listLocalReviews` and a contract method with no parity leg is how M8.10's ruling 1
went unimplemented twice.

**2026-09-02** — **M8.9.2 done.** `DirectStore.markLocalReviewArchived(localId, prNumber)` — write-once in
SQL (`… WHERE id = ? AND archived_pr IS NULL`), one column, `updated_at` untouched, `RangeError` before the
db on a non-positive / non-integer / local-band number, unknown id a documented no-op, `StoreWriteError`
through the shared `write()` wrapper. `store.test.ts` 178 → **188 pass** (+10, seven first-red on
`is not a function`, three allowlist self-checks green on arrival and proven by control); the raw-`Database`
`archived_pr = 4242` workaround at the one-way-door test now goes through the method; `LOCAL_STORE_METHODS`
and the surface-count literal (20 → 21) updated; `reconcile.test.ts`'s total fake store gained the member.
`DELETE FROM` allowlist pinned as a literal: `blobs, drafts, immutables, local_drafts, local_reviews,
local_reviews_submitted, local_snapshots, local_threads, local_viewed` (ten sites). **Negative controls,
observed:** a `DELETE FROM local_drafts` inside the method turned the untouched-rows case red
(`local_drafts: 2 → 0`) but left the allowlist **green** — the allowlist catches a *new table*, not a new
statement against a known one; a second control with `DELETE FROM prefs` turned the allowlist red
(`+ "prefs"`). Both reverted, diff verified. So the two guards divide the labour: the row-count case is the
one that sees a destructive archive; the allowlist sees a new eviction target.

**2026-09-02** — **M8.9.1 and M8.9.3 done** (one worker, since both write `local-archive.ts`). Predicate in
`packages/shared/src/lib/local-archive.ts`: `bareBranchName` (`refs/heads/x` → `x`, `refs/remotes/<r>/x` →
`x`, anything else untouched — deliberately not `local-sync.ts`'s display `shortRefName`, which keeps the
remote), `supersedes` (repo identity + bare head + bare base, open PRs only, blank identity or blank ref on
either side never matches), `supersedingPull` (lowest-numbered open match), and `archivedReviewRefusal` (the
one sentence every transport answers; added by the orchestrator, 22 shared tests). Detector in
`packages/revud/src/direct/local-archive.ts`: `createLocalArchiveDetector({ source?, store, warn? })`,
import-pure (only `@revu/shared`; locally declared `SupersedingPullSource` + `ArchiveStore` ports);
short-circuits on an archived row, a non-`owner/name` identity, or no source; a failing source warns
name-only and answers not-archived; the standing store value is what it reports. 20 tests including the
zero-requests case with its `fetch` double + positive control, and the purity scan with its self-checks.
Seam: `github-client.ts` `SupersedingPullClient.listOpenPullsForPair` →
`/repos/{o}/{r}/pulls?state=open&head={o}:{head}&base={base}&per_page=100` (every piece
`encodeURIComponent`-ed, plain `getJson`, no ETag) — 6 tests; `context.ts` exposes `supersedingPulls` on the
GitHub half only (the same client instance); `index.ts` `pairListingFor(client, repo)` wires it in
`mainDirect` (only when both halves exist — `--local-only` passes nothing) and `mainBroker` (from
`pollClient`). `DirectApiDeps.supersedingPulls?` added. `context.test.ts` 36 green unamended. **Negative
control observed:** `import type { GithubClient } from './github-client'` in the detector → red naming the
specifier in both purity tests; reverted; verified by `diff --no-index` against a pre-control copy. All
first reds were `Cannot find module` / `is not a function`.

**2026-09-02** — **The mock is the specification (D6), done** in `packages/app/src/api/mock/local.ts` +
`local-archive.test.ts` (new) + `store.test.ts`: `syncLocalReview` runs freeze check → detection over the
mock's own fixture pulls (`store.listEffectiveRemotes()`, `supersedingPull` from `@revu/shared`) → sync;
the four write verbs answer `archivedReviewRefusal` (submit as a `forbidden` value, the rest as
`ApiError('forbidden')`) right after `requireLocalReview`; drafts, viewed and every read stay open; a
review on `main ← fix/cache-ttl-jitter` archives to **#101** on its first sync (312/347/355/362/389/410/415
pinned as the other covered pairs; the seeded review and the conformance pair stay live). `adapter.ts`
untouched — its ETag hashes the whole item and moves on archive; pinned in `store.test.ts` with the
compare-key-only hash as the observed-red control (`notModified: true` where a 200 was due). Mock dir
134 → **149 pass**. Three extra break-observe-revert controls after the fact (refusals deleted → four reds,
one per verb; freeze deleted → two reds; detection made unconditional → the "number never moves" case red),
each revert verified byte-identical. Honest weakness recorded: the "stays archived when nothing covers the
pair any more" case is falsified only by an explicit `archivedPr = null`; its sibling with a hand-set `4242`
on a covered pair is the mutation-killing one.

**2026-09-02** — **M8.9.6 done** (`packages/app/src`, outside `api/`): `showsInInbox` in
`lib/inbox-sections.ts` (open, or a local review in any state) drives `visibleRows` and the inbox's
zero-state count (`totalListed`); predicates `reviewArchived` / `reviewComposerHidden` /
`supersededBadgeShown` in `lib/review-mode.ts`; copy `supersededBadgeCopy` (`archived · superseded by
#101`), `supersededBannerCopy` (title `Archived — superseded by pull request #101`, hint `This review is
read-only and frozen at its last sync. Every thread and draft in it is kept; nothing in it was sent to that
pull request.`) and `stateChipVariant` (a local closed chip is no longer danger-red) in `lib/mode-copy.ts`;
`archivedPrUrl` in `lib/local-reviews.ts` (`owner/name` only, else `null`); `components/review/
superseded-banner.tsx` (props-only, self-gating, `<a rel="noreferrer">` when a URL derives, the number as
text otherwise) first in `pr-layout.tsx`'s banner slot; `RowBadges` gains the badge; the review bar hides
Start-review, the composer, the verdict picker and Submit on an archived review (`writesHidden`, read once;
Discard and the pending roster stay; the daemon refusal remains the backstop for an unread row).
`chrome-sweep.test.ts` grants exactly one function (`supersededBannerCopy`) the phrase "pull request",
with four controls holding the grant narrow. App tests 825 → **906 pass** (with the mock worker's 15).
Follow-up found: `components/palette.tsx` still filtered its jump list to `state === 'open'` — closed by
the orchestrator (below).

**2026-09-02** — **Palette follow-up closed** (orchestrator): `components/palette.tsx` `paletteReviews` now
filters through `showsInInbox` (the rows offered are exactly the rows the inbox lists), so an archived
local review stays reachable from any screen. `palette.test.ts`: the closed case is now explicitly a closed
pull request, plus `an archived local review is still offered, in the local group` with the closed pull
request as its in-body control. The fix and the test landed in one edit, so the red was obtained by
control: the old `state === 'open'` filter restored → exactly that test red (`Expected - 3 / Received + 1`),
12 pass; reverted, `cmp` byte-identical to the pre-control copy, 13 pass. `.claude/launch.json` gained an
`app-dev` configuration (vite on 5173) for the `?mock=1` walk.

**2026-09-02** — **`?mock=1` walk (corroboration for M8.9.6), and the defect it found.** Created
`main ← fix/cache-ttl-jitter` → `/pr/1000000002`, chip `in review`, never synced → **Sync now** → the mock's
persisted store showed `archivedPr: 101` at once, but the page kept `in review`, no banner, Start review
present — after 16 s and beyond. Cause: `useSyncPull.onSuccess` refreshed the snapshot, draft, viewed and
rate queries but neither the pull list nor the annotations, and the pull list's 15 s poll pauses in a
background tab (the pane reported `visibilityState: 'hidden'`) while the annotation query has no interval at
all; a reload rendered everything correctly (chip `archived`, no danger tint, banner
`Archived — superseded by pull request #101` + hint, `<a href="https://github.com/meridian-labs/atlas/pull/101"
rel="noreferrer" target="_blank">`, no Start review, no Submit). **Fix (orchestrator, test-first):**
`refreshAfterLocalReviewSync(qc, prNumber)` in `state/queries.ts` — for a local review only, invalidate
`qk.pulls` + `qk.localReviews` with `refetchType: 'all'`, exactly as a create does — called from the sync
mutation's `onSuccess`. `state/local-reviews.test.ts` 6 → 8: first red `Export named
'refreshAfterLocalReviewSync' not found`; the control (a pull request's sync leaves both caches' update
counts and `isInvalidated` untouched) observed red with the mode guard removed, revert `cmp` byte-identical.
Re-walk on `main ← feat/gateway-rate-limiting` → `/pr/1000000003` → Sync now → **without a reload**: chip
`archived`, banner naming #312, link to `…/pull/312`, no Start review. Inbox: three local rows, two carrying
`archived · superseded by #312` / `#101`, zero state not triggered. Screenshots taken in-session.

**2026-09-02** — **M8.9.4, M8.9.7 done, M8.9.5 pinned** (one fable-tier worker; `packages/revud/src`).
`direct-api.ts` `syncLocalPull`: the band dispatch sits ahead of the gate; (1) the surface's new
ownership-guarded `getLocalReview` (foreign rows answer `not_found` and never reach the seam); (2) if live and
a seam exists, `detect(review)` **outside** the gate (pinned `inFlight: 0` at the ask, git calls of the same
sync see `1`); (3) `syncLocalReview` inside the gate; (4) `reclaimUnreferenced()` only when `!outcome.frozen`.
Detector built once per api (`null` with no seam). Sink: `LocalWriteStore.getLocalReview` + one
`archivedRefusal(deps, id)` helper as the **first statement** of each verb (submit → the `forbidden` value,
ahead of the head guard; reply/resolve/react → `ApiError('forbidden')`, ahead of every lookup);
`local-writes.test.ts` 127 → 142 with serialization byte-identical after each refusal and four live controls.
Fence: `'./local-archive'` in `BANNED_SIBLINGS`, control observed (`banned: ./local-archive` + the outside-set
label), 58 → 59. Freeze in `local-surface.ts`: `LocalSyncOutcome` is now a union — `{ frozen: true, snapshot,
pin: null }` when the row is archived **and the stored snapshot already reads `closed`** (a mark the snapshot
does not yet reflect — fresh detection, another daemon, a hand edit — gets exactly one catch-up sync; an
unreadable stored snapshot counts as absent, so "re-sync to rebuild" stays true); `local-resync.test.ts`
19 → 36 with the not-chosen branch (freeze deleted) observed as nine reds. New `local-archive-sync.test.ts`
(18): the two-review sweep (one match, one fork with the identical head → exactly one archived; returned
**and stored** snapshot `closed`; list row `closed`), seam absent → zero requests under the `fetch` double
with its control, closed-without-merge no-op with an open sibling archiving in the same run, a rejecting
source never fails the sync (one name-only warn), the four writes re-asserted `forbidden` after a frozen sync
with no prune, a foreign-repo row never asked. **M8.9.5 as pins:** `direct-api.test.ts` 10 → 12 (ETag
byte-identical across an unchanged list, moves on archive with every other input constant),
`direct-router.test.ts` 60 → 61 (200 → 304 bodiless → archive → the same pre-archive ETag gets a **200**
whose body still lists the review, `closed`) — green on arrival as predicted, falsified by the compareKey-only
hash (both pins red plus two existing ETag tests). Also: `local-git.test.ts` was red on the branch (its module
sweep found `local-archive.ts` unlisted) — one entry added; `bun:test` typings lack `spyOn`, so the warn is
captured by hand. Worker's suites 604 pass / 0 fail across 13 files.

**2026-09-02** — **Mock aligned to the daemon's freeze condition** (orchestrator): the mock froze on
`archivedPr !== null && snapshot exists`, the daemon on `… && the stored snapshot reads closed`; through the
api the two are indistinguishable, but a mark the snapshot does not reflect (a hand edit, another writer)
would have frozen the mock onto a snapshot still calling itself open. `local.ts` now uses the daemon's rule;
`local-archive.test.ts` gained `a mark the stored snapshot does not yet reflect earns exactly one catch-up
sync` (first red `Expected "closed" / Received "open"`; a `syncedAt`-moves assertion was dropped because two
syncs in one millisecond share it — the state flip is the proof). Mock dir 150 pass. Full gate after wave 2:
**3195 · 1 · 0 · 116** (`env -u GH_TOKEN -u GITHUB_TOKEN TZ=UTC bun run check`).

**2026-09-02** — **M8.9.8 done** (appended unit: the three-transport parity block).
`packages/shared/conformance/local-archive.ts` `runLocalReviewArchiveConformance` — 17 tests in seven steps:
live before the PR (row open, 304 replay, a draft saved), the sync that finds it (the **stored** snapshot
reads closed and the read agrees with the return; `archivedPr`; the ETag moved and the row is still listed,
closed), frozen (identical bytes, compareKey + `lastSyncedAt` unchanged, no ETag churn), the four refusals
one per test with `{snapshot, summary, draft, viewed}` byte-identical after, reads/drafts/viewed open, a
restart (same number, same closed row, byte-identical frozen snapshot, submit still refused in the same
words), sticky. Legs: E 18 → 35, F 18 → 35, G 20 → 38 (+ the runner's "asked exactly one question"); G uses a
second base branch `release/archive-conformance` at the fixture's base sha and a module-scope recording
source shared by `makeApi` and `restart`, assembled into the archive block only — the `fetch` tripwire's
`attempted` list stays empty. Observed reds: E/F with an uncovered pair → 12 of 17 red starting at
`Expected "closed", Received "open"`; G with an empty source → 13 red (the re-ask count falsifies "exactly
one question"). `bun run conformance:matrix`: A/B/E/F/G PASS, C/D SKIP. **Two findings, pinned not papered:**
`RevuApi.listPulls(opts?: { etag })` and `DirectApi.listPulls(ifNoneMatch)` are not mutually assignable
(bridged by the router today; the block carries a documented `listPulls` hook); `createLocalReview` answers
`headSha: null` on every transport (pinned as a contract fact). Full `bun test` 3248 · 1 · 0.

**2026-09-02** — **Adversarial review of the full diff (fable tier, own worktree at the tip, 21 mutations
run).** Verdict: no blocker — nothing on the local path writes to GitHub, nothing is copied, nothing
destructive; every mutation of the feature turned named tests red except two (an equivalent mutant in the
mock's double `putLocalReview`, and finding 5 below). Findings, each being fixed before the PR opens:
(1) **a malformed listing row** — the client maps a missing `number` to `0`, the lowest number wins, and
`markLocalReviewArchived(id, 0)` threw a `RangeError` out of `syncPull` (a 500) — fixed in the shared
predicate (a non-positive / non-integer number never supersedes; case-insensitive repo compare for (3)),
plus defence in depth in the detector; (2) **half-archived forever** — git failing *after* the mark (head
branch deleted once the PR was open) left the row archived, the snapshot `open`, and every sync re-failing;
fix: the catch-up sync's failure rewrites the stored snapshot's state to `closed` once and freezes at the
last successful sync; (3) **case-sensitive repo identity** — a clone URL spelled `PatMW/Revu` never
archived, silently; (4) **the bar's `forbidden` branch** rerouted an archived review's saved verdict to
COMMENT and used the 190-char sentence as a toast title — reroute gated on github mode, sentence as the
body; (5) **"one column" pinned only via `updated_at`** — a `title` rewrite stayed green; fix: the whole row
pinned before/after. Notes taken: no deadline on the one network call (a deadline added to the detector);
`state=open`'s merged-and-branch-deleted route into stuck reviews is closed by (2); `archivedPrUrl` accepted
`owner/..` (charset restricted); the guide's "`gh` login" sentence corrected to the credential boot actually
needs and to `--local-only`. Both conformance findings (the `listPulls` signature split, `headSha: null` on
create) confirmed **pre-existing on the base branch**.

**2026-09-02** — **Review findings landed** (three commits). Shared predicate: a non-positive / non-integer
number never supersedes, repo identities compare lower-cased (25 shared tests; three first reds). App: the bar
reroutes a refused submit to COMMENT only through `forbiddenSubmitRerouteAllowed(mode)` (github → true,
local → false) and toasts `{ title: 'Review not accepted', detail: result.reason }` (`review-mode.test.ts`
+5, two reds then three named source-pin reds); `archivedPrUrl` restricts each half to
`/^[A-Za-z0-9][A-Za-z0-9._-]*$/` (three reds on `owner/..`, `owner/.`, `own er/name`); the guide's §7 now
states the real precondition — the GitHub half of direct mode (an `origin` on github.com **and** a
credential: a `gh` login or `GH_TOKEN`/`GITHUB_TOKEN`), absent exactly when that half is dropped, which
`--local-only` makes droppable rather than required (my brief had said "absent under `--local-only`", which
`run-modes/direct.mdx` ~:209 and `context.ts` contradict; the worker wrote what boot does). Daemon:
`syncLocalReview` runs the catch-up sync inside a try when the row is archived over an `open` snapshot; on
failure it rewrites the stored snapshot's `pull.state` to `closed` (the only write) and freezes there — nine
tests on a real fixture with the head branch deleted after the PR appeared, first red an `ApiError
not_found` out of the range refusal; live-review and never-synced controls unchanged. Detector: a
`deadlineMs` race (default `DEFAULT_ARCHIVE_DEADLINE_MS = 10_000`, timer cleared on both outcomes, expiry
handled as a rejection — three reds, two of them the test's own 5 s timeout) and a store `RangeError` warned
name-only and answered not-archived (a plain `Error` still propagates — the control). `[pull(0)]` resolves
live, `[pull(77), pull(0)]` archives to 77 (bite proven by replacing `supersedingPull` with a bare
lowest-number pick → `Expected 77, Received null`). Store: the whole review row pinned across an archive
(`toEqual({ ...seededRow, archivedPr: 7 })`) — a `title` rewrite mutation now fails exactly that one test.
`index.ts` type imports grouped. revud 2102 · 1 · 0; app 953 · 0.

**2026-09-02** — **In Review on [#82](https://github.com/pat-mw/revu/pull/82)**, base `m8.11/conformance-e2e-docs`,
thirteen commits. Final runs on the tip: gate **3276 · 1 · 0 · 116**; `test:e2e` both drivers
`ALL CHECKS PASSED`; `conformance:matrix` A/B/E/F/G PASS. 8/8 units, `Verify` run except the scratch-repo
both-halves proof (no token this session — recorded above). Not merged; the owner merges bottom-up.

**2026-09-02 (later session)** — **The scratch-repo both-halves proof ran; the last unrun `Verify` line is
ticked.** Setup: `tmp/revu-sandbox` (a clone of `pat-mw/revu-sandbox`), `proof/archive-2026-09-02` branched off
`origin/main` with one commit (`ce1dab4`, adds `src/proof-archive.ts`), pushed; revud `--direct` from that clone
via a temporary launch entry (`REVU_PORT=4781`, temp `REVU_DATA_DIR`, `REVU_DIST_DIR` at a fresh `build:e2e`
dist, no `GH_TOKEN` in the shell so the `gh auth token` path was the one exercised). Startup line:
`mode=direct, repo=pat-mw/revu-sandbox, viewer=pat-mw`. Walk, in the served app: `New local review` →
`main ← proof/archive-2026-09-02` → id `1000000000` → `Sync now` (seal `⧗ ce1dab4 · synced just now`, 1 file
+14) → inline comment on line 4 → `Add to review` → `Submit review · 1` → toast **`Review saved` · 1 comment on
this branch**; then a pending draft on line 10 left unsubmitted. A second review `1000000001`
(`fixture/base-advances-target ← proof/…`, same head, created and synced over the API). Pre-PR state read
back: draft `null` after the submit, one thread (`local:1000000000:9000000000001`), `mutable.reviews: []`.
`gh pr create --repo pat-mw/revu-sandbox --base main --head proof/archive-2026-09-02` →
**<https://github.com/pat-mw/revu-sandbox/pull/6>** (`head.repo.full_name: pat-mw/revu-sandbox`).

**Observations, in order.** (1) `Re-sync` on the first review → toast `Snapshot updated`, chip **`archived`**,
banner **`Archived — superseded by pull request #6 · This review is read-only and frozen at its last sync. Every
thread and draft in it is kept; nothing in it was sent to that pull request.`** with `#6` linking to
`https://github.com/pat-mw/revu-sandbox/pull/6`; the thread (open, 1 comment) and the pending draft (`pending ·
only visible to you`) still render; the review bar shows `1 pending · saved · workspace · Discard` and **no**
submit, **no** verdict picker; the Conversation tab lists the thread under `Review threads`. Store:
`local_reviews.archived_pr = 6` on `1000000000` only, `local_reviews_submitted` holds review `9000000000000`
(`COMMENTED`), `local_threads = 1`, `local_drafts = 1`; the stored snapshot's `pull.state` is `closed`,
`syncedAt 17:57:24Z`. (2) The second review re-synced → `archivedPr: null`, `pull.state: open` — **OQ8 holds
live**. (3) **D1:** `gh api repos/pat-mw/revu-sandbox/pulls/6/comments` → `[]`, `…/pulls/6/reviews` → `[]`,
`…/issues/6/comments` → `[]`, `…/issues/6/reactions` → `[]`, `gh pr view 6 --comments` → nothing; the PR's
`comments: 0, review_comments: 0`. (4) Write refusals over the API: `POST …/review` → **200**
`{ status: 'forbidden', reason: 'This local review of main ← proof/archive-2026-09-02 is archived: pull request
#6 now covers the same branches, so it is read-only. Nothing in it was sent to that pull request.' }`;
reply / resolve / reaction → **403** `forbidden` with the same sentence; after all four: threads 1, comments 1,
`isResolved false`, `+1: 0`, row `updatedAt` unchanged. (5) `gh pr close 6` (`merged: false`) → the second
review re-synced again → still live; the first re-synced in the app → **still archived**, banner unchanged,
seal still `synced 3m ago` — `lastSyncedAt` did **not** move (frozen, M8.9.7). (6) Head branch deleted locally
and on the remote → `POST …/1000000000/sync` → **200**, the frozen snapshot, `requests: 0` — the "half-archived
forever" repair from the review holds against a real deletion. The inbox listed both under `LOCAL REVIEWS 2`,
the first with `1 pending · just now`, `local`, `archived · superseded by #6`, `1 unresolved`.

**Two non-defect observations, recorded rather than fixed.** (a) The inline gutter composer still opens on the
archived review and a draft `PUT` answers 200 — consistent with M8.9.4's "reads stay open" listing drafts and
with the four refused verbs, but a composer whose text can never be submitted; filed as **OQ10**, the owner's
call. (b) `mutable.reviews` is `[]` on a local review by design (`local-surface.ts` — the submitted summary
lives in `local_reviews_submitted` and is never appended); the Verify's "its submitted review" is therefore the
stored summary plus the thread it produced, and the Conversation tab has no review timeline for a local review.

**Operational notes for the next person.** The sandbox clone had `user.email` but **no `user.name`**, and
direct mode refuses to boot without both — set locally in the clone (`git -C tmp/revu-sandbox config user.name`),
left in place. The handover's "bonus" (`GH_TOKEN=$(gh auth token) bun run conformance:matrix` runs leg C live)
**cannot run**: `packages/revud/src/direct/conformance-live.test.ts` is not committed, so a token only makes the
matrix reseed the scratch repo and report `skipped-deferred` — not attempted, on purpose. A `git fetch --prune`
in the clone drops the `origin/pr/*` refs (they are `refs/pull/*/head`, fetched by the seed) — restored with
`git fetch origin '+refs/pull/*/head:refs/remotes/origin/pr/*'`. zsh's `echo` of a JSON body turns `\n` into
control characters and empties a parsed id — pipe `curl` straight into the parser. Cosmetic: picking the head
branch pre-fills the title with the branch name and typing appends to it (`proof/archive-2026-09-02Proof: …`).
Cleanup: PR #6 closed, `proof/archive-2026-09-02` deleted locally and remotely, the five fixture PRs untouched
and open, the daemon stopped, the temporary launch entry reverted. No code changed; this entry, the Verify tick
and OQ10 ride `board/m8.9-scratch-proof`.

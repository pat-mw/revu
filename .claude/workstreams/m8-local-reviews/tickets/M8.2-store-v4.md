# M8.2 — Store v4: `local_*` tables

| field | value |
| --- | --- |
| **State** | In Progress |
| **Surface** | revu:revud |
| **Kind** | Feature |
| **Depends** | M8.1 |
| **Blocks** | M8.5, M8.8, M8.10 |
| **Branch** | `m8.2/store-v4` |
| **PR** | — |
| **Assignee** | orchestrator (lane A) |

## Goal

Grow the direct-mode SQLite store a parallel, repo-scoped keyspace for local reviews — six `local_*`
tables plus the store methods that read and write them — as a purely additive v3 → v4 step in the
existing migration ladder. This exists because of **D4** (`LOCAL_REVIEWS.md §3`): the reserved id band
is a *contract-level* convenience only, and no synthetic id may ever be written to `snapshots.pr_number`,
`audit_log.pr`, or `pr_author.pr`, because the host-side collector and the broker poll loop read those
columns as real GitHub PR numbers. It also carries the repo-scoping decision from `LOCAL_REVIEWS.md §4.2`:
the store is repo-blind today, and while two repos sharing a `REVU_DATA_DIR` only collide on PR numbers
when the same number exists in both, `main` and `feature/x` collide across *every* repo — so `repo` is
part of the local key from day one. Stable-across-restart local ids (`LOCAL_REVIEWS.md §3.1`) are a store
property, delivered here via `UNIQUE(repo, base_ref, head_ref)` plus `MAX(id)+1` minting. This ticket adds
**no** `RevuApi` method and touches **no** route — the contract surface is M8.1's, the dispatch is M8.5's.

**This ticket owns the `repo` identity rule**, because it is the ticket that bakes the value into a `UNIQUE`
constraint and it lands before every consumer. The rule, recorded once here and referenced (never re-decided)
by M8.3, M8.5 and M8.9:

> `repo` is `owner/name` whenever `git remote get-url origin` resolves to a parseable GitHub-shaped remote —
> i.e. whenever `resolveRepo` (`repo.ts`) succeeds — and otherwise the absolute path
> `git rev-parse --show-toplevel` prints. It is never the empty string, and it is never a hash (M8.9's
> detection compares it against a GitHub `owner/name`, and a hash could never match).

The store is **told** the value; M8.3.1 exposes the function that computes it and M8.5.5 resolves it once at
boot. Two consequences are deliberate and must be stated wherever they surface: a workspace that gains an
`origin` after its first local review keeps the old path-shaped identity (the row is keyed on it), and archive
detection (M8.9) is therefore live exactly in the workspaces that had an origin at creation time.

## Units

Unit numbers are identity, not execution order. **M8.2.7 lands first**: it is the D4 guard rail, it needs
no local table and no store change, and a guard rail belongs in an earlier wave than the code it constrains
— written afterwards it would encode what the local writes do rather than what they must never do.
**M8.2.1 lands second** — nothing after it has tables to talk to, and it shares `store.test.ts` with M8.2.7
so the two serialize. After it, **M8.2.2, M8.2.3, M8.2.4 and M8.2.5 are mutually independent** and can run
in parallel: each adds a disjoint set of methods and its own `describe` block. Their one shared edit is the
append to the `DirectStore` interface (`store.ts:172`), so expect a trivial merge conflict there and nowhere
else. **M8.2.6 depends on all five** and on M8.2.7's harness.

Inside every unit the assertion is written and observed **red** before the code that greens it; each unit's
**Do** names the assertion that goes first and what its red looks like, because a red that only says
"function is not defined" proves less than one that reproduces the bug the assertion exists to catch.

### M8.2.1 — v4 schema: six tables, one guarded ladder step
**Do:** Bump `STORE_VERSION` 3 → 4. Add six `CREATE TABLE IF NOT EXISTS` statement constants beside
`CREATE_AUDIT_LOG` (`store.ts:599`) and `CREATE_PR_AUTHOR` (`store.ts:612`), one per table in the
`LOCAL_REVIEWS.md §4.2` DDL — `local_reviews`, `local_snapshots`, `local_threads`,
`local_reviews_submitted`, `local_drafts`, `local_viewed`. Run each in the fresh-file shape section of
`migrate` (after `store.ts:663`) **and** again inside a new `if (current < 4) { … }` guarded step (after
`store.ts:704`), exactly as v2 and v3 each do — one statement constant, two call sites, so the fresh-file
shape and the in-place step can never drift. Carry `archived_pr INTEGER` (D1) and
`dirty INTEGER NOT NULL DEFAULT 0` (D3) now even though nothing writes them until M8.9 / M8.3. Seed one
**`meta` row for the local entity-id high-water mark** in the same step (default `LOCAL_ENTITY_ID_BASE - 1`):
M8.4.2 needs a persisted, monotonic-across-restart entity-id source, comment ids live *inside* the stored
`ReviewThread` documents rather than in a column (so a `MAX(id)` scan would mean parsing every thread row),
and `meta` is the table that already carries `store_version`. Seeding it here is what keeps M8.4's minter from
becoming an unowned schema addition after v4 has shipped. No `DirectStore` surface change in this unit — the
allocator that reads and bumps the row is M8.2.2's. **Test first:** write Checks (1) and (2) below before a
single `CREATE TABLE`, and observe them red on `no such table: local_reviews`.
**Files:** `packages/revud/src/direct/store.ts`, `packages/revud/src/direct/store.test.ts`
**Check:** Four durable tests in `store.test.ts`, in one `describe` beside the migration block at
`store.test.ts:265`.
(1) **v3 → v4 in place**, modelled line-for-line on the v2 → v3 test at `store.test.ts:473`: write a draft,
a viewed row, an audit row and a `pr_author` row; close; `DROP TABLE` all six local tables, `DELETE` the
seeded high-water `meta` row, `UPDATE meta SET value = '3'`; reopen — assert the draft body reads back
verbatim, `getViewed` still reports the file viewed, `listAudit()` still has its row, `getPrAuthor` still
returns the recorded driver, `meta.store_version` equals `STORE_VERSION`,
`SELECT name FROM sqlite_master WHERE type='table'` contains all six local names, **and the high-water
`meta` row is present again at `LOCAL_ENTITY_ID_BASE - 1`** — the guarded step re-seeds it, not only the
fresh-file path, or every migrated workspace hits M8.2.2's allocator with no row to bump.
(2) **The column shape is pinned, not incidental**: `PRAGMA table_info(<t>)` for each of the six tables
yields exactly the expected column names in order, with `local_reviews.dirty` `notnull` and defaulting `0`,
`local_reviews.archived_pr` nullable with no default, and `local_reviews_submitted.review_id` declared
`INTEGER` (never `REAL` — a 9e12 id round-tripped through a float loses its low bits). This is what makes a
later column rename, a dropped default, or a drift from M8.1's `LocalReviewSummary` fail loudly instead of
silently, and it is the only durable assertion the two dead-until-M8.9/M8.3 columns can carry.
(3) **No v1–v3 table is rebuilt**: capture
`SELECT name, sql FROM sqlite_master WHERE type='table' AND name IN ('immutables','snapshots','blobs','drafts','viewed','prefs','audit_log','pr_author') ORDER BY name`
from (1)'s reconstructed v3 file **before** the reopen, capture it again after, and assert `toEqual` — no PK
altered, no table rebuilt.
(4) **The ladder step exists**: read `store.ts`'s own source
(`readFileSync(new URL('./store.ts', import.meta.url), 'utf8')`, exactly as `audit-integrity.test.ts:110-113`
already reads this file) and assert that for every `n` from `2` to `STORE_VERSION` the source contains a
`current < n` guard. Structural rather than behavioral on purpose: the fresh-file `CREATE TABLE IF NOT
EXISTS` block makes a missing ladder step behaviorally invisible (Landmines), so no runtime assertion can
catch it — and expressing it as a loop to `STORE_VERSION` means v5 inherits the guard without rewriting it.

### M8.2.2 — `local_reviews`: mint, read, list
**Do:** Add `createLocalReview`, `getLocalReview`, `listLocalReviews`, and a narrow sync-state patch
(`base_sha` / `merge_base_sha` / `head_sha` / `dirty` / `last_synced_at` / `updated_at`) to `DirectStore`.
Minting is `COALESCE(MAX(id), LOCAL_REVIEW_ID_BASE - 1) + 1`, floored at `LOCAL_REVIEW_ID_BASE` (imported
from `@revu/shared`, added by M8.1), computed and inserted **inside one `db.transaction`**; a duplicate
`(repo, base_ref, head_ref)` returns the existing row instead of minting a second
(`LOCAL_REVIEWS.md §8` case 6). Also add `nextLocalEntityId()` — read-and-bump the `meta` high-water row
M8.2.1 seeded, inside one `db.transaction`, returning a value `>= LOCAL_ENTITY_ID_BASE` that is strictly
increasing across `close()` + reopen. This is the allocator M8.4.2's port takes as an injected function and
M8.5 wires; keeping the counter durable is what makes "never re-issues a prior id" true across a daemon
restart. Every write goes through the existing `write()` wrapper (`store.ts:291`)
so a failure is a `StoreWriteError`, never swallowed. State `listLocalReviews`'s order in its docstring, as
M8.2.5 does for threads and for the same reason. **Test first:** the `>= LOCAL_REVIEW_ID_BASE` assertion is
the guard against the rowid landmine below — write it, then write the naive `INSERT` that omits `id`, and
observe it red as `1 !== 1000000000`. That red is the point; a red that only says `createLocalReview is not
a function` would not have proven the assertion catches the bug.
**Files:** `packages/revud/src/direct/store.ts`, `packages/revud/src/direct/store.test.ts`
**Check:** `bun test packages/revud/src/direct/store.test.ts` with new cases asserting: the first mint is
`>= LOCAL_REVIEW_ID_BASE` (never 1 — an auto-assigned rowid would land inside GitHub's PR-number range);
creating the same `(repo, base, head)` twice yields the same id and exactly one row; the same
`(base, head)` under two different `repo` values yields two distinct ids; ids survive `close()` +
reopen of the same data dir; `createLocalReview` against a closed database throws `StoreWriteError`. Plus:
**two independently opened stores over the same data dir** (`openDirectStore` twice, both live) creating the
same triple yield the same id and leave `SELECT COUNT(*) FROM local_reviews` at 1 — the two-process claim
the `db.transaction` + `UNIQUE` serializer makes, exercised the only way one test process can, and the case
that goes red if the mint is computed outside the transaction. Plus: the sync-state patch writes **exactly**
its named fields — `repo`, `base_ref`, `head_ref`, `title` and `created_at` read back byte-identical after
it while `updated_at` moves — and a patch against a closed database throws `StoreWriteError`. Plus:
`listLocalReviews` returns each created review exactly once in its documented order, and that order holds
across `close()` + reopen. Plus, for the entity allocator: the first value is `>= LOCAL_ENTITY_ID_BASE`,
1000 successive calls are strictly increasing and all `Number.isSafeInteger`, and a `close()` + reopen
continues above the last issued value rather than restarting at the base.

### M8.2.3 — `local_snapshots`, reusing `immutables` untouched
**Do:** Add `getLocalSnapshot(localId)` / `putLocalSnapshot(snapshot)` mirroring `getSnapshot`
(`store.ts:328`) and `putSnapshot` (`store.ts:364`): the same envelope/immutable split, the same single
`db.transaction` so a snapshot never references an immutable half that is not on disk, the same
`StoreUnreadableError` when the referenced `compareKey` is absent, and — critically — the same
preservation of the immutable row's *own* `partial` rather than clobbering it with the merged one
(`store.ts:375-397`). The immutable half lands in the existing `immutables` table keyed by
`compareKey`; `local_snapshots` holds only the envelope. **Test first:** the `partial`-preservation case in
the Check goes before the implementation and is observed red against a deliberately simplified
`putLocalSnapshot` that writes the merged `partial` onto the immutable row — that simplification is the
Landmine, and a Check that never saw it fail does not guard it.
**Files:** `packages/revud/src/direct/store.ts`, `packages/revud/src/direct/store.test.ts`
**Check:** Round-trip a local snapshot and assert `immutable.compareKey` and `files[0].filename` come
back intact; delete the `immutables` row out from under it and assert `getLocalSnapshot` throws
`StoreUnreadableError` (mirrors `store.test.ts:251`); after a `putLocalSnapshot`,
`SELECT COUNT(*) FROM snapshots` is `0` and `SELECT COUNT(*) FROM immutables` is `1`. Plus **the immutable
row keeps its OWN `partial`**, mirroring `store.test.ts:185`: `putImmutable(imm, { missingBlobShas: [],
reason: 'capped at N files' })`, then `putLocalSnapshot` of a snapshot whose envelope `partial` carries a
blob-missing reason — afterwards `getImmutable(compareKey)!.partial!.reason` is still `'capped at N files'`
and `getLocalSnapshot(id)!.partial` is the merged envelope value. Every other assertion in this unit passes
against a `putLocalSnapshot` that clobbers the immutable row; only this one goes red.
The `COUNT(*) FROM snapshots === 0` assertion is an assertion of absence and does not prove itself — its
negative control is M8.2.7's armed tripwire, which is what makes a stray write loud rather than counted.

### M8.2.4 — `local_drafts` + `local_viewed`, per-human
**Do:** Add `getLocalDraft` / `putLocalDraft` / `deleteLocalDraft` keyed `(human_id, local_id)` and
`getLocalViewed` / `setLocalViewed`, mirroring `store.ts:444-484` statement for statement. The stored
document is an unchanged `ReviewDraft` whose `prNumber` field carries the local id — no new draft type,
so `anchor.ts` and `reconcile.ts` stay untouched. **Test first:** the cross-human read goes before the
getter, and its red must be a real leak rather than a missing symbol — key the first getter on `local_id`
alone, watch `h2` read back `h1`'s body, then add the `human_id` predicate that greens it.
**Files:** `packages/revud/src/direct/store.ts`, `packages/revud/src/direct/store.test.ts`,
`packages/revud/src/draft-isolation.test.ts`
**Check:** Store-level isolation cases in `draft-isolation.test.ts`: a local draft written for `h1` reads
back `null` for `h2`; a raw
`SELECT human_id FROM local_drafts` after both writes returns only the two distinct keys and never one
human's row under the other's key; a corrupt `local_drafts` row throws `StoreUnreadableError` rather than
`null` (mirrors `store.test.ts:236`); **`deleteLocalDraft(h1, id)` removes exactly one row** — `h2`'s draft
for the same local id still reads back intact and `SELECT COUNT(*) FROM local_drafts` drops by one, which is
the assertion that keeps this store's second `DELETE` as narrow as its first. Plus, in `store.test.ts`
beside the viewed case at `store.test.ts:163`: local viewed state round-trips per `(human, local id)`, a
second human reads back `{}` (the correct empty answer, never a throw), a corrupt `local_viewed` row throws
`StoreUnreadableError`, and viewed state written under a local id is invisible to `getViewed` on the
PR-keyed table with the same number. The **HTTP** leg of isolation — a spoofed body `humanId` overwritten
by the session id, as at `draft-isolation.test.ts:178` — needs the M8.5 dispatch and is explicitly *not*
in this ticket; say so in the test's docstring rather than leaving a gap a reader mistakes for coverage.

### M8.2.5 — `local_threads` + `local_reviews_submitted`
**Do:** Add `listLocalThreads(localId)` / `getLocalThread(localId, threadId)` / `putLocalThread`, and
`listLocalSubmittedReviews(localId)` / `putLocalSubmittedReview`, keyed `(local_id, thread_id)` and
`(local_id, review_id)`. Stored documents are unchanged `ReviewThread` / `ReviewSummary`. Upsert on
conflict (a resolve rewrites the same thread row). `listLocalThreads` must have a deterministic order —
pick one (`thread_id ASC` or insertion `rowid ASC`) and state it in the docstring, because the UI renders
the list in the order it arrives. **Test first:** the upsert assertion (`COUNT(*)` stays 1 after a re-put,
and the read reflects the flipped `isResolved`) goes before the statement, red against a plain `INSERT`
that duplicates the row — the shape a resolve would otherwise leave behind twice.
**Files:** `packages/revud/src/direct/store.ts`, `packages/revud/src/direct/store.test.ts`
**Check:** Round-trip; a second `putLocalThread` for the same `(local_id, thread_id)` replaces rather
than duplicates (`SELECT COUNT(*)` stays 1) and the read reflects the flipped `isResolved`; threads for
one local id are invisible to another; a corrupt row throws `StoreUnreadableError`; the documented list
order holds across a close + reopen. The same four for submitted reviews, plus the precision pin the
Landmines demand: a summary stored at `review_id = LOCAL_ENTITY_ID_BASE + 7` reads back `===` that exact
number after a `close()` + reopen, and `SELECT typeof(review_id) FROM local_reviews_submitted` is
`'integer'` — a `REAL` column round-trips 9e12 + 7 without complaint and loses the low bits, which no
value-equality assertion on a smaller id would catch.

### M8.2.6 — the D4 containment proof + module docstring
**Do:** Extend the module docstring's table inventory (`store.ts:15-62`) with the six local tables, the
repo-scoping rationale, and the explicit statement that the local path writes neither `audit_log` nor
`pr_author` — that invisibility to the audit trail is deliberate, because local reviews are invisible to
the client repo. Add the containment sweep on top of M8.2.7's armed harness. Keep the docstring free of any
M-id or tracking reference (AGENTS.md code-style rule).
**Negative control, and it is not optional.** The sweep asserts an absence, so prove it can fail: point
`putLocalSnapshot`'s envelope insert at `snapshots` instead of `local_snapshots`, run the sweep, observe it
red naming the trigger, revert. Record the red and the restored green in the **Log**. Without that
observation a sweep that silently stopped installing its triggers reads identically to a clean one.
**Files:** `packages/revud/src/direct/store.ts`, `packages/revud/src/direct/store.test.ts`
**Check:** A test in M8.2.7's `describe` that opens a store, installs the tripwires through M8.2.7's helper,
then drives **every** local method added by M8.2.2–M8.2.5 end to end — create, mint an entity id, put and
read a snapshot, a draft, viewed state, a thread, a submitted review, patch sync state, delete a draft. It
passes only if not one statement fires a trigger, and it sits beside M8.2.7's armed assertions so a harness
that stopped arming fails in the same run rather than passing quietly. This is what fails the day someone
routes a local write into a PR-keyed table — the failure D4 exists to prevent, and the one a
`COUNT(*) === 0` assertion would miss if a row were written and then removed. It must stay green when run
alone (`bun test packages/revud/src/direct/store.test.ts -t 'local writes never touch'`), since a sweep that
only passes after another test's fixtures ran is measuring the wrong thing.

### M8.2.7 — the PR-keyed tripwire harness, armed (lands first)
**Do:** Add a `describe('local writes never touch a PR-keyed table', …)` block to `store.test.ts` with a
local helper that arms the tripwires: `close()` the store, open a raw `Database` on `direct.sqlite`, install
`CREATE TRIGGER … BEFORE INSERT|UPDATE|DELETE ON snapshots|audit_log|pr_author BEGIN SELECT RAISE(ABORT, '…'); END`
— nine triggers, three per table — close the raw handle, reopen the store. Land it with the **armed proof**:
with the tripwires installed, `putSnapshot`, `appendAudit` and `recordPrAuthor` each throw `StoreWriteError`
(the existing `write()` wrapper turns the ABORT into one), and `openDirectStore` still boots — `migrate` runs
only `CREATE TABLE IF NOT EXISTS` plus a `meta` write, so it touches no guarded table, and if a future
migration step ever writes one this boots red, which is exactly the signal wanted. The helper is a function
in the test file, not a new module: M8.2.6's sweep lives in the same file, so nothing outside the three files
this ticket owns needs to exist. **No `store.ts` change in this unit.** It lands before M8.2.1 precisely
because it constrains code that does not exist yet, and it can be written entirely against today's store.
**Observe red first** by arming the triggers on a misspelled table name and watching the three
throw-assertions fail; correct the name and record both observations in the **Log**.
**Files:** `packages/revud/src/direct/store.test.ts`
**Check:** `bun test packages/revud/src/direct/store.test.ts -t 'local writes never touch'` green, with the
three armed assertions (`putSnapshot`, `appendAudit`, `recordPrAuthor` each throw `StoreWriteError` under the
tripwires) plus the boot-under-tripwires assertion. This is the permanent negative control for every
absence-shaped Check in this ticket: "no local write reached a PR-keyed table" is worth something only while
the tripwires demonstrably still fire, and these three prove they do on every future gate.

## Verify

Additional to `bun run check`:

- `bun test packages/revud/src/direct/store.test.ts` — all new local-table cases green, including
  M8.2.1's four migration/shape tests and M8.2.7's armed tripwires.
- `bun test packages/revud/src/draft-isolation.test.ts` — the extended store-level isolation leg green,
  and the pre-existing route-table and HTTP cases **unchanged** (this ticket adds no route).
- `bun test packages/revud/src/collector/audit-integrity.test.ts` — green **unamended**. It already
  source-scans `direct/store.ts` (`:109-133`) and bans `INSERT OR IGNORE INTO` anywhere in the file, so
  every local upsert must be `ON CONFLICT … DO NOTHING`; a red here means a local statement form regressed
  the audit journal's guarantees.
- The v3 → v4 in-place migration is proved by M8.2.1's Check (1) — a file already holding a draft, a viewed
  row, an audit row and a `pr_author` row, all reading back identical after the reopen with
  `meta.store_version` at `STORE_VERSION` and the high-water `meta` row re-seeded — not by a one-time manual
  exercise, so it re-runs on every future gate.
- `SELECT name, sql FROM sqlite_master` for the eight v1–v3 tables is unchanged by the v4 open
  (M8.2.1 Check (3)), and the ladder carries an `if (current < n)` step for every `n` up to
  `STORE_VERSION` (M8.2.1 Check (4)).
- The containment sweep from M8.2.6 and the armed proof from M8.2.7 are green, and stay green run in
  isolation (`bun test packages/revud/src/direct/store.test.ts -t 'local writes never touch'`).
- **Both negative controls are recorded in the Log with their red and their restored green**: M8.2.7's
  misspelled trigger table, and M8.2.6's `putLocalSnapshot` temporarily pointed at `snapshots`. A ticket
  whose absence-assertions were never observed failing has not been verified.
- Nothing outside `packages/revud/src/direct/store.ts`, its test, and `draft-isolation.test.ts` is
  modified: `git diff --stat` lists exactly those three files (plus the ticket's own log entry).

## Context — verified seams

| anchor | what is there, and why it binds |
| --- | --- |
| `packages/revud/src/direct/store.ts:65` | `export const STORE_VERSION = 3` — the one number to bump. Every migration decision hangs off it. |
| `packages/revud/src/direct/store.ts:631` | `function migrate(db)` — the whole ladder. Its docstring (`store.ts:616-630`) *is* the migration doctrine: "never drops or reseeds a table — that would wipe drafts". |
| `packages/revud/src/direct/store.ts:638-653` | The v1 shape: `immutables(compare_key PK)`, `snapshots(pr_number PK)`, `blobs(sha PK)`, `drafts(human_id, pr_number)`, `viewed(human_id, pr_number)`, `prefs(human_id PK)`. Every one of these PKs is why the local tables exist instead of a widened key. |
| `packages/revud/src/direct/store.ts:599` / `:612` | `CREATE_AUDIT_LOG` / `CREATE_PR_AUTHOR` — the exact pattern to copy: one statement constant, called from the fresh-file shape *and* from the guarded step, "so the two paths cannot drift". |
| `packages/revud/src/direct/store.ts:691-708` | The guarded ladder — `if (current < 2)`, `if (current < 3)`, then the version stamp. The new `if (current < 4)` goes between 704 and 706. |
| `packages/revud/src/direct/store.ts:680-684` | `if (current > STORE_VERSION) return` — a v4 file opened by a v3 build is left alone, not downgraded. Local rows survive a rollback of the daemon. |
| `packages/revud/src/direct/store.ts:172-249` | The `DirectStore` interface. The local methods append here; note it is already the seam every consumer types against (`sync.ts`, `writes.ts`, `reconcile.ts`, `blobs.ts`, `write-decorator.ts`). |
| `packages/revud/src/direct/store.ts:291-297` | `write(table, fn)` — the only wrapper that turns a failed persist into `StoreWriteError`. Every new local write must go through it. |
| `packages/revud/src/direct/store.ts:328-362` | `getSnapshot` + the missing-immutable-half throw at `:341-352`. `getLocalSnapshot` mirrors it exactly, including the refusal to fabricate an empty immutable half. |
| `packages/revud/src/direct/store.ts:364-415` | `putSnapshot` — the subtle bit: the envelope carries the *merged* `partial`, the immutable row carries only its *own*. Reproducing this wrong resurrects a stale blob-missing reason on a warm compareKey reuse. |
| `packages/revud/src/direct/store.ts:444-484` | `getDraft` / `putDraft` / `deleteDraft` / `getViewed` / `setViewed` — the per-human pattern the local pair copies, down to the `${humanId}/${prNumber}` row key passed to `parseRow`. |
| `packages/revud/src/direct/store.ts:252-258` | `parseRow` — the absent-vs-unreadable mechanism. Every local getter routes its JSON through it or the distinction is silently lost. |
| `packages/revud/src/direct/store.ts:464` | The **only** `DELETE` in the entire store (`deleteDraft`). Retention (M8.10) is the second; nothing here should add a third. |
| `packages/revud/src/direct/sync.ts:283-294` | `assembleCommentAuthors` reads `store.listAudit({ pr: prNumber })` and maps `githubId → humanId`. A local id in `audit_log.pr` would put local comment ids into a GitHub comment-id keyspace. |
| `packages/revud/src/broker/poll-loop.ts:500-511` | `annotationsFor(pull)` calls `getPrAuthor(pull.number)` for every **GitHub** pull. A local id in `pr_author.pr` is a row the loop can never legitimately read — D4's second reader. |
| `packages/revud/src/broker/out-of-band-writes.ts:584` | `journal.listAudit({ pr: prNumber })` in the out-of-band-write detector — D4's third reader, and the one whose meaning ("writes that reached the client repo") a local row would falsify. |
| `packages/revud/src/direct/direct-api.ts:197` + `:275-281` | `const humanId = deps.session.human.id`, and `saveDraft` rebuilding the draft as `{ ...draft, humanId, updatedAt: now() }`. The store keys on that id; a client-supplied `humanId` never survives to the store. The local draft path must inherit this in M8.5 — the store makes it *possible*, it does not enforce it. |
| `packages/revud/src/direct/store.test.ts:473-501` | The v2 → v3 migration test. The v3 → v4 test is this test with different table names — copy its shape, including the `DROP TABLE` + `UPDATE meta` reconstruction of a genuine older file. |
| `packages/revud/src/direct/store.test.ts:236-262` | The present-but-corrupt and missing-immutable cases. Both need local twins. |
| `packages/revud/src/collector/audit-integrity.test.ts:109-133` | An existing durable test that **reads `direct/store.ts` as text** and bans `UPDATE`/`DELETE FROM`/`DROP TABLE` on `audit_log` and `INSERT OR IGNORE INTO` anywhere in the file. Two consequences: every local upsert must be spelled `ON CONFLICT … DO NOTHING` (an `INSERT OR IGNORE` turns this red from a file it does not even mention), and it is the precedent for M8.2.1's source-level ladder assertion — reading this module's own source in a test is established here, not invented by this ticket. |
| `packages/revud/src/draft-isolation.test.ts:116-135` | The route-table invariant: every path param is one of `n | sha | threadId | id`, and no path matches `/email|human|owner|user/i`. This ticket must not change it — if it turns red, a route leaked in. |
| `packages/revud/src/direct/repo.ts:16-19` | `interface RepoRef { owner, repo }` — the only repo identity revud has today, and it is GitHub-shaped and unavailable in a clone with no origin. It is the `owner/name` half of the recorded `repo` rule in **Goal**; the toplevel path is the fallback half. |
| `packages/revud/src/direct-router.ts:147-151` | `Number.isInteger(n) && n > 0` — the gate D2's reserved band was chosen to satisfy. Unchanged by this ticket; cited so nobody "helpfully" widens it. |
| `packages/revud/src/index.ts:167-171` | Boot opens the store once and migrates in place; the comment there already promises "a present-but-unreadable row throws here, failing startup loudly rather than reseeding over real drafts". A v4 step that throws breaks boot for every user, not just local-review users. |
| `packages/shared/src/api/types.ts:436-447` | `ReviewDraft` — `prNumber` is the review id, so a local draft is an unchanged `ReviewDraft` with the local id in that field. No new type. |

## Constraints that bind this ticket

- **Migration doctrine: additive only, no PK altered, no table rebuilt** (`store.ts:616-630`,
  `LOCAL_REVIEWS.md §4.2`). SQLite cannot alter a primary key without rebuilding the table, and a rebuild
  is exactly what the doctrine forbids — this is *the* reason the design rejected widening
  `snapshots(pr_number)` into `(repo, id)` and grew parallel tables instead.
- **Drafts survive everything** (`memories/hard-constraints.md`). The v3 → v4 step reads no row and
  rewrites no row; `drafts` and `viewed` must be provably untouched by the open.
- **Absent ≠ unreadable** (`store.ts:56-61`). A missing local row is `null` (the correct empty answer);
  a present row whose JSON will not parse throws `StoreUnreadableError` and is never reseeded over.
- **No swallowed write failures** (`store.ts:85-100`, `:291`). Durability is a daemon concern — a local
  write that did not reach disk surfaces as `StoreWriteError` so the daemon can answer `persist_failed`
  rather than a 200 the client trusts as saved (`durable-flush.test.ts:105-132` is that contract at the
  router).
- **D4 — the reserved band is contract-level only.** Nothing in this ticket may write `snapshots`,
  `audit_log`, or `pr_author`. Their three readers are anchored above; all three would silently
  misinterpret a synthetic id as a GitHub PR number.
- **Two-half caching** (`memories/hard-constraints.md`): local snapshots reuse `immutables` keyed by
  `compareKey = merge_base…head`, never by head SHA alone, and with no TTL.
- **Identity**: `human_id` stays the lowercase git-config email, supplied by the session and overwritten
  on write (`direct-api.ts:197`, `:275-281`). It is a store key and never a path parameter — the route
  invariant at `draft-isolation.test.ts:117` must stay green untouched.
- **The frozen contract is not touched here.** `RevuApi` and the wire types are M8.1's surface; this
  ticket is internal to revud. If a local table seems to demand a contract change, that is a signal the
  boundary was drawn wrong — report it rather than editing `packages/shared/src/api/*`.
- **AGENTS.md code style**: comments and docstrings are self-contained. No `M8.2`, no ticket id, no
  "phase" in any line of code or SQL comment. `import type` for types; no unused bindings.
- WAL + `synchronous = FULL` are already set once at open (`store.ts:285-287`); new tables inherit them.
  Do not add per-table pragmas.

## Landmines

- **`id INTEGER PRIMARY KEY` is a rowid alias.** Insert a `NULL` (or omit the column) and SQLite
  auto-assigns `1` — a value squarely inside GitHub's PR-number range, in the one table whose entire
  purpose is to stay out of it. `COALESCE(MAX(id), LOCAL_REVIEW_ID_BASE - 1) + 1` on an empty table, and
  a test asserting the first mint is `>= LOCAL_REVIEW_ID_BASE`, are the guard. Miss it and D2's band
  argument is void from the first review ever created.
- **`MAX(id)+1` outside a transaction races.** `bun:sqlite` is synchronous, but two `revud` processes can
  share one `REVU_DATA_DIR`. Compute-and-insert inside `db.transaction`, and let
  `UNIQUE(repo, base_ref, head_ref)` be the real serializer via `ON CONFLICT … DO NOTHING` followed by a
  `SELECT`. Miss it and duplicate creation mints a second review for the same branch pair — breaking the
  "creating the same review twice returns the existing one" claim in `LOCAL_REVIEWS.md §3.1`.
- **Bumping `STORE_VERSION` without adding the guarded step still appears to work**, because the
  fresh-file `CREATE TABLE IF NOT EXISTS` block at the top of `migrate` runs on every open and the stamp
  at `store.ts:706` moves the version forward regardless. The ladder step is what documents intent and
  what survives someone reordering `migrate`. Skipping it leaves a silent hole for the next migration.
- **Do not reach for `snapshots` "because the id is an integer anyway."** It is the single most natural
  wrong move in this ticket — the column accepts the value, the tests pass, and the damage surfaces
  months later in the host collector's PR attribution. The M8.2.6 trigger test exists precisely because
  this mistake is invisible to ordinary assertions.
- **`putSnapshot`'s immutable-`partial` preservation is not boilerplate** (`store.ts:375-397`). A
  simplified `putLocalSnapshot` that writes the merged `partial` onto the immutable row will resurrect a
  stale "blobs missing" reason on the next warm `compareKey` hit, after those blobs were provisioned.
- **`getSnapshot` throws on a missing immutable half** (`store.ts:341-352`). `LOCAL_REVIEWS.md §8` case 15
  wants a graceful "objects missing, re-sync to rebuild" state — that is **M8.8's** job, at a layer above.
  Do not soften the store's error here to get there early; a corrupt store must stay loud.
- **Id reuse after deletion.** `MAX(id)+1` re-mints a deleted review's id. M8.10 deletes local reviews,
  and any `local_drafts` / `local_viewed` / pinned-ref row that outlives the delete would then be
  silently adopted by an unrelated new review. See Open questions — decide before M8.10, not after.
- **Composite PK index direction.** `PRIMARY KEY (human_id, local_id)` indexes `human_id` first, so
  M8.10's "delete everything for local id N" is a full scan on `local_drafts` / `local_viewed`. Small
  today; note it in the docstring rather than adding an index nobody has measured a need for.
- **Test hygiene**: `store.test.ts` gives every test a fresh `mkdtempSync` data dir
  (`store.test.ts:30-38`). Local-table tests must do the same. (The shared-mock-`localStorage` hazard in
  `memories/known-landmines.md` does not apply here — this suite is disk-local and network-free — but
  reusing one dir across tests reintroduces the same class of order-dependent flake.)
- **`local_reviews_submitted.review_id` will carry ids from the `LOCAL_ENTITY_ID_BASE` band (9e12).**
  Well inside JS's safe-integer range and inside SQLite's 64-bit INTEGER, so no precision hazard — but
  the column must be `INTEGER`, never `REAL`, or a round-trip through a float loses the low bits.

## Testing exceptions

Two things in this ticket cannot carry a behavioral assertion. Both are named here so a later reader does
not mistake the gap for coverage.

1. **M8.2.6's module docstring.** Prose is not executable. The compensating assertion is M8.2.6's sweep plus
   M8.2.7's armed proof: the docstring's one falsifiable claim — that the local path writes neither
   `audit_log` nor `pr_author` — is exactly what the tripwires enforce, so the docstring cannot drift from
   the code without a red gate. The table inventory and repo-scoping rationale remain unverified prose, and
   `audit-integrity.test.ts`'s source scan is the only thing that reads this file mechanically.
2. **M8.2.1's guarded `if (current < 4)` step.** It is behaviorally indistinguishable from the fresh-file
   `CREATE TABLE IF NOT EXISTS` block — that is the Landmine, and it means no runtime assertion can tell a
   present ladder step from a missing one. M8.2.1's Check (4) pins it **structurally** instead, by reading
   `store.ts`'s own source, in the shape `audit-integrity.test.ts:110` already uses on this file. A
   structural pin is weaker than a behavioral one and is recorded as such: it proves the step is written,
   not that it runs.

## Rulings — settled, and what they close

Recorded here rather than by rewriting the questions below, so each override stays auditable against the text
it replaces. **Where a ruling and this ticket's text disagree, the ruling wins.**

**R-A — open question 1 is SETTLED: an archived triple is a ONE-WAY DOOR.** (Owner, 2026-08-14.) An archived
`(repo, base_ref, head_ref)` does **not** mint a successor; the archived review stays the review for that
triple. This is what the mock does today, so it costs **zero mock change** — and the mock is the specification.

> **Closes the question this ticket calls a blocker.** Read literally, open question 1 below still says it
> "must be decided before M8.2 ships". It has been. **M8.2 is not blocked on it and never was after
> 2026-08-14** — the ruling simply had not been propagated into this file. The **generation discriminator stays
> in the unique key** (`UNIQUE(repo, base_ref, head_ref, generation)`) as a no-migration escape hatch for a
> later milestone, so M8.2.1's DDL carries it and M8.2.2's mint returns the existing **non-archived** row.
> M8.9's open question 6 is closed by reference rather than reopened.

**R-B — open question 3 is SETTLED: ids come from a monotonic HIGH-WATER MARK in `meta`, never `MAX(id)+1`.**
M8.2.1 already seeds that row, so the marginal cost is one row in a table this ticket touches anyway, and it
removes a whole class of downstream bug: with `MAX(id)+1`, deleting the newest review reissues its id to the
next one, and any dependent row the delete missed silently re-attaches to a different review.

> **This does not close M8.10's open question 4**, which is about what `deleteLocalReview` must prove it
> removes. That one is still open and still gates M8.10 — but it no longer gates **this** ticket, because a
> high-water mark makes id reuse impossible regardless of how thorough the delete turns out to be.

**R-C — the builder lives in `direct/`, and dirty is `git status --porcelain -uno`.** Both are M8.3's
questions; recorded here because M8.2's `dirty` column stores the answer to the second.

## Open questions

1. **Does a create on an archived `(repo, base_ref, head_ref)` triple mint a new review?** `§8` case 22 says
   "a new local review can be created" after a PR supersedes one; `§4.2`'s `UNIQUE(repo, base_ref, head_ref)`
   plus `§3.1`'s "creating the same triple returns the existing one" says it cannot. M8.9's open question 6
   raises this and states plainly that it must be **decided before M8.2 ships** — which is this ticket, three
   merges before anyone reads M8.9. The resolutions and their cost here: widen the unique key with a
   generation or archived discriminator (a schema decision that is free now and a migration later); let a
   create on an archived triple mint a successor and re-key the old row (no schema change, but the mint path
   grows a branch); or accept the design amendment that an archived branch pair is a one-way door. Whichever
   is chosen, M8.2.1's DDL and M8.2.2's mint must both reflect it, and M8.9's open question 6 is closed by
   reference rather than re-opened.
2. **Does the local surface belong on `DirectStore`, or on a sibling `LocalStore` returned from the same
   `openDirectStore`?** Adding ~14 methods roughly doubles an interface that five modules already type
   against. A separate slice sharing the one `Database` handle would also give M8.4 a natural place to
   depend on *only* the local surface, which serves D7's structural-absence argument. The design doc does
   not address the surface shape at all.
3. **Id reuse after delete.** `MAX(id)+1` (per `§3.1`) versus a monotonic high-water mark in `meta`.
   `§8` case 23 introduces deletion but the two sections are not reconciled. Deciding for the high-water
   mark now costs one `meta` row — and M8.2.1 already seeds one for entity ids, so the marginal cost is a
   second row in a table this ticket is touching anyway — and removes a whole class of M8.10 bug; deciding
   for `MAX(id)+1` means M8.10 must prove it deletes every dependent row, including pinned refs. M8.10's open
   question 4 says plainly that it "should not be started until that is settled".
4. **Ship `archived_pr` and `dirty` in v4, or add them when M8.9 / M8.3 need them?** The `§4.2` DDL
   includes both, so v4 carrying them matches the design — but they are dead schema until those tickets
   land. An `ALTER TABLE … ADD COLUMN` later is equally additive and legal; the tradeoff is one migration
   step now versus two later. Recommendation is to ship them now (the DDL is the spec); flagging it so the
   choice is deliberate.
5. **Repo-scoping the existing PR-keyed tables** is explicitly raised and explicitly not answered by
   `§4.2` ("a separate question this milestone raises but does not answer"). Nothing in M8.2 depends on it;
   recorded so a later reader does not mistake the omission for an oversight.

## Log

_Append `**YYYY-MM-DD** — <what changed, files, gate result>` entries as work lands. Newest last._

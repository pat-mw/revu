# M8.5 — Daemon wiring: dispatch, routes, `listPulls`, boot relaxation

| field | value |
| --- | --- |
| **State** | In Review |
| **Surface** | revu:revud |
| **Kind** | Feature |
| **Depends** | M8.1, M8.2, M8.3, M8.4 |
| **Blocks** | M8.8, M8.9, M8.10, M8.11 |
| **Branch** | `m8.5/daemon-wiring` |
| **PR** | [#76](https://github.com/pat-mw/revu/pull/76) |
| **Assignee** | orchestrator |

## Goal

Join the three daemon-core pieces — the `local_*` store (M8.2), the git-only snapshot builder (M8.3), and
the local write sink (M8.4) — into a daemon that actually serves a local review over HTTP. This is where
**D5** (`LOCAL_REVIEWS.md §3`) is cashed in: `REVU_MODE` is a credential-custody + bind axis, not a
data-source axis, so local reviews arrive as a *capability inside* direct/broker mode, dispatched on the
reserved id band from **D2**, and `resolveMode` gains no fourth member. Three things change: every id-keyed
method on the direct surface branches on `isLocalReviewId(n)` before it can reach a `GithubClient`
(which is also how **D7**'s structural absence is enforced at the dispatch boundary); the create/list/delete
and branch-listing routes M8.1 added to `ROUTES` get served; and `listPulls`, a 501 in direct mode today,
returns local reviews with a `compareKey`-derived ETag and a synthetic `RateLimitInfo` per `§4.1`. The
subtle half is **boot relaxation**: `resolveDirectContext` today refuses to start without a parseable
github.com origin **and** an obtainable token **and** a successful `GET /user`, and a local-only review
needs none of the three — `buildBrokerSession` already proves a fully offline session is buildable, so the
relaxation follows an existing path rather than inventing one.

## Units

Default execution order. **M8.5.1 lands first** — it is the type every other unit dispatches through.
**M8.5.4 is fully independent of M8.5.1–M8.5.3** (it touches `direct/context.ts` + `direct/session.ts`,
they touch `direct-api.ts` + `direct-router.ts`) and can run in parallel from the start. After M8.5.1,
**M8.5.2 and M8.5.3 are independent of each other** — both append to the route-dispatch chain in
`direct-router.ts`, so expect a trivial merge there and nowhere else. **M8.5.5 needs M8.5.1 + M8.5.4**;
**M8.5.6 needs M8.5.5**; **M8.5.8 needs M8.5.1** and shares `direct-router.ts` with M8.5.2/M8.5.3, so it
serializes against them exactly as they serialize against each other; **M8.5.7 needs all seven.**

Within every unit the test is written and **observed red before the code**, and no unit is done until
`bun run check` is green. Three orderings are forced rather than preferred:

- **M8.5.1's `direct-api.test.ts` is written first**, against today's api, where it is red because
  `DirectApiDeps` has no `localReviews` and every id-keyed method reaches `deps.github`. That red is what
  proves the dispatch turned it green, rather than the fake surface.
- **M8.5.8's broker-gate case lands red immediately after M8.5.1 and before M8.5.8's router edit.** Against
  today's router a local-id write in a bot-less broker answers 501; the test asserts it is served. A gate
  fixed first and asserted afterwards encodes the fix instead of the requirement.
- **M8.5.5's fourth-mode tripwire is green the day it is written** — there is no fourth mode — so it proves
  nothing until its negative control runs. Add the member, watch it go red, revert, and record both
  observations in `## Log`. Every assertion of an absence below names its positive or negative control the
  same way.

### M8.5.1 — Band dispatch on the direct surface
**Do:** Add an optional `localReviews?: LocalReviewSurface` to `DirectApiDeps`, following the
`pullList?: PullListSource` precedent exactly (`direct-api.ts:183`, `:191-193`): a narrow interface the api
depends on for the operations only, never on the store/runner lifecycle. In `createDirectApi`
(`direct-api.ts:196`), make every **id-keyed** method branch on `isLocalReviewId(n)` (from `@revu/shared`,
added by M8.1) **before** it touches `deps.github` or the `writeDeps` bundle (`direct-api.ts:203-210`):
`syncPull`, `getSnapshot`, `getDraft` / `saveDraft` / `discardDraft`, `reconcileDraft`, `getFileViewed` /
`setFileViewed`, and all four writes. A local id with no local surface wired throws a typed `not_found`
`ApiError` — the same shape `listPulls` already uses at `direct-api.ts:227-230` — and never falls through
to the GitHub path. Three surfaces take **no** dispatch and must be left alone: `getBlob` (content-addressed,
keyed by `:sha`, and M8.2 reuses the one `blobs` table), `getPreferences`/`setPreferences`, and
`getSession`. Drafts stay keyed by `deps.session.human.id` (`direct-api.ts:197`, `:278`) on both branches.
**Test first:** `direct-api.test.ts` is written before `direct-api.ts` is touched and observed red — the dep
does not exist and every id-keyed method reaches the throwing client.
**Files:** `packages/revud/src/direct/direct-api.ts`, `packages/revud/src/direct/direct-api.test.ts` (new —
there is no such file today; the surface is currently only covered through the router and the conformance
suites).
**Check:** `bun test packages/revud/src/direct/direct-api.test.ts`. Build one api with
`throwingGithubClient()` (`direct/github-write-stubs.ts:50`) plus a fake local surface, then drive **every**
id-keyed method above with an id `>= LOCAL_REVIEW_ID_BASE`: all succeed and not one `GithubClient` method
throws. Drive the same api with `204` and assert each call reaches the GitHub path — that run is the
**positive control** for the absence above: the throwing stub fires, proving the branch is on the id rather
than on the presence of a local surface, and that the "no GitHub call" assertion is capable of failing.
Assert a local id against an api with `localReviews` omitted throws `ApiError('not_found')`, never a GitHub
call. Derive the case table from the surface rather than hand-listing it: two literal name lists — the
id-keyed methods that must dispatch, and the ones that must not (`listPulls`, `getRateLimit`, `getBlob`,
`getPreferences`, `setPreferences`; `getSession` is the router's surface, not the api's) — asserted to union
to exactly the function-valued keys of `Object.keys(api)`. A method added to `DirectApi` later then fails this test until someone classifies it,
which is the only thing that keeps "every id-keyed method" true a year from now; it is the shape M8.4.1 uses
on `Object.keys(makeLocalDeps())`.

### M8.5.2 — Create / list / branches routes (delete stays 501 here)
**Do:** Serve **three** of M8.1's four new routes in `handleDirectApi` — `POST /api/local-reviews`,
`GET /api/local-reviews`, `GET /api/branches` — inserted before the known-but-unimplemented fall-through
(`direct-router.ts:495-516`). Read
bodies through `readJsonBody` (`direct-router.ts:236`) and validate with M8.1's validators; normalize and
validate every ref with M8.3's shared `git check-ref-format` helper **before** any git command runs
(`LOCAL_REVIEWS.md §8` case 7 — a ref beginning with `-` is read by git as a flag). A duplicate
`(repo, base, head)` returns the existing review as a 200, not a 409 (`§8` case 6). Extend
`envelopeForError` (`direct-router.ts:186-234`) or make the local layer throw `ApiError` so the typed
creation failures — `base == head`, unrelated histories, shallow clone, missing ref, bad ref name — carry
contract codes instead of landing in the generic `broker_unreachable`-500 branch at `direct-router.ts:232-233`.
Map M8.3's builder error union onto whatever `ApiErrorCode` M8.1.2 settled (M8.1's open question 7).

Two boundaries are explicit, not incidental:
- **`GET /api/branches` is a git read, not a store read.** It serves M8.3.7's `listBranches` over the boot
  runner + discovered repo root. Nothing about branches is in any table, and `§8` edge case 3 requires
  remote-tracking refs the store has never seen.
- **`DELETE /api/local-reviews/:n` is deliberately left unserved by this ticket.** The store method behind it
  is M8.10.1's (`deleteLocalReview` — the store's second `DELETE`, with a transaction and per-table counts),
  and inventing a bare row delete here would collide with that contract. Until M8.10 lands the route answers
  the honest known-route **501** it already gets for free from `isKnownApiPath` (`direct-router.ts:157-175`,
  fall-through at `:495-516`) — the same mechanism that made M8.1's routes safe to add before this ticket.
  M8.10.6 takes over serving it. Exclude it from this unit's route table; a table row for a route this ticket
  does not serve is the failure mode to avoid.

**Files:** `packages/revud/src/direct-router.ts`, `packages/revud/src/direct/direct-api.ts`,
`packages/revud/src/direct-router.test.ts`
**Check:** `bun test packages/revud/src/direct-router.test.ts` with a table-driven case per **served** route
(happy path + each typed failure → its expected `{code, status}`), plus: a create body whose head ref is
`--upload-pack=/bin/sh` is rejected with a 400-level typed error and the fake runner records **zero**
invocations; creating the same triple twice returns one id and a 200 both times; `GET /api/local-reviews`
returns `dirty` and `archivedPr` on every item (M8.1's decision D-c routes both local-only annotations
through `LocalReviewSummary`, so this route is their only wire path) — **including one row that pins the
tri-state squash M8.3.6 explicitly hands to this caller and no other unit owns**: drive a local sync whose
`detectDirtyWorktree` resolves `'unknown'` (a fake runner suffices), then assert the served `dirty` is the
value decided here (proposed `false` — under-warning beats a banner that cries wolf on every degraded probe;
either answer is legal, an *implicit* answer is not), so the wire meaning of `dirty: false` — "clean, or
unknowable" — is a tested decision rather than a coercion accident that silently mutes M8.7.8's banner; and an explicit case asserting
`DELETE /api/local-reviews/<localId>` answers **501**, so the interim behavior is pinned rather than assumed
and M8.10 has a test to rewrite. And
`bun test packages/revud/src/draft-isolation.test.ts` — the route-param allowlist at
`draft-isolation.test.ts:117-128` must stay green **unamended** with the new routes in `ROUTES`.
The zero-invocation assertion carries its **positive control in the same test**: the happy-path create must
record at least one invocation on that same fake runner, so "zero" cannot pass because the runner was never
wired in. And reject a *table* of hostile refs rather than one — `--upload-pack=/bin/sh`, a bare leading
`-`, `..`, a trailing `/`, a control char — so a normalizer that only strips a leading dash is
distinguishable from one that actually calls M8.1.2's `isValidRefName`.

### M8.5.3 — `listPulls` in direct mode, local reviews merged
**Do:** Replace the `mode === 'broker'` condition on the `listPulls` branch (`direct-router.ts:295-299`)
with a **capability** gate on the api — mirroring how the broker write routes gate on
`api.brokerWritesEnabled` (`direct-api.ts:64`, `direct-router.ts:273-287`) rather than on mode — so an api
with neither a poll loop nor a local surface still answers the honest 501 it answers today. In
`createDirectApi`'s `listPulls` (`direct-api.ts:218-233`), when `deps.pullList` is absent but the local
surface is present, build the list from the local reviews: a synthesized `PullSummary` per `§4.1`'s field
table and a **complete** `BrokerPullMeta` (`shared/src/api/types.ts:278-301`) — `authorHumanId: null`,
`canApprove: true` (`§6` — there is no self-review rule to enforce), `unresolvedThreads` counted from
`local_threads`, `compareKey` from the review row, `commitCount` from the snapshot's commits, and `checks`
**omitted** (the field is optional and its absence already means "nothing has reported"). That list is
exhaustive: `BrokerPullMeta` is frozen and carries **no** `dirty` and **no** archived field, and per M8.1's
decision D-c neither is smuggled in here — both ride `LocalReviewSummary` through `listLocalReviews`
(M8.5.2), which is also why archiving cannot move this ETag on its own (M8.9.5 owns that). ETag is a stable
hash of the local reviews' `compareKey`s (`§4.1`); `rateLimit` is the synthetic large-limit / `used: 0`
shape (`§4.1`) so the honest-error copy reads "not rate limited". Honor `CONDITIONAL_LIST_304_RULE`
(`shared/src/http.ts:113-114`) exactly as `poll-loop.ts:765-788` does — on an `If-None-Match` match return
`{ items: [], etag, notModified: true, rateLimit }` and let the router emit the bodiless 304 at
`direct-router.ts:302-306`. When a poll loop **is** wired (broker mode), merge both halves and derive the
ETag from both, so a change in either invalidates.
**Files:** `packages/revud/src/direct/direct-api.ts`, `packages/revud/src/direct-router.ts`,
`packages/revud/src/direct-router.test.ts`
**Check:** `bun test packages/revud/src/direct-router.test.ts`. The existing case at
`direct-router.test.ts:361-371` is **rewritten, not deleted**: `GET /api/pulls/204/threads` still 501s,
while `GET /api/pulls` on a local-capable api is a 200 carrying an `etag` header; replaying that etag as
`If-None-Match` gives a 304 whose body is asserted empty (`expect(await res.text()).toBe('')`, not merely a
304 status) with the etag echoed; an api with neither source still 501s. A local item's
`broker.canApprove` is `true`, `broker.checks` is `undefined`, and `rateLimit.used` is `0`.
Two further cases, each guarding something named in Landmines or Open questions and otherwise asserted
nowhere: `GET /api/pulls/<localId>/threads` **also** 501s — local threads ride the snapshot's mutable half,
and a second thread read that can disagree with the snapshot is the failure mode; and the **merged**
broker+local list is exercised — an api wired with both a fake `pullList` and a local surface returns both
halves' items, mutating only the local half's `compareKey` moves the etag, mutating only the poll half's
etag moves it too, and an `If-None-Match` replay 304s only while both are unchanged. Open question 4's
hazard is an inbox that 304s forever and never shows a new PR; nothing else in this ticket would catch it.

### M8.5.4 — Boot relaxation: no origin, no token, no `GET /user`
**Do:** Give `resolveDirectContext` (`direct/context.ts:91`) a `requireGithub?: boolean` option defaulting
to `true`, so today's behavior is byte-for-byte unchanged. When `false`: a failed `resolveRepo`
(`context.ts:103-105`) is recorded as "no GitHub repo" instead of thrown as `DirectStartupError`;
`validateToken` defaults to `false` (`context.ts:116`), so the token probe is skipped; and the session is
built with `buildBrokerSession` (`session.ts:172-191`) — the proven zero-GitHub-call path — rather than
`buildDirectSession`'s `GET /user` (`session.ts:107`). Make the GitHub half of `DirectContext`
(`context.ts:25-34`) **typed-absent**, not empty-string-present. `Session.workspace` interpolates
`owner`/`repo` (`session.ts:120`) and needs a defined value with no repo. The git-identity guard stays
**hard** on this path: `MissingGitIdentityError` (`session.ts:39-48`) is the draft key, so an unset
`user.email` still refuses to start.
**Files:** `packages/revud/src/direct/context.ts`, `packages/revud/src/direct/session.ts`,
`packages/revud/src/direct/context.test.ts`
**Check:** `bun test packages/revud/src/direct/context.test.ts` with new cases modelled on the existing
offline-boot block at `context.test.ts:211-268`: a `scriptRunner` whose `origin` is `false` and whose
`ghToken` is `false`, plus a `fetchImpl` that throws on **any** call → `requireGithub: false` resolves,
`session.human.id` is the git-config email, `session.viewerLogin` is `undefined`, and the fetch was never
invoked. (`failingFetch` at `context.test.ts:59-61` returns a response rather than throwing, so it proves no
request only indirectly; a throwing, call-counting fetch makes the absence a direct assertion and is its own
control — flip `requireGithub` back to `true` in that case and it throws.) A second case: `user.email` unset
still throws `DirectStartupError` containing `git config`. Plus the two landmine assertions in the same
block: the GitHub half of the returned context is **typed-absent** (`ctx.repo === undefined`) and never
`{ owner: '', repo: '' }` — the papered-over form that builds request paths like `/repos///pulls/204` — and
`ctx.session.workspace` is a non-empty string containing neither the substring `undefined` nor the empty
`direct--` interpolation. And the whole `resolveDirectContext — refuse to start` block
(`context.test.ts:101-209`) passes **unedited** under the default.

### M8.5.5 — `mainDirect` assembly; `resolveMode` untouched
**Do:** In `mainDirect` (`index.ts:157-208`): resolve whether GitHub is required (see Open questions for
the switch), pass it into `resolveDirectContext` (`index.ts:162-165`), then **resolve the repo root once**
through M8.3.1's `discoverRepoRoot(context.runner, context.cwd)` and the repo identity through
`repoIdentity(...)`, and assemble the local surface over the already-opened store plus `context.runner` and
that **discovered toplevel** (`index.ts:170-181`) before passing it to `createDirectApi` as M8.5.1's dep.
`context.cwd` (`context.ts:167`) is a bare `process.cwd()` that nothing discovers — `§8` edge case 8 — so
handing it to the local surface writes pin refs (M8.8) and reads blobs against whichever directory the daemon
happened to start in, which is a different repository whenever the daemon is started from a subdirectory or a
linked worktree. Thread the discovered value, never the context's. If discovery fails (a bare repo, or no
repository at all), the local surface is **not assembled** and local ids answer M8.5.1's typed `not_found` —
a daemon with no repository is a daemon with no local reviews, not a daemon that pins into the wrong one.
`resolveMode` (`index.ts:62-72`) and `BootMode` (`index.ts:52`) are **not
touched** — that is D5 — and `mainBroker` gets the same local surface for free through the same
`createDirectApi` call (`index.ts:303-315`) rather than a copied boot path. Rewrite the startup line
(`index.ts:203-207`) so a local-only boot never prints `repo=undefined/undefined` or `viewer=?`.

A `main*` function is assertable only by spawning a process, so land the three boot decisions as **exported
seams** in `index.ts` and keep `mainDirect` to wiring over them — the pure-predicate move the protocol asks
for wherever a decision hides inside untestable wiring:

- `resolveGithubRequirement(argv, env)` — Open question 1's switch as a pure function of args + env.
- `resolveLocalSurfaceRoot(runner, cwd)` — the `discoverRepoRoot` + `repoIdentity` pair, resolving to `null`
  when discovery fails, so "no repository → no local surface" is a returned value rather than a
  control-flow accident buried in boot.
- `directStartupLine({ … })` — the line at `index.ts:203-207` as a pure string builder.

**Files:** `packages/revud/src/index.ts`, `packages/revud/src/local-boot.test.ts` (new)
**Check:** `bun test packages/revud/src/local-boot.test.ts`, four blocks:

1. **The D5 tripwire, with teeth.** `mode-select.test.ts:36-48` only asserts the error message names the
   three modes, so it stays green if a fourth is added — it is a regression proof, not a tripwire — and
   `grep -n "BootMode"` guards nothing after the day it runs. Assert instead that `resolveMode` **throws**
   for every plausible fourth value (`local`, `local-only`, `localonly`, `offline`, `git`) and returns
   exactly `mock` / `direct` / `broker` for the three that exist. Negative control: add a fourth member to
   `BootMode` and to `resolveMode`'s allowlist, watch this block go red, revert.
2. **The switch.** `resolveGithubRequirement` table-driven: with no flag and no env it is `true`, so today's
   boot is byte-for-byte unchanged; with whatever switch Open question 1 settles on it is `false`. If the
   answer is *automatic*, the table must carry the transient-`gh`-failure row and pin the chosen behavior,
   so the silent-degradation risk that question names is asserted here rather than discovered in a user's
   empty inbox.
3. **Root discovery.** `resolveLocalSurfaceRoot` against a fake runner whose `git rev-parse --show-toplevel`
   answers `/repo` while the cwd is `/repo/packages/app` resolves to `/repo` — the assertion today's
   `context.cwd` fails and the fix passes; the linked-worktree row (the toplevel is the worktree path, not
   its parent); and a non-zero `rev-parse` resolving to `null`, with a companion assertion that a `null`
   root means `createDirectApi` is called with no `localReviews`, so a local id lands on M8.5.1's typed
   `not_found` instead of pinning into whichever repository the daemon happened to start in.
4. **The startup line.** `directStartupLine` with an absent repo and an absent viewer contains neither the
   substring `undefined` nor `viewer=?`; with both present it equals a pinned fixture of today's line, so
   the GitHub-capable line cannot drift — `revud.test.ts:103` reads the bound port out of it with a regex,
   and a reformat there breaks every daemon-spawning suite in the package.

Plus `bun test packages/revud/src/mode-select.test.ts` green with **zero edits**, kept as the regression
proof that the three real modes still resolve. M8.5.7 carries block 3's end-to-end twin over HTTP.

### M8.5.6 — Honest degradation of the GitHub-only surfaces
**Do:** With no repo and no token wired, the GitHub-only routes must fail as *typed contract answers*, not
as stack-shaped 500s. `GET /api/rate-limit` currently goes `direct-router.ts:331-333` →
`direct-api.ts:249-251` → `deps.github.getRateLimit()`, which throws `NoTokenError` straight into
`envelopeForError`'s catch-all at `direct-router.ts:232-233`. Decide once, document in the router docstring,
and implement: either a typed `not_implemented` (501) or the synthetic rate shape. Same treatment for
`POST /api/pulls/:n/sync`, the four writes, and `GET /api/pulls/:n/snapshot` on a **non-local** `:n` — each
answers a typed code whose message says this daemon has no GitHub repository configured. Every one of these
behaviors is unchanged when a repo and token **are** present.
**Files:** `packages/revud/src/direct-router.ts`, `packages/revud/src/direct/direct-api.ts`,
`packages/revud/src/direct-router.test.ts`
**Check:** `bun test packages/revud/src/direct-router.test.ts`: a suite driven against a GitHub-less api
asserts, per route, the exact `{ code, status }` pair **and** a message matching `/no GitHub repository/i` —
an "actionable message" that is not asserted is a comment. The api's client is `throwingGithubClient()`, so
reaching GitHub is a loud failure rather than a silent pass, and the pre-existing cases in that file, run
against the GitHub-capable fake, are the standing positive control that these same routes *do* reach GitHub
when one is configured; they must not change. Classify the whole route table rather than a hand-picked list:
assert that the routes a GitHub-less daemon degrades, the routes it serves in full, and
`NOT_IMPLEMENTED_ROUTES` (`direct-router.ts:92-94`) **partition** `Object.values(ROUTES)` exactly — the same
bijection M8.1.7 pins on the contract side — so a route added later cannot quietly fall into the
`broker_unreachable`/500 catch-all at `:232-233`. Negative control: delete one route's degradation branch,
watch that row answer `broker_unreachable`/500, revert.

> **Scope widened, 2026-08-19 — M8.5.6 now also unblocks the milestone's headline exit criterion.**
> Boot assembly landed the token/viewer half of the local-only switch: inside a GitHub clone, `--local-only`
> skips both probes and serves local reviews. **A repository with no origin still refuses to start**, because
> `DirectApiDeps` declares `github: GithubClient` and `repo: RepoRef` as non-optional, so `mainDirect` must
> still funnel through the narrowing guard. The only ways to satisfy those types without a repo are a blank
> `{ owner: '', repo: '' }` stand-in — which the typed-absent design exists to forbid, since it builds request
> paths like `/repos///pulls/204` — or a cast. Neither is acceptable.
>
> Making `github` and `repo` **optional on `DirectApiDeps`** is exactly what "a GitHub-less api degrades
> honestly" means, so it belongs here rather than in a new unit. This unit therefore also drops the
> narrowing guard from `mainDirect` and adds `packages/revud/src/index.ts` to its Files.
>
> **This is a hard ordering constraint on M8.5.7**, whose entire premise is a repository with no remote: the
> offline proof cannot pass until this lands.

### M8.5.7 — The end-to-end HTTP proof, offline
**Do:** A new integration suite modelled on `revud.test.ts:80-126`: build a real temp git repo with two
branches and **no remote**, start `revud` as a child process in direct mode with a temp `REVU_DATA_DIR`, a
**stub** `REVU_DIST_DIR` (`revud.test.ts:37-55`, `:49` — the gate runs `bun test` before `vite build`, so
`packages/app/dist` does not exist during tests), and `GIT_CONFIG_GLOBAL`/`GIT_CONFIG_SYSTEM` pointed at
nonexistent paths in the child env — the daemon's own git subprocesses otherwise inherit whatever global
diff/log config the machine carries, and this suite is the one place they spawn outside M8.3.8's isolated
harness. Then drive create → sync → save draft → submit → reply →
resolve entirely over `fetch`. Include the **local twin of the HTTP draft-isolation case** that M8.2.4
explicitly deferred to this ticket: a `PUT` whose body carries a foreign `humanId` must land under the
session id (`draft-isolation.test.ts:178-190` is the shape).

Start the child through a **fetch tripwire** rather than trusting the fixture: spawn
`['bun', '--preload', <tripwire>, ENTRY]`, where `packages/revud/src/no-network-preload.ts` replaces
`globalThis.fetch` with one that throws naming the URL it was handed. Verified against this repo's Bun
(1.3.11): `--preload` installs the patch before the entry module, leaves `import.meta.main` true so
`index.ts` still boots, and patches `fetch` only — the git subprocesses this flow depends on are untouched.
That turns "no outbound network" from a property of the fixture into an assertion about the process. It is
test-support code in the shape `direct/github-write-stubs.ts` establishes: nothing in the daemon imports it.
**Files:** `packages/revud/src/local-reviews-serve.test.ts` (new; named after `broker-serve.test.ts`),
`packages/revud/src/no-network-preload.ts` (new; test-support)
**Check:** `bun test packages/revud/src/local-reviews-serve.test.ts`, green with no `gh`, no token and no
origin, under the tripwire — so any outbound request during the flow fails the suite naming the URL it
tried. Assertions, in the order the flow reaches them:

- **The three transport semantics on the local leg**, which this ticket's Constraints require and which are
  otherwise asserted nowhere locally: `GET /api/pulls/<localId>/snapshot` on the freshly-created,
  never-synced review is a **200 with a JSON `null` body**, never a 404; `POST …/sync` may resolve `partial`
  at 200; and — one extra fixture step, since `§6` keeps it live locally — amending the head commit and
  submitting the draft with its now-stale `expectedHeadSha` answers **200** with `status: 'head_moved'`,
  never an error status.
- **The deferred draft-isolation twin:** `PUT /api/pulls/<localId>/draft` carrying
  `humanId: 'victim@corp.com'` answers 200, the response `humanId` is the session id, a read-back over HTTP
  returns the body, and nothing is stored under the foreign id.
- **Degradation, not a network error:** every GitHub-only route answers the M8.5.6 typed code, and **no**
  response anywhere in the suite carries `broker_unreachable` — the code every real network failure maps to
  through `envelopeForError` (`direct-router.ts:225`, `:230`, `:233`). That is the discriminator between
  "answered honestly with no GitHub configured" and "tried GitHub and failed".
- **The list:** `GET /api/pulls` lists the created review, and a replayed etag 304s with an empty body.
- **Discovery, end-to-end** (M8.5.5 block 3's twin): the same flow with the daemon's cwd set to a
  **subdirectory** of the fixture repo, and again from a `git worktree add` linked worktree, stores the
  review under the repository's toplevel — the case that passes against `context.cwd` only when the daemon
  happens to start at the root.
- **Restart:** killing and restarting the daemon returns the same local id and the same draft body — the
  `UNIQUE(repo, base_ref, head_ref)` + `MAX(id)+1` claim from `§3.1`, proven over HTTP.

Negative controls, run once and recorded in `## Log`: point one served route at `deps.github.getRateLimit()`
and watch the tripwire fail the suite; remove one M8.5.6 degradation branch and watch the
no-`broker_unreachable` assertion go red. Revert both.

### M8.5.8 — The write path's router-level band handling (needs M8.5.1)
**Do:** Two places on the write path read or gate a review id in the **router**, above anything M8.5.1's
api-level dispatch can see. First, the broker reads-only write gate (`direct-router.ts:273-287`) matches on
the route template alone, so a bot-less broker answers 501 to `POST /api/pulls/<localId>/review` — a write
that touches no GitHub and needs no bot identity, refused in exactly the deployment the feature exists for.
Make the gate skip local ids (or run it after the band check), leaving its behavior on a GitHub id
byte-for-byte unchanged. Second, `addReaction` (`:463-488`) resolves the owning review from `?pr=` or a body
`prNumber` at `:472-478`; the band decision must read **that same resolved value**, never a second parse of
its own, or a local comment id arrives carrying a GitHub PR number and the write goes to the wrong sink —
silently, since both are positive integers.
**Test first:** the broker-gate case is written and observed **red** against today's router (which 501s it)
before this unit edits a line, per the Units preamble.
**Files:** `packages/revud/src/direct-router.ts`, `packages/revud/src/direct-router.test.ts`
**Check:** `bun test packages/revud/src/direct-router.test.ts`, three cases:

- In `mode: 'broker'` against an api with `brokerWritesEnabled: false`, all four write routes on a **local**
  id are served (200) and reach the local surface, while the same four on `204` still answer
  `not_implemented` (501) with the bot-identity message unchanged. Direct mode is unaffected on both ids.
- `POST /api/comments/<localEntityId>/reactions?pr=<localReviewId>` reaches the local sink, and the
  contradictory pairing — `?pr=` naming a local review while the body's `prNumber` names `204` — hands the
  api the same value the router validated, asserted by recording the api's **arguments** rather than by
  reading the response.
- The id survives the parse: `Number(String(id))` for an id at the top of the entity band round-trips
  exactly, pinning the `Number()` at `:466` against a band that later grows past `Number.MAX_SAFE_INTEGER`
  — the Landmines entry that says "assert the ceiling rather than assuming it" and is owned by no other unit.

### M8.5.9 — Two daemons on one data dir: the mint must wait, not fail
**Do:** `openDirectStore` sets exactly two pragmas — `journal_mode = WAL` and `synchronous = FULL` — and no
busy timeout, so two connections with an interleaved commit raise `database is locked` *before* `ON CONFLICT`
can matter. The store's own comment at the mint says so and defers the fix to whatever opens the connection.
Two changes, each where it belongs:

1. `PRAGMA busy_timeout` on the connection in `openDirectStore`, immediately after the existing pragmas. That
   alone fixes `nextLocalEntityId`, whose mint is a single autocommit `UPDATE … RETURNING` — plain
   `SQLITE_BUSY`, which a busy handler retries.
2. `createLocalReview`'s transaction runs as **`BEGIN IMMEDIATE`**, not the deferred `BEGIN` it uses today.
   The busy timeout alone is *not* sufficient there: that transaction reads (the existing-row lookup) and then
   writes, so in WAL a commit landing in between fails the write-lock upgrade with `SQLITE_BUSY_SNAPSHOT` —
   for which SQLite does **not** call the busy handler, because waiting cannot refresh a stale snapshot.
   Taking the write lock up front means the lookup's snapshot cannot go stale, and the waiting happens at
   `BEGIN` where the timeout applies. After the wait the lookup sees the winner's row and returns it, so a
   concurrent duplicate create resolves to **success returning the winner** rather than a write failure.

**Files:** `packages/revud/src/direct/store.ts`, `packages/revud/src/direct/store.test.ts`
**Check:** The existing two-handle case is sequential and never interleaves, so it cannot catch this — a new
test must actually hold a lock. Over a **file-backed** temp store (`:memory:` cannot exercise cross-connection
locking), spawn a child that opens the same database, runs `BEGIN IMMEDIATE`, signals readiness, holds briefly,
commits an insert of the **same** `(repo, baseRef, headRef)` triple, and exits. The parent's store calls are
synchronous, so the lock holder must live off-thread. The parent waits for the signal, then calls
`createLocalReview` with that triple and `nextLocalEntityId`. Assert neither throws, the create returns the
child's id, `local_reviews` holds exactly **one** row, and the entity id is above the winner's.
**Negative control**, recorded in `## Log`: without the pragma and with the deferred transaction, the parent's
call throws `StoreWriteError` wrapping `database is locked`. Observe that red, then restore both.

### M8.5.10 — The local surface factory
**Do:** `local-surface.ts` declares the `LocalReviewSurface` interface; this unit implements
`createLocalReviewSurface({ store, runner, toplevel, repo, session, now? })` behind it. The interface's
declaration and its assembly were split because the ticket as written assumed the factory existed — M8.5.5's
four Check blocks all test boot seams and none of them touches the wiring, so the factory had no owner.

Three things this unit alone gets right or wrong:

- **The write port is mapped member by member, never spread.** Two members are renamed between the store and
  the port (`putLocalReviewSummary` ← `putLocalSubmittedReview`, `nextEntityId` ← `nextLocalEntityId`) and
  three have no store source at all (`session`, `resolveHead`, `now`). A spread compiles and smuggles the whole
  store onto a port whose entire value is being a short, pinnable list of names — including a draft writer the
  port deliberately omits, the audit journal a local write must never enter, and the pull-request-keyed tables.
- **`resolveHead` does not exist yet and must be compare-scoped.** Its `commitCount` is subtracted from the
  stored compare's commit list to answer "how many commits are new", so it counts the merge base to the head
  and never the branch's whole history — otherwise a moved head reports the repository's age as new work.
- **`defaultBranch` has a declared field and a consumer but no producer**; the only supplier today is a test
  constant. It comes from the branch listing's default marker, which is derived from the origin's `HEAD`
  symbolic ref — so a repository with no origin marks nothing, which is exactly the case this feature exists
  for, and the value falls back to the empty string that the existing mappers already use for an unknown
  default branch.

**Files:** `packages/revud/src/direct/local-surface.ts`, `packages/revud/src/direct/local-surface.test.ts` (new)
**Check:** the mapping proven behaviourally (a submit lands in the submitted-review table, which a spread would
leave uncalled); the compare-scoped count asserted against a fixture whose branch length and repository length
**genuinely differ**, plus the issued argv carrying the range; `defaultBranch` present with an origin and empty
without one; the object handed to the write sink carrying **exactly** the port's member names, as the durable
guard against a future spread; `commentAuthors` carried forward across two syncs; and blob provisioning called
with the client omitted.

## Verify

Additional to `bun run check`:

- `bun test packages/revud/src/direct/direct-api.test.ts` — the band-dispatch suite, including the
  surface-classification assertion that goes red when a method is added to `DirectApi` unclassified.
- `bun test packages/revud/src/direct-router.test.ts` — new route cases, the rewritten `listPulls` case, the
  merged broker+local etag case, the degradation suite, and M8.5.8's broker-gate and reaction cases all
  green; `GET /api/pulls/204/threads` **and** `GET /api/pulls/<localId>/threads` both still 501.
- `bun test packages/revud/src/direct/context.test.ts` — the refuse-to-start block unedited and green under
  the default; the new relaxed-boot cases green.
- `bun test packages/revud/src/local-boot.test.ts` — the D5 tripwire (with its negative control recorded in
  `## Log`), the GitHub-requirement switch table, root discovery, and the startup line.
- `bun test packages/revud/src/mode-select.test.ts` — green with **no edits to the file**. If it needed an
  edit, D5 was violated. It is the regression proof; `local-boot.test.ts` is the tripwire.
- `bun test packages/revud/src/draft-isolation.test.ts` — the route-param allowlist and the HTTP isolation
  cases green, unamended.
- `bun test packages/revud/src/local-reviews-serve.test.ts` — the full loop over HTTP under the fetch
  tripwire, in a repo with no remote and no token.
- A GitHub PR on a **GitHub-capable** daemon is unaffected, asserted rather than reviewed:
  `bun test packages/revud/src/direct/conformance-broker.test.ts
  packages/revud/src/direct/conformance-broker-writes.test.ts packages/revud/src/direct/writes.test.ts
  packages/revud/src/direct/sync.test.ts` green with **zero edits to any of the four**.
- **Corroboration, not Checks** — neither guards anything after the day it runs, so both are recorded in
  `## Log` rather than relied on:
  1. The **manual offline drill** that is the milestone's headline exit criterion: `git init` a scratch repo
     with two branches and no `origin`, run `revud` directly (not through `bin/revu` — see Open questions)
     with `REVU_DIST_DIR` pointed at a built or stub dist, and complete create → sync → comment → submit in
     a browser or with `curl`. Its durable twin is `local-reviews-serve.test.ts`; the drill only adds that a
     human can drive the same loop through the real UI.
  2. `git diff --stat main -- packages/shared/src/api/` empty — the frozen contract was not edited.

## Context — verified seams

| anchor | what is there, and why it binds |
| --- | --- |
| `packages/revud/src/direct-router.ts:250-255` | `handleDirectApi(req, session, api, mode)` — the one entry point. `mode` is threaded from boot and never read from the environment; every gate in this ticket hangs off `api` or the id, not off a new mode. |
| `packages/revud/src/direct-router.ts:147-151` | `prNumberOf` — `Number.isInteger(n) && n > 0`. This is the gate D2's reserved band was chosen to satisfy. It must not be widened, narrowed, or given a local special case. |
| `packages/revud/src/direct-router.ts:92-94` | `NOT_IMPLEMENTED_ROUTES` holds **only** `listReviewThreads`. `listPulls` is not in it — it 501s in direct mode via the fall-through at `:495-516`. Knowing which mechanism 501s a route is the difference between a one-line change and a wrong one. |
| `packages/revud/src/direct-router.ts:273-287` | The broker reads-only write gate. It matches on the **route template only**, so it currently 501s a write to a *local* review id in a broker with no `REVU_BOT_LOGIN`. See Landmines. |
| `packages/revud/src/direct-router.ts:295-314` | The `listPulls` branch: gated `mode === 'broker'`, emits the ETag, and returns a bodiless 304 on a match. M8.5.3 changes the gate and nothing else about this block. |
| `packages/revud/src/direct-router.ts:186-234` | `envelopeForError` — the full typed-error map, ending in a generic `broker_unreachable`/500 at `:232-233`. Any local error that is not an `ApiError` lands there and reads as a daemon bug. |
| `packages/revud/src/direct-router.ts:361-368` | `getBlob` keyed by `:sha`, not `:n`. Content-addressed and provenance-blind, and M8.2 reuses the one `blobs` table — so this route needs **no** local dispatch at all. |
| `packages/revud/src/direct-router.ts:382-388` / `:431-433` | The draft PUT and the submit POST both reject a body whose `prNumber` disagrees with the path. A local review's draft and submit carry the local id in **both** places. |
| `packages/revud/src/direct-router.ts:463-488` | `addReaction`: `Number.isInteger(commentId) && commentId > 0` at `:467`, and the owning review taken from `?pr=` or a body `prNumber` at `:472-478`. The band check must read that same value. |
| `packages/revud/src/direct-router.ts:495-516` | The known-but-unimplemented fall-through: `isKnownApiPath` (`:157-175`) reads `Object.values(ROUTES)`, so M8.1's new routes become "known" automatically and 501 until M8.5.2 serves them. |
| `packages/revud/src/direct/direct-api.ts:183` + `:191-193` | `pullList?: PullListSource` and its one-method interface — the exact precedent for how M8.5.1's `localReviews?` dep should be shaped: the api depends on the read, not on the lifecycle. |
| `packages/revud/src/direct/direct-api.ts:203-210` | The `writeDeps` bundle. It **always** contains `deps.github`, so every one of the four writes has a `GithubClient` in scope. D7 means the local branch is taken before this bundle is consulted, and the local write module never receives it. |
| `packages/revud/src/direct/direct-api.ts:218-233` | `listPulls` — synchronous (not `async`), and throws a typed `not_found` when no source is wired. The local list must also be synchronous. |
| `packages/revud/src/direct/direct-api.ts:197` + `:275-281` | `const humanId = deps.session.human.id`, and `saveDraft` rebuilding the draft as `{ ...draft, humanId, updatedAt: now() }`. The local draft path inherits this verbatim — a client-supplied `humanId` never survives to the store. |
| `packages/revud/src/direct/direct-api.ts:64` | `brokerWritesEnabled` — the established pattern for "the router gates on an api capability, never on mode or session shape, and absence fails closed". M8.5.3's list gate copies it. |
| `packages/revud/src/direct/context.ts:97-106` | Repo resolution, step 1 of boot: any `resolveRepo` failure is an immediate `DirectStartupError`. This is the first of the three guards local-only boot must relax. |
| `packages/revud/src/direct/context.ts:115-126` | The token probe: `validateToken` defaults `true`, a `NoTokenError` becomes a `DirectStartupError`. Guard two. |
| `packages/revud/src/direct/context.ts:144-157` | The session branch — `buildDirectSession` (with `GET /user`) when validating, `buildBrokerSession` (no GitHub call at all) when not. Guard three, and the relaxation's ready-made escape hatch. |
| `packages/revud/src/direct/context.ts:25-34` | `DirectContext`: `repo: RepoRef` is non-optional and `github` is always present. Making the GitHub half absent is a type change every consumer sees. |
| `packages/revud/src/direct/context.ts:167` | `const cwd = opts.cwd ?? process.cwd()` — the repo root is never discovered (`§8` case 8). Worktrees and bare repos need `git rev-parse --show-toplevel`. |
| `packages/revud/src/direct/session.ts:172-191` | `buildBrokerSession` — the offline-session precedent this ticket leans on: identity from git config only, no GitHub request at boot, `viewerLogin` absent by design. |
| `packages/revud/src/direct/session.ts:39-48` | `MissingGitIdentityError` — `user.name`/`user.email` are still hard requirements. Relaxing GitHub must not relax this; the email is the draft key. |
| `packages/revud/src/direct/session.ts:120` | `workspace: \`direct-${owner}-${repo}\`` — interpolates the repo, so a no-repo boot needs a defined replacement rather than `direct-undefined-undefined`. |
| `packages/revud/src/index.ts:62-72` | `resolveMode` — three modes, and the error message names all three. Untouched by this ticket; `mode-select.test.ts:36-48` is the tripwire. |
| `packages/revud/src/index.ts:157-208` | `mainDirect` — the boot this ticket edits: context → store → `createDirectApi` → `startServer` → startup line at `:203-207`. |
| `packages/revud/src/index.ts:303-315` | `mainBroker`'s `createDirectApi` call. Because both mains funnel through this one factory, the local surface reaches broker mode without a copied boot path — which is precisely D5's argument against a fourth mode. |
| `packages/revud/src/server.ts:285-303` | `startServer`'s direct/broker branch — both use `createDirectFetchHandler` (`:227-243`). Nothing in this file needs to change; cited so nobody adds a mode here. |
| `packages/revud/src/broker/poll-loop.ts:765-788` | The reference `listPulls` implementation: the exact 304 semantics (`items: []`, echoed etag, `notModified: true`) the local list must reproduce. |
| `packages/shared/src/http.ts:40-70` | `ROUTES` — where M8.1's new entries land, and what `isKnownApiPath` enumerates. Read-only input for this ticket (AGENTS.md ownership). |
| `packages/shared/src/http.ts:113-114` | `CONDITIONAL_LIST_304_RULE` — the frozen wording for the one conditional surface. A local list that skips the ETag breaks the app's polling contract. |
| `packages/shared/src/api/types.ts:278-301` | `BrokerPullMeta` — every field a local item must fill legally; `checks` is optional and its absence already means "nothing reported", which is exactly true for an unpushed branch. |
| `packages/shared/src/api/types.ts:308-314` | `PullListResponse` — `items` / `etag` / `notModified` / `rateLimit`, all four required. |
| `packages/revud/src/direct/writes.ts:45-53` | `WriteDeps` carries `github: GithubClient` as a required field. The local write sink cannot satisfy this interface without a client, which is the type-level statement of D7. |
| `packages/revud/src/direct/github-write-stubs.ts:50-72` | `throwingGithubClient()` — the tool that turns "no GitHub call happened" from a comment into an assertion. Test-support only; imported by `*.test.ts`. |
| `packages/revud/src/direct-router.test.ts:361-371` | The case asserting `GET /api/pulls` is a 501 in direct mode. M8.5.3 makes this red on purpose — rewrite it, never delete it. |
| `packages/revud/src/direct/context.test.ts:211-268` | The "broker boot never probes the viewer" block — the shape M8.5.4's new cases copy, including the `failingFetch` that proves no request was made. |
| `packages/revud/src/draft-isolation.test.ts:117-128` | The route-param allowlist (`n \| sha \| threadId \| id`) and the no-`/email\|human\|owner\|user/i` rule. M8.1's new routes must already satisfy it; if this turns red on this branch, a route leaked in. |
| `packages/revud/src/draft-isolation.test.ts:178-190` | The spoofed-`humanId` HTTP case. M8.2.4 explicitly deferred its local twin to this ticket. |
| `packages/revud/src/revud.test.ts:37-55` + `:80-126` | The stub-dist helper and `startDaemon` — how a test starts the real daemon on an ephemeral port and reads the bound port out of the startup line. |
| `packages/revud/bin/revu:60-76` + `:189-191` + `:106-120` | `detect_repo` refuses a repo with no github.com origin, `cmd_open`/`start`/`restart` die on that refusal, and `start_daemon` hardcodes `REVU_MODE=broker` and passes `REVU_REPO`. Relaxing `revud` does not relax the CLI. |
| `packages/revud/src/api-router.ts:191-194` | The mock-only `/api/dev` gate. Local reviews add no dev route and must not create a second identity-selecting surface. |
| `packages/revud/src/direct/store.ts:65` | `STORE_VERSION` — M8.2 moves it to 4. This ticket must open a v4 store and never migrate anything itself. |

## Constraints that bind this ticket

- **D5 — a capability, not a mode.** `resolveMode`/`BootMode` (`index.ts:52`, `:62-72`) stay as they are;
  the local surface reaches both mains through the single `createDirectApi` factory. If this branch adds a
  fourth `REVU_MODE` value, `mode-select.test.ts:36-48` turns red and the design has been reversed.
- **D7 — no GitHub client in the local write path**, structurally. `writeDeps` (`direct-api.ts:203-210`)
  always holds one, and `WriteDeps` (`writes.ts:45-53`) requires one, so the local branch must be taken
  *above* both. A conditional inside a shared write function does not satisfy D7.
- **D2 — the reserved band satisfies the existing gates.** `prNumberOf` (`direct-router.ts:148-151`) and
  `Number.isInteger(commentId) && commentId > 0` (`:467`) are unchanged. New routes reuse only the
  allowlisted `:n` param so `draft-isolation.test.ts:117-128` passes unamended.
- **D4 — no synthetic id in the PR-keyed tables.** Dispatch must land on M8.2's `local_*` methods; a local id
  reaching `store.getSnapshot`/`putSnapshot`, `audit_log`, or `pr_author` corrupts the host collector's and
  poll loop's reading of "PR #N".
- **The frozen contract is a read-only input.** `packages/shared/src/api/client.ts` and `types.ts` (and the
  `ROUTES` table in `http.ts`) belong to M8.1. If serving a route seems to require editing them, the boundary
  was drawn wrong — report it rather than editing (AGENTS.md ownership).
- **The three transport semantics survive on the local leg** (`hard-constraints.md`, `http.ts:226-233`):
  `getSnapshot` returns a JSON `null` at 200 for a never-synced **local** review; `syncPull` may resolve
  `partial` at 200; `submitReview` returns `head_moved` as a 200 **value** — and `§6` keeps `head_moved`
  live locally, where an amend moves head with no PR event to announce it.
- **`CONDITIONAL_LIST_304_RULE`** (`http.ts:113-114`): `listPulls` remains the only conditional surface, and
  a 304 carries no body.
- **Identity**: drafts and viewed state stay keyed by the session's `Human.id`
  (`direct-api.ts:197`, `:275-281`); no route gains an identity path param; the synthesized local `GhUser`
  carries the git-config **name**, never the email (`§4.1`, `§8` case 26).
- **Durable write failures are `persist_failed` (500)**, never a 200 the client trusts as saved
  (`direct-router.ts:187-189`). The local write routes inherit this unchanged.
- **`/api/dev` stays mock-only** (`api-router.ts:191-194`, `known-landmines.md`): no local-review analogue,
  no unauthenticated state-changing route.
- **Two-half caching**: a local snapshot's immutable half is keyed by `compareKey = merge_base...head` in the
  shared `immutables` table, with no TTL and no head-SHA short-circuit.
- **The gate runs `bun test` before `vite build`**, so `packages/app/dist` does not exist during tests: any
  test that starts `revud` points at a stub dist via `REVU_DIST_DIR` plus a temp `REVU_DATA_DIR`.
- **AGENTS.md code style**: no ticket id, phase, or tracking reference in any comment or docstring;
  `import type` for types; no unused bindings; complete files, no stubs.

## Landmines

- **The broker reads-only write gate silently swallows local writes.** `direct-router.ts:273-287` matches on
  the route template alone, so in broker mode without `REVU_BOT_LOGIN` a `POST /api/pulls/1000000001/review`
  answers 501 — "this broker has no bot identity" — for a write that never touches GitHub and needs no bot
  identity at all. The gate must skip local ids (or run after the band check). Miss it and local reviews are
  reads-only in exactly the deployment — a contractor's disposable workspace — the feature exists for, with an
  error message that sends the reader hunting for a GitHub App.
- **Dropping the `mode === 'broker'` guard on `listPulls` changes the answer for an api with no list source.**
  Today `GET /api/pulls` 501s in direct mode via the fall-through (`:495-516`); a naive removal routes it to
  `api.listPulls`, which throws `ApiError('not_found')` (`direct-api.ts:227-230`) → a **404**. Gate on a
  capability flag instead, the way `brokerWritesEnabled` does, so the honest 501 survives for a daemon that
  genuinely serves no list.
- **`envelopeForError` has no branch for the local errors.** `direct-router.ts:186-234` maps eight typed
  errors and then falls to `broker_unreachable`/500 at `:232-233`. A "shallow clone", "unrelated histories",
  or "bad ref name" that is not an `ApiError` renders in the UI as an unreachable-broker 500 — the single
  most confusing possible message for a feature whose entire premise is that there is no broker.
- **Widening `DirectContext.repo` to nullable ripples.** `createDirectApi({repo})`, `runSyncPull`,
  `provisionBlobs`, and the poll loop all take a `RepoRef`. Papering over the absence with
  `{ owner: '', repo: '' }` builds request paths like `/repos///pulls/204`, which 404 against GitHub with a
  message that blames the PR. Make the absence typed.
- **A relaxed boot must not relax the git-identity guard.** `MissingGitIdentityError` (`session.ts:39-48`)
  is what keys drafts and viewed state. Softening it so the daemon "starts anywhere" files every human's
  drafts under one blank id — and the store cannot tell them apart afterwards.
- **`bin/revu` refuses before `revud` is ever executed.** `detect_repo` (`bin/revu:60-76`) returns non-zero
  for a repo with no github.com origin and `cmd_open` / `start` / `restart` (`:189-191`, `:251`, `:255`) die
  on it; `start_daemon` (`:106-120`) additionally forces `REVU_MODE=broker` and passes `REVU_REPO`. The
  milestone's headline exit criterion is not reachable through the CLI on this branch — plan the drill
  against `revud` directly, and see Open questions.
- **`listPulls` merging local reviews is NOT the PR listing M8.9 needs.** `§8` case 21 says archive detection
  needs a **GitHub** pull listing that direct mode does not have; this ticket adds a **local** one. The board
  records M8.5 as blocking M8.9, and it does — but only for the routing, not for the detection input. Do not
  let a green `listPulls` here read as "M8.9's data source exists".
- **`listReviewThreads` must stay 501.** Local threads ride the snapshot's mutable half exactly as GitHub
  threads do, and no app component calls the method (it exists in the adapters only). Implementing it
  "because the data is local now" grows a second thread read that can disagree with the snapshot.
- **The `?pr=` reaction param is the band input.** `direct-router.ts:472-478` takes the owning review from
  the query or the body; if the dispatch reads a different value than the router validated, a local comment
  id arrives with a GitHub PR number and the write goes to the wrong sink — silently, since both are
  positive integers.
- **Locally minted entity ids sit at 9e12.** Well inside `Number.MAX_SAFE_INTEGER` (~9.007e15) and inside
  SQLite's 64-bit INTEGER, so no precision hazard today — but the router parses ids with `Number()`
  (`direct-router.ts:466`), so an id band that ever grows past the safe range silently rounds. Assert the
  ceiling rather than assuming it.
- **The 501 test at `direct-router.test.ts:361-371` will turn red**, correctly. Rewriting it to keep proving
  the threads route still 501s is the point; deleting the case lets the daemon grow a surface nobody asserts.
- **Boot must stay loud about a corrupt store.** `index.ts:167-171` opens and migrates before anything else,
  and its comment already promises a present-but-unreadable row fails startup rather than reseeding over
  drafts. Boot relaxation is about GitHub preconditions only — never about store errors.

## Testing exceptions

One claim this ticket makes is asserted only indirectly, and is better named than quietly implied.

- **"The daemon made no outbound network request"** is not directly observable from outside the process.
  M8.5.7's `--preload` fetch tripwire is the closest durable approximation — it fails the suite the moment
  anything in the daemon calls `globalThis.fetch` — but it does not cover a raw socket, a `Bun.connect`, or
  a call made by a subprocess the daemon spawns. Three compensating assertions cover the claim that matters
  (D7), each durable and each failing independently: M8.4's import-graph proof that no `GithubClient` is
  reachable from the local write path at all; M8.5.1's dispatch proof over `throwingGithubClient()`; and
  M8.5.7's assertion that no response in the offline flow carries `broker_unreachable`, the code every real
  network failure maps to. The first of those is the structural enforcement; the tripwire is belt to its
  braces, not the proof itself.

Every other unit in this ticket carries a durable automated Check. Nothing here is exempt on UI grounds —
this ticket touches no component.

## Open questions

1. **How is the local-only capability switched on?** A `--local-only` flag, an env var, or automatically
   (relax whenever repo/token resolution fails)? Automatic is friendliest and riskiest: a transient `gh`
   failure inside a genuine GitHub clone would silently boot a daemon that can only do local reviews and
   shows an empty inbox, which reads as data loss. An explicit switch is loud but adds a knob and a docs
   line. `§3` D5 says local reviews are a capability inside direct/broker but never says how the capability
   is enabled. **Owner or implementer decides before M8.5.4** — it determines the option's shape. Whatever
   the answer, it is pinned by M8.5.5's `resolveGithubRequirement` table, including the transient-failure row
   if the answer is automatic.
2. ~~**What string is `repo` with no origin?**~~ **Settled by M8.2**, which owns the rule because it bakes the
   value into `UNIQUE(repo, base_ref, head_ref)` and lands three merges earlier: `owner/name` when an origin
   parses, else the absolute `git rev-parse --show-toplevel` path. M8.3.1 exposes `repoIdentity(...)`; M8.5.5
   calls it once at boot. Nothing in this ticket re-decides it.
3. **What does `GET /api/rate-limit` answer on a local-only daemon?** `§4.1` pins a synthetic `rateLimit`
   *inside* `PullListResponse` so honest-error copy reads "not rate limited", but says nothing about the
   standalone route. 501 is more honest; the synthetic shape keeps the rate-chip code path uniform. M8.7
   suppresses the chip on a local review, but the chip is a global surface, not per-review — so the two are
   not the same decision. Either answer is pinned by M8.5.6's per-route `{ code, status }` table.
4. **How do a poll loop's items and local reviews combine?** `§4.1` gives the local ETag as a hash of the
   local `compareKey`s and stops there. Ordering and ETag composition for the merged broker list are
   unspecified; getting the composition wrong yields an inbox that 304s forever and never shows a new PR.
   M8.5.3's merged-list case asserts the property the answer must have — a change in either half moves the
   etag — without prescribing the composition.
5. **What is the module seam M8.5 dispatches into?** M8.3 and M8.4 name no module paths, and M8.2's Open
   question 2 asks whether the store's local surface hangs off `DirectStore` or a sibling. A single
   `LocalReviewSurface` assembled from the store slice + the git builder + the write sink would give D7 a
   clean structural home (the local write module receives a store slice and a `CommandRunner`, and there is
   no `GithubClient` in its import graph to receive). This is M8.5's central type — decide it before M8.5.1.
6. **Does `bin/revu` gain a local-only path?** Out of scope as the ticket is scoped (`Surface: revu:revud`),
   but the milestone exit criterion "a local review can be created … in a repo with no GitHub remote" is not
   reachable through the command a contractor actually types until it does. Either widen the scope here,
   open a follow-up, or state explicitly that the criterion is proven against `revud` directly.
7. ~~**Do local reviews belong in `listPulls` at all?**~~ **Settled by M8.1's decision D-a**: yes, and the mock
   is the spec for it. `PrLayout` resolves a review only out of that list (`pr-layout.tsx:280-282`), so the
   alternative renders the "isn't in this installation" screen on every local review. The real behavior change
   for existing direct-mode users — an inbox that is non-empty for the first time — is accepted deliberately;
   `listLocalReviews` stays for management surfaces and for the two local-only annotations (D-c).

## Log

_Append `**YYYY-MM-DD** — <what changed, files, gate result>` entries as work lands. Newest last._

**2026-08-19** — Ticket picked up. **OQ5 settled** (the ticket's central type): the seam is a single
`LocalReviewSurface` in a new `packages/revud/src/direct/local-surface.ts`, assembled from the store, a
`CommandRunner`, the **discovered** toplevel, the repo identity and the boot `Session`. Each method mirrors the
sync/async shape of the `DirectApi` method it backs, so band dispatch is one branch per method.
`deleteLocalReview` is deliberately absent — the store delete belongs to retention.

**OQ1 settled — an explicit switch, never automatic.** Automatic relaxation on a failed repo/token probe means
a transient `gh` failure inside a genuine GitHub clone boots a daemon that can only do local reviews and shows
an empty inbox, which reads as data loss. **OQ3 settled — `GET /api/rate-limit` degrades to the `501`
`not_implemented` convention the router already uses in three places.** Note `not_implemented` is **not** an
`ApiErrorCode` and has no `HTTP_STATUS_BY_CODE` entry; adding it to the frozen union would be a §5.2 stop, so
the raw-501 convention is the answer rather than a union member. **OQ4 settled** — the merged ETag hashes both
halves so either moving invalidates; ordering is broker items in poll order, then local by id descending.
**OQ6 settled** — `bin/revu` gains no local-only path here; the headline exit criterion is proven against
`revud` directly, and the CLI path is a follow-up rather than a silent absorption.

**Finding 3 was checked and is NOT already satisfied, contrary to a first reading.** `LocalReviewIdentity`
does declare `defaultBranch`, and `synthesizeLocalPullDetail` consumes it as the synthesized pull's
`repo.default_branch` — but the **only** thing that supplies a value is a test constant. No production code
sets it. The wiring sources it from `listBranches`, whose `isDefault` marker comes from
`git symbolic-ref refs/remotes/origin/HEAD`. **A repository with no origin therefore marks nothing**, which is
exactly the local-only case, so the value falls back to the empty string — the spelling the two existing
GitHub mappers already use for an unknown default branch.

**M8.5.9 appended** (not absorbed) for the two-daemon mint, per the finding handed to this ticket. It is
sequenced last so it cannot destabilize the join.

**2026-08-19** — **M8.5.4 landed** (`5bfdf38`) — 2453 pass · 1 skip · 0 fail · 91 files.
`resolveDirectContext` gained `requireGithub` (default `true`, so the existing boot is byte-for-byte
unchanged); `DirectContext` became a discriminated union so the GitHub half is absent by type rather than
blank. Red observed first on all 7 new cases. The `requireGithub: true` control is a separate test with a
byte-identical setup and only the flag restored — it was green in the red run and stayed green after, so the
relaxed case's pass is attributable to the flag rather than to the fixture. A counting fetch that **throws**
on any call replaces the existing one that merely returns a failure response, making "no request" a direct
assertion. The identity guard is untouched and still refuses an unset `user.email`.
*Ripple, which the ticket's own Units preamble denied:* five reads of `context.repo` and two of
`context.github` in `index.ts`, folded behind one exported total narrowing function.

**2026-08-19** — **M8.5.1 landed** (`369af01`) — 2458 pass · 1 skip · 0 fail · 92 files. Band dispatch is the
first statement of every id-keyed method, above the write bundle that always carries a GitHub client. Red
observed first: a band id fell through to `github.getPullDetail`, and `getSnapshot` **returned** a value from
the pull-request table instead of throwing.
*Two corrections to the plan, both from the implementer:* only **five** of the twelve id-keyed methods ever
reach GitHub on the pull-request branch — the other seven are store-only on both branches — so the positive
control was split rather than asserted of all twelve, and all twelve are additionally asserted to leave the
local surface unconsulted on a pull-request id, which is what proves the branch is on the **id** and not on the
dep's presence. And the four writes had to become `async`: they returned a promise without being async, so the
band error would have escaped **synchronously** and slipped past every awaiting caller's `try`/`catch`.

**2026-08-19** — **M8.5.9 landed** (`6a837ae`) — 2460 pass · 1 skip · 0 fail · 92 files. Red observed first:
both new cases threw `StoreWriteError … database is locked`.
*Two controls, and the second is the one that matters.* Control A (both changes reverted) reproduced the
original red. **Control B — busy timeout restored, transaction left deferred — was run to test whether the
second change is redundant, and it is not:** the entity mint passed and the run's wall time went 291ms → 793ms,
proving it genuinely waited out the holder, while the review mint still failed, because a deferred
transaction's read snapshot goes stale and no amount of waiting refreshes it. The design claim is measured
rather than argued. Revert verified by SHA-256 against a post-fix snapshot, after this project damaged
unrelated code three times with anchored reverts.
*Outside the brief:* `bun-env.d.ts`'s hand-written `bun:sqlite` declaration had `db.transaction` returning a
bare function, so `.immediate` did not type-check. Widening it also closes a finding previously recorded as
unowned.

**2026-08-19** — **M8.5.2 landed** (`4a281ba`) — 2474 pass · 1 skip · 0 fail · 92 files. Red observed first:
all twelve new router cases answered 501. The hostile-ref table asserts **both** that no argv was issued and
that the surface was never reached, so a refusal cannot pass because the argv seam blocked it *after* the
value had already been handed onward; the happy path on the same harness records three invocations, which is
what makes the zero meaningful. `draft-isolation.test.ts` stayed green unamended.
*Flagged and acted on:* the tri-state dirty squash **cannot live in the router** — the summary field and the
store column are both boolean, so the collapse happens in the factory, and this unit's test sits downstream of
that write. The constraint was relayed to the factory unit mid-flight rather than discovered at integration.

**2026-08-19** — **M8.5.10 landed** (`d239db6`) — 2528 pass · 1 skip · 0 fail · 93 files. A module-load red
proves nothing per-test, so each required check was falsified by **mutating the finished implementation** —
ten mutations, each with a named red. The spread mutation put 41 members on a 10-member port; the
branch-scoped revs mutation returned `newCommits: 12` where 0 was due.
*The fixture was rebuilt rather than reused:* the shared one has a 3-commit branch in a 5-commit repository, a
margin of one and too small to be convincing, so a 12-commit trunk with a 2-commit branch was built —
compare-scoped **2** against branch-scoped **14**. The argv is asserted over **shape** (a single rev containing
`..`) rather than as a blocklist of `--all`/`HEAD`/branch-name, because a blocklist of three spellings passes
on the fourth.

**2026-08-19** — **M8.5.5 landed** (`eb1eb5c`) — 2563 pass · 1 skip · 0 fail · 94 files. Switch spelling:
`--local-only` / `REVU_LOCAL_ONLY`, flag winning over the variable as `--repo` already does, and an
unrecognized value **refused** rather than read as "off" — the same silent degradation the explicit switch
exists to prevent.
*The negative control measured what the ticket only claimed:* with a fourth mode added the new tripwire went
red **and `mode-select.test.ts` stayed green (12 pass, 0 fail)** — direct confirmation it is a regression proof
rather than a tripwire, because its message assertion still finds all three names inside a message naming four.
Revert verified by SHA-256 against a pristine copy plus a re-read of both control sites.
*Blocked half, recorded above as M8.5.6's widened scope:* a repository with no origin still refuses to start.

**2026-08-19** — **M8.5.3 landed** (`fd5aca1`) — 2568 pass · 1 skip · 0 fail · 94 files. The list is gated on
`pullListEnabled`, mirroring the broker write capability. The landmine guard pins **both** that a
source-less api still answers 501 **and** that its typed not-found is genuinely reachable, so the 501
assertion cannot be satisfied by accident. Both ETag halves were falsified by mutation — dropping either
returns a 304 where a 200 is due, which is the inbox that never shows a new pull request.
*Corrections to the brief:* `BrokerPullMeta` also requires `assignedReviewerHumanIds` (set `[]` — no reviewer
can be assigned where there is no pull request), and the synthetic allowance is a large limit rather than
GitHub's 5000, which would be a fabricated reading of a bucket this list never spent from; merged mode keeps
the poll source's live allowance.

**2026-08-19** — **M8.5.8 landed** (`c462105`) — 2571 pass · 1 skip · 0 fail · 94 files. Red observed before
the router edit, as the ordering requires: `POST /api/pulls/<localId>/review` on a reads-only broker answered
501. The unchanged-message proof reads an **independent literal written in the test**, never the router's own
output, and three pre-existing suites hold the same line. The contradictory pairing recorded
`{ prNumber: <localId>, commentId: <entityId> }` — asserted on the api's **arguments**, since a response can
look right while the wrong number went downstream.
*Restraint worth keeping:* the query/body strictness asymmetry was **not** tightened. Single-binding the
resolution satisfies the requirement; tightening would turn today's accepted `prNumber: 0 | -5 | 1.5` into a
400, a behaviour change outside this unit.

**2026-08-19** — **M8.5.6 landed** (`5b7057c`) — 2577 pass · 1 skip · 0 fail · 94 files. Both halves: the api's
GitHub deps became optional (absence stays typed — no blank owner/name, no cast), and the GitHub-bound routes
degrade to a typed answer naming the missing repository.
*The prior behaviour was worse than this ticket described.* Not seven catch-all 500s — `getSnapshot` returned a
**200 with a JSON `null` body**, telling the reader to sync something that could never sync, and
`replyToThread` returned a **404 blaming the thread**. Both are harder to diagnose than the 500 they were
assumed to be.
*Partition measured, not asserted:* 7 degrade + 14 served + 2 unimplemented = **23**, compared equal to the
route table itself. No route needed a fourth bucket, confirming that serving the local-review routes is what
made the partition well-defined. Negative control: deleting one degradation branch dropped that row to
`broker_unreachable`/500 **and** independently broke a second bare-api case, so the branch is load-bearing for
both api shapes. Revert by byte-exact copy + SHA-256, never an anchored string edit.
*Deliberate:* the internal narrowing guards throw **plain** errors, so a hole in the router's gate reads as a
loud failure rather than a plausible-looking contract answer. The pre-existing GitHub-capable cases are the
standing positive control and were **not edited** — both test diffs are purely additive.

**2026-08-19** — **The manual offline drill, run by the orchestrator in the main tree** (corroboration, not a
Check; its durable twin is M8.5.7). Scratch repository in a temp dir, two branches, **no remotes at all**,
`GH_TOKEN`/`GITHUB_TOKEN` unset, stub dist, temp data dir, `revud --direct --local-only`:

- **It started**, printing `mode=direct` with **no** `repo=`, **no** `viewer=`, and no `undefined` — the
  absent halves omitted rather than placeholdered.
- `GET /api/branches` → **200**, both local branches, `isDefault: false` on both (correct: no origin, so
  nothing is marked, which is the case the wire type's "exactly one carries true" docstring gets wrong).
- `POST /api/local-reviews` → id **1000000000**, the bottom of the reserved band; `repo` recorded as the
  **toplevel path**, the documented fallback when no origin parses; both refs normalized to fully qualified.
- `POST …/sync` → **200**; `GET …/snapshot` → **200**; `GET /api/pulls` listed it, with the synthesized pull's
  user carrying the git-config **name** (`Drill`), never the email.
- `GET /api/rate-limit` and `GET /api/pulls/204/snapshot` → **501** `not_implemented`, each naming the missing
  repository.

**The milestone's headline exit criterion is reachable and was observed directly.** Not completed in the
drill: the draft and submit legs — the orchestrator's hand-written request bodies omitted `headSha` and
`expectedHeadSha`, which the validators correctly refused with typed 400s. That is a drill-payload error, not a
defect, and those legs are M8.5.7's durable proof rather than worth hand-crafting twice.

**2026-08-19** — **M8.5.7 landed** (`6f52c96`) — 2605 pass · 1 skip · 0 fail · 95 files. The whole loop over
HTTP against a real child daemon and a real remote-less repository, under a `--preload` tripwire that replaces
`fetch` with one throwing the URL it was handed. **No production code changed**, which is the right outcome:
everything under test was supposed to already work.
*Nine assertion groups falsified by breaking what each guards*, each reverted from a byte-exact copy verified
by diff and SHA-256 — never an anchored string revert.
*Two claims it could NOT falsify, and said so rather than claiming them:* a populated `partial` is unreachable
across a process boundary (`git diff --numstat` reads the object and exits 128 in both constructions tried, so
the sync refuses outright instead of degrading) — asserted where a runner can be substituted instead; and the
304's empty body is unfalsifiable from the server side, because the transport strips a 304 body before the
client sees it. Both are labelled at their assertion sites.
*Two false-negative controls found, and the second is a real gap:* removing the draft re-key from the api layer
alone stayed green because the surface layer re-keys independently — genuine defence in depth, but it means
neither layer's own test would catch the other's loss. And **dropping `putLocalThread` from submit left
everything green**, because within one daemon lifetime the verbs read threads off the snapshot envelope, while
`syncPull` rebuilds that envelope from the durable table — so losing the write would silently drop a submitted
comment on the *next* sync. Handed to the pre-merge review as a named lead.

**2026-08-19** — **`Verify` GREEN.** 171 pass · 0 fail across the seven Verify suites; 92 pass · 0 fail across
the four that must be untouched — and `git diff` against the branch point confirms all four **are** byte-
unchanged, so "a GitHub pull request is unaffected" is asserted rather than reviewed. `mode-select.test.ts`
unchanged, so the mode axis held. `packages/shared/src/api/` unchanged, so the frozen contract was not edited.
`draft-isolation.test.ts` changed **prose only** — no assertion moved. Full diff: 26 files, 7792 insertions.
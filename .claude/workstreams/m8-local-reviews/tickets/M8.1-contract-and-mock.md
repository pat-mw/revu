# M8.1 — Contract additions + the mock as the spec

| field | value |
| --- | --- |
| **State** | In Review |
| **Surface** | revu:shared, revu:app |
| **Kind** | Feature |
| **Depends** | none |
| **Blocks** | M8.2, M8.3, M8.4, M8.5, M8.6, M8.7 |
| **Branch** | `m8.1/contract-and-mock` |
| **PR** | [#70](https://github.com/pat-mw/revu/pull/70) — base `m8/local-reviews-design` (#69), retargets to `main` when #69 merges |
| **Assignee** | — |

## Goal

This ticket **is** the specification for local-only reviews. It implements decision **D6** (`LOCAL_REVIEWS.md`
§3): the mock is the permanent oracle, its semantics *are* the contract, and revud conforms to it afterwards
— never the reverse. Concretely it lands three things: the reserved id bands of **D2** (§3.1) in
`packages/shared` with their disjointness argument encoded as tests; the smallest possible additive extension
to `RevuApi` + `ROUTES` (`listBranches`, `createLocalReview`, `listLocalReviews`, `deleteLocalReview`) using
only allowlisted `:param` names so no invariant test needs amending; and a working local-review implementation
in the mock adapter, reachable under `?mock=1` with no network. Because D2 keeps review identity a positive
integer, **every existing `RevuApi` signature, every existing route, both routers' integer gates, the `/pr/:n`
route family and every React Query key stay byte-identical** — this ticket adds, it never edits. It gates all
of M8.2–M8.7: the store tables, the git snapshot builder, the write sink, the daemon dispatch and the two app
tickets all conform to what lands here.

## Units

Default execution order below, with one deviation the doctrine forces: **M8.1.7 lands first**, because it is
the guard rail for M8.1.4 and a guard written afterwards encodes what the code does rather than what it must
never do. **M8.1.1, M8.1.2 and M8.1.7 are independent of each other** and can run in parallel (different files,
no shared symbol). **M8.1.3 depends on M8.1.1 and M8.1.2**; **M8.1.4 depends on M8.1.3 and M8.1.7**;
**M8.1.5 and M8.1.6 both depend on M8.1.4** and are independent of each other. The decomposition is chosen so
that every unit leaves `bun run check` green on its own — the frozen-contract widening (M8.1.4) is deliberately
*one* commit because it breaks compilation at four sites simultaneously (see Landmines).

Within every unit the test is written and **observed red before the code**. For M8.1.4 that red is M8.1.7's
route-surface bijection: it goes red the instant `RevuApi` widens and green only when both adapters and the
router are filled in, which is exactly the window the atomic commit spans. Every Check below names a file and
an assertion that runs on each future gate; `tsc -b` and `git diff` appear only as corroboration, never as a
unit's Check.

### M8.1.1 — Id bands + the disjointness proof
**Do:** Add `LOCAL_REVIEW_ID_BASE`, `LOCAL_ENTITY_ID_BASE` and `isLocalReviewId` exactly as declared in
`LOCAL_REVIEWS.md` §3.1, in a new `packages/shared/src/api/ids.ts`, re-exported from
`packages/shared/src/index.ts`. Write the disjointness argument as executable assertions, not prose: real
GitHub PR numbers (≤ 6 digits) are below the review band; GitHub comment/review ids (~1e9–3e9) are below
`LOCAL_ENTITY_ID_BASE`; the mock's `ID_BASE = 700_000_000` band is below both; the app's optimistic synthetic
ids are negative and therefore below every band; `LOCAL_ENTITY_ID_BASE` plus a plausible mint count stays
under `Number.MAX_SAFE_INTEGER`. Include the *namespace* caveat as a test with a comment explaining it: the
two bands are **not** mutually exclusive by value (`isLocalReviewId(LOCAL_ENTITY_ID_BASE)` is `true`), so
`isLocalReviewId` is only meaningful when applied to a **review id**, never to a comment id.
Two of those facts are about *other packages*' constants and `@revu/shared` is a leaf that cannot import them,
so asserting them in `ids.test.ts` only pins a copied literal — the exact drift the tests exist to stop. Add
the cross-package half on the app side against the **real** value: `export` the existing `ID_BASE` from
`packages/app/src/api/mock/store.ts` (a one-line export; M8.1.3 edits that file later in sequence, never
concurrently) and assert the band relation against the import.
**Files:** `packages/shared/src/api/ids.ts` (new), `packages/shared/src/api/ids.test.ts` (new),
`packages/shared/src/index.ts`, `packages/app/src/api/mock/store.ts` (export `ID_BASE`),
`packages/app/src/api/mock/id-bands.test.ts` (new).
**Check:** `bun test packages/shared/src/api/ids.test.ts` passes, and includes an assertion that
`isLocalReviewId(415) === false`, `isLocalReviewId(1_000_000_001) === true`,
`isLocalReviewId(700_000_042) === false`, `isLocalReviewId(-1) === false`,
`isLocalReviewId(1_000_000_000.5) === false` (the `Number.isInteger` half of the predicate, which a
`>=`-only implementation would pass), and `LOCAL_ENTITY_ID_BASE + 1_000_000 < Number.MAX_SAFE_INTEGER`.
**Plus** `bun test packages/app/src/api/mock/id-bands.test.ts`, which imports `ID_BASE` from the mock store
(never a literal) and asserts `ID_BASE < LOCAL_REVIEW_ID_BASE` and `ID_BASE < LOCAL_ENTITY_ID_BASE` — so
raising the mock's band into the local one fails the gate instead of silently making every mock-minted id
answer `true` to `isLocalReviewId`.

### M8.1.2 — Local-review wire types, validators, and the pure ref-name validator
**Do:** Add the local-review wire types to `packages/shared/src/api/types.ts` (broker-shaped section, below
`BrokerPullMeta`): `BranchRef`, `CreateLocalReviewInput`, `LocalReviewSummary`. Derive `LocalReviewSummary`
field-for-field from the `local_reviews` columns in `LOCAL_REVIEWS.md` §4.2 so M8.2's table and the wire type
cannot drift — **including `dirty` and `archivedPr`**, which are the two local-only annotations decision D-c
(Open questions) routes through this type rather than through the frozen `BrokerPullMeta`. The `repo` field
carries whatever string M8.2's recorded repo-identity rule produces; this ticket does not re-derive it.
Mirror each with a `v*` validator in `packages/shared/src/http-validators.ts` and export the
transport-level `validate*` wrappers alongside the existing ones. Add a **pure, syntactic** ref-name validator
(`isValidRefName` / `normalizeRefName`) that rejects a ref beginning with `-`, rejects the shapes
`git check-ref-format` rejects that are checkable without a subprocess (empty, `..`, ` ~^:?*[`, trailing `/`
or `.lock`, control chars), and normalizes a bare branch name to `refs/heads/…`. `@revu/shared` is
dependency-free and has no `node:child_process`, so the real `git check-ref-format` call belongs to the daemon
(M8.3/M8.5) — this unit ships the syntactic half both sides import. **Settle the creation-error codes here**
(Open question 7): `ApiErrorCode` (`types.ts:538`) has no `invalid`/`bad_request`, and M8.3's builder produces
`same_ref`, `unrelated_histories` and `shallow_clone` with no home. If the answer is a new code, it is added to
the union, to the `code` literal list inside `validateHttpErrorBody` (`http-validators.ts:745-756`) and to
`HTTP_STATUS_BY_CODE` (`http.ts:162`) **in this unit** — nowhere else may touch that union. `tsc` ties the
union to the status map (`Record<Exclude<ApiErrorCode, 'network'>, number>`) but **nothing** ties either to the
validator's literal list, so a new code compiles green and is then rejected on the wire as a `ValidationError`;
close that with the sync test below.
**Files:** `packages/shared/src/api/types.ts`, `packages/shared/src/http-validators.ts`,
`packages/shared/src/http.ts` (the status map only — `ROUTES` is M8.1.4's),
`packages/shared/src/http.test.ts` (new), `packages/shared/src/lib/refs.ts` (new),
`packages/shared/src/lib/refs.test.ts` (new), `packages/shared/src/index.ts`,
`packages/app/src/api/http-contract.test.ts`.
**Check:** three durable assertions, each in a named file.
1. `bun test packages/shared/src/lib/refs.test.ts` — `isValidRefName('--upload-pack=/bin/sh') === false`, one
   case per rejected shape (empty, `..`, each of ` ~^:?*[`, a control char, trailing `/`, trailing `.lock`)
   so a validator that rejects everything and one that rejects only the leading `-` are distinguishable, plus
   `normalizeRefName('feature/x') === 'refs/heads/feature/x'` and an already-qualified ref returned unchanged.
2. `bun test packages/app/src/api/http-contract.test.ts` — all three new wire types round-trip losslessly:
   `validateBranchRef`, `validateCreateLocalReviewInput` and `validateLocalReviewSummary` each `toStrictEqual`
   their own `JSON.parse(JSON.stringify(x))` input, with `dirty: false` and `archivedPr: null` **present as
   keys** in the summary case (a validator that omits a key from its shape strips it silently, and `strictEqual`
   over the wire form is the only thing that catches it). Plus one tampered-field case per type asserting
   `ValidationError`, mirroring that file's existing "the validators actually validate" block.
3. `bun test packages/shared/src/http.test.ts` — the three copies of the error-code union cannot drift: for
   every key of `HTTP_STATUS_BY_CODE` **plus** `'network'`, `validateHttpErrorBody({ code, message: 'm' })`
   returns it unchanged; an unknown code throws `ValidationError`; and `statusForApiError` throws for
   `network`. This assertion is owed whether or not Open question 7 adds a code — it is what makes the answer
   safe either way.

### M8.1.3 — Mock local-review engine (not yet on the contract)
**Do:** Build the local-review machinery inside the mock as ordinary module functions, before any interface
changes: local review records in the mock store (bump `STORE_VERSION` 3→4 **with a migrate-in-place step**,
never a reseed — see Constraints), local id minting from `LOCAL_REVIEW_ID_BASE`, local entity id minting from
`LOCAL_ENTITY_ID_BASE`, and the synthesizers that produce a `PullDetail` / sentinel `GhUser` / `ReviewSummary`
/ `ReviewThread` per the field table in `LOCAL_REVIEWS.md` §4.1. The sentinel `GhUser` carries a display
**name**, `type: 'Bot'`, empty `avatar_url`/`html_url` — **never an email**. Local submit materializes threads
with **positive** ids and does **not** call `prefixBody`: locally there is one author and the store holds the
real `Human.id`, so stamping is a GitHub-only concern (§6, "No stamping"). **Mint thread ids in the shape
Open question 8 settles** — this unit is where that shape becomes the spec, because D6 means M8.4 conforms to
whatever ships here rather than the reverse. Minting also answers Open question 4, and duplicate creation on
the same `(repo, baseRef, headRef)` must return the **existing** review (§8 edge 6) so the mock and M8.2 agree.
Per decision **D-b** (Open questions), a submitted local
`ReviewSummary` is persisted as a local record but is **not** appended to `snapshot.mutable.reviews`: that
array stays `[]` on a local review forever, so §7's "issue comments and submitted reviews are always empty" is
true after the first submit as well as before it. Keep the functions exported from a
new `packages/app/src/api/mock/local.ts`; nothing imports them yet except their test.
**The store's migration is untestable as it stands** — `load()` runs once at module import, takes no input,
and is not exported, so no test can hand it a `v: 3` document. Before bumping the version, extract the
parse-and-migrate step into an exported pure function (`migrateStoreDocument(parsed: unknown): StoreShape |
null`, `null` meaning "corrupt / unknown version, reseed") that `load()` calls, and test that function rather
than the module singleton. Order it so the red is real: write the migration test **first**, against a `v: 2`
document and today's behaviour — it cannot even resolve its import until the seam exists — then extract the
seam with no behaviour change to make it green, and only then bump to v4 and extend the test. A migration
test authored after the bump proves only that the bump is self-consistent.
**Files:** `packages/app/src/api/mock/local.ts` (new), `packages/app/src/api/mock/local.test.ts` (new),
`packages/app/src/api/mock/store.ts`, `packages/app/src/api/mock/store.test.ts`.
**Check:** `bun test packages/app/src/api/mock/local.test.ts` passes with `mockDev.reset()` in `beforeAll`
(the store is one process-wide document — see Landmines); it asserts a synthesized `PullDetail`
satisfies `vPullDetail` unchanged, that the synthesized `GhUser` contains no `@`, and that over a loop of
mints every minted review id and entity id is a **positive** safe integer at or above its own band (one
sample proves nothing about a counter that wraps or rebases). Plus the stamp assertion **with its positive
control in the same test**: no local comment body matches the `**Name** (role)` stamp shape, *and* the same
matcher applied to `prefixBody(human, 'x')` output **does** match — otherwise a typo'd regex passes the
absence assertion vacuously forever. Plus a case pinning D-b: after a submit, `snapshot.mutable.reviews` and
`snapshot.mutable.issueComments` are both still `[]` while the submitted summary is readable from the local
record. Plus idempotent creation: creating the same `(repo, baseRef, headRef)` twice returns the same id and
leaves exactly one record.
**And** `bun test packages/app/src/api/mock/store.test.ts` — `migrateStoreDocument` on a hand-built `v: 3`
document containing a draft returns a `v: 4` document with that draft still readable, and (the negative
control, durable and in-file rather than a break-and-revert) the same document with a core field deleted
returns `null`, proving the function can still refuse rather than waving everything through.

### M8.1.4 — The contract extension (one atomic commit)
**Do:** Add the four methods to `RevuApi` (`packages/shared/src/api/client.ts`) and the four entries to
`ROUTES` (`packages/shared/src/http.ts`), then satisfy every site the widening breaks, in the same commit:
the mock adapter (delegating to M8.1.3's engine), the HTTP fetch adapter, and revud's mock-mode
`api-router.ts` switch. Proposed route spellings — `GET /api/branches`, `POST /api/local-reviews`,
`GET /api/local-reviews`, `DELETE /api/local-reviews/:n` — use **only** `:n`, which is the whole point of D2.
Document on each new `ROUTES` entry what its body carries, in the style of the existing entries. M8.1.7's two
guards are already in the tree: the bijection goes **red** the moment `RevuApi` grows and stays red until both
adapters carry the four methods, which is this unit's test-first observation, and the dispatch sweep must be
green over all 23 routes before the commit closes. Extend `packages/app/src/api/mock/local.test.ts` with the
four adapter-level cases in the Check — the engine is tested in M8.1.3, but nothing yet asserts the four
contract methods behave.
**Files:** `packages/shared/src/api/client.ts`, `packages/shared/src/http.ts`,
`packages/app/src/api/mock/adapter.ts`, `packages/app/src/api/http/adapter.ts`,
`packages/revud/src/api-router.ts`, `packages/app/src/api/mock/local.test.ts`.
**Check:** `bun test packages/app/src/api/mock/local.test.ts` covers all four methods through
`createMockApi()`: `listBranches` resolves a `BranchRef[]` whose every element satisfies `validateBranchRef`
and in which **exactly one** entry carries the default marker (Open question 3's shape, pinned here for both
transports); `createLocalReview` resolves a `LocalReviewSummary` whose id satisfies `isLocalReviewId`;
`listLocalReviews` contains that summary with `dirty` and `archivedPr` present; `deleteLocalReview` removes it
from `listLocalReviews` at whatever boundary Open question 5 settles, asserted explicitly either way.
**And** `bun test packages/revud/src/draft-isolation.test.ts packages/revud/src/broker/token-custody.test.ts`
passes unamended — the two independent invariant tests that pin the `:param` allowlist. **And** M8.1.7's two
guards pass over 23 routes with no edit to either file. `bunx tsc -b --force 2>&1 | head -40` clean is
corroboration: the exhaustiveness guard at `packages/revud/src/api-router.ts:375` proves at compile time that
a handler exists for every route, but it cannot see route **shadowing**, which is a runtime property of
`matchRoute`'s segment-count-and-insertion-order matching — that is what the sweep exists for.

### M8.1.5 — A fixture local review reachable under `?mock=1`
**Do:** Seed one local review into the fixture set so the demo build shows the feature with no network and no
git: a `base ← head` pair against `fixtureDB.repo`, a synthesized snapshot built through the same
`buildSnapshot` path the PR fixtures use, and no `checks` / `issueComments` / `reviews` (all legal empty
states per §4.1). Per decision **D-a** (Open questions) the fixture **does** appear in `listPulls`, so the two
count assertions in `scenarios.test.ts` move in this commit deliberately; it also appears in
`listLocalReviews`, which carries the local-only annotations D-c routes there.
**Files:** `packages/app/src/fixtures/local/` (new module),
`packages/app/src/fixtures/local/local-review.test.ts` (new), `packages/app/src/fixtures/contract.ts`,
`packages/app/src/fixtures/index.ts`, `packages/app/src/api/mock/store.ts` (seed path).
**Check:** `bun test packages/app/src/api/mock/scenarios.test.ts` passes with the two count assertions at
`scenarios.test.ts:61` and `:64-67` updated **deliberately in this commit** — `toHaveLength(11)` and the exact
pull-number array extended with the fixture's local id, both still hardcoded pins. Neither may be loosened
into a computed length or a `toContain`: they exist to make an unintended change to the fixture set fail.
The same reconciliation is owed to `scripts/smoke.ts:54-61`, which is in no gate (M8.11.2 owns that half —
see Testing exceptions). **Plus** `bun test packages/app/src/fixtures/local/local-review.test.ts`, asserting
against `fixtureDB` rather than a browser: the fixture local review is registered in `fixtureDB.pulls`, its
`pull.number` satisfies `isLocalReviewId`, its snapshot built through `buildSnapshot` satisfies
`validateSnapshot` over the wire form (`toStrictEqual` its `JSON.parse(JSON.stringify(x))`), and
`mutable.checks` / `issueComments` / `reviews` are each `[]`. `bun run build:app` — already inside
`bun run check` — is corroboration that the fixture module survives into the production bundle; the demo's
HTTP-freedom is pinned by `selectApi`'s existing unit test and by M8.1.6, not by opening a browser.

### M8.1.6 — The local-review scenario walk + the three frozen semantics on a local id
**Do:** Add a mock-backed walk that drives the whole loop against a local review id: create → sync → save a
draft with an inline comment → submit → reply → resolve, with `mockDev.reset()` in `beforeAll` (mandatory —
the mock store is one process-wide document shared across every `bun test` file). Assert the three frozen
transport semantics explicitly **on the local path**, because M8.4/M8.5 will conform to whatever this asserts:
`submitReview` with a stale `expectedHeadSha` returns `{ status: 'head_moved', … }` as a resolved **value**
and never throws; `getSnapshot(localId)` on a created-but-never-synced local review resolves `null`, not a
thrown `not_found`; a synced local snapshot carries `partial: null` as a present key (never omitted, never
`undefined`) so `vSnapshot` still validates it. Run the whole walk with `globalThis.fetch` replaced by a
throwing stub (the stub-and-restore pattern `packages/app/src/api/http/adapter.test.ts:23-27` already uses),
restored in `afterAll` — that is the in-gate, durable form of Verify 4's network-tab observation.
**Files:** `packages/app/src/api/mock/local-scenario.test.ts` (new).
**Check:** `bun test packages/app/src/api/mock/local-scenario.test.ts` passes with `mockDev.reset()` in
`beforeAll`, asserting on the local id: `head_moved` arrives as a resolved value (the assertion is on the
returned object, and any throw fails the test); `getSnapshot(localId)` before the first sync resolves `null`;
and the synced snapshot carries `partial` as an **own key** — `Object.prototype.hasOwnProperty.call(snap,
'partial')` **and** `validateSnapshot(JSON.parse(JSON.stringify(snap)))` `toStrictEqual` the wire form,
because a key whose value went `undefined` disappears in serialization and a `=== null` check alone would
not survive the round trip that actually matters. The no-network assertion carries its **positive control**:
one direct call to the installed stub asserting it throws, so a walk that passes because the tripwire was
never armed cannot be mistaken for a walk that passes because nothing called `fetch`. **And**
`bun test packages/app/src/api/mock/conformance.test.ts packages/app/src/api/http/conformance.test.ts` is
green **unchanged** — the existing conformance suite must not need a single edit.

### M8.1.7 — Route-table guard rails (lands before M8.1.4)
**Do:** Land the two assertions that make M8.1.4's widening provable, **before** M8.1.4 writes a line. Both
pass on today's 19 routes; both are what goes red while the widening is half-done. First, a **route-surface
bijection**: `ROUTES`'s own docstring claims "every `RevuApi` method maps to exactly one route, keyed by
method name" and nothing enforces it — assert `Object.keys(ROUTES)` is exactly the set of function-valued keys
on **both** implementations (`createMockApi()` and `createHttpApi('http://x')`), and pin the 19 existing
`method + path` pairs as a literal table so an edit or deletion inside `ROUTES` fails the gate forever. That
table is the durable form of Verify 2's `git diff` eyeball: additive-only becomes an assertion instead of a
review habit. Second, a **mock-mode dispatch sweep** in revud: build a `MockBundle` whose `api` is a recording
proxy — the pattern `packages/revud/src/dev-gate.test.ts:41-59` already establishes — and drive
`handleApi(new Request(fillPath(route.path, { n, sha, threadId, id })), mock, 'mock')` for every `ROUTES`
entry, asserting each request reaches **its own** handler: the recorded `api` call equals the route name, or —
for the handlers that validate the request body before touching `api` — a 400 from that route's own validator.
What must never appear is the `No route for` envelope, or a *different* route's recorded call. Neither file
spawns a daemon, so neither needs `REVU_DIST_DIR`.
**Files:** `packages/app/src/api/route-surface.test.ts` (new), `packages/revud/src/api-router.test.ts` (new).
**Check:** both files pass over the 19 routes that exist before M8.1.4, **and** both are proven falsifiable by
negative controls run once and recorded in `## Log` — the sweep asserts an absence (`No route for` never
appears) and the bijection asserts a set equality that is trivially true the day it is written. Controls:
(a) append a second route sharing an earlier route's method, segment count and literal prefix → the sweep goes
red naming the shadowing route, which is the `api-router.ts:133` landmine caught for the first time;
(b) delete one method from the mock adapter → the bijection goes red; (c) change one character of an existing
`ROUTES` path → the pinned table goes red. Revert all three.

### M8.1.8 — Local-id dispatch in the mock adapter (appended 2026-08-13; runs before M8.1.5 and M8.1.6)

**Why this unit exists.** M8.1.4 discovered that the engine M8.1.3 built is **unreachable through the
contract**: `requireRemote()` (`packages/app/src/api/mock/adapter.ts:87`) is called at five sites — `:270`,
`:366`, `:426`, `:458`, `:495` — and throws `not_found` for any id with no remote pull, which is every local
review. So `syncPull` / `submitReview` / `replyToThread` / `resolveThread` all reject a local id today, and
**M8.1.6's walk is unsatisfiable as written** (its Files line names only its own test file). This is a missing
unit inside this ticket's own Goal — "a working local-review implementation in the mock adapter, reachable
under `?mock=1`" — not new scope, so it is appended and numbered rather than absorbed into another unit or
escalated as §5.8 growth.

**Do:** Dispatch every `RevuApi` method that takes a review id to the local engine when `isLocalReviewId(n)`,
**above** the `requireRemote()` lookup. The branch is taken above the remote resolution, never as a
conditional inside a function that has already resolved a remote — the daemon's D7 structural separation is
written against this shape, and a conditional buried under a shared lookup would become the spec for it.
Cover at minimum every method M8.1.6's walk drives (`getPull`, `getSnapshot`, `syncPull`, the draft verbs,
`submitReview`, `replyToThread`, `resolveThread` / `unresolveThread`, `listReviewThreads`, `reconcileDraft`,
`getBlob`, `addReaction`), adding engine entry points where one is missing. Per **D-a**, `listPulls` must also
include local reviews — and it must do so through **one** path, so the fixture M8.1.5 seeds and a
runtime-created review cannot both be listed by different mechanisms (the "two truths" hazard D-c names).
Drafts already key `humanId → prNumber` and a local id is just another `prNumber`, so **no parallel local-draft
keyspace may be introduced** — verify rather than build.

**Files:** `packages/app/src/api/mock/adapter.ts`, `packages/app/src/api/mock/local.ts` (new entry points if
needed), `packages/app/src/api/mock/local.test.ts`.

**Check:** `bun test packages/app/src/api/mock/local.test.ts` with `mockDev.reset()` in `beforeAll` — each
dispatched method exercised **through `createMockApi()`** on a local id, asserting it reaches the local engine
rather than throwing `not_found`. **Plus the negative control that makes the dispatch honest:** a
**non**-local id still reaches the remote path unchanged for each of the same methods, so the dispatch cannot
degenerate into a blanket bypass that quietly serves local behaviour to real pull requests. The three frozen
transport semantics remain M8.1.6's to assert.

### M8.1.9 — Refuse `submitReview` before the first sync (appended 2026-08-14)
**Do:** The owner ruled that submitting on a never-synced local review must be **refused**, not silently
succeed. Before this unit it returned `{status:'ok'}` with a real review id while `getSnapshot` stayed `null`
and `listReviewThreads` stayed `[]` — a complete record with nothing readable, whose visible symptom was an
unresolved-thread count of `1` with nothing openable. Nothing was lost, but the state was invisible and it
contradicts the premise that the client invalidates expecting the threads to appear.

`submitLocalReview` throws `ApiError('unprocessable')` when the review has no snapshot. The refusal is taken
**above the head-moved check**, not merely above the writes: with no snapshot there is no compare behind
`expectedHeadSha`, so answering `head_moved` would route the reviewer into reconcile against a snapshot that
does not exist — a worse dead end than the refusal. This weakens the frozen "`head_moved` is a returned value,
never a throw" semantic nowhere it is asserted, because both the conformance suite and the scenario walk reach
their head-guard assertions on **synced** reviews. The guard reads the **snapshot's presence** rather than
`lastSyncedAt === null`: the snapshot is what submit actually depends on, so guard and dependency cannot drift
(a sync writes both, a delete drops both).

This sits inside this ticket's own Goal — a working local-review implementation in the mock, which is the
specification every other transport conforms to — so it is an appended unit rather than scope growth, exactly
as M8.1.8 was. Existing units are not renumbered. **M8.4's sink must refuse identically.**

**Files:** `packages/app/src/api/mock/local.ts`, `packages/app/src/api/mock/local.test.ts`.
**Check:** `bun test packages/app/src/api/mock/` green (the directory, not the file — the shared mock store
makes a single file passing prove less). The refusal is asserted at both levels, and **each carries a positive
control running the identical `SubmitReviewInput` after a sync** — engine level (`status:'ok'`, snapshot has
one thread, verdict on the record) and API-surface level (`listReviewThreads` returns the comment verbatim).
Without those, a blanket `throw` in `submitLocalReview` would satisfy the refusal assertions. Also assert
nothing was written on the way to the refusal: no snapshot, no submitted verdict, and the row's
`unresolvedThreads` is `0` rather than the `1` that was the whole complaint.

## Verify

Additional to the gate (`bun run check`):

1. `bun run conformance:matrix` exits **zero**. Its exit code is the durable half; "no diff to
   `packages/shared/conformance/suite.ts`" corroborates the same fact that M8.1.6 pins by running both
   conformance runners unamended. If that file changed, the contract was edited rather than extended, and
   this ticket is wrong.
2. `bun test packages/app/src/api/route-surface.test.ts` — the additive-only property is **asserted**, not
   eyeballed: the pinned table of the 19 pre-existing `method + path` pairs and the `ROUTES` ↔ adapter
   bijection fail the gate on any deletion or signature edit in `client.ts` / `http.ts`.
   `git diff --stat main -- packages/shared/src/api/client.ts packages/shared/src/http.ts` showing only
   insertions is the reviewer's corroboration of it.
3. `bun test packages/revud/src/draft-isolation.test.ts packages/revud/src/broker/token-custody.test.ts`
   passes with **zero edits to either file**. These are the invariant tests D2 exists to avoid amending.
4. `bun test packages/app/src/api/mock/local-scenario.test.ts` — the full create → sync → comment → submit →
   reply → resolve walk on a local id with `globalThis.fetch` armed to throw. `bun run build:app &&
   bun run preview`, opening `?mock=1` and watching the network tab, is the human-legible corroboration of
   the same fact, not the Check. (`?mock=1` purity itself is already asserted in a real browser by
   `e2e/happy-path.ts:52-68`, which is a `bun run` script outside `bun run check`; extending that walk to a
   local review is M8.11's.)
5. `bun test packages/app/src/api/http-contract.test.ts` — every new wire type round-trips losslessly through
   `JSON.parse(JSON.stringify(x))`, the property that file exists to enforce, with `archivedPr: null` present
   as a key.
6. Every Check above asserting an **absence** — no stamp on a local body, no `@` in the sentinel user, no
   `fetch` during the local walk, no submitted review in `snapshot.mutable.reviews`, no shadowed route — has
   its negative or positive control run, with **both observations recorded in `## Log`** (broke it, saw red,
   reverted, saw green). An assertion that has never failed is not yet evidence.

## Context — verified seams

| anchor | what is there / why it matters here |
| --- | --- |
| `packages/shared/src/api/client.ts:39` | `export interface RevuApi` — the frozen contract. **19 methods** today (lines 41–115), not the 21 the design doc claims; count from the file, not the prose. This ticket appends four and edits none. |
| `packages/shared/src/http.ts:40` | `export const ROUTES = { … } as const satisfies Record<string, Route>` — **19 entries** (lines 41–69). The four new entries go here; the `satisfies` clause means a malformed entry is a compile error. |
| `packages/shared/src/http.ts:26-29` | The `Route.path` docstring naming the only legal params: `:n`, `:sha`, `:threadId`, `:id`. Two tests enforce it (below). |
| `packages/shared/src/http.ts:123` | `fillPath` — rejects `undefined` **and** empty string for a param. Branch names must therefore ride in a JSON body, never a path segment. |
| `packages/shared/src/http.ts:226` | `TRANSPORT_SEMANTICS` — the three frozen non-error semantics, spelled out. M8.1.6 asserts all three on a local id. |
| `packages/shared/src/http-validators.ts:92` | `vNumber` — accepts any finite number, **no range check**. This is why a 1e9/9e12 id needs no validator change (D2's §3.1 claim, verified). |
| `packages/shared/src/index.ts:7-12` | The single barrel. New `ids.ts` / `lib/refs.ts` must be re-exported here or the app and revud cannot see them. |
| `packages/revud/src/api-router.ts:373-376` | `default: { const _never: never = name … }` — the exhaustiveness guard over `keyof typeof ROUTES`. **Adding a route without a handler here is a `tsc` failure**, which is why M8.1.4 is one commit. |
| `packages/revud/src/api-router.ts:133` | `for (const [name, route] of Object.entries(ROUTES))` — the mock-mode matcher. Segment-count + literal matching, insertion order; a `:param` segment matches anything, so a new route sharing a literal prefix and segment count with an earlier one would be shadowed. |
| `packages/revud/src/direct-router.ts:150` | `Number.isInteger(n) && n > 0 ? n : null` — the direct-mode PR-number gate a local id must pass. `1_000_000_001` does; a string or negative id would not. |
| `packages/revud/src/direct-router.ts:157` | `isKnownApiPath` iterates `ROUTES`, so a route added to the table automatically becomes "known" and answers an honest **501** in direct mode (line 518 is the unknown-path `No route for …` 404). That is why M8.1 can add routes before M8.5 wires direct mode. |
| `packages/revud/src/draft-isolation.test.ts:117-127` | `const allowed = new Set(['n','sha','threadId','id'])` over `Object.values(ROUTES)`, **plus** `expect(route.path).not.toMatch(/email\|human\|owner\|user/i)`. Both must pass unamended. |
| `packages/revud/src/broker/token-custody.test.ts:243-256` | A second, independent sweep: `sweepParams = { n, sha, threadId, id }` fed to `fillPath` for **every** route, asserting no response contains `'No route for'`. A new param name makes `fillPath` **throw**; an unhandled route that 404s instead of 501ing fails the string assertion. |
| `packages/app/src/api/mock/adapter.ts:211-212` | `createMockApi(): RevuApi` / `const api: RevuApi = {` — one of exactly **two** `RevuApi` implementations; the other is `packages/app/src/api/http/adapter.ts:163`. Both break at `tsc` when the interface grows. |
| `packages/app/src/api/mock/adapter.ts:497-509` | `submitReview` returning `{ status: 'head_moved', … }` — **returned, not thrown**. The local submit must mirror this shape exactly. |
| `packages/app/src/api/mock/adapter.ts:335-338` | `getSnapshot` is `store.getSnapshot(prNumber)` with no `not_found` branch — the "null, not 404" semantic, already structural. Local ids get it for free by reusing the same store path. |
| `packages/app/src/api/mock/adapter.ts:396,531,557` | The three `prefixBody(human, …)` calls on the GitHub write path. The local path must have **none** of them (§6, "No stamping"). |
| `packages/app/src/api/mock/store.ts:32-37` | `STORAGE_KEY = 'revu.broker.v1'`, `STORE_VERSION = 3`, `ID_BASE = 700_000_000` — the mock's own id band the new bands must clear. |
| `packages/app/src/api/mock/store.ts:176-228` | The migration doctrine in code: bump `STORE_VERSION`, add a default-the-field step, **never** add the new field to the corruption check at `:189-202` (doing so wipes every draft on upgrade). |
| `packages/app/src/api/mock/store.ts:522-526` | `nextId()` = `ID_BASE + ++counter`, persisted. Local entity ids need their own counter or an explicit rebasing decision (Open questions). |
| `packages/app/src/api/mock/scenarios.test.ts:59-67` | `expect(list.items).toHaveLength(10)` and the exact `[101,204,…,415]` array. A fixture local review entering `listPulls` breaks both. |
| `packages/app/src/fixtures/contract.ts:59-78` | `FixtureDB` — the shape a fixture local review must be threaded into (`pulls`, `seededSnapshots`, `seededDrafts`, `seededViewed`, `seededBlobs`). |
| `packages/app/src/fixtures/index.ts:37-48` | The assembled `fixtureDB`. A new local-review fixture module is registered here. |
| `packages/app/src/fixtures/helpers.ts:143` | `buildSnapshot(remote, syncedAt, opts)` — the canonical remote→cache copy. The local snapshot synthesizer should reuse it rather than hand-rolling a `Snapshot`. |
| `packages/app/src/state/threads.ts:120-123` | `let syntheticSeq = -1; … syntheticSeq--` — the optimistic **negative** id space. Locally minted ids must be positive or they collide with these (§8, edge 17). |
| `packages/app/src/state/queries.ts:35-43` | `qk` — `snapshot(n)`, `draft(n)`, `viewed(n)` keyed by number. A distinct integer hashes distinctly; a `NaN` from a string id would collapse every local review onto one cache entry. Unchanged by D2. |
| `packages/app/src/components/app-shell.tsx:44`, `packages/app/src/components/palette.tsx:51` | `/^\/pr\/(\d+)(?:\/\|$)/.exec(pathname)` — the two independent PR-path parsers. A 10-digit positive integer matches; nothing here changes. |
| `packages/app/src/api/select.ts:30-34` | `selectApi(base, forceMock)` — `?mock=1` short-circuits **before** `base` is consulted, so the demo is HTTP-free by construction. |
| `packages/shared/conformance/suite.ts:42-55` | `ConformanceScenarios` — the per-runner fixture map. M8.1 must leave this untouched; the local leg is M8.11's. |

## Constraints that bind this ticket

- **The frozen contract is `client.ts` + `types.ts`** (hard-constraints; AGENTS.md "Ownership & verification"
  lists both as read-only inputs). This ticket is the sanctioned exception, and only for **additive** change:
  a new method, a new type, a new route entry. Any deletion or signature edit in those files is a defect.
- **The three transport semantics must survive on the local path** (`http.ts:226`): `submitReview` →
  `head_moved` is a 200-level **value**; `syncPull` may resolve with `partial` set (locally it will be `null`,
  but the key must be present); `getSnapshot` → `null`, never 404-as-error. M8.1.6 asserts each on a local id
  because M8.4/M8.5 conform to what the mock does, not to what the doc says.
- **D2's payoff is only real if the routes use allowlisted params.** `:n` only. Enforced twice, in
  `draft-isolation.test.ts:118` and `token-custody.test.ts:243`, and neither file may be edited.
- **The mock adapter is never deleted and `?mock=1` stays reachable in production builds** (hard-constraints).
  Local reviews must therefore work fully in the mock with no daemon and no git.
- **Migration doctrine applies to the mock store too.** `store.ts:176-179` states it: bump the version, add a
  defaulting step, never touch the corruption check. A v3 document with drafts must open, migrate, and keep
  every draft.
- **Identity: email is a key, never a body.** The synthesized `GhUser` carries the git-config *name*
  (§4.1); `Human.id` (the lowercase email) may be stored on a local comment record but never rendered into a
  comment body.
- **No stamping locally** (§6). `prefixBody` exists only because N humans share one GitHub bot; the mock's
  local submit must skip it, or M8.4 will faithfully reproduce the wrong spec.
- **Draft isolation is preserved verbatim.** The mock keys drafts `humanId → prNumber`
  (`store.ts:73`); a local id is just another `prNumber`, so isolation holds with no new mechanism. Do not
  introduce a parallel local-draft keyspace in the mock.
- **Code comments carry no tracking artifacts** (AGENTS.md). No `M8.1`, no ticket ids, no phase names in any
  source or docstring this ticket writes. M-ids belong in this file only.

**Three decisions are recorded here rather than left open**, because M8.1 merges first and downstream tickets
are already written against the answers. Reversing one is a contract change, not a preference; later tickets
cite them by letter.

- **D-a — the mock's `listPulls` includes local reviews.** M8.5.3 mandates the daemon-side merge, M8.6.2/M8.6.4
  render the inbox from `usePullList`, and `PrLayout` resolves a review only out of that list
  (`pr-layout.tsx:280-282`) — a local review absent from `listPulls` renders the "isn't in this installation"
  screen on every open. `listLocalReviews` still exists, for management surfaces (delete, M8.10) and for the
  local-only annotations under D-c. The cost is deliberate: `scenarios.test.ts:61`/`:64-67` and
  `scripts/smoke.ts:54-61` are updated in M8.1.5 and M8.11.2 rather than loosened.
- **D-b — a submitted local `ReviewSummary` never enters `snapshot.mutable.reviews`.** It is persisted as a
  local record (`local_reviews_submitted`, M8.2.5) and read by nothing on the render path, so §4.1's and §7's
  "issue comments and submitted reviews are always empty" stays true after the first submit. The alternative
  (append on submit) would silently falsify M8.7.4's threads-only Conversation tab. M8.4.3's snapshot write and
  M8.7.4's premise both cite this.
- **D-c — the two local-only annotations (`dirty`, `archivedPr`) ride `LocalReviewSummary`, not
  `BrokerPullMeta`.** `BrokerPullMeta` (`types.ts:278-301`) is frozen and its §4.1 field table has no room for
  either; `LocalReviewSummary` is new here and already mirrors the `local_reviews` columns, both of which are
  columns. So: **`listPulls` stays the row source** (D-a) and `listLocalReviews` supplies only the local-only
  annotations, queried under its own key. Nothing renders a row from both sources, which is the "two truths"
  hazard M8.6's open question 1 named. Consumers: M8.5.2 (the route must return both fields), M8.6.4 (the
  dirty badge), M8.7.8 (the dirty banner), M8.9.5 (the superseded state — its open question 3 resolves here
  unless its owner escalates).

## Landmines

- **Widening `RevuApi` breaks four files at once.** `packages/app/src/api/mock/adapter.ts:212`,
  `packages/app/src/api/http/adapter.ts:163` (the only two implementations), and — via `ROUTES` — the
  exhaustiveness guard at `packages/revud/src/api-router.ts:375`. Splitting these across commits leaves the
  tree red; that is why M8.1.4 is deliberately atomic. *Missed:* the branch cannot pass `bun run check` at any
  intermediate commit and bisect becomes useless.
- **`fillPath` throws on an unknown param.** `token-custody.test.ts:244` sweeps every route through
  `fillPath(route.path, { n, sha, threadId, id })`. A route with `:branch` or `:ref` throws inside a test that
  never mentions local reviews, producing a confusing failure far from the change. *Missed:* a red gate whose
  cause is three packages away.
- **The two id bands are not mutually disjoint.** `LOCAL_ENTITY_ID_BASE` (9e12) is above
  `LOCAL_REVIEW_ID_BASE` (1e9), so `isLocalReviewId(someLocalCommentId)` returns **true**. The predicate is
  namespace-scoped: apply it to review ids only. *Missed:* a comment id routed as a review id in M8.5's
  dispatch — a bug that will look like data corruption.
- **The fixture local review in `listPulls` breaks `scenarios.test.ts` — and D-a guarantees it will.** `:61`
  asserts exactly 10 items and `:64-67` asserts the exact pull-number array; `scripts/smoke.ts:54-61` asserts
  the same two things and is in **no gate**. *Missed:* a failing test that reads like a flake and gets "fixed"
  by loosening an assertion that was pinning real behaviour — or, for smoke.ts, a silent breakage nobody sees
  until someone runs it by hand.
- **The shared mock store leaks across `bun test` files.** One process-wide `localStorage` shim, debounced
  ~1s persistence; any suite driving `createMockApi`/`mockDev` must `mockDev.reset()` in `beforeAll`.
  *Missed:* green locally, red on the slower CI runner, order-dependent.
- **Bumping `STORE_VERSION` without a migration step, or adding the new field to the corruption check**
  (`store.ts:189-202`), reseeds the document. *Missed:* every existing draft is destroyed on upgrade — a
  direct violation of "drafts survive everything".
- **Stamping on the local path.** Because of D6 the mock defines the spec; a `prefixBody` call left in the
  local submit becomes the contract M8.4 conforms to, and the two latent optimistic-path bugs named in §6
  (unconditional stamping in the synthetic reply, empty-`brokerLogin` attribution on resolve) get inherited
  rather than avoided. *Missed:* `**Name** (role)` rendered as literal body text on a local review.
- **Negative or zero ids anywhere on the local path.** `direct-router.ts:150` rejects them, and negatives are
  reserved for optimistic synthetics (`threads.ts:121`). *Missed:* a locally minted comment id that orphans the
  optimistic entry it was meant to replace (§8, edge 17).
- **The design doc's method/route arithmetic is wrong.** §2 says "21 `RevuApi` methods" and §3.1 says "all 21
  HTTP routes"; the code has **19** of each. Harmless here, but do not derive anything from the doc's counts —
  re-count from `client.ts:39-116` and `http.ts:40-70`.
- **Route shadowing in `api-router.ts:133`.** Matching is by segment count with `:param` matching anything, in
  insertion order. Any new route that shares its segment count and literal prefix with an earlier entry is
  silently swallowed by it, and `tsc` cannot see it — the exhaustiveness guard proves a handler *exists*, not
  that it is ever *reached*. Verified safe for the four proposed paths by hand; M8.1.7's dispatch sweep makes
  that verification permanent, so a later spelling change is caught by the gate rather than by remembering to
  re-check.

## Testing exceptions

Two facts this ticket relies on are **not** guarded by `bun run check`. Both are acknowledged rather than
papered over with a Check that does not exist.

- **`scripts/smoke.ts:54-61` is in no gate.** It asserts the same pull count and pull-number array that
  M8.1.5 deliberately changes, so it goes stale the moment the fixture local review lands, and nothing fails.
  *Compensating:* the identical assertions live in `packages/app/src/api/mock/scenarios.test.ts:61,64-67`,
  which **is** in the gate, and M8.1.5 updates those in the same commit. M8.11.2 owns reconciling the script.
  Until then, treat a green smoke run as no signal on fixture counts.
- **Nothing in `bun run check` loads the built bundle in a browser.** `bun run build:app` proves the fixture
  module compiles into the production build; it does not prove `?mock=1` serves a local review.
  *Compensating:* `selectApi`'s `?mock=1` short-circuit is unit-tested in
  `packages/app/src/api/http/adapter.test.ts`, the HTTP-free property is asserted in-process with an armed
  `fetch` tripwire by M8.1.6, and `e2e/happy-path.ts:52-68` asserts zero `/api/*` requests from a real
  `?mock=1` page — the last of which runs under `bun run test:e2e`, not the gate.

## Open questions

1. **Wire shapes for `BranchRef`, `CreateLocalReviewInput` and `LocalReviewSummary` are not specified.**
   §4.2 gives store columns and §7 gives picker requirements; no wire type exists. Proposal: mirror §4.2's
   columns in camelCase for `LocalReviewSummary`. Implementer decision, but it freezes on merge.
2. **Exact route spellings.** `/api/branches` + `/api/local-reviews` are a proposal, not a decision. Every
   later ticket and the fetch adapter hard-code them; pick once.
3. **What does `listBranches` return in the mock, and does `BranchRef` carry a default-branch marker?**
   There is no git repo behind the mock, so a small fixed fixture branch list is the obvious answer — but the
   *shape* is decided here for both transports, and two consumers depend on it: M8.6's picker needs local
   branches **and** remote-tracking refs (§8 edge 3), and M8.6's open question 7 asks what the base picker
   defaults to, which is answerable only if `BranchRef` carries a `default` marker. Answer the shape question
   once, here; M8.3's `listBranches` git read and M8.6's picker both conform to it.
4. **How does the mock mint local ids?** §3.1 specifies `MAX(id) + 1` from a SQL `UNIQUE(repo, base_ref,
   head_ref)`; the mock has no SQL. Either a second persisted counter alongside `store.ts:522` or an
   in-document max scan. Whichever is chosen must make duplicate creation return the **existing** review
   (§8 edge 6) so the mock and M8.2 agree.
5. **Scope boundary on `deleteLocalReview`.** M8.1 declares the method and route; M8.10 owns deleting rows,
   pinned refs and snapshots. Decide whether M8.1's mock implementation deletes the full local record (and
   M8.10 only adds ref/immutable pruning) or whether M8.1 stops at removing the review from the list. The
   daemon-side half of the same boundary is settled: M8.5.2 leaves `DELETE /api/local-reviews/:n` on the
   honest known-route 501 until M8.10 lands.
6. **`getRateLimit` on a local review.** §7 says suppress the rate chip on local reviews, but `getRateLimit`
   is a global read with no PR argument, so nothing in the contract changes. Confirm that the suppression is
   purely an M8.7 rendering concern and that no local-aware variant is needed here.
7. **Which `ApiErrorCode` do the creation errors map to?** `types.ts:538` offers only
   `network | rate_limited | not_found | forbidden | conflict | broker_unreachable | persist_failed` — there is
   no `invalid`/`bad_request`. `ref_not_found` → `not_found` is clear; `same_ref`, `unrelated_histories` and
   `shallow_clone` have no home. M8.3 raises this and correctly names it **this ticket's territory** — but
   M8.1 is the first ticket to merge, so by the time M8.3's implementer reaches the question the union is
   already frozen, which is why the decision moved here. Decide in M8.1.2: squeeze all three into
   `conflict`, or add exactly one
   code (and with it the `vApiErrorCode` entry and the `http.ts:165` status). M8.3.2 produces the typed builder
   errors and M8.5.2 maps them; both need this settled before they start.
8. **What shape is a thread id?** Nothing parses them (`direct-router.ts:440-456` passes `params.threadId`
   through opaquely, `fillPath` encodes it), so both a `PRRT_`-shaped id and a `local:<n>:<seq>` form are safe.
   M8.4's open question 2 framed this as an implementer call to be "mirrored back into the mock under M8.1" —
   but M8.1 gates M8.4 and will already have merged with the mock minting *something*, and D6 makes the mock
   the spec. So the choice is made **here**, in M8.1.3, and M8.4.2 conforms to it. The tradeoff: maximal
   client-indistinguishability (`PRRT_`-shaped) versus obvious locality (a `local:` prefix makes a mis-routed
   local id visible in a log).

## Log

_Append `**YYYY-MM-DD** — <what changed, files, gate result>` entries as work lands. Newest last._

**2026-08-13** — **Wave 1 landed: M8.1.1, M8.1.2, M8.1.7 + Spike B.** Branch `m8.1/contract-and-mock` is
based on `m8/local-reviews-design` (PR #69), not `main` — the board and `docs/agent/LOCAL_REVIEWS.md` exist
only on that branch, and #69 carries zero files under `packages/`, so the code diff is identical to a `main`
base and GitHub retargets to `main` when #69 merges.

Gate: `bun run check` green at the wave tip — **1181 pass · 1 skip · 0 fail · 65 files**, then `build:app`
exit 0. *Honest caveat on gate cadence:* the three units shared one working tree, so the gate ran green over
the **wave**, not after each unit individually; the three commits were then split so each is a
deletion-free subset of that green tree. Every unit additionally carries its own targeted red→green evidence
below.

**M8.1.1 — id bands** (`164b1d7`). `packages/shared/src/api/ids.ts` + `ids.test.ts` (new),
`packages/shared/src/index.ts` (+1 barrel line), `packages/app/src/api/mock/store.ts` (`export` on `ID_BASE`,
+1/-1), `packages/app/src/api/mock/id-bands.test.ts` (new). Values taken verbatim from `LOCAL_REVIEWS.md`
§3.1, predicate body included. RED: `Cannot find module './ids'`, then a second, sharper red isolating the
cross-package half — `SyntaxError: Export named 'ID_BASE' not found in module … store.ts` — captured *after*
`ids.ts` landed, so the surviving red proved the app-side assertion and not the shared module. GREEN 14 + 5.
Implementer notes: the optimistic-negative-id fact is asserted as a property of **sign** over the whole
negative range rather than as a copied counter literal (`syntheticSeq` is module-local and unexported), which
is strictly stronger; the namespace caveat is pinned in both directions, including a real GitHub comment id
(`3_000_000_000`) satisfying the predicate, which makes the landmine executable.

**M8.1.2 — wire types, validators, ref-name validator** (`d0d1ce8`). `types.ts`, `http.ts` (status map only,
`ROUTES` untouched), `http-validators.ts`, `http.test.ts` + `lib/refs.ts` + `lib/refs.test.ts` (new),
`index.ts` (+1 barrel line, orchestrator-owned), `http-contract.test.ts`. **Additive-only confirmed:
87 insertions / 0 deletions in `types.ts`.** RED→GREEN: refs `Cannot find module './refs'` → 31 pass;
http-contract `Export named 'validateBranchRef' not found` → 37 pass; `http.test.ts` **two-stage** red — the
second stage observed the exact hole the Check exists to close, a code present in the union and the status map
but absent from the validator's literal list being rejected on the wire as a `ValidationError` → 14 pass.
`draft-isolation.test.ts` and `token-custody.test.ts` pass unamended.

**M8.1.7 — route-table guard rails** (`0d2c1b0`). `packages/app/src/api/route-surface.test.ts` and
`packages/revud/src/api-router.test.ts` (both new; **no production code touched**). Route count confirmed
**19** at runtime, from `ROUTES` and from the mock's method set — the design doc's 21 is wrong, as the ticket
says. GREEN 25 pass / 84 expect across the two files. **All three negative controls observed red and
reverted**, each with a *named* failure:
(a) a shadowing route appended to `ROUTES` → the sweep red, test title naming the shadowed route and the diff
naming the shadowing one (`getBlob`);
(b) `getRateLimit` deleted from the mock adapter → the bijection red naming the missing method;
(c) one character changed in `/api/pulls/:n/reconcile` → the pinned table red on the exact pair.
Reverts were hand-applied inverse edits verified by content hash (`http.ts` and `adapter.ts` both back to
their pre-control hashes) rather than snapshot restores, because two other agents were writing the tree
concurrently; `git diff --stat` on both files empty afterwards. The sweep is held to the stronger available
bar — every one of the 19 routes reaches **its own `api` method** with a valid body, none falling back on the
weaker "a 400 from its own validator" evidence — and carries an in-file positive control (an unknown path
still produces the `No route for` envelope with zero `api` calls) so the absence assertion cannot pass
vacuously.

**2026-08-13 — two orchestrator rulings on M8.1.7's shape.** Both resolve real problems rather than
preferences, and later units depend on them.
1. **The pinned table is subset-pinned, not exact-equality.** This ticket contradicts itself: M8.1.4's *Do*
   says "the table is updated in the same commit as the widening", while M8.1.4's *Check* says the guards
   "pass over 23 routes with no edit to either file" — exact-equality makes those mutually unsatisfiable.
   Subset-pinning satisfies both and loses no coverage: a **deletion or edit** is caught by the pinned table,
   an **unintended addition** by the bijection (a `ROUTES` entry with no method on *both* adapters fails set
   equality). A second test asserts the table can never shrink below its pinned entries, so the pin cannot be
   neutered by deleting a pin alongside its route. **M8.1.4 appends its four new pairs**, taking the table to
   23 — "no edit" is read as "no amendment weakening an existing assertion"; an append is not an amendment.
2. **`api-router.test.ts` deliberately carries no `mockDev.reset()` and no `@revu/app/mock` import.** The
   roadmap called it a mock-driving file; it was built so that it is not one — a recording proxy for `api` and
   a `{ flush, flushOrThrow }` stub for `store`. Importing the mock *solely* to reset it would introduce the
   process-wide store contact (module-load `load()` + debounced flush) that the reset rule exists to bound.
   Strictly safer than the plan.

**2026-08-13 — Open question 7 settled (frozen; nothing else in the workstream may touch the union).**
Exactly **one** new `ApiErrorCode`: **`unprocessable`, HTTP 422**. `ref_not_found` → `not_found` as given;
`same_ref`, `unrelated_histories` and `shallow_clone` share one honest class — the request was understood but
cannot be satisfied as given, which is 422's literal meaning — and the syntactic ref-name rejection maps there
too, so the daemon's validation path needs no further code. One code suffices because the app switches on
`code` for the class and renders `message` for the cause; no client flow branches between the three, so
per-cause codes would be dead vocabulary. **Reusing `conflict` was rejected**: it already carries the
load-bearing draft-conflict meaning ("the review moved under you"; a 422 after the head guard keeps the draft
and surfaces `conflict`), and rendering "you picked the same branch twice" with that copy would misdescribe
the failure. The §5.2 hard-stop was **not** triggered — one code was enough. All three copies of the
vocabulary updated in that single unit, and `http.test.ts` now pins them against each other.
Verified separately by the orchestrator: **no consumer maps over `ApiErrorCode` exhaustively** —
`components/review/error-copy.ts` special-cases only `rate_limited` and renders `message` otherwise — so the
new code lands with honest copy and leaves no rendering hole.

**2026-08-13 — Open question 1 settled (frozen wire shapes).** `BranchRef` = `ref` (fully qualified),
`name` (short display), `kind: 'local' | 'remote'` (§8 edge 3 — the picker offers both), `isDefault: boolean`
required with exactly one `true` per listing (this also settles the *shape* half of open question 3, which
M8.1.4's Check presumes). `CreateLocalReviewInput` = `baseRef`, `headRef`, `title?` (absent → server defaults
to the head branch name per §4.1); **no `repo` field** — the server derives repo identity, so a client can
never write another repo's namespace. `LocalReviewSummary` mirrors §4.2's `local_reviews` columns in
camelCase, and **every nullable column is a required nullable key** (always present on the wire), which is
exactly what makes the `dirty: false` / `archivedPr: null` present-as-key assertions meaningful; `dirty` is
boolean on the wire against INTEGER 0/1 in SQL, and a leaked `0` is **rejected, not coerced** — pinned by test.

**2026-08-13 — Spike B (throwaway, nothing committed; worktree removed).** Probe: `DirectContext.github`
made typed-absent (`github?: GithubClient`), type-only, no call site fixed. **Total new breaks: 2** —
mechanical **0**, needs-a-real-local-mode-branch **2**, dangerous **0** (six pre-existing unrelated baseline
errors excluded). Both are `github: context.github` funnelling into `createDirectApi`, at
`packages/revud/src/index.ts:174` (`mainDirect`) and `:305` (`mainBroker`).
**The §5.6 signal is green:** `MissingGitIdentityError`, the identity guard, and `context.test.ts`'s
refuse-to-start block (`:101-209`) appear **nowhere** in the break set — confirmed by grep over the `tsc`
output and by the diff itself. Identity resolution lives in `session.ts` and is fed by `runner`/`cwd`, neither
of which changes type.
**Carry the caveat, not just the number:** the probe ran against `177068a`, which has no per-method
`isLocalReviewId` dispatch, so the small blast radius is an artifact of the pre-M8 tree. Once local dispatch
and the write sink land, the same edit surfaces the real choice — optional-deps-with-guards vs.
never-assemble-the-api when GitHub is absent — at every GitHub-touching method rather than at two boot-time
call sites. The risk is reading "2 breaks" as "typed-absent is cheap"; the cost is deferred into M8.5.1/M8.5.5,
not avoided.

**2026-08-13 — M8.1.3 landed** (`e60f9da`). `packages/app/src/api/mock/local.ts` + `local.test.ts` (new),
`store.ts`, `store.test.ts`, **plus `packages/revud/src/revud.test.ts`** (see the finding below). Gate:
`bun run check` **exit 0 — 1193 pass · 1 skip · 0 fail · 66 files**.

**The three-stage migration red was real, and staged exactly as ordered.** (a) The test authored against a
`v: 2` document and *today's* behaviour, before the seam existed: `SyntaxError: Export named
'migrateStoreDocument' not found in module … store.ts`. (b) The seam extracted out of `load()` with **no
behaviour change** → 3 pass, and the whole mock directory confirmed unchanged at 56 pass / 4 files. (c) The
test extended *first* for v4 → red on `expect(migrated?.v).toBe(4)` — `Expected: 4  Received: 3` — then the
bump → 6 pass. The corruption check at the load boundary was **not touched**. Both negative controls are
durable and in-file rather than break-and-revert: core-field-deleted → `null`, future-version → `null`.
`local.test.ts`: red `Cannot find module './local'` → green 8 pass. The stamp-absence assertion carries its
positive control in the same test.

**Finding — the ticket's Files list for M8.1.3 was incomplete.** revud reuses the app's mock store, so the
mandated `STORE_VERSION` 3→4 bump moves a pinned literal in `packages/revud/src/revud.test.ts:737`
(`expect(onDisk.v).toBe(3)`), a file the unit's Files line does not name. The worker correctly refused to edit
outside its list and reported it; the orchestrator verified the cause directly before touching it — that test
boots a daemon against a **v2** document, and every draft-survival assertion in it (body, `start_line`,
`line`, `anchor.lineText`, `startLineText` absent, drafts still on disk after the upgrade) **still passes**.
Only the version literal was stale. Moved deliberately to `4` and kept a hardcoded pin, never loosened into a
computed value — the same doctrine this ticket mandates for `scenarios.test.ts:61`. **Any future
`STORE_VERSION` bump must expect this second pin**; it is not in any ticket's Files line.

**Open question 4 settled — minting is persisted high-water counters, not a max-scan.**
`nextLocalReviewId()` / `nextLocalEntityId()` mint `BAND + ++counter` off a persisted `localCounters`, exactly
as `nextId()` already does. A max-scan over live rows **recycles a deleted review's id**, and the recycled id
silently inherits the dead review's drafts, viewed state and client caches. Idempotence is a separate concern,
handled by looking the `(repo, baseRef, headRef)` triple up before minting, so duplicate creation returns the
existing review — the same observable behaviour a SQL `UNIQUE` + insert gives. **Consequence for M8.2:**
persist a high-water mark rather than literal `MAX(id)+1` over live rows; §3.1's spelling under-specifies the
delete case. This is independently consistent with the roadmap's directive that M8.2 adopt a `meta` high-water
mark.

**Open question 5 settled — deletion is full-record removal.** `deleteLocalReview` removes the record
(threads, submitted reviews, `commentAuthors`) **and** its cached snapshot. Per-human drafts and viewed state
are deliberately **orphaned, never destroyed** — drafts survive everything, and a delete must not become the
one path that discards user text; the never-recycled id makes those orphans permanently unreachable rather
than dangerous. Shared content-addressed blobs are untouched (they may serve other snapshots; pruning them and
the pinned refs is M8.10's whole remit). Unknown id → typed `not_found`. Pinned in-file: record gone, snapshot
gone, draft still readable, recreation of the same pair mints a strictly newer id.

**Open question 8 settled — thread ids are `local:<reviewId>:<rootCommentId>`** (exported as
`localThreadId`). Nothing parses a thread id, so the only reader the shape serves is a person reading a log:
obvious locality beats imitating `PRRT_`. The design already spells `pull.node_id` as `local:<id>`, so
mimicry would have been the *inconsistent* choice; embedding the owning review id makes a mis-routed id
self-describing; and deriving the suffix from the root comment's entity id mirrors how the GitHub-shaped mock
derives its `PRRT_` ids, needing no extra persisted counter. Entity node_ids follow: `local:review:<id>`,
`local:comment:<id>`, sentinel user `local:user`. **M8.4.2 conforms to this.**

**As-built shapes the daemon must reproduce** (D6 — these are the spec). `LocalReviewRecord` is
scalar-for-scalar the wire `LocalReviewSummary` (proven by a strict-equality validator round-trip, so nothing
store-only leaks) plus three store-only collections: `submitted: ReviewSummary[]` (the
`local_reviews_submitted` analog, and the only read path for verdicts), `threads: ReviewThread[]` (the
`local_threads` analog; re-sync rebuilds `mutable.threads` from these), and
`commentAuthors: Record<number, string>` (comment id → `Human.id`, **key only, never rendered**). Refs are
stored fully qualified so the uniqueness triple cannot be dodged by spelling; `title` defaults to the head's
short name. Synthesized `PullDetail`: `number` = `id` = the local id (deterministic — re-synthesis never
churns identity), `node_id` = `local:<id>`, `state` `'open'` → `'closed'` once `archivedPr` is set, sentinel
user `{login: <display name>, id: 0, node_id: 'local:user', avatar_url: '', html_url: '', type: 'Bot'}` and
never an email, `mergeable: null` / `mergeable_state: 'unknown'`, `comments: 0` always, `draft: false`,
`body: null`, `labels: []`, `requested_reviewers: []`. Snapshot carries `partial: null` as a **present key**
and `syncStats: {blobsFetched: 0, blobsReused: 0, requests: 0}`. `mutable.checks` / `reviews` /
`issueComments` all `[]`, and `reviews` **stays** `[]` after submit (pinned).

**Four implementer notes for downstream tickets**, recorded so they are not rediscovered:
1. **The local path never spends the rate bucket** — pinned in-test (`store.rateInfo().used === 0` after
   create + sync + submit). M8.1.4's adapter wiring and M8.5's dispatch must not add a rate spend.
2. **Mock sync produces the honest empty compare** — no git objects exist behind a runtime-created pair, so
   `mergeBaseSha = headSha`, no files, no commits (§8 edge 2's legal state). Ref tips are deterministic per
   `(repo, ref)`, so re-sync is stable, and the head guard compares against the live tip — `head_moved` is
   reachable and pinned as a **returned value** with the draft untouched.
3. `fixtures/helpers.emptyReactions()` hardcodes a `meridian-labs/atlas` GitHub URL, so local comments carry
   their own zeroed rollup with `url: ''`. **The daemon must reproduce the empty-URL rollup, not the fixture
   helper's.**
4. Local `diff_hunk` is the minimal `@@ -n,1 +n,1 @@` header — the mock's empty compare has no patch to slice
   and the adapter's real hunk-slicer is unexported. Contract-level the field is a non-empty string;
   **M8.4's git-backed sink should slice real hunks.**
Creation validation is pinned as spec: an invalid ref name, or base == head (including bare-vs-qualified
spellings of the same ref), throws `ApiError('unprocessable')`; a blank or absent `title` falls back to the
head short name.

**2026-08-13 — M8.1.4 landed** (`5bb5e82`), atomic as designed. `client.ts` (+39/-0), `http.ts`,
`mock/local.ts`, `mock/adapter.ts`, `http/adapter.ts`, `revud/api-router.ts`, `mock/local.test.ts`,
`route-surface.test.ts` (+7/-0 append), `revud/api-router.test.ts` (+3/-0, see deviation). Gate:
`bun run check` **exit 0 — 1201 pass · 1 skip · 0 fail · 66 files**.

**The guard rail's red was observed twice, and the second one mattered more.** Guards green over 19 before
the change. After `client.ts` + `http.ts` alone, the bijection went red on both implementations —
`- "createLocalReview", - "deleteLocalReview", - "listBranches", - "listLocalReviews"`, `Expected -4 /
Received +0` — and the sweep went red with four `expect(calls).toEqual([name])` failures, each `- ["listBranches"] / + []`:
a route in the table with nothing reaching it. Then a **sharper second red** after the adapters and router
were filled in: three of four routes dispatched but `POST /api/local-reviews` still recorded `[]`, because its
handler validates the body and the sweep sent none. Green over 23 after: `route-surface.test.ts` 4 pass,
`api-router.test.ts` 25 pass / 96 expect, `Object.keys(ROUTES).length === 23`.

**Open question 2 settled — frozen route spellings, all `:n` only.** `GET /api/branches` ·
`POST /api/local-reviews` (body `CreateLocalReviewInput`) · `GET /api/local-reviews` ·
`DELETE /api/local-reviews/:n` (answers `{ ok: true }`). Shadowing was checked by hand against every incumbent
— `POST` at 2 segments, `DELETE` at 3, the two `GET`s at 2 literal segments, none with an incumbent, and all
appended last — and is now held permanently by the sweep rather than by memory.

**Open question 3 fully settled.** Shape was frozen in M8.1.2; the mock's data is nine fully-qualified
entries with **exactly one** `isDefault: true`, taken from `db.repo.default_branch`: six `kind: 'local'`
(`refs/heads/main` default, `release/0.41`, `chore/node-22`, `feat/gateway-rate-limiting`,
`fix/cache-ttl-jitter`, `marcus/strict-null-checks`) and three `kind: 'remote'`
(`refs/remotes/origin/main`, `origin/release/0.41`, `origin/metering/usage-rollups`). The last is deliberately
remote-only — the "base exists only as a tracking ref" case a later picker must handle. Names mirror the PR
fixtures so a local review over them reads coherently.

**Open question 6 confirmed, not changed.** `getRateLimit` is a global read with no PR argument, so nothing in
the contract moves; suppressing the rate chip on a local review stays purely an M8.7 rendering concern.
Separately pinned by a new test: all four methods leave `store.rateInfo().used === 0`.

**Invariants held.** `draft-isolation.test.ts` + `token-custody.test.ts`: **19 pass · 703 expect, zero edits**
(`git diff --stat` empty on both). No existing pin altered. The whole frozen-contract change is provably
additive — `client.ts` +39/-0, `types.ts` +87/-0, and `http.ts`'s **single** deletion across the branch is one
docstring line (verified by inspecting the diff, not by report).

**Two orchestrator decisions on M8.1.4's edges.**
1. **Accepted a one-line deviation from the Files list.** `api-router.test.ts` gained a `REQUEST_BODIES` entry
   for `createLocalReview`; that file's own docstring mandates it ("A route added to the table therefore needs
   its body here, or the sweep reports it as unreached"). It is an **input** append — no assertion, threshold
   or control touched — and the sweep still holds the new route to the strongest bar (reached its own `api`
   method with a 200). The rejected alternative was dropping body validation in the router so the sweep would
   pass untouched: that would make `isValidRefName` throw a `TypeError` on a non-string ref, turning a
   malformed request into a **500 instead of a typed 400**, and make `createLocalReview` the only
   body-carrying route that trusts untrusted input. Weakening the product to avoid touching a test input is
   the wrong trade.
2. **Corrected the `Route.path` docstring** (`http.ts:26-29`), which read `:n (prNumber)`. It is now also a
   local review id, and the stale text invites exactly the range check that would reject every local review;
   the allowlist and the body-not-path rule are now stated there too. Docstring-only — no signature, no route,
   no behaviour, and the invariant tests read `route.path` values rather than comments.

**2026-08-13 — M8.1.8 landed** (`0aeb75a`), the unit appended today. `mock/adapter.ts`, `mock/local.ts`,
`mock/local.test.ts`. Gate: `bun run check` **exit 0 — 1219 pass · 1 skip · 0 fail · 66 files**. RED first,
and specific: eight failures, each an `ApiError` `not_found` thrown out of `requireRemote` for a local id
(`Pull request #1000000511 does not exist on meridian-labs/atlas`) across `syncPull`, `listPulls`, the draft
verbs, `submitReview`, `replyToThread`, `resolveThread`, `addReaction` and `reconcileDraft`. Green 30 pass /
4257 expect. **The negative-control block was green from the start, as it must be** — it asserts the *remote*
path is unchanged, which was already true.

**The dispatch's own falsifiability was proven**: flipping `isLocalReviewId(prNumber)` to
`isLocalReviewId(prNumber) || true` in `replyToThread` produced exactly one **named** failure —
`No local review with id 347 exists in this workspace` / `a non-local id still takes the GitHub-shaped path,
unchanged` — then was reverted by inverse edit and verified by content hash. That is the control that
distinguishes a correct dispatch from a blanket bypass quietly serving local behaviour to real pull requests.

**All five `requireRemote()` sites resolved deliberately**, each with the local branch **above** the remote
lookup and below the latency delay and failure gate: `syncPull` → `syncLocalReview` (attempt counter, blob
transfer and `spendRate` all stay below); `replyToThread` → `replyToLocalThread` (the `prefixBody` stamp and
broker-bot author stay strictly below); `resolveThread` → `resolveLocalThread`; `addReaction` →
`addLocalReaction`; `submitReview` → `submitLocalReview` (the `canApprove` refusal, stamping and the
`mutable.reviews` append remain remote-only). `requireRemote` now carries a docstring stating that invariant.

**The single path into `listPulls` — and it constrains M8.1.5.** Local reviews are listed from
`listLocalPullRows()`, sourced from the **stored record** and projected through the adapter's existing
`toSummary`, called once. The alternative — seeding a fixture into `fixtureDB.pulls` — was rejected because
such a review would be listed by `listEffectiveRemotes()` **and** by the local path: the same review twice,
which is the "two truths" hazard D-c exists to prevent. **This supersedes M8.1.5's ticket text**, which says
the fixture is "registered in `fixtureDB.pulls`"; M8.1.5 was dispatched with the correction, seeding the
fixture as a local record threaded through `FixtureDB`. Derived broker half on a local row:
`authorHumanId: null`, `canApprove: true`, `assignedReviewerHumanIds: []`, `checks` **absent** (not empty),
live `compareKey`, `commitCount: 0`.

**Draft isolation needed no new keyspace — verified, not assumed.** The draft verbs reach
`store.getDraft(humanId, prNumber)` with a local id as the `prNumber`; asserted that the store holds it under
exactly that key, that a **second human** reading the same local id gets `null`, and that switching back
returns the text. Same for `getFileViewed` / `setFileViewed`.

**Deliberately NOT dispatched**, because they are keyed by review id in the *same* store for both kinds of
review and branching them is how the forbidden parallel keyspace gets built by accident: `getSnapshot`,
`listReviewThreads`, `reconcileDraft`, the three draft verbs, `getFileViewed` / `setFileViewed`. `getBlob`
takes a SHA, not a review id — one content-addressed store serves both. All are exercised on a local id.

**Two findings.** (1) **There is no `getPull` on `RevuApi`** — the appended unit's coverage list named one in
error; the contract has 23 methods and none is `getPull`. The equivalent surfaces are `listPulls` and
`getSnapshot().mutable.pull`, both covered. (2) `submitLocalReview`'s piecemeal snapshot mutation was replaced
by a shared `refreshSnapshotThreads(record)` used by all four local write verbs — behaviour-identical (landed
submit assertions pass unchanged) and it keeps **the record as the one truth with the snapshot rebuilt from
it**, exactly as re-sync does. That is the shape M8.4's sink should copy.

**2026-08-13 — M8.1.6 landed** (`992a220`). One new file, `packages/app/src/api/mock/local-scenario.test.ts`;
**no production code touched**. Gate: `bun run check` **exit 0 — 1230 pass · 1 skip · 0 fail · 67 files**.

**The red had to be manufactured honestly, and was.** Every assertion was green as first authored — the
preceding units had already made the walk work — so a "test-first red" was not available. Rather than claim
one, each of the four live assertions was **inverted once and observed to fail with the real value printed**
(`Received: "resolved"`, `Received: true`, `Received: "head_moved"`, `+ []`), then restored; `grep PROBE`
confirms no residue. Green 11 pass / 53 expect. **This is the correct discharge of §4 when a unit's subject
already exists:** the doctrine's purpose is proving the assertion observes behaviour rather than passing
vacuously, and an inversion proves exactly that.

**All three frozen transport semantics hold on the local path — nothing escalated.**
1. `head_moved` arrives as a **resolved value**: asserted through a `settle()` helper that converts a rejection
   into `{threw: true}`, so *any* throw fails with its `ApiError(code)` printed. Observed
   `{status:'head_moved', currentHeadSha: <live tip>, newCommits}`, with the draft — body **and** its one
   inline comment — intact afterwards.
2. `getSnapshot(localId)` before the first sync resolves **`null`**, not a thrown `not_found`. Same `settle()`
   treatment.
3. `partial` is a **present own key valued `null`** — `hasOwnProperty` **and**
   `validateSnapshot(JSON.parse(JSON.stringify(snap)))` `toStrictEqual` the wire form, asserted twice: on the
   value `syncPull` returns **and** on the cached copy `getSnapshot` serves, the latter having crossed the
   store's JSON persistence boundary, which is where an `undefined` would actually vanish. `vSnapshot`
   declares `partial` required, so an absent key throws.
Two **durable in-file** controls back the absence claims permanently, beyond the one-shot inversions:
`settle()` fed a rejecting promise must report the throw, and a snapshot copy with `partial: undefined` must
fail both the key check and the validator.

**The fetch tripwire is armed before `mockDev.reset()`** — the fixture seed is inside the no-network claim —
restored in `afterAll`, and it **throws synchronously**: a rejected promise could be swallowed by a caller and
surface as a plausible empty result. Its positive control reads `attemptedRequests` *before* firing, so the
control's own call cannot be mistaken for the walk's.

The walk, on local id `1000000001`: create → `getSnapshot` (null) → `syncPull` → `getSnapshot` (cached) →
`saveDraft` (one `PendingComment`, RIGHT/line 12) → `getDraft` → `submitReview` with a 40-zero
`expectedHeadSha` (→ `head_moved`) → `getDraft` (**survived**) → `submitReview` with the real head (→ `ok`,
COMMENTED) → `listReviewThreads` (1 thread, body verbatim) → `getDraft` (**null** — cleared only on confirmed
success) → `replyToThread` → `listReviewThreads` (2 comments) → `resolveThread(true)` → `listReviewThreads`
and `getSnapshot` agree. `setLatency('zero')` **after** reset, since reset restores the `realistic` default.
**Conformance green and unedited**: 34 pass, and `git diff --stat` empty on both conformance files **and**
`packages/shared/conformance/suite.ts`.

**Two orchestrator decisions on M8.1.6's edges.**
1. **`newCommits === 0` deliberately NOT pinned** — only `Number.isInteger`. Accepted the implementer's call.
   D6 freezes the mock's **semantics**, not its fixture *data*: zero is a property of the mock's empty compare
   (no git objects exist behind a runtime-created pair), and a git-backed builder with a real commit list will
   legitimately return non-zero. Pinning the value would freeze an artifact as spec and force M8.3/M8.4 to
   reproduce a number that means nothing. The **presence and type** are the semantics, and those are pinned.
2. **A precision correction worth carrying to M8.4.** The head guard compares against the **live** tip, which
   is what lets a stale caller-supplied SHA fire it — but the mock's tip is deterministic per `(repo, ref)`, so
   a tip cannot move *within* the mock; the staleness must come from the caller (a 40-zero SHA here). So
   `head_moved` is "genuinely reachable" as a property of **the guard's shape**, not of a tip that moves on its
   own. **The daemon's sink must likewise compare against the live tip**, where a real amend or rebase moves it
   for real.

**2026-08-13 — M8.1.5 landed** (`bf13759`), the last unit. `fixtures/local/local-review.ts` +
`local-review.test.ts` (new), `fixtures/contract.ts`, `fixtures/index.ts`, `mock/store.ts` (`seed()` only),
`mock/scenarios.test.ts` (the two pins only). Gate: `bun run check` **exit 0 — 1240 pass · 1 skip · 0 fail ·
68 files**.

RED twice, the second being the real evidence: first `TypeError: undefined is not an object (evaluating
'fixtureDB.localReviews[0]')`; then, after the fixture landed and **before** the pins moved,
`scenarios.test.ts` went red exactly as D-a predicts — `Expected length: 10 / Received length: 11` and the
array diff `+ 1000000001`. That red *is* the proof the fixture reached `listPulls`. Green 10 + 31 pass.

**The two pins moved deliberately and remain hardcoded literals** — `toHaveLength(11)` and the exact array
`[101, 204, 312, 347, 355, 362, 389, 401, 410, 415, 1_000_000_001]`. No `toContain`, no `arrayContaining`, no
computed length; verified directly by the orchestrator against the file. The only other change in that file is
a comment recording why they are exact and that they move rather than loosen. **No other landed assertion
changed anywhere.**

**Seeded through one mechanism, and proven so by control.** `FixtureDB` gained `localReviews:
LocalReviewFixture[]` (**no `repo` field**, mirroring the creation input; synced SHAs and `lastSyncedAt` are
read back off the snapshot so a fixture cannot describe a record and a snapshot that disagree). `seed()` turns
each into a `LocalReviewRecord` exactly as a runtime creation does and **raises `localCounters.review` to a
high-water mark above every seeded id**. Verified three ways: `fixtureDB.pulls` contains no local id at all;
`listPulls()` returns the fixture's number exactly **once**; and a negative control that *also* registered the
synthesized detail as a `RemotePull` reproduced the exact double-row hazard —
`Expected length: 1 / Received length: 2` — then was reverted with `index.ts` verified byte-identical by
sha256. **A second control** forced `localCounters.review` back to `0` and showed a runtime-created review
minting `1000000001` and clobbering the seeded record; reverted, `store.ts` verified by sha256, gate re-run
green. That second control is the one that matters: without the high-water seed, the *first* review a user
creates destroys the fixture.

The fixture: local id **`1_000_000_001`** (from the imported constant, never a literal), `refs/heads/main` ←
`refs/heads/release/0.41` on `meridian-labs/atlas`, titled "Pre-tag review of release/0.41". Both branches
appear in `listBranches`, and `release/0.41` is the one branch with no PR fixture, so nothing reads as a
duplicate of a pull request.

**Judgement call accepted — the fixture is deliberately the empty compare.** A richer fixture (real files,
commits) was rejected because `syncLocalReview` always rebuilds the compare from live tips, so any fixture
carrying real files would render as permanently stale and be emptied by the user's **first sync**. The seeded
state is instead exactly what a sync reproduces, pinned by a test asserting `syncPull(id).immutable`
`toStrictEqual`s the seeded immutable half and that the live row's `compareKey` equals the snapshot's. When a
later ticket gives the mock a git-backed local sink, that test is where a richer fixture becomes safe.

**`packages/app/src/fixtures/contract.ts` is on AGENTS.md's read-only list — the edit is sanctioned, and this
is recorded so a future reader does not read it as a broken rule.** That list is *file-ownership hygiene*
("edit ONLY the files assigned to you… report it instead of editing"); the **frozen contract** is specifically
`client.ts` + `types.ts`. This ticket's `Files` line for M8.1.5 explicitly assigns `fixtures/contract.ts`, and
its Context table names `FixtureDB` as "the shape a fixture local review must be threaded into". The change is
**+34/-0, purely additive** (one new interface, one new field), verified by the orchestrator. The implementer
flagged it in its report, which is exactly what the rule asks for.

**2026-08-13 — ticket Verify run, all green.**
1. `bun run conformance:matrix` **exit 0** — `[A] PASS` mock in-process (17 tests) and `[B] PASS` revud-mock
   over HTTP (17 tests), both required legs; `[C]`/`[D]` SKIP with reasons (no `GH_TOKEN`, no broker creds).
   **`git diff main -- packages/shared/conformance/suite.ts` is 0 lines** — the contract was *extended*, never
   edited, which is the fact this Verify item exists to establish.
2. `route-surface.test.ts` green in-gate; the additive-only property is asserted rather than eyeballed.
3. `draft-isolation.test.ts` + `token-custody.test.ts` — **0 diff lines**, passing unamended.
4. `local-scenario.test.ts` green with the armed `fetch` tripwire (M8.1.6).
5. `http-contract.test.ts` green — every new wire type round-trips with `archivedPr: null` present as a key.
6. Every absence assertion carries its control, each recorded above.

**Additionally run, beyond the ticket's Verify** — because M8.1.5 changed the fixture set the browser gate
depends on: **`bun run test:e2e` exit 0**, `ALL CHECKS PASSED` in real Chrome, including
`app issued at least one real /api/* request` **and** `?mock=1 makes zero /api/* requests` — now with the
local review fixture seeded.

**The recorded `?mock=1` walk** (corroboration, never the Check). Mock build + `vite preview`, driven in a
browser. The local review appears in the inbox as `#1000000001` "Pre-tag review of release/0.41"; opening
`/pr/1000000001?mock=1` renders it with `main ← release/0.41`, `0 files`, the synced seal
`⧗ 678f4b1 · synced 5h ago`, and an empty review bar. **Network requests matching `/api/` over the whole
walk: zero.**

**Two observations from that walk, both confirming later tickets are needed rather than defects here.**
(a) The page shows the synthetic id `#1000000001`, a `Description` tab, a `Checks` tab, an `open` state chip
and an "org PR — approvable" chip in the inbox row — every one of them a GitHub-flavored affordance M8.7
exists to remove. S2's exit condition (tab strip `Conversation · Files · Commits`, no synthetic id, no rate
chip) is therefore genuinely unmet today, exactly as planned.
(b) **The rate counter moved 4,999 → 4,998 on navigation, and it is not a local spend.** `spendRate` appears
in `listPulls`, `syncPull`, `replyToThread`, `resolveThread`, `addReaction` and `submitReview`; in all five
write paths the local branch is taken **above** the spend, so the "local path never spends the bucket" claim
holds *structurally*, not merely in the test's walk. The single unit was `listPulls` re-firing — the
**remote** listing, which spends whether or not one row happens to be local. **Consequence for M8.5:** a
local-only daemon has no remote list to spend on at all, so its `listPulls` merge must tolerate the remote
half being **absent entirely**, not merely empty.

**2026-08-13 — fable adversarial pre-merge review (§8) of the full diff, and the fixes it forced.**
The reviewer read ~4,400 diff lines and every commit message, ran the suite, and ran two reverted
experiments. **It found a real defect and falsified a ruling recorded above.**

**Finding 1 (fixed, `fea7a15`) — the anti-neuter guard was vacuous, and this ticket's Log ruling 1 was
false.** `route-surface.test.ts` asserted `|ROUTES| >= |PINNED_ROUTES|`; both sides move together under a
coordinated deletion, so the assertion can only fail *after* the pin comparison has already failed. The
reviewer deleted the `deleteLocalReview` pin and all four tests stayed green. The dispatch sweep had the same
hole from the other side — its floor was still the literal `19` and was never raised when the table grew to
23 — so **precisely the four routes this ticket added were deletable** end-to-end (interface method + both
adapters + `ROUTES` entry + pin) with the bijection, the pin table, the length guard *and* the sweep all
green. **The additive-only property was not actually asserted.**
Fixed by pinning both counts as deliberate literals (`PINNED_ROUTES` size `23`; sweep floor `23`), the same
doctrine `scenarios.test.ts` uses for its row count. The controls are the acceptance and all three were run:
(a) delete one pin alone → red on the pin count; (b) coordinated deletion → red in **both** files, with the
telling result **`26 pass, 2 fail`** — the bijection and pin-match still pass exactly as the reviewer showed,
and *only* the two new literals catch it; (c) a pure addition (route + both adapters) still passes both files
untouched, proving the subset ruling was not over-tightened. The two literals live in **different packages**,
so concealing a deletion means lowering two numbers in two files. No separate subset assertion was added —
the pin-match test already records an absent route as `'MISSING'`, which no pinned value matches.
*Standing risk, now in a comment:* nothing mechanically forces the two counts to agree; a floor left trailing
the table's real size is the exact failure mode that produced this defect.

**Finding 4 (fixed, `c41ddba`) — the migration could re-arm the id clobber.** `migrateStoreDocument` defaulted
`localCounters` to `{review: 0, entity: 0}` whenever absent, with no repair against `localReviews` already in
the document; the next create then re-minted the lowest id and `putLocalReview` silently overwrote the record
holding it — the clobber the fixture seed prevents, reached by another road. RED first
(`Expected: > 1000000003 / Received: 1000000001`), then clamped to `max(stored, max(id − BASE))` over the
records actually present — review ids from the `localReviews` keys *and* each record's `id`; entity ids from
`submitted[].id`, `threads[].comments[].id` and the `commentAuthors` keys — ignoring values that are not safe
integers at or above their band, **so a partly corrupt document can only ever raise a counter, never lower
one**. Two controls ship with it: a counter already ahead of its records stays put (a max-scan implementation
would regress deletion safety and fail this), and a document with no local reviews still defaults to zeroes,
so both pre-existing migration cases pass unchanged. **Downstream reason, in a self-contained comment:** the
daemon's store gives these ids a `UNIQUE` primary key and would raise **loudly** where the mock overwrote
silently — a conformance divergence otherwise found the hard way. Beyond the overwrite the clamp changes
nothing for a well-formed document (records only ever come from minted ids, so `stored >= derived` always
holds); it additionally normalises two strictly-malformed shapes that previously survived migration and would
have minted `NaN`/fractional ids at first use. `localReviews: null` is untouched — the corruption check was
**not** widened.

**Findings 2 and 3 (pinned, not changed, `c41ddba`) — two undefined behaviours.** An undefined behaviour in
an oracle is precisely where a second implementation diverges with no gate noticing, so both are now pinned
*exactly as they are*. Neither behaviour was altered; each new assertion was inverted once, observed failing
with the real value printed, and restored.
- **The local reaction rollup is shared per review, not per person.** A second human adding an emoji already
  present receives the unchanged rollup and **no error** (`heart: 1, total_count: 1`), while a *different*
  emoji from that same human does register — which is what distinguishes "shared rollup" from "the second
  reviewer cannot react". No per-human reaction state exists, and none was built: the standing project rule
  is that reactions are shared-and-honest and per-human simulation is explicitly not to be built.
  `addLocalReaction`'s docstring now states the rule and its reason so a later reader does not "fix" it.
  **Flagged to the owner** — locally the shared-bot rationale that justifies it upstream is gone.
- **Submit before the first sync is a full success that produces nothing visible.** `status: 'ok'` with a
  positive entity-band review id; no snapshot is created, so `getSnapshot` stays `null` and
  `listReviewThreads` stays `[]`. The record is nonetheless complete — the submitted verdict is readable and
  the pull row's `unresolvedThreads` is already `1`, *an unresolved count with nothing yet openable*, which is
  the visible symptom and is now pinned. The first sync then publishes what the record held, verbatim, with
  `mutable.reviews` still `[]`. **Nothing is corrupted or lost**, so the stop condition did not fire. The test
  is purely descriptive and no docstring editorialises: whether submit should instead **refuse** before a
  first sync is a design question for the owner, and it contradicts §6's premise that the client invalidates
  "expecting them to appear". **Needs a ruling** — carried in HANDOVER.

**Nits fixed:** the frozen `ReviewThread.id` docstring promised a GraphQL `PRRT_…` node id, which a local
thread contradicts — corrected to state that nothing parses the value and no code may assume either shape
(the same precedent as the `Route.path` fix, and for the same reason: the stale text invites a parser that
breaks on one shape). The stale `revud.test.ts` describe title now says "the current version".

**Findings deliberately NOT fixed** — each recorded in HANDOVER with its reason: the synthesized `pull.user`
following the acting human (fixing it means adding a creator column to a frozen wire shape and §4.2's column
set, which M8.2 would have to mirror); the pre-existing unconditional stamping in the optimistic reply path,
which no ticket in this branch owns; direct mode forwarding a local-band id to a real GitHub GET because
`direct-router.ts:148-151` gates only `Number.isInteger(n) && n > 0` (**M8.5's dispatch must land above the
GitHub call, exactly as the mock's does**); `api-router.ts`'s pre-existing `badRequest()` emitting
`{code:'not_found'}` with HTTP 400; and the `listPulls` etag now covering local rows, so purely local activity
costs one rate unit on the next poll — consistent with D-a and intended, recorded because it is a real change
on the pre-existing surface.

**What the reviewer attacked and could not break:** frozen-file additivity; draft loss on any v1/v2/v3
migration (corruption check and version gate byte-identical to `main`); the id-band boundaries, with no
production code applying the predicate to a non-review id; **the three frozen transport semantics, which hold
structurally on the local path rather than only in the test's walk**; every stamp / tripwire / settle /
partial-key / sweep control, all real and non-vacuous; the comment convention (zero tracking artifacts in
added `packages/` lines, re-scanned by the orchestrator at the tip); and the conformance suite, untouched.
Its answers to the four standing questions: **nothing reaches GitHub from the local path** (verified
structurally *and* dynamically — `local.ts` imports no fetch or remote, all five dispatches sit above
`requireRemote`, above `spendRate`, above `bumpSyncAttempts`, and all three `prefixBody` sites are strictly
below); **no local id reaches a real-PR-keyed field** (`archivedPr` carries a real PR number *into* a local
column, the permitted direction); **returned values carry every field the optimistic path copies back**; and
**no existing GitHub-shaped semantics changed**.

**2026-08-13 — final gate after the review fixes.** `bun run check` **exit 0 — 1245 pass · 1 skip · 0 fail ·
68 files**. `bun run conformance:matrix` **exit 0**, `[A] PASS` + `[B] PASS`, `suite.ts` diff **0 lines**,
invariant tests diff **0 lines**. **Frozen contract final: `client.ts` +39/-0, `types.ts` +93/-1,
`http.ts` +27/-1 — and both deletions across all three files are docstring lines that were factually wrong.**
No signature, type or route was deleted or edited anywhere.

**2026-08-14 — M8.1.9 landed, the unit appended today** (unit count **8 → 9**). `mock/local.ts`,
`mock/local.test.ts`. Gate: `bun run check` **exit 0 — 1246 pass · 1 skip · 0 fail · 68 files**.

**The owner's ruling is what reopened this ticket.** The adversarial review pinned submit-before-first-sync
*exactly as it was* and flagged it for a ruling; the ruling came back **refuse**. Because the mock is the
specification, the refusal had to land here rather than in the daemon, and PR #70 is unmerged, so this is an
added commit and not rewritten history.

**RED first, and it was a real one** — the old success behaviour was genuinely reachable:
`expect(thrownCode(() => submitLocalReview(input))).toBe('unprocessable')` → `Expected: "unprocessable"
Received: null`, where `null` is the helper's "did not throw at all". 32 pass · 1 fail before the guard;
33 pass after, and 106 pass · 6 files over the whole `mock/` directory.

**One landed assertion block was rewritten, deliberately.** The test
`'submitting before the first sync succeeds, and its threads surface only at the next sync'` became
`'…is refused, and the same submit lands after it'`. Every moved assertion is recorded rather than summarised:
`status` `'ok'` → `unprocessable`; the submitted-verdict list `[result.review.id]` → `[]`; the row's
`unresolvedThreads` `1` → `0`. The two assertions that did **not** move — `getSnapshot` still `null` and
`listReviewThreads` still `[]` — kept their literal values with their meaning inverted: they used to prove the
success produced nothing readable, and now prove nothing was written on the way to the refusal. The file
docstring was corrected in the same commit; leaving it describing a success is exactly the stale-text trap the
`Route.path` and `ReviewThread.id` fixes were made to avoid.

**Two positive controls, one per level, both run** — each replaying the **identical** `SubmitReviewInput`
after a sync, so a blanket `throw` in `submitLocalReview` fails them: engine level (`status:'ok'`, snapshot
carries one thread, verdict on the record) and API-surface level (`listReviewThreads` returns the comment
verbatim). The refusal assertions are worthless without these and the unit is not done without both.

**Nothing else submits before a sync — verified rather than assumed.** `local-scenario.test.ts` syncs at its
third step ahead of either submit; `packages/shared/conformance/suite.ts` submits only against the seeded-draft
GitHub fixture and never creates a local review, so it needed **no edit** — the frozen shared suite was not
touched. Fixture-seeded local reviews carry both a snapshot and `lastSyncedAt`, so they are unaffected.

**The placement is a ruling in its own right.** The guard sits **above the head-moved check**, and reads the
snapshot's presence rather than `lastSyncedAt === null`. Both choices are argued in the unit's *Do* above; the
short form is that `head_moved` on a snapshotless review would route the reviewer into reconcile against
nothing, and that a guard keyed on its own dependency cannot drift from it.

# M8.12 — Delete confirmation for a review holding a draft

| field | value |
| --- | --- |
| **State** | In Review |
| **Surface** | revu:app |
| **Kind** | Feature |
| **Depends** | M8.6, M8.10 |
| **Blocks** | — |
| **Branch** | `m8.12/delete-confirm` |
| **PR** | [#83](https://github.com/pat-mw/revu/pull/83) (base `m8.9/archive-on-pr`) |
| **Assignee** | session 2026-09-02 (M8.12, orchestrator) |

## Goal

The owner ruled that deleting a local review which holds an **unsubmitted draft** must not simply proceed: it
requires an explicit force. The API half of that — the force flag and its refusal — belongs to
[M8.10](./M8.10-retention-and-gc.md), which owns retention and the delete path. The **user-facing half has no
owner**: there is no confirmation UI anywhere in the app for this, and shipping the refusal without it would
make deletion simply fail with no way through.

This ticket exists because that dialog is net-new scope discovered while ruling on prune defaults, and it was
appended rather than absorbed into M8.10 (whose Surface is `revu:revud`) or M8.6 (whose Goal is creation, not
deletion).

**Note what this is *not* about.** Deletion orphans drafts and viewed state; it never destroys them, and the
never-recycled id makes those orphans permanently unreachable rather than dangerous. So this is a guard against
**surprise**, not against data loss — which is why a confirmation is the right shape and a hard refusal is not.
The copy must be honest about that: it says the draft becomes unreachable, not that text is destroyed.

## Units

### M8.12.1 — The refusal, surfaced honestly
**Do:** Map the typed refusal M8.10 returns for a delete-with-unsubmitted-draft onto its own sentence, in the
established one-honest-sentence-per-typed-error shape (`components/review/error-copy.ts:20-29`). The sentence
names what happens — the review record goes, the draft survives on disk but nothing can reach it again — and
offers the way through. It must not claim text is deleted, and it must not use the word "lost", which the
product reserves for the guarantee that nothing was.

**Files:** `packages/app/src/lib/mode-copy.ts`, `packages/app/src/lib/mode-copy.test.ts`.
**Check:** the copy function returns a non-empty sentence for the refusal code, matches `/unreachable/i`,
matches **none** of `/deleted/i` on the draft noun, and its positive control in the same test is a different
typed error returning a different sentence — so a function that returns one string for everything fails.

### M8.12.2 — The confirm dialog
**Do:** A portal-free `ConfirmDeleteLocalReviewBody` (props: the review's ref pair, whether a draft exists, a
pending flag) plus the thin `Dialog` wrapper around it, following the split
[M8.6](./M8.6-app-creation-flow.md).3 establishes — a Radix `DialogContent` static-renders to the empty string,
so the body must be its own component or the test asserts on `''` and passes forever. The destructive action
uses the red treatment (`globals.css` tokens only; red means destructive) and is **not** the default focus.
Confirming re-issues the delete with the force flag set.

**Files:** `packages/app/src/components/confirm-delete-local-review.tsx` (new) and its `.test.ts` (new — `.ts`,
never `.test.tsx`; `createElement` + `renderToStaticMarkup`, per `components/ui/markdown.test.ts`).
**Check:** static-render the body in both states and assert on real HTML — with a draft, the markup carries the
unreachable-draft sentence; without one, it **does not**, and the positive control in the same test is that the
with-draft render **does** carry it, so the absence assertion cannot pass on an empty render. Assert the
confirm control carries the destructive variant and that the review's `base ← head` pair renders (never its
number — the identity rule holds here as everywhere).

### M8.12.3 — Wiring and the force round-trip
**Do:** Wire the dialog to the delete mutation: first attempt unforced, and on the typed refusal open the
dialog rather than surfacing an error toast. Confirming retries with force. Dismissing changes nothing —
no partial delete is possible, because the refusal happens before any record is touched.

**Files:** `packages/app/src/state/local-reviews.ts`, `packages/app/src/state/local-reviews.test.ts`.
**Check:** drive `createMockApi()` (with `mockDev.reset()` in `beforeAll`, `setLatency('zero')`, restore in
`afterAll`): create a review, save a draft, delete unforced → the typed refusal comes back and the review is
**still listed**; delete forced → gone from `listPulls`, and the draft is still readable under its own key.
That last assertion is the one that proves the orphan-not-destroy rule holds through the UI's path, not only in
the store.

## Verify

The gate (`bun run check`) is assumed. Additional to it:

1. `bun test packages/app/src/components/confirm-delete-local-review.test.ts packages/app/src/lib/mode-copy.test.ts packages/app/src/state/local-reviews.test.ts` green, each unit's test observed red before its code.
2. `?mock=1` walk (corroboration): create a review, write a draft, delete it — the dialog appears, dismissing
   leaves the review in the inbox, confirming removes it, and the copy claims no destruction of text.

## Constraints that bind this ticket

- **Never discard user-written text on failure**, and never *say* text was discarded when it was not. The copy
  is the whole point of the ticket.
- **Red means destructive; violet is reserved for draft/pending state.** `globals.css` is a read-only shared
  input — compose existing tokens, no raw hex, no new class.
- **All data goes through `api` from `@/api`** — never `@/fixtures` or `@/api/mock`.
- **The identity rule holds:** the dialog names the review by its `base ← head` pair, never by its number.
- **Comments and docstrings are self-contained** — no reference to tickets, milestones, phases or any tracking
  artifact.
- **Test-first:** each unit's test is written and observed red first; every absence assertion carries a
  positive control in the same file, and both observations go in the `## Log`.

## Open questions

1. ~~**Does the force flag belong on the wire, or is the confirmation purely client-side?**~~ **Answered by
   the owner, 2026-08-19, jointly with M8.10's Open question 3: there is no force flag, and the refusal is
   server-authoritative.** The daemon refuses `DELETE /api/local-reviews/:n` while any non-empty draft exists
   on that review; the client discards the draft explicitly and then deletes. **The frozen route set is
   untouched** — adding a force parameter was put to the owner as the §5.2 stop condition it is, and
   declined.
   What that makes this ticket: the dialog is **not** the thing that protects the draft, so it must never be
   written as though it were. The server is the authority, and the confirm exists to explain a refusal the
   user would otherwise meet as an error and to offer the discard as a deliberate second act. A scripted
   client that skips the dialog entirely still cannot destroy a draft — which is precisely why the
   client-side-only option was rejected.

## Log

_Append `**YYYY-MM-DD** — <what changed, files, gate result>` entries as work lands. Newest last._

**2026-09-02** — Picked up on `m8.12/delete-confirm`, branched from the `m8.9` tip (#82). Reconnaissance
fact that reshapes the units: **there is no delete affordance, no mutation hook and no dialog in the app**
(`deleteLocalReview` has zero call sites outside the transport adapters), so M8.12.3 builds the affordance and
the hook from nothing. Ruling applied: no force flag — confirm = discard the current human's draft, then
repeat the delete; the copy says "discard", never "lost", and never claims the text is kept.

**2026-09-02** — **M8.12.1, M8.12.2, M8.12.3 done** (one opus worker; commit `2e63f0f`). Copy in
`lib/mode-copy.ts`: `deleteLocalReviewCopy(mode, draft)` → title `Delete this local review?`, body without a
draft `Its threads, its submitted reviews and its synced history go with it. The two branches themselves are
untouched.`, with a draft the added sentence `Deleting first discards your unsubmitted draft — 3 pending
comments and a summary — and that text is not kept anywhere.`, confirm `Delete review` / `Discard draft and
delete`; `deleteLocalReviewRefusedCopy(mode)` → `Nothing was deleted — an unsubmitted draft written by someone
else is still on this review, and only they can discard it.`; an empty draft gets the no-draft copy verbatim
(no discard is sent for it, so none is promised). Dialog `components/confirm-delete-local-review.tsx`:
portal-free body (pair, never the number; destructive confirm not the default focus — cancel autofocused;
refusal sentence rendered verbatim under the frame line; both controls disabled and dismissal inert while
pending) + the Radix shell. Flow `state/local-reviews.ts`: pure `performLocalReviewDelete({ api, reviewId,
discardOwnDraft })` — `api.discardDraft` (the contract's method; my brief had said `deleteDraft`) only when
asked, then the same `deleteLocalReview` unchanged; `unprocessable` → `{ outcome: 'refused', reason }`, other
errors propagate to the error toast; `useDeleteLocalReview` refreshes through the same helper create uses
(`refreshLocalReviewLists`) after removing `qk.snapshot`/`qk.draft`; the header's `DeleteLocalReviewAction`
(local mode only, ghost button, danger on hover/focus) opens the dialog with `held` from `useDraft` +
`draftHoldsText`, navigates to `/` on `deleted`. First reds: `Export named 'deleteLocalReviewCopy' not
found`; the copy sweep's coverage guard (`+ ["deleteLocalReviewCopy", "deleteLocalReviewRefusedCopy"]`);
`Cannot find module './confirm-delete-local-review'`; `Export named 'refreshAfterLocalReviewDelete' not
found`. Falsified after green: `not kept anywhere` → `lost` (3 copy tests red), body always with-draft
(absence + control red), header gate → `true` (structural pin red). App tests 954 → **992 pass**.

**2026-09-02** — **`?mock=1` walk (Verify 2), orchestrator, on the seeded live review `main ← release/0.41`:**
Delete review → dialog `Delete this local review? / main ← release/0.41 / Its threads, its submitted reviews
and its synced history go with it. The two branches themselves are untouched.`, focus on **Cancel**; Cancel →
dialog closed, review intact. Start review → summary typed → draft saved → Delete review → the body adds
`Deleting first discards your unsubmitted draft — a summary — and that text is not kept anywhere.`, confirm
reads `Discard draft and delete` with the danger tokens (`bg-danger/12 text-danger …`); confirm → `/`, the
review absent from the inbox (the two archived walk reviews remain). Copy claims no destruction beyond the
explicit discard, and never says "lost".
On an **archived** review (`main ← feat/gateway-rate-limiting`, superseded by #312): the action renders, the
dialog names the pair with the no-draft body, confirm → `/`, the review gone. Gate on the M8.12 tip:
**3315 pass · 1 skip · 0 fail · 117 files** (`env -u GH_TOKEN -u GITHUB_TOKEN TZ=UTC bun run check`).

**2026-09-02** — **Adversarial review of `2e63f0f` (fable tier, own worktree, 11 mutations): do not open
as-is.** **F1 blocker:** `onSettled` dropped the cached draft whenever a discard was *requested*, including
when the discard threw — and the cache is the editing surface, so an edit whose save had failed (the same
outage) existed nowhere afterwards; demonstrated with `setFailureMode('writes')`. Ticket constraint 1
violated. **Should-fix:** F2 discard-then-failed-delete surfaced without mentioning the draft; F3 an errored
or stale draft read yields "written by someone else" over the reader's own text; F4 both producers put the
synthetic id in the refusal sentence (`Local review 1000000004 still holds…`) and the dialog renders it
verbatim — the identity rule broken exactly in the refusal state; F5 the discard bypassed `dropPersistState`,
so a pending debounced save can re-create the draft between discard and delete or strand an orphan after it
(the mock accepts a save under a deleted id; the daemon refuses); F6 "Nothing was deleted" after the reader's
own draft was discarded; F7 red on hover only. Notes: `qk.viewed` left behind; dismissal inert while the
draft merely loads; focus drops to `body` after a refusal; the header wiring mutations were all vacuous. Six
asked mutations each red. Fixes dispatched as one worker; the PR opens behind them.

**2026-09-02** — **Review findings landed** (commit `dbdf3dd`, one opus worker, each with a red or a
control). `performLocalReviewDelete({ api, reviewId, discard })` never throws and answers
`{ outcome: 'deleted' | 'refused' | 'failed', discarded }`; a failed discard ends the attempt (the delete is
never sent); `applyLocalReviewDeleteToCache` drops `qk.draft` **only when `discarded === true`** (F1's
reproduction — an unsaved edit under `setFailureMode('writes')` — now byte-identical afterwards), refreshes
the lists only on `deleted`, and removes `qk.viewed` too (F8). The discard is an injected callback that runs
the draft store's own path (`dropPersistState` first — F5), order pinned `discard → delete`; the mock's
`saveDraft`/`getDraft`/`discardDraft` on a deleted local id now answer `not_found` like the daemon (F5's
mock half). Both producers' refusal reads `This local review still holds an unsubmitted draft with text in it
— discard that draft, then delete the review.` — no id (F4), `/discard/i` kept, conformance unchanged. Copy:
`deleteLocalReviewRefusedCopy(mode, { discarded })` (`Your draft was discarded, but nothing was deleted: an
unsubmitted draft written by someone else…` / `Nothing was deleted — an unsubmitted draft with text in it is
still on this review, and it has to be discarded before the review can go.` — F3/F6),
`deleteLocalReviewFailedCopy` (`Your draft was discarded, but the review could not be deleted — it is still
here, and that text is not.` / `Couldn't delete this review` — F2), `deleteLocalReviewDraftUnreadableCopy`
(no delete offered while the draft is unread — F3). `busy` vs `draftRead` split (F9), focus returns to
Cancel on a refusal (F10), `focus-visible:` danger tokens (F7), controls moved in-body and eight source-scan
wiring pins each falsified one at a time (F11). App **1025 pass**; revud 2102 · 1 · 0; shared 202.
Contract gap recorded, not a contradiction: no shared block aims a draft verb at a deleted review, so that
`not_found` is pinned per implementation only.

**2026-09-02** — **In Review on [#83](https://github.com/pat-mw/revu/pull/83)**, base `m8.9/archive-on-pr`,
two commits. Gate on the tip **3348 · 1 · 0 · 117**. 3/3 units; Verify 1 green (each unit's test observed
red first), Verify 2 walked on the mock (both dialog states, cancel, confirm on a live and on an archived
review; re-walked after the fixes). Not merged; the owner merges bottom-up.

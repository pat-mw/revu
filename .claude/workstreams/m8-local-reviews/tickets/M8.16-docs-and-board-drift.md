# M8.16 — Docs, design doc, guides, runbook and board: every stale sentence

| field | value |
| --- | --- |
| **State** | Todo |
| **Assignee** | — |
| **Surface** | docs |
| **Kind** | Docs |
| **Depends** | Seven units wait on siblings written in the same session — **M8.16.9** on `M8.13.1` (the live list row), **M8.16.10** on `M8.13.5` (the inbox order), **M8.16.16** on `M8.14.1` (the real-mock parity leg), **M8.16.11** on M8.15's questions **2 and 8** (the pin and the blob rulings — question 2 is *"what bounds a live review's pin set?"*, `M8.15.3` / `M8.15.4`; question 3 is a different ruling entirely), **M8.16.17 item 2** on M8.15's **question 11** (`M8.15.21` / `M8.15.22` — where M8.9 OQ2's accepted cost was carried), and **M8.16.24 / M8.16.25** on M8.15's question 1 (the archived-draft ruling). Confirm those ids against `BOARD.md` before dispatching; every other unit is startable now |
| **Blocks** | the `MILESTONES.md` M8.12–M8.17 mirror step (M8.16.20 leaves it a pointer, not the seed) |
| **Branch** | `m8.16/docs-and-board-drift` |
| **PR** | — |

## Goal

The landing audit of 2026-09-02 read every ticket, every design section and every user-facing page against
the tree. The code came out of it well: **not one of the sentences below is a code defect.** They are all the
same shape — a sentence written before a ruling, an owner call, or a measurement, and never revisited after
the code moved under it. A design document that describes a schema the store does not have, a guide that
promises archive coverage a path-keyed review will never get, a runbook naming a flag no deployment can set,
and eight ticket units that contradict their own Logs.

This ticket closes them, one true sentence at a time. **Each unit names the file, quotes the stale sentence
verbatim, and states the sentence that replaces it**, so a cold agent can do the edit without re-deriving the
truth — but every unit still starts by opening the seam, because a ticket's line pointers rot and the tree is
the authority over this file.

**Scope: prose.** The one exception is a comment inside `packages/revud/src/index.ts` (M8.16.3) and one new
gate test (M8.16.23). No behaviour changes anywhere in this ticket; the gate count is expected to move only
by the tests the guard adds.

## Units

Units .1–.7 are the design doc, .8–.10 the user guide, .11–.12 the operator and security docs, .13–.19 ticket
prose, .20–.22 the milestone records and the board, .23 the guard, .24/.25 the owner's alternatives. **All of
them are mutually independent** and can be fanned out freely, except the **seven** named in the header's
`Depends` row (M8.16.9, .10, .16, .11, .17 item 2, .24 and .25), which wait on a sibling ticket's unit.
**Four deliberately stand down** where a sibling owns the same paragraph — M8.16.10, M8.16.11 and both of
M8.16.24 / M8.16.25 — and a fifth stands down in half: **M8.16.17's item 2** is M8.15's question 11, so it
verifies and records rather than writing the sentence a second time. Saying so beats writing the same
sentence twice in two tickets and clobbering one of them.

### M8.16.1 — Amend §3.1, §4.2, §8 case 6 and §8 #23 to the landed schema, minting and eviction
**Do:** Four sentences in `docs/agent/LOCAL_REVIEWS.md` describe a store that has not existed since M8.2 and
M8.10. Correct each, and say in one clause *why* it is that way, naming the ruling's substance rather than
the ruling's id.

1. **§3.1 (`:102-103`).** Stale: *"Local ids are stable across restarts via a `UNIQUE(repo, base_ref,
   head_ref)` constraint plus `MAX(id) + 1` minting — creating the same review twice returns the existing one
   rather than minting a second."* True: the unique key is `UNIQUE(repo, base_ref, head_ref, generation)`
   with `generation` a constant `0` — a no-migration escape hatch for the day a pair must be re-openable —
   and ids are minted from a durable high-water mark in `meta` (`local_review_id_high_water`), **not** from
   `MAX(id) + 1`, because `MAX(id) + 1` re-issues the id of a review that was deleted. Duplicate creation
   still returns the existing review.
2. **§4.2 DDL (`:149`, `:152`).** The block lacks `generation INTEGER NOT NULL DEFAULT 0` and its `UNIQUE`
   line is three-column. Add the column and the fourth key member so the printed DDL matches
   `store.ts:2375,2379`.
3. **§8 case 6 (`:286`).** Stale: *"Duplicate (repo, base, head) → returns the existing review; the `UNIQUE`
   constraint enforces it."* True: the same, *with the generation column carrying the constraint*, so a
   reader who greps for the constraint finds the four-column one.
4. **§8 #23 (`:336`).** Stale, and in the present tense: *"**Zero eviction exists anywhere in the store
   today** — the only `DELETE` is `deleteDraft`."* That was the premise M8.10 was written from and is now
   false. True: state it as the premise it was, then what shipped — `deleteLocalReview`, `deleteImmutables`
   and `deleteBlobs`, the immutable prune running after every successful local sync and after a delete, and
   the blob pass shipping dark behind a policy the caller has to pass.

**Shared paragraph:** M8.15's pin ruling (answer A) appends a sentence to this same `§8` #23 — *the pins of a
live review are never dropped*. Whichever unit runs second lands **on top of** the other; neither reverts it,
and the reader of #23 should end up with one paragraph carrying both facts.
**Files:** `docs/agent/LOCAL_REVIEWS.md`.
**Check:** `grep -n "MAX(id) + 1" docs/agent/LOCAL_REVIEWS.md` returns **only** the sentence that explains why
the store does *not* mint that way (zero bare promises); `grep -c "UNIQUE(repo, base_ref, head_ref)$"` is 0
and `grep -c "UNIQUE(repo, base_ref, head_ref, generation)"` is ≥ 2; `grep -n "Zero eviction exists"` returns
nothing in the present tense. Corroboration, run before and after: the four-column key and the meta key are
still what the store carries — `grep -n "UNIQUE(repo, base_ref, head_ref, generation)\|local_review_id_high_water" packages/revud/src/direct/store.ts`
prints `:2379` and `:2448`. **Control:** the greps above are red before the edit (each stale string is
present) and green after; record both runs in the `## Log`. There is no gate test over this file — that
absence is exactly what M8.16.23 argues about, and it is why the before/after grep pair is the honest Check
rather than a green test.
**Closes:** D3-g1, D4-g1, D8A-g8, D8B-g1, D9-g5.

### M8.16.2 — State the `-uno` exclusion in D3, §4.2, §8 case 16 and the guide §3
**Do:** The dirty-worktree banner is driven by `git status --porcelain=v1 -uno`, so **an untracked file never
makes a worktree dirty** and never raises the banner, even though its content is genuinely outside the review.
That narrowing is a recorded owner ruling; three docs describe the wider behaviour.

1. **`docs/agent/LOCAL_REVIEWS.md` D3 (`:68`).** Stale: *"`git status --porcelain` drives an explicit 'you
   have uncommitted changes that are not in this review' banner."* True: `git status --porcelain=v1 -uno`
   drives it — **tracked** modifications raise the banner; a file git has never been told about does not,
   because "dirty" means work git is already tracking has moved, and an untracked file is not yet part of the
   branch at all.
2. **§4.2's `dirty` column line (`:149`, the `dirty INTEGER NOT NULL DEFAULT 0,` row).** Add the same
   qualifier in half a clause: `dirty` records tracked modifications only; **an untracked file never sets
   it.** Say "untracked" in as many words — the Check below counts that word in each of the four places, so a
   paraphrase that drops it fails a correctly executed unit.
3. **§8 case 16 (`:313`).** Stale: *"Worktree dirty at sync → recorded on the review and surfaced as a banner
   (D3)."* True: add *"— tracked changes only; an untracked file is not dirty."*
4. **The guide `packages/docs/content/docs/guides/local-review.mdx` §3 (`:76-91`).** The section says edits in
   the working tree are in none of the review (true, keep) and that the banner appears *"when the working tree
   was dirty at the last sync"* (`:80-82`). Add one sentence after the banner blockquote: a file you have not
   added to git yet is not in the review **and** does not raise the banner — commit it, or `git add` it and
   commit, to bring it in. Do not weaken the §3 promise; this makes it precise.

**Files:** `docs/agent/LOCAL_REVIEWS.md`, `packages/docs/content/docs/guides/local-review.mdx`.
**Check:** `grep -rn "porcelain" docs/agent/LOCAL_REVIEWS.md` shows the `-uno` form and no bare
`--porcelain`; `grep -n "untracked" docs/agent/LOCAL_REVIEWS.md packages/docs/content/docs/guides/local-review.mdx`
returns a hit in each of the four places above (zero before the edit — that is the control). Corroboration:
`sed -n '1494,1510p' packages/revud/src/direct/local-sync.ts` still shows
`args: ['status', '--porcelain=v1', '-uno']`, and `bun test packages/revud/src/direct/local-sync.test.ts`
stays green with the row named *"an uncommitted change is what makes a worktree dirty, and an untracked file
is not one"*. `bun run docs:build` green (the guide is MDX; a malformed edit breaks that job and nothing
else).
**Closes:** D3-g2, D4-g4.

### M8.16.3 — Say "direct mode alone" in D5, in both decision records, and in the `index.ts` comment
**Do:** Five sentences, across four files, claim local reviews ride inside direct **and broker** mode. Only `mainDirect` builds the
local surface: `index.ts:387-391` constructs `createLocalReviewSurface` and passes `localReviews` into
`createDirectApi`; the broker assembly at `:563-580` passes none, so a local-band id in broker mode meets a
typed `not_found` before any client read. (The broker *does* wire the archive listing seam at `:577` — see
M8.16.17; a seam with no local review to ask about is inert, and that is a separate sentence.)

1. **`docs/agent/LOCAL_REVIEWS.md` D5 (`:70`).** Stale: *"**A capability inside direct/broker mode, not a
   fourth mode.**"* True: *"**A capability inside direct mode, not a fourth mode.**"* Keep the rationale
   sentence — `REVU_MODE` is a credential-custody and bind axis — and add the fact: the direct boot is the
   only one that builds the local surface today, so nothing local exists in broker mode to serve or to leak.
2. **`.claude/workstreams/m8-local-reviews/MILESTONE.md` D5 row (`:72`).** The same phrase, the same fix.
3. **`packages/revud/src/index.ts:127-129`.** Stale: *"local reviews are a capability that rides inside
   direct and broker mode, which is why nothing here touches `resolveMode` or `BootMode`."* True: a capability
   that rides inside a boot mode rather than being one — **built by the direct boot alone today** — which is
   why nothing here touches `resolveMode` or `BootMode`. Self-contained wording only: no ticket id, no audit
   id, no PR number in the comment.
4. **`docs/agent/MILESTONES.md:426` and `:473`.** Two more carriers of the same phrase, and the second is a
   live local-review sentence, not a summary: `:426`'s decision list reads *"D5 a capability inside
   direct/broker mode, not a fourth mode"*, and `:473`, the first line of the `### Issue M8.5` seed, reads
   *"Local reviews are a capability inside direct/broker mode (D5), dispatched on the id band."* Same phrase,
   same fix — direct mode — keeping *"not a fourth mode"* and *"dispatched on the id band"* untouched. Two
   other units write to this file (M8.16.20 appends after `:521`, M8.16.21 amends `:434`); the three sets of
   lines are disjoint, but land them on one branch and re-read the file before pushing.

The user-facing page already says it correctly (`packages/docs/content/docs/run-modes/index.mdx:32`,
*"a capability *inside* direct mode alone"*) — do not touch it; it is the sentence the others are being
brought into line with.
**Files:** `docs/agent/LOCAL_REVIEWS.md`, `docs/agent/MILESTONES.md`,
`.claude/workstreams/m8-local-reviews/MILESTONE.md`, `packages/revud/src/index.ts` (comment only).
**Check:** scoped to the four files this unit edits, and to nothing else —
`grep -n "direct/broker\|direct and broker" docs/agent/LOCAL_REVIEWS.md docs/agent/MILESTONES.md .claude/workstreams/m8-local-reviews/MILESTONE.md packages/revud/src/index.ts`
is **5** hits before (`LOCAL_REVIEWS.md:70`, `MILESTONES.md:426`, `MILESTONES.md:473`, `MILESTONE.md:72`,
`index.ts:128`) and **0** after. Do **not** substitute a repo-wide `grep -rn ... docs packages
.claude/workstreams`: it can never return nothing, so it asserts nothing.
`packages/revud/src/server.ts:219` (*"Build the direct/broker request handler"*) and
`packages/revud/src/direct/failure-drills.test.ts:3` are about the shared **request handler**, are true, are
code a docs unit must not touch, and stay; `HANDOVER.md:57`, `tickets/M8.5:19` and `:612`,
`tickets/M8.11:833`, both `AUDIT-2026-09-02` files and this ticket's own quotation of the stale sentence are
append-only records and stay; and the recursive walk also crawls `packages/*/node_modules` and the untracked
`packages/revud/dist`.
`git diff 84393c0 -- packages/revud/src/index.ts` shows **comment lines only** — every changed line begins
with ` *` or `//` — which is the assertion that this ticket changed no behaviour. The baseline is the
branch's **base tip `84393c0`, never `main`**: nothing of M8 has merged, so
`git diff --numstat main -- packages/revud/src/index.ts` is `284 18` — the whole M8 boot wiring — and would
drown the one comment this unit touches.
`env -u GH_TOKEN -u GITHUB_TOKEN TZ=UTC bun test packages/revud/src/local-boot.test.ts packages/revud/src/mode-select.test.ts`
green. **Control for the code file:** the diff assertion is falsifiable by construction — a stray non-comment
line fails it; run `git diff 84393c0 -- packages/revud/src/index.ts | grep '^[+-]' | grep -v '^[+-][+-]'` and
read every line before claiming it.
**Closes:** D3-g3, D6-g1, D7-g4, EC3-g4.

### M8.16.4 — §5 and §5.1: the absent client, the real pin ref names, the re-sync refusal
**Do:** Three consecutive paragraphs of `docs/agent/LOCAL_REVIEWS.md` describe an implementation that was
deliberately built differently, each with the reasoning already recorded in the code.

1. **§5 (`:197-199`).** Stale: *"`provisionBlobs` is reused as-is except for one flag: it must be told
   **local-only, never fall back to the API**."* True: **there is no flag.** `blobs.ts:60-84` makes `github`
   and `repo` optional dependencies that travel together, and the local sync path omits both
   (`local-surface.ts:735-743`), so the hosted tier is not entered at all — an absent client is a stronger
   statement than a flag saying "do not call it", because a flag leaves a callable client one forgotten
   branch away. That structural absence is also how D7 is enforced on this path.
2. **§5.1 (`:208`).** Stale: *"write a ref per snapshot under `refs/revu/reviews/<id>/<compareKey>`"*. That
   name **cannot be created**: a compare key is `<mergeBase>...<head>` and git rejects any ref name containing
   `..`. True: two refs per synced compare,
   `refs/revu/reviews/<localId>/<mergeBaseSha>-<headSha>/base` and `…/head`, written by one
   `git update-ref --stdin` batch so a half-pinned compare is unrepresentable; the `...` → `-` substitution
   happens in exactly one place and is deliberately not invertible (callers list refs, they never parse them).
3. **§5.1 (`:203-206`).** Stale: the mass-`lost` degradation presented in the present tense as the live
   failure mode — *"a rebase that changed nothing in a file could mass-classify every comment on it as
   `lost`, purely from missing objects."* True: keep it as the **hazard the design is built against**, then
   say what shipped — a pre-flight probes `hasBlob` for every anchor object before any classification runs
   and refuses the whole reconcile with a typed `not_found` naming a re-sync instead of classifying anything,
   and the API edge answers the same typed error for a snapshot whose objects are gone. Absence of the object
   and absence of the line are different facts and no longer share an answer.

**Files:** `docs/agent/LOCAL_REVIEWS.md`.
**Check:** `grep -n "one flag" docs/agent/LOCAL_REVIEWS.md` returns nothing;
`grep -n "reviews/<id>/<compareKey>" docs/agent/LOCAL_REVIEWS.md` returns nothing outside a sentence that says
git rejects it; `grep -n "/base\`, and\|/head\`" docs/agent/LOCAL_REVIEWS.md` finds the real pair — the file
operand is not optional, a grep without one reads stdin and hangs. Corroboration, run against the tree:
`bun test packages/revud/src/direct/local-pins.test.ts` green with the row *"the design's literal compare-key
form is rejected, which is why the substitution exists"* (`:125`), and
`bun test packages/revud/src/direct/reconcile.test.ts packages/revud/src/direct/local-objects-missing.test.ts`
green with *"nothing is classified when an object is missing"* (`reconcile.test.ts:469`) and *"the edge
answers a re-syncable not_found, not a corrupt daemon"* (`local-objects-missing.test.ts:227`). **Control:**
before/after greps for each of the three stale strings, both runs in the `## Log`.
**Closes:** D5-g1, D5-g2, D5-g3.

### M8.16.5 — Correct §4.1's `etag` and `rateLimit` rows
**Do:** Two rows of the synthesis table in `docs/agent/LOCAL_REVIEWS.md` describe neither transport
precisely.

1. **`:129`.** Stale: *"`PullListResponse.etag` | hash of the local reviews' `compareKey`s"*. True: the etag
   is composed from **both halves always** — the poll source's own etag (the empty string when no loop is
   wired) and a hash of the **whole serialized local rows as served**, never a projection of them. That is
   what makes the etag move when an archive flips a row's state or a thread resolves, and it is why a client
   replaying a matching etag can never be starved of the other half's change.
2. **`:130`.** Stale: *"`PullListResponse.rateLimit` | the mock's synthetic shape (large limit, `used: 0`)"*.
   True: **the two transports differ, deliberately.** revud reports the unspent shape
   (`limit`/`remaining` at `Number.MAX_SAFE_INTEGER`, `used: 0`, epoch reset) only for a list assembled
   without touching GitHub, and the poll's live figure for a merged list; the mock reports its **shared 5000
   bucket** and spends 1 on an etag miss, because in the mock a local review is one row among pull requests in
   the same list. Both are honest about what the call spent; neither is "the mock's synthetic shape".

**Files:** `docs/agent/LOCAL_REVIEWS.md`.
**Check:** `grep -n "synthetic shape" docs/agent/LOCAL_REVIEWS.md` returns nothing. Corroboration:
`sed -n '456,476p' packages/revud/src/direct/direct-api.ts` still shows `reviewListEtag(pollEtag, localItems)`
hashing both halves and `unspentRateLimit()` returning `used: 0`; `sed -n '276,287p' packages/app/src/api/mock/adapter.ts`
still shows `store.rateInfo()` and `store.spendRate(1)`. **Control:** the two before/after greps.
**Closes:** D4-g3.

### M8.16.6 — Three one-line inaccuracies: the board pointer, D2's routers, §7's query keys
**Do:** Three single sentences in `docs/agent/LOCAL_REVIEWS.md`, each wrong in a different way.

1. **`:5`.** Stale: *"`docs/agent/MILESTONES.md` carries the ticket breakdown, and the Linear board carries
   the units."* It is the first sentence a reader follows and it points at a board that cannot hold them —
   the workspace hit its free issue cap. True: the ticket breakdown and the units both live on the local
   board at `.claude/workstreams/m8-local-reviews/` (`BOARD.md` first), which `MILESTONES.md:5` and `:422`
   already say.
2. **`:67` (D2's rationale), and the same phrase in `.claude/workstreams/m8-local-reviews/MILESTONE.md`'s D2
   row.** Stale: *"both routers' `Number.isInteger(n) && n > 0` gates"*. True: the direct router gates
   `Number.isInteger(n) && n > 0` (`direct-router.ts:259`); the mock router's `intParam`
   (`api-router.ts:115-120`) checks `Number.isInteger` only, identically on `main`. D2's claim survives
   either way — the band is positive — so state the two gates as they are rather than flattening them.
3. **`:249`.** Stale: *"That is D2's payoff — no new routes, no duplicated regexes, no new query-key
   factories, no `NaN` collisions."* Literally false: `qk.branches` and `qk.localReviews` were both added
   (`queries.ts:45-52`) for the two new list endpoints. True: **no new *per-review* query key** — every key
   under `/pr/:n` is reused unchanged with the synthetic `n`; the two new keys belong to the two new list
   endpoints, and they are deliberately their own entries so no review is ever described by two cached
   truths.

**Files:** `docs/agent/LOCAL_REVIEWS.md`, `.claude/workstreams/m8-local-reviews/MILESTONE.md`.
**Check:** all three greps scoped to the two files this unit edits, as the other two already are —
`grep -n "Linear board carries" docs/agent/LOCAL_REVIEWS.md` returns nothing;
`grep -n "both routers" docs/agent/LOCAL_REVIEWS.md .claude/workstreams/m8-local-reviews/MILESTONE.md` is
**2** hits before (`LOCAL_REVIEWS.md:67`, `MILESTONE.md:69`) and **0** after;
`grep -n "no new query-key factories" docs/agent/LOCAL_REVIEWS.md` returns nothing. A repo-wide
`grep -rn "both routers" docs .claude/workstreams` can never go green and must not be substituted: the phrase
also lives in `tickets/M8.1-contract-and-mock.md:23` (its Goal), `LOG.md:27`, `HANDOVER.md:1853`, both
`AUDIT-2026-09-02` files and this ticket's own quotation — append-only records, all of which stay.
Corroboration:
`grep -n "isInteger" packages/revud/src/api-router.ts packages/revud/src/direct-router.ts` still shows the two
different gates, and `sed -n '45,52p' packages/app/src/state/queries.ts` still shows both new keys.
**Control:** the three before/after greps, recorded.
**Closes:** D5-g5, D3-g5, D7-g5.

### M8.16.7 — Move §8 cases 4 and 5 to first sync
**Do:** `docs/agent/LOCAL_REVIEWS.md` §8 lists *"4. Unrelated histories, no merge base → typed error naming
the two refs"* and *"5. Shallow clone → `git merge-base` can fail; detect and say so"* (`:284-285`) under
**Creation**. `createLocalReview` deliberately resolves **no SHAs** (pinned by `local-surface.test.ts:417`,
*"resolves no SHAs at create — the review starts unsynced"*), so neither condition can surface at creation:
the dialog lands the user on the review and **the first sync** reports both. Move the two cases into the
Re-sync group (or add a "First sync" line above case 11) and say plainly that create resolves nothing, so
every ref-relationship failure is a sync answer. Add one clause noting the consequence the app carries: the
create dialog's unrelated-histories copy is a client-side sentence for a server answer that arrives later —
`packages/app/src/components/create-local-review.test.ts:311` exercises exactly that message.
**Files:** `docs/agent/LOCAL_REVIEWS.md`.
**Check:** `sed -n '/^### Creation/,/^### Re-sync/p' docs/agent/LOCAL_REVIEWS.md | grep -c "merge base\|Shallow"`
is 0, and the same grep over the Re-sync group is ≥ 2. Corroboration:
`bun test packages/revud/src/direct/local-surface.test.ts` green with the named row. **Control:** the two
greps swap values across the edit; record both.
**Closes:** D8A-g3.

### M8.16.8 — Add the path-keyed identity caveat to the guide §7 and to `direct.mdx`
**Do:** `packages/docs/content/docs/guides/local-review.mdx:181-182` says *"A local-only daemon that *did*
resolve an origin and a credential keeps the half and archives exactly as any other does."* §7 reads as the
complete account of when a local review archives, and this is the one condition it never states: **a local
review's repository identity is fixed when the review is created** — `owner/name` if an `origin` parsed then,
otherwise the repository's absolute path (`local-git.ts:212-238`, whose docstring records that rows keyed
earlier keep the path-shaped identity "deliberately"). The archive detector never asks about a path-shaped
identity (`local-archive.ts:193`, `if (!isOwnerNameShaped(review.repo)) return { archivedPr: null, requested:
false }`, pinned by `local-archive.test.ts:184`), and a daemon that later boots *with* the origin does not
serve that row at all — `listLocalReviews` is `WHERE repo = ?`, so the path-keyed review is not in its inbox.

Add one sentence to §7, after the sentence above: a local review is keyed to the repository identity the
daemon had when the review was created, so one made in a workspace that had no `origin` is never archived —
and once an `origin` is added, that daemon no longer lists it at all; create the review again under the new
identity if archiving matters. Mirror the same fact in one clause in
`packages/docs/content/docs/run-modes/direct.mdx:179-183`, whose archive bullet today covers only *"absent in
a workspace with no remote"*. This is the obligation M8.2's identity rule already recorded — that the
consequence "must be stated wherever it surfaces" — and the guide is where it surfaces.
**Files:** `packages/docs/content/docs/guides/local-review.mdx`,
`packages/docs/content/docs/run-modes/direct.mdx`.
**Check:** `grep -n "created" packages/docs/content/docs/guides/local-review.mdx | sed -n '/identity/p'`
finds the new sentence, and `grep -rn "archives exactly as any other does" packages/docs` returns the sentence
only where the caveat follows it within two sentences (read it, do not trust the grep alone). Corroboration:
`env -u GH_TOKEN -u GITHUB_TOKEN TZ=UTC bun test packages/revud/src/direct/local-archive.test.ts` green with
*"a path-shaped repository identity is never asked about"*. `bun run docs:build` green. **Control:** the guide
grep is empty before the edit — record that run.
**Closes:** D8B-g2.

### M8.16.9 — Reword the guide §5's "can go stale" to match the landed staleness
**Do:** `packages/docs/content/docs/guides/local-review.mdx:136-140` says *"The merge base is computed against
the live tip of the base branch, so the review can go stale — and its compare key can move — with zero new
commits on the head."* The compare key half is true and tested. The staleness half is not reachable today:
the list row's `compareKey` and `head.sha` are both stamped by the same sync that stored the snapshot, so the
row and the snapshot always agree and the seal never turns gold before a re-sync. **This unit runs after
`M8.13.1` (serve the local list row from live ref tips)** and does one of two things, decided by what
actually landed:

- If M8.13.1 made the row live (a base advance shows as stale without a sync), **the sentence is already true**
  — leave it, and record in the `## Log` that it was re-read against the landed behaviour and kept, naming
  the test that now proves it.
- If M8.13.1 did not, **reword**: the merge base is computed against the live tip of the base branch, so a base
  advance changes what the review covers with zero new commits on the head — and the next sync is when you
  see it. A pull request cannot do that at all: GitHub only refreshes a pull request's base SHA when it
  records a synchronize event.

Either way the D8 difference stays stated; only the promise about *when it is visible* moves.
**Files:** `packages/docs/content/docs/guides/local-review.mdx`.
**Check:** whichever branch is taken, the claim in the guide and the assertion in the gate agree. If kept:
the test M8.13.1 landed asserting a local row goes stale with no re-sync is green, and its **control** (revert
the live-row change, watch that test go red) is recorded. If reworded: `grep -n "can go stale"` on the guide
returns nothing, and `grep -n "next sync"` finds the replacement. `bun run docs:build` green.
**Closes:** D8A-g2.

### M8.16.10 — Verify the guide §2's "drops below" rule against the landed order
**Do:** **Read M8.13.5 before touching anything here.** `packages/docs/content/docs/guides/local-review.mdx:61-65`
says the Local reviews section *"sits at the top while any of them is open — work in hand outranks pull
requests — and drops below every other section once they are all closed"*. Today's code does not do that:
`inbox-sections.ts:316-327` places the section first whenever **any local row is listed** (`local.length > 0`),
and an archived review is still listed (`showsInInbox` keeps every local item regardless of `pull.state`,
deliberately, because a local review has nowhere else to be read) — so a reader whose only local review is
archived sees the section first. **M8.13.5 rules that the guide is the promise and the code is what moves**,
and says in its own words that it changes no doc.

So this unit does not rewrite the sentence. It **checks it**, after M8.13.5 lands:

- If M8.13.5 landed, re-read §2 against the landed order — the section leads on an *open* local row and sits
  last, still carrying its rows, otherwise — and record in the `## Log` that the sentence was verified true,
  naming the test that proves it. Add nothing.
- If M8.13.5 did **not** land (the plan changed, or the owner declined), the guide is then describing
  behaviour that does not exist, and only then reword it: the section leads whenever the inbox is listing any
  local review, archived ones included, and drops below every other section when the workspace holds local
  reviews but none of them is in the list in front of you.

Either way, one nuance survives both branches and is worth one clause if §2 is edited at all: the bottom slot
also fires when a **filter** hides every local row (`inbox-sections.test.ts:339`), which is not "all closed".
**Files:** `packages/docs/content/docs/guides/local-review.mdx` — **and only if the second branch is taken.**
**Check:** whichever branch is taken, the guide and the gate agree, asserted rather than assumed. If verified:
`env -u GH_TOKEN -u GITHUB_TOKEN TZ=UTC bun test packages/app/src/lib/inbox-sections.test.ts` green with
M8.13.5's new case (an archived-only reader gets `[...GITHUB_SECTIONS, 'local']` **with the row still in it**),
and the `## Log` carries the run. If reworded: `grep -n "once they are all closed" ` on the guide returns
nothing, and `bun run docs:build` is green.
**Closes:** — (task scope; no audit id. The sentence was flagged by the task; reading the seam showed the
defect is in the code, and M8.13.5 owns it.)

### M8.16.11 — Operator runbook: the residue M8.15's rulings do not cover
**Do:** **Read M8.15's questions 2 and 8 before touching this file** — question **2** is *"what bounds a live
review's pin set?"* (`M8.15.3` / `M8.15.4`); M8.15's question 3 is *"Deleting an archived review: allowed or
refused?"* (`M8.15.5` / `M8.15.6`) and is not this unit's business. Both of the runbook's stale paragraphs
are already owned there, as owner questions with both answers written as units, and this ticket must not write
either sentence a second time:

- *"**Blob reclamation is off by default**, behind an explicit policy flag a deployment has to set"*
  (`:201-205`) — measured false today: the switch is `DirectApiDeps.blobRetention` (`direct-api.ts:408`,
  consumed at `:643`), no boot site passes one, and the environment table (`:378-390`) names no variable for
  it. **M8.15.15 wires `REVU_RECLAIM_BLOBS` and makes the sentence true; M8.15.16 rewords it instead.** Either
  way the paragraph is theirs.
- The "what grows" account (`:195-224`) names immutables and blobs and not **pins**, which grow two refs per
  re-sync and are dropped only by a review's delete. **M8.15.3 documents the growth as accepted; M8.15.4
  prunes it.** Also theirs.

What is left for this ticket is the one operator fact neither answer carries, recorded in M8.8's Landmines and
never given a doc line: **pinned refs live in the common git dir**, so a pin written from a linked worktree is
visible from the main one and from every other linked worktree of the same repository — which is what makes
`git for-each-ref refs/revu/reviews/` the same answer wherever it is run, and what makes a `git gc` in any one
of them the operation that actually reclaims the space once pins are dropped. Add that clause to whichever pin
paragraph M8.15's answer leaves in the file, and verify in the same pass that the blob paragraph now matches
the tree.
**Files:** `docs/operator-runbook.md`.
**Check:** `grep -n -i "worktree" docs/operator-runbook.md` finds the new clause in the retention section (zero
there before — the control). Verification of the two paragraphs this unit does **not** own, run and pasted
into the `## Log` so the collision is visibly resolved rather than assumed: `grep -rn "blobRetention\|REVU_RECLAIM_BLOBS" docs/operator-runbook.md packages/revud/src/index.ts`
agrees with whichever answer M8.15 kept, and `grep -n "refs/revu/reviews" docs/operator-runbook.md` is
non-empty. If M8.15's answers have not landed when this unit runs, **stop and record that** — do not write the
paragraphs on their behalf.
**Closes:** — (task scope; no audit id of its own. The blob flag's gap and the pin growth are both owned by
M8.15's questions 8 and 2 respectively, and are closed there, not here.)

### M8.16.12 — Security review: pinned refs are real refs, and the one way they leave the workspace
**Do:** `docs/security-review.md`'s local-review section (`:62-89`) accounts for the write path, the archive
read and the journal, and says nothing about the pins. They are the only artifact of a local review that has
a name outside the SQLite file, and one landmine recorded in M8.8 belongs in a security document rather than
in a ticket: **pinned refs are real refs.** No default refspec fetches or pushes them, and keeping them under
`refs/revu/` (never `refs/heads/`) is what keeps them out of every ordinary push — but `git push --mirror`, or
an explicit `refs/*:refs/*`, would carry them to a remote. Add a short paragraph saying exactly that, and what
it would and would not disclose: the ref names carry the local review's id and the two object names of its
compare, and they hold commits the branch already contains — **no thread, comment, draft, verdict or reaction
is in a ref**, because none of that is a git object. State the remedy in the same breath: do not mirror-push a
workspace that holds local reviews, or delete the reviews first, which drops their pins.
**Files:** `docs/security-review.md`.
**Check:** `env -u GH_TOKEN -u GITHUB_TOKEN TZ=UTC bun test packages/revud/src/security-review-claims.test.ts`
green — every path the new paragraph names in a backtick span must resolve, which is a real test over this
exact file. **Control, obtained not asserted:** write the new paragraph naming
`packages/revud/src/direct/local-pins.ts`, run the test green; then mistype it as `local-pinz.ts`, run it and
observe the failure naming the missing path; revert and re-run green, and confirm the revert with
`git diff docs/security-review.md` byte-for-byte against the intended text. Record both runs in the `## Log`.
**Closes:** — (task scope; no audit id — from M8.8's Landmines, which asked for a doc line and never got one.)

### M8.16.13 — Annotate M8.1's OQ5 with the delete-boundary supersession
**Do:** `.claude/workstreams/m8-local-reviews/tickets/M8.1-contract-and-mock.md:678-684` records *"Open
question 5 settled — deletion is full-record removal … Per-human drafts and viewed state are deliberately
**orphaned, never destroyed**"*, and the mock — the specification — no longer behaves that way. Do **not**
rewrite the entry: it is the record of what was settled on the day it was settled. Append a marked
supersession note directly beneath it: the owner's ruling of 2026-08-19 (recorded at M8.10 OQ3 / M8.12 OQ1 and
indexed on `BOARD.md:117`) replaced the boundary — `deleteLocalReview` now **refuses** with `unprocessable`
while any human's draft holds text (`packages/app/src/api/mock/local.ts:458-466`), and once no draft holds
text the delete removes the empty per-human draft and viewed rows **with** the review. Name the two mock tests
that pin it (`local.test.ts:525` *"is refused whole while a draft holds text, and removes everything once it
is discarded"* and `:610` *"an empty draft … never blocks, and goes with the review"*) so the next reader can
check rather than trust. Code and tests already agree with the ruling; only this record lagged.
**Files:** `.claude/workstreams/m8-local-reviews/tickets/M8.1-contract-and-mock.md`.
**Check:** `grep -n "orphaned, never destroyed" .claude/workstreams/m8-local-reviews/tickets/M8.1-contract-and-mock.md`
still finds the original line (the record is preserved) **and** a supersession note follows within ten lines
naming `unprocessable`. Corroboration:
`env -u GH_TOKEN -u GITHUB_TOKEN TZ=UTC bun test packages/app/src/api/mock/local.test.ts` green with both
named rows. **Control, defined positionally because a bare `grep -n unprocessable` over this file is *not*
empty today** (`:297`, `:604`, `:723`, and three more in the Log — none of them near OQ5):
`sed -n '678,694p' .claude/workstreams/m8-local-reviews/tickets/M8.1-contract-and-mock.md | grep -c unprocessable`
is **0** before the edit and **≥ 1** after, the window being the OQ5 block plus the OQ8 block that follows it.
Re-read the window first — if the settled-OQ5 paragraph has moved, take the window from the sentence, not
from the number.
**Closes:** M8.1-g1.

### M8.16.14 — Correct M8.4's Check literals and its unreachable-`forbidden` claim
**Do:** Three stale statements in `.claude/workstreams/m8-local-reviews/tickets/M8.4-local-write-sink.md`.

1. **M8.4.1's Check (`:77-80`)** lists a **ten**-key port set. The tree pins **eleven**:
   `local-writes.test.ts:159-171` `PORT_MEMBERS` adds `getLocalReview` (added by M8.9 and recorded only in
   M8.9's Log). Add it to the literal list, in sorted position, so the ticket's Check and the test's
   assertion say the same thing.
2. **M8.4.6's assertion 4 (`:192-196`)** names a **four**-file scanned set. The tree pins **five**:
   `local-write-isolation.test.ts:71-77` `LOCAL_MODULE_FILES` also carries `local-ids.test.ts`. The ticket's
   own "guard correction" Log entry recorded this and it was never folded into the unit.
3. **M8.4.3 item 2 (`:126`) and the Context row (`:308`)** say `forbidden` is unreachable locally and
   *"nothing returns it"*. Since M8.9 the local sink returns `{ status: 'forbidden', reason }` as the
   **archived-review refusal** (`local-writes.ts:420`, with the three other verbs throwing
   `ApiError('forbidden', …)` at `:671`, `:770`, `:833`), pinned on three transports by the conformance
   archive block. True: the *approve gate* is what is unreachable locally — `canApprove` is true — and the
   `forbidden` variant is now the archived refusal, which is why the variant earns its place in the frozen
   union rather than merely surviving in it. Fix the "Constraints" line at `:344-345` in the same edit; it
   repeats the claim.

**Files:** `.claude/workstreams/m8-local-reviews/tickets/M8.4-local-write-sink.md`.
**Check:** the ticket's literal key list equals the test's, checked mechanically:
`grep -o "'[a-zA-Z]*'" ` over `PORT_MEMBERS` in `local-writes.test.ts` and over M8.4.1's Check line produce
the same eleven names; likewise five names for `LOCAL_MODULE_FILES`. For item 3 the assertion is narrowed to
the claim being removed, because a bare `grep -n "unreachable"` over this ticket can never go green — the file
has six hits and only three are this unit's: `:409` (*"`conflict` becomes unreachable"*, true and unrelated),
`:485` (*"unreachable in practice"*, about a dispatch that does not exist yet) and `:1132` in the append-only
`## Log` all stay. So:
`grep -n "forbidden.*unreachable\|unreachable.*forbidden" .claude/workstreams/m8-local-reviews/tickets/M8.4-local-write-sink.md`
is **3** hits before (`:126`, `:308`, `:345`) and **0** after outside the `## Log`. Corroboration:
`env -u GH_TOKEN -u GITHUB_TOKEN TZ=UTC bun test packages/revud/src/direct/local-writes.test.ts packages/revud/src/direct/local-write-isolation.test.ts`
green. **Control:** the count comparison is red before the edit (10 vs 11, 4 vs 5) — record both runs.
**Closes:** M8.4-g1, D6-g2.

### M8.16.15 — Restate M8.7.7's corroboration against the true baseline
**Do:** `.claude/workstreams/m8-local-reviews/tickets/M8.7-app-local-chrome.md:521-523` says
`git diff --stat main --` over `queries.ts`, `shortcuts.ts`, `head-moved-dialog.tsx` and `error-copy.ts`
*"reporting zero changed files stays as corroboration on the day the ticket lands"*. Measured on the stack tip
today: `shortcuts.ts` is **+1** (M8.6.5's `g l` chord) and `queries.ts` is **+36** (M8.6.2's `+8` and
M8.9.6's `+28`); `head-moved-dialog.tsx` and `error-copy.ts` are genuinely zero. The ticket's own Log already
records this as Finding B (`:1561-1567`) — with a stale figure of its own, "+8" for `queries.ts`, which was
true when the finding was written and is not now.

True, and stated as **a commit range rather than a diff**: *no M8.7 commit touched any of the four* —
`git log --oneline m8.6/app-creation-flow..m8.7/app-local-chrome --` over them is **empty**, which is exactly
the claim the unit needs and the only form of it that survives the stack. Do **not** restate it as a diff
against the M8.7 branch tip: `git diff --stat m8.7/app-local-chrome --` over the four, run from the current
tip, prints `queries.ts | 28 +`, because M8.9.6's `edb3ec0` landed **after** M8.7 (M8.9 depends on M8.7; the
chain runs M8.7 → M8.8 → M8.10 → M8.11 → M8.9) and is not an ancestor of that tip. For the same reason, do
not write that the two `main`-side changes "arrived before M8.7 started": M8.6.2 (`113aee8`, `+8`) and M8.6.5
(`836b841`, `+1`) did; M8.9.6 (`edb3ec0`, `+28`) did not. Correct Finding B's "+8" in the same edit to *"+8
from M8.6.2 before M8.7, +28 more from M8.9.6 after it"*, or drop the figure and name the two commits.
**Files:** `.claude/workstreams/m8-local-reviews/tickets/M8.7-app-local-chrome.md`.
**Check:** the ticket's restated clause is true when run, both commands pasted into the `## Log` —
(a) `git log --oneline m8.6/app-creation-flow..m8.7/app-local-chrome -- packages/app/src/state/queries.ts packages/app/src/lib/shortcuts.ts packages/app/src/components/review/head-moved-dialog.tsx packages/app/src/components/review/error-copy.ts`
prints **nothing**, and
(b) `git diff --stat main -- packages/app/src/state/queries.ts packages/app/src/lib/shortcuts.ts packages/app/src/components/review/head-moved-dialog.tsx packages/app/src/components/review/error-copy.ts`
prints exactly two files, `shortcuts.ts | 1 +` and `queries.ts | 36 +`. **Under zsh, quote or `setopt
shwordsplit` if you put those four paths in a variable** — zsh does not word-split an unquoted expansion, so
`-- $FILES` silently matches nothing and prints an empty diff that looks like a green result.
**Control:** the old clause ("zero changed files" against `main`) is falsified by command (b); that run *is*
the red.
**Closes:** M8.7-g3.

### M8.16.16 — Record M8.8's Verify clauses 6, 7 and 8
**Do:** `.claude/workstreams/m8-local-reviews/tickets/M8.8-resync-and-pinning.md`'s
`## Verify — run 2026-08-20, green` (`:1031-1058`) enumerates clauses **1–5** only, while `## Verify`
(`:449-487`) has eight. The missing three are what let a never-written leg hide inside a green-looking record.
Append them:

- **Clause 6 (ref legality)** — discharged by `local-pins.test.ts`, which runs `git check-ref-format` over
  every name `pinRefsFor` produces and asserts the design's literal compare-key form is rejected (`:125`).
  Name the test and the run.
- **Clause 8 (objects-missing)** — discharged by `reconcile.test.ts:469` and
  `local-objects-missing.test.ts:227`, plus the row proving the store itself still throws
  (`local-objects-missing.test.ts:279`). Name them and the run.
- **Clause 7 (mock parity, driven against the real mock)** — **not discharged.** `reconcile.test.ts` imports
  no `createMockApi`; the two transports are pinned by two independent tests instead, which is not what the
  clause asks for. Record it as **open**, pointing at `M8.14.1` (the real-mock reconcile parity leg), and do not
  mark this Verify green until that leg runs.

**This unit runs after `M8.14.1`**, so clause 7 can be recorded as run rather than as owed. If the
owner ships M8 without it, record the deviation here in the same shape M8.10's `Verify` uses for its one
recorded deviation — an explicit, reasoned exception, never a silent gap.
**Files:** `.claude/workstreams/m8-local-reviews/tickets/M8.8-resync-and-pinning.md`.
**Check:** the run record enumerates 8 of 8 clauses, each with the test file and the pass count that produced
it; `grep -c "^[0-9]\." ` over the run section is 8. Corroboration: every named test is re-run under
`env -u GH_TOKEN -u GITHUB_TOKEN TZ=UTC bun test <path>` and its counts pasted, not remembered.
**Closes:** M8.8-g6.

### M8.16.17 — Record the broker archive-seam's evidence status in M8.9's Log, and stand down on OQ2
**Do:** Two records in `.claude/workstreams/m8-local-reviews/tickets/M8.9-archive-on-pr.md` — **one written
here, one owned by M8.15's question 11.** Item 1 is startable now; item 2 waits.

1. **The Log's M8.9.3 entry (`:750`)** claims *"`index.ts` `pairListingFor(client, repo)` wires it in
   `mainDirect` (only when both halves exist — `--local-only` passes nothing) and `mainBroker` (from
   `pollClient`)."* Both wirings exist (`index.ts:414` and `:577`), but their evidence is not equal, and the
   entry names none. Append the honest status: the **direct** wiring is corroborated live by the scratch-repo
   proof (a real pull request archived a real local review through a real `--direct` daemon); the **broker**
   wiring is exercised by nothing — no `index.ts` test, no walk, no e2e — and it is **inert by construction**,
   because `mainBroker` builds no local-review surface (`:563-580` passes no `localReviews`), so broker mode
   holds no local review for the seam to be asked about. It is wiring kept for the day a broker boot serves
   local reviews, not a live path.
2. **OQ2 (`:593-602`, "What triggers the sweep?") — stands down; do not write it here.** OQ2 ends at `:602`
   with *"it is a copy problem for M8.9.6's banner."* The residue is real: the accepted cost went into the
   **guide**, not the banner — `packages/docs/content/docs/guides/local-review.mdx` §7 says *"It runs only
   when you sync: a review nobody re-syncs stays open until its next sync, however long a pull request has
   been open."*, while M8.9.6's banner says what an archived review *is* (`mode-copy.ts:173`,
   `archived · superseded by #<n>`). But that residue is **M8.15's question 11** (*"M8.9 OQ2's 'carry the cost
   into UI copy': live-review copy, or the guide?"*), and both of its answers already write the sentence:
   `M8.15.21` adds it to `syncCostCopy('local')` and *"records the choice at M8.9 OQ2"*; `M8.15.22` amends
   OQ2's ruling text to say *carried into the guide … and the banner*. Writing it here too is either wrong
   (under answer A the cost also lands in the live-review copy) or the same sentence written twice into the
   same paragraph — the clobber this ticket's Landmines name.

   So, after M8.15's question 11 is answered and its unit has landed: **read OQ2, verify the appended
   sentence is there and is true against the guide, and record the verification in this ticket's `## Log`,
   naming the M8.15 unit that wrote it.** Add nothing to M8.9's file. If question 11 has not been answered
   when this unit runs, item 1 still lands on its own and item 2 is recorded as still owed — do not write the
   paragraph on M8.15's behalf.

**Files:** `.claude/workstreams/m8-local-reviews/tickets/M8.9-archive-on-pr.md` (item 1 only; item 2 writes
no file here).
**Check (item 1):** `grep -n "mainBroker" .claude/workstreams/m8-local-reviews/tickets/M8.9-archive-on-pr.md`
finds the inertness sentence within the M8.9.3 entry. Corroboration, run and pasted:
`grep -n "localReviews" packages/revud/src/index.ts` shows the key only in the `mainDirect` assembly
(`:387`, `:421`) and nowhere between `:563` and `:580`. **Control — grep for a token the new text introduces,
not for `mainBroker`, which is *not* empty before the edit** (`:445` in the Context table and `:751`, the very
Log line this item amends — two hits today):
`grep -n "inert by construction\|exercised by nothing" .claude/workstreams/m8-local-reviews/tickets/M8.9-archive-on-pr.md`
is **0** before and **≥ 1** after, and the hit falls inside the M8.9.3 Log entry — read it there, do not
trust the count. Record the actual before and after counts of both greps.
**Check (item 2):** verification only. `grep -n "stays open until its next sync" packages/docs/content/docs/guides/local-review.mdx`
is non-empty (it is today), and OQ2 carries M8.15's appended sentence. Note that
`grep -n "guides/local-review"` on M8.9's ticket is **not** empty before either — `:711` already cites the
guide — so it is no control for anything; the control belongs to M8.15's unit, which owns the write.
**Closes:** M8.9-g2 (item 1). Item 2 closes nothing here — M8.15's question 11 owns it.

### M8.16.18 — Correct M8.10's Verify Log "unedited" wording
**Do:** `.claude/workstreams/m8-local-reviews/tickets/M8.10-retention-and-gc.md:887` reports
*"`sync.test.ts` + `reconcile.test.ts` + `conformance-reads.test.ts` — **43 pass, unedited**."* The
behavioural claim holds — the prune is invisible to the sync engine and the read path — but the file was
edited: M8.10 added **eight** lines to `reconcile.test.ts`'s total `DirectStore` fake, all `unexpected(…)`
stubs, across four commits — `deleteLocalReview`, `listLiveCompareKeys`, `listImmutableKeys` (`3423324`),
`deleteImmutables` (`974513a`), `listBlobShas`, `listReferencedBlobShas`, `deleteBlobs` (`566539e`) and
`listLocalDrafts` (`faa1ad6`). True: *"43 pass; `reconcile.test.ts` gains eight `unexpected(…)` stubs on its
total store fake and no behavioural line — the stubs throw if reconcile ever reaches a retention method, which
is a stronger statement than leaving the fake incomplete."* Say it that way; the stubs are the evidence for
the claim the old word was trying to make. Leave `:889`'s `audit-integrity.test.ts` **UNAMENDED** line alone —
it is verified by diff and true.
**Files:** `.claude/workstreams/m8-local-reviews/tickets/M8.10-retention-and-gc.md`.
**Check:** `grep -n "43 pass, unedited" ` returns nothing. Corroboration, pasted into the `## Log`: for each
of the four commits, `git show <sha> --stat -- packages/revud/src/direct/reconcile.test.ts` (3 + 1 + 3 + 1 =
8 insertions, 0 deletions) and
`env -u GH_TOKEN -u GITHUB_TOKEN TZ=UTC bun test packages/revud/src/direct/reconcile.test.ts` green.
**Control:** the grep is red (one hit) before the edit.
**Closes:** M8.10-g3.

### M8.16.19 — Rewrite M8.12's unit text to the no-force ruling, and give it a Landmines section
**Do:** `.claude/workstreams/m8-local-reviews/tickets/M8.12-delete-confirm.md` still describes the force-flag
design the owner struck on 2026-08-19. The code is right, the Log records the reshaping, and the units
contradict both. Rewrite four places to what shipped, and add the section the ticket never had.

1. **Goal (`:16-30`).** Stale: *"deleting a local review which holds an **unsubmitted draft** must not simply
   proceed: it requires an explicit force"*, and *"Deletion orphans drafts and viewed state; it never destroys
   them … The copy must be honest about that: it says the draft becomes unreachable, not that text is
   destroyed."* True: **the server refuses** while any human's draft holds text, and there is no force
   parameter anywhere; the confirmation exists to explain that refusal and to offer the discard as a
   deliberate second act. The delete removes the empty per-human draft and viewed rows with the review, so the
   copy must not say the draft is kept — the landed copy says the text *"is not kept anywhere"* and the gate
   bans the word *unreachable* outright.
2. **M8.12.1's Check (`:41-44`).** The `matches /unreachable/i` clause is on `:41`; `:42-44` carry the
   `/deleted/i` clause, the positive control and a blank line. Stale: *"matches `/unreachable/i`"*. True: the copy must match the discard
   sentence and match **neither** `/unreachable/i` nor `/lost/i` — `mode-copy.test.ts:935` (*"and nothing is
   described as merely unreachable"*) asserts exactly the opposite of what the unit demands.
3. **M8.12.2's Do (`:51`).** Stale: *"Confirming re-issues the delete with the force flag set."* True:
   confirming discards the reader's own draft through the draft store's own path (`dropPersistState` first,
   so a pending debounced save cannot re-create it), then repeats the **same, unchanged** delete.
4. **M8.12.3's Do and Check (`:61-71`), including its title.** The heading itself is stale — *"Wiring and the
   force round-trip"* (`:61`) — so rewrite it to the no-force shape (e.g. *"Wiring, and the discard that
   precedes the delete"*). A unit **title** is not its id: the board's append-only rule fixes the number
   `M8.12.3`, which never changes and is what commits and branches reference; the words after the dash are
   prose and are corrected like any other stale sentence. Say so in M8.12's `## Log`. Stale: *"first attempt unforced … Confirming retries with force"*
   and *"the draft is still readable under its own key"*. True: `performLocalReviewDelete({ api, reviewId,
   discard })` never throws and answers `{ outcome: 'deleted' | 'refused' | 'failed', discarded }`; a failed
   discard ends the attempt and the delete is never sent; after a successful delete the draft verbs answer
   `not_found` on that id in **both** transports, so the old Check asserts the opposite of the contract.

**Add `## Landmines`** between `## Constraints that bind this ticket` and `## Open questions`, carrying at
minimum the three this ticket's own review proved: a failed discard must never drop the cached draft (the
cache is the editing surface, and an outage that failed the save is the outage that would erase the edit); the
refusal sentence must never carry the synthetic id, which is the identity rule breaking exactly in the state
where a user is most alarmed; and a discard that bypasses `dropPersistState` leaves a pending save able to
re-create the row between discard and delete.
**Files:** `.claude/workstreams/m8-local-reviews/tickets/M8.12-delete-confirm.md`.
**Check:** `grep -n -i "force" .claude/workstreams/m8-local-reviews/tickets/M8.12-delete-confirm.md` returns
only sentences saying there is **no** force flag;
`grep -c "^## Landmines" .claude/workstreams/m8-local-reviews/tickets/M8.12-delete-confirm.md` is 1; the
ticket's unit text and the landed tests agree, checked by re-running
`env -u GH_TOKEN -u GITHUB_TOKEN TZ=UTC bun test packages/app/src/lib/mode-copy.test.ts packages/app/src/state/local-reviews.test.ts packages/app/src/components/confirm-delete-local-review.test.ts`
green and reading the three assertions the units now name. **Control, with the real counts:** that grep is
**11** hits before the edit, not three — **7** in the Goal and Units, which are the ones this unit rewrites
(`:17`, `:51`, `:61` the M8.12.3 heading, `:62`, `:63`, `:68`, `:69`), plus **4** in OQ1 and the `## Log`
(`:96`, `:97`, `:100`, `:115`) that already say there is no force flag and stay untouched. After the edit the
count is those same 4 plus whatever no-force sentences the rewrite adds, and **zero** of the 7 survive —
`:61` included, which is why the heading is retitled rather than left to match forever. Record both runs.
**Closes:** M8.12-g2.

### M8.16.20 — Reconcile the landed `MILESTONES.md` seed with M8.12's ticket

**Do:** the seed for `### Issue M8.12` … `### Issue M8.17` **already landed** in the same commit that created
this ticket, so the drift `M8.12-g1` names (the document seeded M8.1–M8.11 only while the board carried
M8.12) is closed on disk before this unit starts. What is NOT yet verified is that the seed text *matches the
ticket it was written from*: it was written while these tickets were still being reviewed. Re-read
`### Issue M8.12` against `tickets/M8.12-delete-confirm.md` — its three units and its Verify — and correct any
sentence the ticket does not support. Change nothing about M8.13–M8.17 here: their units may still move while
this ticket and its siblings are executed, and reconciling them is M8.17.6's job, after they are all done.
**Files:** `docs/agent/MILESTONES.md` (the `### Issue M8.12` block only).
**Check:** `awk '/^### Issue M8\.12/,/^### Issue M8\.13/' docs/agent/MILESTONES.md` carries one `[sub]` line
per unit heading in `tickets/M8.12-delete-confirm.md` (`grep -c '^### M8\.12\.'` is **3**, the independent
literal) and a `**Verify:**` line whose claim appears in that ticket's `## Verify`. The control: change one
`[sub]` line to name a fourth unit and the count assertion goes red. `docs/agent/MILESTONES.md` is not part of
the docs site, so `bun run docs:build` is not triggered by it — say so in the `## Log` rather than looking for
a green job that never ran.
**Closes:** M8.12-g1 (verifying on the tip what the plan commit landed).

### M8.16.21 — Amend criterion 6 in both milestone records to carry ruling R2
**Do:** Exit criterion 6 reads *"No GitHub-flavored affordance renders on a local review: no Checks or
Description tab, **no rate-limit cost copy**, no 'approves on github.com' lock, no 'posted in one API call'
toast"* in `.claude/workstreams/m8-local-reviews/MILESTONE.md:42` and
`docs/agent/MILESTONES.md:434`. The rate chip is a recorded deviation, not a defect: M8.7's ruling R2 (owner
call, 2026-08-14) made the topbar chip **workspace-scoped** — `showRateChip({ rateAvailable })`
(`review-mode.ts:381`) takes no mode, and `app-shell.tsx:65` renders it from that alone — so in a
GitHub-capable workspace a local review does show the chip and its tooltip. R2 says that is not swept. Amend
both copies of the criterion to carry the deviation in its own clause: the chip is a property of the
workspace, not of the open review, and it stays; what criterion 6 forbids is per-review GitHub copy — a cost
attached to *this* review's sync, an approval instruction, a posted-to-GitHub claim, a Checks or Description
tab. Change both files in the same edit; they must never drift.
**Files:** `.claude/workstreams/m8-local-reviews/MILESTONE.md`, `docs/agent/MILESTONES.md`.
**Check:** both criterion-6 texts carry the same key clause, **not** the same bytes. A byte diff of the two
extracted lines can never be empty in either direction: `MILESTONE.md`'s criterion is a wrapped two-line
checklist item (`:42-43`, prefix `- [ ] `, continuation indented six spaces) while `MILESTONES.md:434` is a
single `- ` bullet. So either normalize before comparing — join the wrapped lines, collapse runs of
whitespace, strip the leading `- [ ] ` / `- ` — or, simpler and falsifiable on its own, assert the shared
clause verbatim: `grep -c "property of the workspace" .claude/workstreams/m8-local-reviews/MILESTONE.md docs/agent/MILESTONES.md`
is **0** and **0** before the edit and **1** and **1** after. (Word the amendment so that clause survives
`MILESTONE.md`'s line wrap intact — a phrase broken across two lines is not greppable.) Corroboration:
`env -u GH_TOKEN -u GITHUB_TOKEN TZ=UTC bun test packages/app/src/lib/review-mode.test.ts` green with the row
*"and the chip is a property of the workspace, not of the open review"*. **Note on wording:** the amendment
carries the deviation's **substance** — the chip is scoped to the workspace, not to the open review, and it
stays — never the label "R2". `MILESTONES.md` may name ticket ids; a ruling id is a tracking artifact and
belongs in this file and in the commit message.
**Closes:** EC6-g1.

### M8.16.22 — Board: the follow-ups this ticket and this audit leave behind
**Do:** `.claude/workstreams/m8-local-reviews/BOARD.md` is the only file that claims to describe right now,
and three recorded facts are missing from its follow-up lists.

1. **M8.5 OQ6's deferral has no home on the board.** The ticket settled it (`M8.5:662`: *"`bin/revu` gains no
   local-only path here; … the CLI path is a follow-up rather than a silent absorption"*) and it is a
   follow-up nobody wrote down — verified: `packages/revud/bin/revu` contains no `--local-only` path, while
   `index.ts:101-106` has the flag and the variable. **M8.15's question 9 puts the same deferral to the
   owner**, so read that first: if its kept answer already records the follow-up on the board, this item is
   done and says so in the `## Log`; if it does not, add it beside the existing follow-ups (the live direct
   runner, OQ10, the create dialog's title field, `scripts/` outside `tsc -b`, `Bun.fetch`).
2. **The `MILESTONES.md` seed debt** M8.16.20 leaves as a pointer: the mirror of M8.12–M8.17 is owed and is
   its own step.
3. **The guides have no gate guard** until M8.16.23 lands, and if the owner declines it (**M8.16.23's own
   question at OQ2 below** — OQ1 is M8.16.24/.25's) the board carries the decision rather than the ticket
   alone.

Update this ticket's row and `State` in the same edit, per the board's own rule.
**Files:** `.claude/workstreams/m8-local-reviews/BOARD.md`,
`.claude/workstreams/m8-local-reviews/tickets/M8.16-docs-and-board-drift.md` (this file's header + `## Log`).
**Check:** `grep -n "bin/revu\|MILESTONES.md" .claude/workstreams/m8-local-reviews/BOARD.md` finds both new
follow-ups; the board's M8.16 row and this file's `State` say the same thing, checked by reading both.
**Control:** both greps empty before the edit.
**Closes:** — (task scope; no audit id.)

### M8.16.23 — The smallest gate guard for present-tense claims in the guides
**Do:** No test anywhere reads a `.mdx` file — `grep -rln "\.mdx" packages/*/src scripts` is empty and
`packages/docs` carries no test files — so the class of defect M8.11's adversarial pass caught by hand (*a
guide describing an archive detector that does not exist*) can recur unobserved. Land the smallest guard that
would actually catch it, in `packages/revud/src/guide-claims.test.ts`, beside the one that already does this
job for one document (`security-review-claims.test.ts`).

**The obvious guard does not transfer, and the unit must record why.** `security-review-claims.test.ts`
extracts backtick-span paths and asserts each resolves. Measured against the guides, that finds **one** token
across all three files — `release/0.41`, a branch name in an example, not a path — because the user-facing
guides deliberately name no code. A path guard over them would be vacuous and would also mis-read branch
names. So the guard is over the other thing the guides do: **they quote the product's own copy.**

Shape:
- A written-out list of the UI sentences the guides quote — the dirty banner *"Uncommitted changes are not in
  this review"* and its hint *"This review covers committed content only. Commit the rest and re-sync to bring
  it in."*, the submit toast *"Review saved"*, the draft whisper *"saved · workspace"*, the inbox section
  title *"Local reviews"* — each entry naming the guide file it appears in and the package it must be produced
  by. Every one was verified present in both today: `mode-copy.ts:563` (title), `:564` (hint), **`:326`
  (`title: 'Review saved'`)** and `:434` (`label: 'saved · workspace'`); `inbox-sections.ts:318`; the guide at
  `:85-86`, `:144`, `:146`, `:62`.
- **The comparison is normalized, and the normalizer is the first thing the test proves.** Two of the five
  entries do not survive a raw `grep` of the guide, and neither is a defect: the hint **wraps across
  `local-review.mdx:85-86` inside a `> ` blockquote** (`…covers committed\n> content only…`), so
  `grep -c "<the whole hint>"` on the guide is **0** today; and the guide bolds the quoted strings
  (`**Review saved**`, `**saved · workspace**`, and a trailing period inside the banner's bold). So before
  comparing, the guide side is: strip a leading `> ` from every line, join the lines, collapse runs of
  whitespace to one space. Bold markers need no handling — none of the five pinned strings contains `**`
  internally, so each is still a substring of its normalized line. Write that reasoning into the test's
  docstring, in self-contained wording.
- Two assertions per entry: the sentence appears in the named guide **after normalization**, and it appears
  verbatim under `packages/app/src`. Either side moving alone is red — which is the point, and why **the list
  is written out in the test and never extracted from the guide**: a list derived from the document it checks
  moves with it and asserts nothing.
- **Three** non-vacuity self-assertions, in the shape the sibling file already proves: a floor on the number
  of entries chosen independently of the list's current length; the matcher run over an inline fixture whose
  correct verdict is written here rather than read out of a file; and — because the normalizer is now load-
  bearing — that fixture includes **a wrapped `> ` blockquote whose sentence must match and an unwrapped line
  that must not**, so a normalizer that silently degrades to "contains" is red. A normalizer proved by
  nothing would let the hint entry pass on a guide that no longer carries the hint at all.

**Non-goals, recorded rather than silently skipped:** the guard cannot check a semantic claim (*"archives
exactly as any other does"* is not a quoted string), and it does not attempt to. It buys the copy-drift half
of the class only. Sharing the extractor with `security-review-claims.test.ts` is also a non-goal here: that
file's value is that it is independently proved non-vacuous, and editing it to export a helper puts a green
guard's own guarantees in play for no behavioural gain — unify them later, deliberately, if a third document
wants the same treatment.
**Files:** `packages/revud/src/guide-claims.test.ts` (new).
**Check:** `env -u GH_TOKEN -u GITHUB_TOKEN TZ=UTC bun test packages/revud/src/guide-claims.test.ts` green.
**Reds obtained by control, both recorded:** (a) change one pinned sentence in the app source
(`mode-copy.ts`'s `'Review saved'` → `'Review stored'`), run the test, observe it fail **naming that
sentence**, revert, re-run green, and confirm the revert with `git diff packages/app/src/lib/mode-copy.ts`
empty; (b) delete the quoted banner sentence from the guide, observe the same test fail on the guide side,
revert, verify. A guard whose failure has never been seen is not yet a guard.
**Closes:** D8B-g4.

### M8.16.24 — *(alternative A, owner decides before this unit starts)* Mirror the withheld-composer wording into `direct.mdx`
**Do:** Only if the owner keeps **M8.15.1** at M8.15's question 1 — the draft write is refused on an archived
review and the gutter composer is hidden. M8.15.1 rewrites the guide's own archive paragraph
(`local-review.mdx:167-172`); it does **not** touch the run-modes page, and
`packages/docs/content/docs/run-modes/direct.mdx:179` says only *"A local review is archived read-only"*,
which is the same over-broad word one file over. Mirror M8.15.1's landed sentence there in one clause: an
archived review keeps every thread and draft, and a draft on it can still be read and discarded but no longer
edited. **Copy the wording that landed** rather than inventing a second phrasing — two pages describing one
behaviour in two vocabularies is the drift this ticket exists to remove.
**Files:** `packages/docs/content/docs/run-modes/direct.mdx`.
**Check:** `grep -n "read-only" packages/docs/content/docs/run-modes/direct.mdx` finds the qualifier in the
same sentence, and the qualifying clause appears **verbatim** in both files —
`grep -c "<the landed clause>" packages/docs/content/docs/guides/local-review.mdx packages/docs/content/docs/run-modes/direct.mdx`
is 1 and 1. `bun run docs:build` green. **Control:** the grep over `direct.mdx` is empty before the edit.
**Closes:** — (M8.15's question 1 owns the ruling and the guide; no audit id here.)

### M8.16.25 — *(alternative B, owner decides before this unit starts)* Mirror the writable-draft wording into `direct.mdx`
**Do:** Only if the owner keeps **M8.15.2** — the draft stays writable on an archived review and the docs say
so. Same residue, opposite sentence: M8.15.2 rewrites `local-review.mdx`'s archive paragraph and callout and
`LOCAL_REVIEWS.md` §8 #22, and again leaves `direct.mdx:179`'s bare *"archived read-only"* alone. Mirror the
landed wording into that bullet: archived means nothing new enters the review's record — no thread, reply,
resolve, reaction or submitted verdict — while your own draft on it stays yours to edit and can never be
submitted.
**Files:** `packages/docs/content/docs/run-modes/direct.mdx`.
**Check:** as above — the landed clause appears verbatim in both files, `bun run docs:build` green, and the
`direct.mdx` grep is empty before the edit. **Exactly one of M8.16.24 / M8.16.25 runs**; the other is closed
as not-taken in the `## Log`, with M8.15's ruling quoted.
**Closes:** — (M8.15's question 1 owns the ruling and the guide; no audit id here.)

## Verify

The gate (`env -u GH_TOKEN -u GITHUB_TOKEN TZ=UTC bun run check`) is assumed. Additional to it:

1. **The docs site still builds.** `bun run docs:build` green — the only job that compiles the `.mdx` this
   ticket edits, and the one CI leg a prose PR can genuinely break.
2. **The two doc-claims guards are green.**
   `env -u GH_TOKEN -u GITHUB_TOKEN TZ=UTC bun test packages/revud/src/security-review-claims.test.ts packages/revud/src/guide-claims.test.ts`
   — the first because M8.16.12 adds paths to the document it guards, the second because M8.16.23 creates it.
   Each must have shown its red by control, recorded in the `## Log`.
3. **No behaviour moved — measured against the branch's base tip, `84393c0`, never against `main`.** Nothing
   of M8 has merged, so `git diff --stat main` for this branch lists every M8 code file (209 files) and
   `git diff main -- packages/revud/src/index.ts` is the whole M8 boot wiring (`284` insertions, `18`
   deletions); both are true and neither says anything about this ticket. The assertion is:
   `git diff --stat 84393c0` lists only `.md` / `.mdx` files, plus exactly two exceptions —
   `packages/revud/src/guide-claims.test.ts` (new) and `packages/revud/src/index.ts`, whose diff contains
   **comment lines only**, verified by reading
   `git diff 84393c0 -- packages/revud/src/index.ts | grep '^[+-]' | grep -v '^[+-][+-]'` line by line, not
   by trusting the file count. Record `84393c0` in the `## Log` when the branch is cut; if it is cut from a
   later tip, that sha is the baseline and this assertion moves with it.
4. **The gate count moved only by the new test file.** Against the base tip's `3348 pass · 1 skip · 0 fail ·
   117 files`, the run after this ticket is `117 + 1` files and `3348 + <the guard's test count>` passes, with
   skips and failures unchanged. A pass count that moved further than the guard's own tests means something
   in this ticket was not prose.
5. **Every stale sentence is gone and its replacement is present.** Run the per-unit greps from every Check
   above in one pass and paste the block into the `## Log`; each stale string returns zero hits and each true
   string returns at least one.
6. **Every path this ticket wrote into prose resolves.** For `docs/security-review.md` that is assertion 2's
   test; for `docs/agent/LOCAL_REVIEWS.md`, `docs/operator-runbook.md` and the board files, run the same
   extraction by hand (backtick spans, a separator and an extension) and open anything that does not resolve.
   A path a reader cannot follow is the same defect class as the sentences this ticket exists to fix.

## Context — verified seams

Every anchor below was opened on `board/m8-closeout` at `84393c0` while writing this ticket. Line numbers
rot; the quoted text is the identity.

| anchor | what is there, and why a unit attaches to it |
| --- | --- |
| `docs/agent/LOCAL_REVIEWS.md:5` | *"`docs/agent/MILESTONES.md` carries the ticket breakdown, and the Linear board carries the units"* — the first sentence a reader follows (M8.16.6). |
| `docs/agent/LOCAL_REVIEWS.md:67` | D2's rationale, *"both routers' `Number.isInteger(n) && n > 0` gates"* (M8.16.6). |
| `docs/agent/LOCAL_REVIEWS.md:68` | D3, *"`git status --porcelain` drives an explicit … banner"* (M8.16.2). |
| `docs/agent/LOCAL_REVIEWS.md:70` | D5, *"A capability inside direct/broker mode, not a fourth mode"* (M8.16.3). |
| `docs/agent/LOCAL_REVIEWS.md:102-103` | *"a `UNIQUE(repo, base_ref, head_ref)` constraint plus `MAX(id) + 1` minting"* (M8.16.1). |
| `docs/agent/LOCAL_REVIEWS.md:129-130` | The `etag` and `rateLimit` rows of §4.1's synthesis table (M8.16.5). |
| `docs/agent/LOCAL_REVIEWS.md:149,152` | The DDL's `dirty` column and its three-column `UNIQUE` line (M8.16.1, M8.16.2). |
| `docs/agent/LOCAL_REVIEWS.md:197-199` | *"`provisionBlobs` is reused as-is except for one flag"* (M8.16.4). |
| `docs/agent/LOCAL_REVIEWS.md:203-208` | §5.1's mass-`lost` paragraph and the `refs/revu/reviews/<id>/<compareKey>` name (M8.16.4). |
| `docs/agent/LOCAL_REVIEWS.md:249` | §7's *"no new query-key factories"* (M8.16.6). |
| `docs/agent/LOCAL_REVIEWS.md:284-286` | §8 Creation cases 4, 5 (unrelated histories, shallow clone) and 6 (the `UNIQUE` duplicate rule) (M8.16.7, M8.16.1). |
| `docs/agent/LOCAL_REVIEWS.md:313` | §8 case 16, the dirty banner (M8.16.2). |
| `docs/agent/LOCAL_REVIEWS.md:336` | §8 #23, *"Zero eviction exists anywhere in the store today"* (M8.16.1). |
| `docs/agent/MILESTONES.md:5` | *"This document and the board must never drift: if scope changes, update both in the same PR"* — the rule M8.16.20 and M8.16.21 are discharging. |
| `docs/agent/MILESTONES.md:422` | *"Live tracking for this workstream is the local board"* — the pointer M8.16.6 aligns the design doc with. |
| `docs/agent/MILESTONES.md:426` | The M8 decision list, *"D5 a capability inside direct/broker mode, not a fourth mode"* — the fourth carrier of the stale phrase (M8.16.3). |
| `docs/agent/MILESTONES.md:434` | Exit criterion 6, *"no rate-limit cost copy"* — a **single** `- ` bullet, where `MILESTONE.md:42-43` wraps under `- [ ] `; the two can never be byte-identical (M8.16.21). |
| `docs/agent/MILESTONES.md:473` | The first line of the `### Issue M8.5` seed, *"Local reviews are a capability inside direct/broker mode (D5), dispatched on the id band."* — a live local-review sentence, not a summary (M8.16.3). |
| `docs/agent/MILESTONES.md:517-521` | The M8.11 seed block, the last in the file — where M8.16.20's pointer goes. |
| `.claude/workstreams/m8-local-reviews/MILESTONE.md:42` | The board's copy of exit criterion 6 (M8.16.21). |
| `.claude/workstreams/m8-local-reviews/MILESTONE.md:69,72` | The D2 and D5 decision rows carrying the same two stale phrases as the design doc (M8.16.3, M8.16.6). |
| `.claude/workstreams/m8-local-reviews/BOARD.md:111-125` | The owner's rulings index — ruling 1 (delete refusal) and ruling 4 (keep every pin) are the two this ticket's prose leans on. |
| `packages/docs/content/docs/guides/local-review.mdx:61-65` | *"drops below every other section once they are all closed"* (M8.16.10). |
| `packages/docs/content/docs/guides/local-review.mdx:76-91` | §3, the committed-content promise and the banner blockquote at `:85` (M8.16.2). |
| `packages/docs/content/docs/guides/local-review.mdx:136-140` | *"the review can go stale … with zero new commits on the head"* (M8.16.9). |
| `packages/docs/content/docs/guides/local-review.mdx:170` | *"it goes read-only"* (M8.16.24/.25). |
| `packages/docs/content/docs/guides/local-review.mdx:181-182` | *"archives exactly as any other does"* (M8.16.8). |
| `packages/docs/content/docs/run-modes/direct.mdx:175-183` | The "Nothing is ever published" bullet, whose archive clause covers only *"a workspace with no remote"* (M8.16.8, M8.16.24). |
| `packages/docs/content/docs/run-modes/index.mdx:32` | *"a capability *inside* direct mode alone"* — already correct; the sentence M8.16.3 brings the others to. |
| `docs/operator-runbook.md:201-205` | *"behind an explicit policy flag a deployment has to set"* (M8.16.11). |
| `docs/operator-runbook.md:219-224` | *"Dropping a review's pins makes its git objects **collectable**, not collected"* — the paragraph M8.16.11 extends with what grows. |
| `docs/operator-runbook.md:378-390` | The environment-variable table: no entry for blob reclamation, which is what makes the "flag" sentence false (M8.16.11). |
| `docs/security-review.md:62-89` | The local-review section — write path, archive read, journal — with no sentence about pins (M8.16.12). |
| `packages/revud/src/security-review-claims.test.ts:1-54,99-107` | The existing doc guard: backtick-span path extraction, an ordered root list, a `MINIMUM_PATHS` floor chosen independently, and a fixture proving the extractor fires. The template M8.16.23 follows and the test M8.16.12 must keep green. |
| `packages/revud/src/direct/store.ts:2375,2379` | `'generation INTEGER NOT NULL DEFAULT 0, …'` and `'UNIQUE(repo, base_ref, head_ref, generation))'` — the landed key (M8.16.1). |
| `packages/revud/src/direct/store.ts:2432-2449` | The high-water docstring (*"not `MAX(id) + 1`"*) and `LOCAL_REVIEW_ID_META_KEY` / `LOCAL_ENTITY_ID_META_KEY` (M8.16.1). |
| `packages/revud/src/direct/local-sync.ts:1494-1510` | `detectDirtyWorktree` running `['status', '--porcelain=v1', '-uno']` (M8.16.2). |
| `packages/revud/src/api-router.ts:115-120` | `intParam` — `Number.isInteger(n) ? n : null`, no positivity gate (M8.16.6). |
| `packages/revud/src/direct-router.ts:259` | `return Number.isInteger(n) && n > 0 ? n : null` — the router that does gate (M8.16.6). |
| `packages/revud/src/direct/local-pins.ts:14-32,57,109-126` | Why the ref name is not the compare key, `PIN_ROOT`, `pinKeyForCompare` (`...` → `-`, deliberately not invertible) and `pinRefsFor` returning `…/base` and `…/head` (M8.16.4, M8.16.12). |
| `packages/revud/src/direct/local-pins.test.ts:125` | *"the design's literal compare-key form is rejected, which is why the substitution exists"* (M8.16.4, M8.16.16). |
| `packages/revud/src/direct/blobs.ts:60-84` | `github?` / `repo?` as optional deps that travel together, with the docstring saying an absent client beats a flag (M8.16.4). |
| `packages/revud/src/direct/local-surface.ts:735-743` | The call that omits both, with the comment saying they are omitted rather than passed as undefined (M8.16.4). |
| `packages/revud/src/direct/local-surface.test.ts:417` | *"resolves no SHAs at create — the review starts unsynced"* (M8.16.7). |
| `packages/revud/src/direct/reconcile.ts:104-132` | The pre-flight: `hasBlob` per anchor object, then a typed `not_found` naming a re-sync, with the comment separating absence-of-object from absence-of-line (M8.16.4). |
| `packages/revud/src/direct/reconcile.test.ts:469` | *"nothing is classified when an object is missing"* (M8.16.4, M8.16.16). |
| `packages/revud/src/direct/local-objects-missing.test.ts:227,279` | *"the edge answers a re-syncable not_found, not a corrupt daemon"* and *"the store itself still throws, which is what keeps it honest"* (M8.16.16). |
| `packages/revud/src/index.ts:127-129` | *"a capability that rides inside direct and broker mode"* — the comment M8.16.3 rewrites. |
| `packages/revud/src/index.ts:346-353` | `pairListingFor(client, repo)` — the repo closed over at boot. |
| `packages/revud/src/index.ts:387-391,414,421` | `createLocalReviewSurface` and `localReviews` — the direct assembly, the only one that builds the local surface (M8.16.3, M8.16.17). |
| `packages/revud/src/index.ts:563-580` | The broker assembly: `supersedingPulls: pairListingFor(pollClient, context.repo)` at `:577` and **no `localReviews`** — the seam that is wired and inert (M8.16.3, M8.16.17). |
| `packages/revud/src/direct/direct-api.ts:408,643` | `blobRetention?: BlobRetentionPolicy` and its only consumer; no boot path passes one (M8.16.11). |
| `packages/revud/src/direct/direct-api.ts:440-476` | `reviewListEtag` (both halves, whole serialized rows) and `unspentRateLimit` (M8.16.5). |
| `packages/app/src/api/mock/adapter.ts:244-287` | The mock's `listPulls`: local rows appended, `store.rateInfo()` returned and `store.spendRate(1)` on an etag miss (M8.16.5). |
| `packages/app/src/api/mock/local.ts:458-466` | `deleteLocalReview` refusing with `unprocessable` while a draft holds text (M8.16.13). |
| `packages/app/src/api/mock/local.test.ts:525,610` | The two rows pinning the refusal and the empty-draft removal (M8.16.13). |
| `packages/app/src/state/queries.ts:45-52` | `qk.branches` and `qk.localReviews` — the two new keys §7 says do not exist (M8.16.6). |
| `packages/app/src/lib/inbox-sections.ts:141-158,316-327` | `showsInInbox` keeping every local item regardless of state, and the placement rule `local.length > 0 ? top : (hasLocalReviews ? bottom : none)` (M8.16.10). |
| `packages/app/src/lib/inbox-sections.test.ts:325,339` | The two rows that pin the bottom placement, both about rows **not listed** rather than about closed reviews (M8.16.10). |
| `packages/app/src/lib/review-mode.ts:381` | `showRateChip({ rateAvailable })` — no mode parameter (M8.16.21). |
| `packages/app/src/components/app-shell.tsx:65` | `if (!showRateChip({ rateAvailable })) return null` — the workspace-scoped render (M8.16.21). |
| `packages/app/src/lib/mode-copy.ts:326` | `title: 'Review saved'` — the submit toast M8.16.23 pins and mistypes for its control; **not** in the `:434`/`:563` range the unit first cited. |
| `packages/app/src/lib/mode-copy.ts:434,563-566` | `'saved · workspace'` (`:434`), `dirtyWorktreeCopy`'s title (`:563`) and its hint (`:564`) — three more of M8.16.23's pinned literals. |
| `packages/docs/content/docs/guides/local-review.mdx:85-86` | The banner blockquote: the hint **wraps** across the two lines behind `> `, so a raw `grep -c` for it on the guide is 0 — the normalizer M8.16.23 must state and prove. |
| `packages/app/src/lib/mode-copy.ts:173` | `` `archived · superseded by #${prNumber}` `` — what M8.9.6's banner actually says (M8.16.17). |
| `packages/app/src/lib/mode-copy.test.ts:935-941` | *"and nothing is described as merely unreachable"*, `expect(DELETE_VOCABULARY).not.toMatch(/unreachable/i)` — the assertion M8.12.1's Check contradicts (M8.16.19). |
| `packages/revud/src/direct/local-writes.ts:420,671,770,833` | `{ status: 'forbidden', reason }` and the three `ApiError('forbidden', …)` throws — the archived refusal M8.4's text calls unreachable (M8.16.14). |
| `packages/revud/src/direct/local-writes.test.ts:159-171` | `PORT_MEMBERS`, **eleven** names including `getLocalReview` (M8.16.14). |
| `packages/revud/src/direct/local-write-isolation.test.ts:71-77` | `LOCAL_MODULE_FILES`, **five** names including `local-ids.test.ts` (M8.16.14). |
| `packages/revud/src/direct/local-archive.ts:160-163,193` | `isOwnerNameShaped` and the silent `{ archivedPr: null, requested: false }` return (M8.16.8). |
| `packages/revud/src/direct/local-archive.test.ts:184` | *"a path-shaped repository identity is never asked about"* (M8.16.8). |
| `packages/revud/src/direct/local-git.ts:212-238` | `repoIdentity` — `owner/name` when the origin parses, else the toplevel path, with the docstring recording that earlier rows keep the path identity deliberately (M8.16.8). |
| `packages/revud/bin/revu` | The CLI: no `--local-only` path, against `index.ts:101-106`'s flag and variable — M8.5 OQ6's deferral, still true and unrecorded on the board (M8.16.22). |
| `.claude/workstreams/m8-local-reviews/tickets/M8.1-contract-and-mock.md:678-684` | *"Open question 5 settled … orphaned, never destroyed"* (M8.16.13). |
| `.claude/workstreams/m8-local-reviews/tickets/M8.4-local-write-sink.md:77-80,126,192-196,308,344-345` | The ten-key Check, the four-file scanned set, and the three `forbidden`-is-unreachable claims (M8.16.14). |
| `.claude/workstreams/m8-local-reviews/tickets/M8.7-app-local-chrome.md:521-523,1561-1567` | The zero-diff corroboration clause and Finding B, which already names it a wording defect and carries its own stale figure (M8.16.15). |
| `.claude/workstreams/m8-local-reviews/tickets/M8.8-resync-and-pinning.md:449-487,1031-1058` | The eight `Verify` clauses and the run record that reports five (M8.16.16). |
| `.claude/workstreams/m8-local-reviews/tickets/M8.8-resync-and-pinning.md:588-600` | The pin landmines: `git push --mirror` carrying `refs/revu/*`, and refs living in the common git dir (M8.16.11, M8.16.12). |
| `.claude/workstreams/m8-local-reviews/tickets/M8.9-archive-on-pr.md:750` | The M8.9.3 Log entry claiming both wirings with no evidence named (M8.16.17). |
| `.claude/workstreams/m8-local-reviews/tickets/M8.9-archive-on-pr.md:445,751` | The two existing `mainBroker` hits — the Context row and the Log line M8.16.17 amends. `grep -c mainBroker` is **2** today, which is why it is no control. |
| `.claude/workstreams/m8-local-reviews/tickets/M8.9-archive-on-pr.md:593-602` | OQ2, ending *"it is a copy problem for M8.9.6's banner"* — the cost was carried into the guide instead. **Owned by M8.15's question 11** (`M8.15.21` / `M8.15.22`), which M8.16.17 stands down to. |
| `.claude/workstreams/m8-local-reviews/tickets/M8.9-archive-on-pr.md:711` | An existing `guides/local-review.mdx` citation — `grep -c "guides/local-review"` is **1** today, so that grep is no control either (M8.16.17). |
| `.claude/workstreams/m8-local-reviews/tickets/M8.15-owner-rulings.md:154,229,454,587` | Question 2 (*pin set bounds*, `M8.15.3` / `.4`), question 3 (*deleting an archived review*, `.5` / `.6`), question 8 (*the operator blob flag*, `.15` / `.16`) and question 11 (*where M8.9 OQ2's cost was carried*, `.21` / `.22`) — the four this ticket's `Depends` row leans on (M8.16.11, M8.16.17). |
| `.claude/workstreams/m8-local-reviews/tickets/M8.10-retention-and-gc.md:887` | *"43 pass, unedited"* (M8.16.18). |
| `.claude/workstreams/m8-local-reviews/tickets/M8.4-local-write-sink.md:409,485,1132` | The three **other** `unreachable` hits — `conflict`, "unreachable in practice", and the `## Log` — all true, none M8.16.14's to touch, and why the bare grep can never go green. |
| `.claude/workstreams/m8-local-reviews/tickets/M8.12-delete-confirm.md:16-30,41-44,51,61-71` | The Goal and three units still written to the force-flag design; `:41` carries the `/unreachable/i` clause and `:61` the stale heading *"Wiring and the force round-trip"*. `grep -n -i force` is **11** hits — 7 here, 4 already-correct in OQ1 and the Log. The file has no `## Landmines` section (M8.16.19). |

## Constraints that bind this ticket

- **Read the seam before editing.** Every `path:line` in this file was verified on `board/m8-closeout` at
  `84393c0`. Line numbers move; the sentences are the identity, not the numbers.
- **`84393c0` is this branch's base tip, and it is the baseline every diff assertion uses.** Nothing of M8
  has merged: `main` is 205 commits and 209 files behind this tip, so a `git diff main` says nothing about
  what *this* ticket changed (`git diff --numstat main -- packages/revud/src/index.ts` alone is
  `284 18`, the whole M8 boot wiring). Cut `m8.16/docs-and-board-drift` from `84393c0`, record the sha in the
  `## Log` at cut, and write every no-behaviour assertion against **that sha** — M8.16.3's comment-only diff
  and `## Verify` #3 both depend on it.
- **Comments and docstrings are self-contained** (`AGENTS.md:33-34`). No unit, PR number, phase, session,
  agent or audit id may enter a comment or docstring anywhere under `packages/**`, nor the user-facing guide
  prose under `packages/docs`. That binds M8.16.3's code comment and every line of M8.16.23's new test file
  absolutely. It does **not** bind `.claude/workstreams/**` or `docs/agent/MILESTONES.md`: the latter is a
  seed document that already names `### Issue M8.1` … `### Issue M8.11` by id, which is why M8.16.20 may write
  *"M8.12 and the close-out tickets M8.13–M8.17"* into it and M8.16.21 may amend a criterion there. Audit ids
  (`D8B-g4` and the like) stay in this file and in commit messages, everywhere without exception.
- **A guard comparing two quantities that move together asserts nothing.** M8.16.23's copy list is written
  out in the test, never extracted from the guide it checks.
- **A fix landing in the same edit as its test has no observed red.** Where a unit has a real test (M8.16.23,
  M8.16.12), the red is obtained by control: restore the defect, watch the named test fail, revert, verify
  the revert byte-for-byte.
- **A Check that can never go green is not a Check.** Several of this ticket's stale strings are quoted, by
  design, in append-only records — `LOG.md`, `HANDOVER.md`, sibling tickets, both `AUDIT-2026-09-02` files
  and this ticket itself. Every grep here is therefore **scoped to the files its own unit edits**; a
  repo-wide `grep -rn` over `docs packages .claude/workstreams` additionally crawls `packages/*/node_modules`
  and the untracked `packages/revud/dist`. If a Check below still reads as repo-wide, it is a defect in the
  Check, not a licence to edit a record.
- **The mock is the specification.** Where a doc sentence and the mock disagree, the mock is right and the
  sentence is what changes — unless the unit says otherwise and names the ruling that made it so.
- **`BOARD.md` wins.** M8.16.22's edits to the board ride this PR, and the ticket's `State` and the board row
  change in the same edit.

## Landmines

- **Ticket line pointers rot.** Every anchor above was read on `84393c0`. Open the seam and match the quoted
  sentence before editing; a wrong line number sends the next agent somewhere confidently wrong.
- **A guard comparing two quantities that move together asserts nothing.** M8.16.23's copy list and floor are
  written out in the test. A list extracted from the guide, or a floor computed from the list, would pass
  forever while checking nothing — the exact failure `security-review-claims.test.ts`'s `MINIMUM_PATHS`
  comment exists to prevent.
- **A fix landing in the same edit as its test has no observed red.** For M8.16.23 and M8.16.12 the red is
  obtained by control — restore the defect, watch the named test fail, revert, verify the revert byte-for-byte
  — and both runs go in the `## Log`. For the pure-prose units the honest control is the before/after grep
  pair; say so rather than implying a test that does not exist.
- **Code comments and docstrings name no ticket, PR, phase, session, agent or audit.** This binds M8.16.3's
  comment and every line of M8.16.23's new test file. Audit ids belong in this file and in commit messages.
- **Do not "fix" the code to match a sentence.** Every defect here is prose. If a unit's true sentence looks
  wrong to you, the tree is the authority and the sentence is what changes — and if you believe the tree is
  wrong, that is a new ticket and an owner question, not an edit smuggled into a docs PR.
- **The mock is the specification.** Where the guide, the design and the mock disagree about a local review's
  semantics, the mock wins; a doc sentence is never evidence about behaviour.
- **`BOARD.md` and the ticket header change in the same edit** (M8.16.22). A docs PR that leaves the board
  describing a different "right now" has recreated the drift this ticket exists to remove.
- **Several sentences here are conditional on a sibling ticket** — M8.16.9 on `M8.13.1`, M8.16.10 on
  `M8.13.5`, M8.16.16 on `M8.14.1`, M8.16.11 and M8.16.24/.25 on M8.15's rulings. Writing the post-fix
  sentence before the fix lands is how a guide becomes wrong in the other direction, and writing a paragraph a
  sibling owns is how two tickets clobber each other in the same file. Read the sibling unit first; if it has
  not landed, stop and record that.
- **Every path written into prose must resolve.** `docs/security-review.md` has a gate test for exactly this;
  the other documents do not, so the discipline is manual there — and that asymmetry is itself the argument
  M8.16.23 is making.

## Open questions

**OQ1 — which mirror sentence goes into `direct.mdx`?** *(The ruling itself is **not this ticket's**: it is
M8.15's question 1, and it decides M8.15.1 vs M8.15.2. Owner decides there before M8.16.24 / M8.16.25 starts;
exactly one of those two runs, and the other is closed as not-taken in the `## Log` with the ruling quoted.)*

The scratch-repo proof (2026-09-02) observed that on an archived review the inline gutter composer still opens
and a draft `PUT` is accepted, while the review bar's composer and verdict picker are withheld as promised.
Everything that would change the review's record is refused (`local-writes.ts:420,671,770,833`). That is M8.9
**OQ10**, and M8.15 puts it to the owner with both answers written as units — including the guide prose under
either. What is left unowned is one bullet in `packages/docs/content/docs/run-modes/direct.mdx:179`, which
says *"archived read-only"* and nothing else.

- **If M8.15.1 is kept** (composer hidden, draft read-and-discard only): **M8.16.24** mirrors that clause.
- **If M8.15.2 is kept** (draft stays writable and unsubmittable): **M8.16.25** mirrors that one.

Either way this ticket writes no behaviour and invents no wording; it copies the sentence that landed, so the
two pages cannot describe one behaviour two ways.

**OQ2 — does the guides guard ship in this ticket, or is its absence recorded as accepted?** *(Owner decides
before M8.16.23 starts.)*

M8.16.23 is the only unit here that adds a test, and this ticket's surface is `docs`.

- **Answer A — ship it.** M8.16.23 runs as written: one new file, no production code, two controls recorded.
  The class of defect M8.11 caught by hand gets a gate for its copy-drift half. Cost: a `docs` ticket lands a
  test, and the gate's file count moves by one.
- **Answer B — record why not.** Then M8.16.23 becomes a note instead: the measured finding that the
  path-existence technique does not transfer (one token across all three guides, and it is a branch name),
  the copy-list design that would work, and the explicit acceptance that guide prose has no gate — written
  into `BOARD.md`'s follow-ups by M8.16.22 so the next session inherits the decision rather than the silence.
  Cost: the drift this whole ticket is fixing stays unguarded, and the next audit finds it again.

**OQ3 — does `MILESTONES.md` get the M8.12–M8.17 seed in this PR after all?** *(Owner or the next planning
session decides; M8.16.20 assumes "no".)*

The document's own rule says it and the board must never drift, and M8.16.20 satisfies it with a pointer
rather than a mirror, on the reasoning that seed text written before the tickets' units settle drifts on the
day it lands. If the owner wants the seed now, it is a separate unit in the mirror step's ticket — appended
there, never renumbered into this one.

## Log

2026-09-02 — created from the landing audit (AUDIT-2026-09-02.md).

2026-09-03 — **review applied; eighteen findings, all accepted, no unit added or removed.** Seven were major.
Everything below was re-measured against the tree at `84393c0` before the ticket was edited.

- **Baseline recorded: `84393c0`** is this branch's base tip and the baseline for every no-behaviour
  assertion. `main` is 205 commits / 209 files behind it, and
  `git diff --numstat main -- packages/revud/src/index.ts` is `284 18` — so M8.16.3's "comment lines only"
  diff and `## Verify` #3's file-list assertion were both unrunnable as written against `main`. Both now read
  `84393c0`, and the sha is pinned in `## Constraints that bind this ticket`.
- **Four Checks could never return their asserted value; all four are now scoped to the files their own unit
  edits.** M8.16.3's `grep -rn "direct/broker\|direct and broker" docs packages .claude/workstreams` (kept
  alive by `server.ts:219` and `failure-drills.test.ts:3`, which are about the request handler and stay, by
  `HANDOVER.md:57`, `M8.5:19,612`, `M8.11:833`, both audit files and this ticket, and by the walk crawling
  `packages/*/node_modules` and the untracked `packages/revud/dist`) → scoped, **5 before / 0 after**.
  M8.16.6's `grep -rn "both routers" docs .claude/workstreams` (`M8.1:23`, `LOG.md:27`, `HANDOVER.md:1853`)
  → scoped, **2 before / 0 after**. M8.16.14's bare `grep -n unreachable` on M8.4 (6 hits; `:409`, `:485`,
  `:1132` are not its business) → `grep -n "forbidden.*unreachable\|unreachable.*forbidden"`, **3 before
  (`:126`, `:308`, `:345`) / 0 after outside the Log**. M8.16.21's byte diff of the two criterion-6 lines
  (`MILESTONE.md:42-43` wraps under `- [ ] `, `MILESTONES.md:434` is one `- ` bullet) → `grep -c "property of
  the workspace"`, **0 / 0 before, 1 / 1 after**.
- **A stale sentence the audit's grep found but no unit owned:** `docs/agent/MILESTONES.md:473` (*"Local
  reviews are a capability inside direct/broker mode (D5)"*) and `:426` (the decision list) are now items in
  M8.16.3's Do and the file is in its Files row, so the drift no longer survives the unit that names it.
- **Two controls were false and are replaced.** M8.16.17's "both greps are empty before the edit": `grep -c
  mainBroker` on M8.9 is **2** (`:445`, `:751`) and `grep -c "guides/local-review"` is **1** (`:711`) — now
  `grep -n "inert by construction\|exercised by nothing"`, 0 before / ≥ 1 after. M8.16.13's "the supersession
  grep is empty before the edit" named no command and the obvious one is not empty (`M8.1:297`, `:604`,
  `:723`) — now positional: `sed -n '678,694p' … | grep -c unprocessable`, **0 before / ≥ 1 after** (verified
  0 today).
- **M8.16.15's replacement claim was itself wrong.** `edb3ec0` (M8.9.6, `queries.ts +28`) is **not** an
  ancestor of `m8.7/app-local-chrome` — M8.9 depends on M8.7 (M8.7 → M8.8 → M8.10 → M8.11 → M8.9) — so
  `git diff --stat m8.7/app-local-chrome --` over the four paths prints `queries.ts | 28 +`, not nothing, and
  "M8.6 and M8.9 changes that arrived before M8.7 started" is false for M8.9. The corroboration is now the
  empty commit range `git log --oneline m8.6/app-creation-flow..m8.7/app-local-chrome --` over the four
  (verified empty) plus `git diff --stat main --` printing exactly `shortcuts.ts | 1 +` and `queries.ts | 36
  +` (verified). Finding B's "+8" becomes "+8 from M8.6.2 (`113aee8`) before M8.7, +28 more from M8.9.6
  (`edb3ec0`) after it". A zsh trap is recorded with it: `-- $FILES` unquoted does not word-split, and prints
  an empty diff that reads as green.
- **M8.16.17 item 2 was writing a paragraph M8.15 owns.** M8.15's question 11 decides where M8.9 OQ2's cost
  was carried, and both `M8.15.21` and `M8.15.22` write into OQ2 themselves. Item 2 now stands down —
  verify-and-record-in-Log, as M8.16.10 and M8.16.11 do — and `M8.16.17 item 2` joins the header's `Depends`
  row (seven waiting units, matching the Units intro, which said five).
- **M8.16.11 was pointing at the wrong M8.15 question:** the pin ruling is **question 2** (`M8.15.3` /
  `M8.15.4`); question 3 is *"Deleting an archived review: allowed or refused?"*. Corrected in the header, the
  Do and the Closes line.
- **M8.16.19's anchor was off by one** (`matches /unreachable/i` is `M8.12:41`, not `:42-44`) and its control
  undercounted: `grep -n -i force` is **11** hits, not 3 — 7 in Goal/Units, 4 already-correct in OQ1/Log. The
  M8.12.3 heading (`:61`, *"Wiring and the force round-trip"*) is one of the 7 and is now explicitly
  retitled, with the note that a unit's **number** is the append-only id and its title is prose.
- **M8.16.23's hint entry would have failed on day one:** the banner hint wraps across
  `local-review.mdx:85-86` inside a `> ` blockquote, so `grep -c` for it on the guide is **0** today. The unit
  now states the normalizer (strip `> `, join, collapse whitespace), requires it proved in the inline fixture
  by a wrapped case that must match and an unwrapped one that must not, and cites `mode-copy.ts:326` for
  `'Review saved'` — which is nowhere near the `:434`/`:563` the unit had.
- **M8.16.2's Check contradicted its own item 2** (the prescribed `dirty` wording contained no "untracked");
  the wording now carries the word the Check counts.
- **M8.16.4's third grep had no file operand** and would have read stdin and hung; `docs/agent/LOCAL_REVIEWS.md`
  appended. **M8.16.22 item 3** pointed at OQ1's owners for OQ2's question; corrected.
- **Shape:** `## Binding constraints` renamed to `## Constraints that bind this ticket` and moved after
  `## Context — verified seams`, per the README's ticket shape and every sibling — a heading-keyed migration
  would have missed it. Its self-contained-prose constraint is now scoped to comments and docstrings under
  `packages/**` and the user-facing guides (`AGENTS.md:33-34`), exempting `docs/agent/MILESTONES.md` by name,
  which it had forbidden while M8.16.20 and M8.16.21 write ticket ids into it. A new constraint states that
  every grep here is scoped to its unit's own files, and why.
- Twelve seam rows added to `## Context — verified seams` for the anchors this pass measured.
- **Nothing rejected.** No audit id was removed: M8.16.3 still closes D3-g3 / D6-g1 / D7-g4 / EC3-g4,
  M8.16.17 still closes M8.9-g2 (on item 1), and every other `Closes` line is unchanged.

# deps (docs keeps its own nested install, isolated from the root workspace)
/node_modules

# fumadocs-mdx generated content index
.source

# next build output
/.next/
/out/
/build
*.tsbuildinfo
next-env.d.ts

# build-time mirror of the repo's canonical docs/assets (see scripts/copy-assets.mjs)
/public/assets

# misc
.DS_Store
.env*.local
.vercel

# M8.8 — Re-sync, rebase safety, and object pinning

| field | value |
| --- | --- |
| **State** | In Progress |
| **Surface** | revu:revud |
| **Kind** | Feature |
| **Depends** | M8.2, M8.3, M8.5 |
| **Blocks** | M8.11 |
| **Branch** | `m8.8/resync-and-pinning` |
| **PR** | — |
| **Assignee** | — |

## Goal

Make re-sync survive the two things a local branch does constantly and a pull request almost never does:
being amended and being rebased. This ticket delivers the object pinning of `LOCAL_REVIEWS.md §5.1` (a git
ref per synced snapshot, so `git gc` can never take the objects the local blob tier is the *only* source
for), replaces the silently under-reporting author-date fallback with explicit rewrite detection
(`LOCAL_REVIEWS.md §8` case 12), turns "the content this snapshot references is gone" from a hard unreadable
error into an honest re-syncable state (case 15), keeps a deleted or renamed branch from costing a single
draft (case 14), fixes rather than reproduces the latent stale-`draft.headSha` bug a conflict-terminated
reconcile leaves behind (case 18), and states the **D8** base-advance behaviour honestly — a local `compareKey`
can change
with zero new head commits, because the merge base is recomputed against the live base tip. It implements
**D8** directly and is the enforcement arm of **D7**: with the GitHub blob tier structurally absent from the
local path (M8.3.6), a pruned object has no second source, so retention of git objects becomes a
correctness property rather than a convenience.

## Units

Default execution order is 1 → 8. **M8.8.1 is a hard prerequisite for M8.8.2** (nothing to wire without the
seam). After M8.8.1 lands, **M8.8.3, M8.8.4, M8.8.5, M8.8.6 and M8.8.7 are mutually independent of each other
and of M8.8.2** — they touch disjoint files (M8.8.3: shared reconcile + mock; M8.8.4: the reconcile pre-flight;
M8.8.5: the local sync's typed-error path; M8.8.6: the app's staleness/dialog copy; M8.8.7: the draft's head
bookkeeping) and can run in parallel — with **one** exception the Check now makes explicit: M8.8.6's
base-advance leg lands in M8.8.5's `local-resync.test.ts`, so those two serialize on that file even though
nothing else about them overlaps.
M8.8.3 and M8.8.6 both end up rendering rewrite state, so they share **one** line of the reconcile dialog —
M8.8.6 owns every app-side file; M8.8.3 owns revud + the mock and must not edit the dialog. M8.8.6 and M8.8.7
both edit `reconcile-dialog.tsx` and must run in sequence.

**Test-first ordering inside the units.** Three assertions land *before* the code they constrain, red, and
each unit's **Do** says so at the point it applies: M8.8.2's pin-ordering assertion (red on today's tree
because no `update-ref` appears in the argv sink at all), M8.8.3's mock-side rewrite assertion (**D6** — the
mock moves first, so its test is written against `adapter.ts` before `reconcile.ts` is touched), and M8.8.5's
store-deletion tripwire (a guard rail: it must exist before any sync-failure path can reach a `delete*`).
**M8.8.8 is appended, not renumbered in** — it owns the prune-survival suite the `Verify` section describes
and which no unit owned. It extends M8.8.5's `local-resync.test.ts`, so it runs after M8.8.5, after M8.8.2
(there is nothing to prove before the pin call is wired), and never concurrently with M8.8.6's fixture-repo
leg, which lands in the same file.

**Why the Depends row is three tickets, not one.** Only M8.8.1/M8.8.2/M8.8.3 are completable against M8.3
alone. M8.8.4's second half maps `getLocalSnapshot` (M8.2.3) at the API edge, onto the local dispatch M8.5.1
creates; M8.8.5's Check calls `getLocalDraft` / `listLocalThreads` / `getLocalSnapshot` (M8.2.4, M8.2.5); and
M8.8.6's staleness half needs a live list row, which for a local review arrives with M8.5.3's `listPulls`
merge (until then `useStaleness` returns `null` and the banner degrades rather than blocking — that half is a
soft dependency, and the unit text already says so). An agent scheduling from the header alone would otherwise
start M8.8 after M8.3 and find half the units blocked on store methods that do not exist.

### M8.8.1 — The pin seam: a ref name git will actually accept

**Do:** Add `packages/revud/src/direct/local-pins.ts`, the only module that names or writes a pin ref. It
takes a `CommandRunner` and a repo root and **never** a `GithubClient` (D7 is structural here too). Exports:

- `pinKeyForCompare(compareKey)` — a pure function turning `${mergeBaseSha}...${headSha}` into a
  **ref-legal** path component. The design's literal `refs/revu/reviews/<id>/<compareKey>` **cannot be
  created**: `git check-ref-format` rejects any name containing `..`, and a compareKey contains three of
  them (verified: `git check-ref-format refs/revu/reviews/1000000001/aaaa...bbbb` exits **1**). Do the
  substitution in exactly one place — `...` → `-`, giving `<mergeBaseSha>-<headSha>` — and never re-derive
  the compareKey from a ref name (list refs, do not parse them).
- `pinRefsFor(localId, compareKey)` → the two ref names
  `refs/revu/reviews/<localId>/<pinKey>/base` and `.../head`. `<localId>` and `<pinKey>` must always be
  **directories, never refs**: a ref at `refs/revu/reviews/<id>` makes every child unwritable
  (verified: `fatal: update_ref failed … 'refs/revu/reviews/<id>/…/head' exists; cannot create
  'refs/revu/reviews/<id>'`).
- `pinSnapshotObjects(runner, cwd, localId, { mergeBaseSha, headSha })` — one `git update-ref --stdin`
  invocation writing both refs, so a crash cannot leave half a pin. Validate `localId` is a positive
  integer and each SHA matches `/^[0-9a-f]{40,64}$/` (40 for sha1, 64 for a sha256 repo) **before** it
  enters an argv; route the argv through M8.3.1's `runGit` so `--end-of-options` discipline is inherited.
  Idempotent: re-pinning the same compare rewrites the same two refs.
- `listPins(runner, cwd, localId?)` — `git for-each-ref --format='%(refname)%00%(objectname)'` over
  `refs/revu/reviews/` (optionally narrowed to one id). Always pass an explicit `--format`; the default
  output is not a parsing contract.

**This module writes and lists pins; it does not delete them.** An earlier shape of this ticket also exported
`unpinReview(runner, cwd, localId)` "as the primitive M8.10 consumes" — but M8.10.2 independently specifies
`dropPinnedRefs({runner, cwd}, localId)` in its own `retention.ts` with prefix discovery, M8.10 depends on
M8.2 and M8.5 rather than on this ticket, and its landmine correctly requires discovery over reconstruction.
Two agents would have built the same deletion twice and this one would be dead code. Ref deletion is
**wholly M8.10.2's**. What this unit owes M8.10 instead is the namespace *shape*, which is why `pinRefsFor`
puts `<localId>` and `<pinKey>` in directory position: every ref this module writes lives under the prefix
`refs/revu/reviews/<localId>/`, so M8.10.2's `for-each-ref` over that prefix is correct under any encoding of
the compare key this unit picks (M8.10's open question 5 asks exactly that, and this sentence is its answer).

**Write first, red:** the ref-legality table below lands before `pinKeyForCompare` / `pinRefsFor` exist. It
is the assertion the whole unit turns on and it is trivially satisfiable in the wrong direction (any string
substitution makes *some* legal ref), so it must be seen failing against real `git check-ref-format` before
the substitution is written.

**Files:** `packages/revud/src/direct/local-pins.ts` (new),
`packages/revud/src/direct/local-pins.test.ts` (new).

**Check:** `bun test packages/revud/src/direct/local-pins.test.ts` passes and contains:

- **(a) Ref legality against real git.** Run the **real** `git check-ref-format` over both names
  `pinRefsFor(1_000_000_001, 'aaaa...bbbb')` produces and assert exit 0, plus a companion assertion that
  `check-ref-format` on the design's literal `<id>/<compareKey>` form exits 1 — so the substitution is
  documented as load-bearing, not cosmetic.
- **(b) Validation rejects before argv, with its negative control.** `mergeBaseSha = '-x'` (and a
  non-integer `localId`) yield a typed rejection with a **recording fake runner whose arg sink is empty**.
  That is an assertion of absence, so it is paired with a permanent control in the same table: the *same*
  runner, a *valid* id and SHA pair, asserts the sink length is `> 0`. Without the pair, an empty sink is
  indistinguishable from a runner that is never wired to anything.
- **(c) Round-trip against the fixture repo.** `pinSnapshotObjects` then `listPins` returns both ref names
  and both object ids; re-pinning the same compare leaves exactly the same two refs (idempotence). The
  captured `for-each-ref` argv is asserted with `toEqual` on the **whole array** — not `toContain` — so a
  silently dropped `--format`, which would hand the parser git's unspecified default output, is red. Each
  captured argv also satisfies M8.3.1's `isHardenedArgv`.
- **(d) Degradation never throws.** A runner whose `update-ref` exits 128 (the bare-repo / locked-ref case
  in Landmines) yields a typed failure **result**, asserted in the `await expect(...).resolves` form so a
  throw fails the test rather than surfacing as an unhandled rejection. M8.8.2's "a pin failure is not a
  sync failure" contract is unimplementable if this primitive can throw.

### M8.8.2 — Pin **before** the first object read, on every local sync

**Do:** Call `pinSnapshotObjects` from the local sync path immediately after `resolveLocalRange`
(M8.3.2) returns and **before** the first `git diff --raw`, `--numstat`, `-U3`, `git log` or `git cat-file`.
Ordering is the whole point: a prune that lands between the diff (which produces the blob SHAs) and the
`cat-file` reads (which fetch their bytes) turns every one of those SHAs into a `missing` entry
(`blobs.ts:284`) with no API tier to recover them, and the snapshot persists `partial` with the content the
draft anchors against absent. Pinning first closes that window; pinning after it does not.

A pin failure is **not** a sync failure — a review whose objects are unpinned still works today, it just has
no retention guarantee — so record the outcome on the sync result rather than throwing. Do **not** fold it
into `Snapshot['partial']`: `partial` means "content is missing", and an unpinned-but-complete snapshot is
not missing anything (see Open questions for where it should surface instead). The interim carrier is a
`pin: { ok: boolean; reason?: string }` field on the local sync result — a shape, not an answer to Open
question 3, but a concrete one so the assertion below can name it.

**Write first, red:** the ordering assertion lands **before** the `pinSnapshotObjects` call is wired. On
today's tree it is red for the strongest possible reason — no argv in the sink contains `update-ref` at all
— which is exactly what proves the assertion runs and is not comparing two `-1`s. Record both observations
in the Log.

**Files:** `packages/revud/src/direct/local-sync.ts`, `packages/revud/src/direct/local-sync.test.ts`.

**Check:** `bun test packages/revud/src/direct/local-sync.test.ts` gains:

- **The ordering assertion.** A recording fake `CommandRunner` asserts the index of the first argv
  containing `update-ref` is `>= 0` **and** strictly lower than the index of the first argv containing
  `diff` and the first containing `cat-file` — an ordering assertion, not a "was it called" assertion, with
  the `>= 0` guard so two absent commands cannot compare equal and pass.
- **A pin failure is non-fatal, and is recorded.** A runner whose `update-ref` exits non-zero asserts the
  sync still resolves with a complete snapshot (`partial === null`) and `result.pin.ok === false`. Paired
  with the success row asserting `result.pin.ok === true` on the same fixture, so the failure assertion
  cannot pass because the field is always falsy or always absent.
- **`partial` stays out of it.** Both rows assert `result.snapshot.partial` is **identical** between the
  pin-success and pin-failure runs — the durable form of "do not fold the pin outcome into `partial`", which
  otherwise is a rule that lives only in prose.

### M8.8.3 — Rewrite detection replaces the silent under-report (shared reconcile + mock, in lockstep)

**Do:** In `reconcile.ts:92-96`, when `commits.findIndex(c => c.sha === draft.headSha)` is `-1` the draft's
head is not in the current compare at all — the branch was rewritten. Today that falls to
`commits.filter(c => c.commit.author.date > draft.createdAt)`, which **under-reports by construction**: a
rebase rewrites committer date but preserves author date, so every rebased commit keeps a date older than
the draft and is filtered out. Replace the heuristic with the honest answer — when the draft head is absent,
every commit in the current compare is new relative to a head that no longer exists, so `newCommits` is the
**whole** `commits` list. Make the identical change in the mock (`adapter.ts:639-643`), which today is
line-for-line parallel including the comment; **D6** makes the mock the spec, so it changes first or the two
transports disagree. Rewrite the comment blocks in both to describe the new rule with no reference to any
ticket or phase (AGENTS.md code style).

This changes the GitHub PR path too — that is deliberate: `anchor.ts` / `reconcile.ts` are shared, not
duplicated (hard constraint), and a force-push on a PR has the identical defect. `reconcile.test.ts:428`
("falls back to author-date when the draft head fell out of the rewritten compare") pins the old behaviour
and must be rewritten, not deleted.

**Write first, red, and in the mock first.** The mock-side assertion is written and **observed red** against
today's `adapter.ts` before either implementation changes; the revud-side rewrite of `reconcile.test.ts:428`
follows. Record both reds in the Log.

**Files:** `packages/revud/src/direct/reconcile.ts`, `packages/revud/src/direct/reconcile.test.ts`,
`packages/app/src/api/mock/adapter.ts`.

**Check:** `bun test packages/revud/src/direct/reconcile.test.ts`, containing all three of:

- **The rewritten case.** `report.newCommits.map(c => c.sha)` is `['X0','X1','X2']` (all three), plus the
  property that catches a regression rather than the literal: `report.newCommits.length ===
  snapshot.immutable.commits.length` whenever `draftHeadSha` is absent from `commits`.
- **The old heuristic, asserted to be strictly worse — permanently, not in the Log.** The same test computes
  `commits.filter(c => c.commit.author.date > draft.createdAt)` inline and asserts its length is
  **strictly less** than `report.newCommits.length`. That one line is the whole regression guard: a future
  edit that restores the author-date fallback makes it red, whereas the count assertion alone could be
  satisfied by a fixture change.
- **Parity against the REAL mock, not a transcription.** `reconcile.test.ts:289`'s `oracleResults()` is a
  hand-copied replay of the mock's reconcile body — a parity assertion against *it* stays green even if
  `adapter.ts` is never edited, which is precisely the divergence D6 exists to prevent. So the parity leg
  imports `createMockApi` / `mockDev` from `@revu/app/mock` (the same seam `failure-drills.test.ts:35`
  already uses from revud), calls `mockDev.reset()` + `setLatency('zero')` + `setFailureMode('none')` in
  `beforeAll` (the shared-`localStorage` landmine), syncs a mock PR, saves a draft whose `headSha` is a SHA
  absent from that snapshot's commit list, and asserts `reconcileDraft`'s `newCommits.length` equals the
  snapshot's full `commits.length`. `oracleResults()`'s transcribed comment block is updated in the same
  pass so it does not describe a rule the mock no longer implements.

`bun test packages/revud/src/direct/conformance-reconcile.test.ts` stays green unchanged (its draft head is
still in the list, at `:97`) — the durable proof that the change is scoped to the head-absent branch.

### M8.8.4 — "Objects missing, re-sync to rebuild" instead of a lie

**Do:** A blob the store cannot produce resolves to `null` at `anchor.ts:417`, which
`classifyAnchor` reads as "no content to match" and reports as `lost` / `line-deleted`
(`anchor.ts:283-285`) — a claim the human acts on by dropping a comment whose line is, in fact, still
there. Add a pre-flight to the reconcile path (local and PR alike): before classifying, walk the draft's
comments, select each anchor blob SHA through the shared `selectAnchorBlobSha` (`anchor.ts:94`, the same
selector the dialog uses at `reconcile-dialog.tsx:159-160`), and probe `store.hasBlob`
(`store.ts:417-420`). If any anchor blob is absent, do **not** classify: answer a typed, actionable
`ApiError('not_found', …)` naming the count of unavailable objects and instructing a re-sync. Never
re-implement the side selection — importing the shared selector is what keeps a preview and this report from
disagreeing.

Second half: `getLocalSnapshot` (M8.2.3) mirrors `getSnapshot` (`store.ts:341-352`), which throws
`StoreUnreadableError` when the envelope references a `compareKey` with no immutable row. That is right for
a corrupt store and wrong for a moved data dir, and the two are indistinguishable to the caller today. Keep
the store throwing (it must not report a present row as absent), and map it at the API edge to the same
re-syncable answer so a moved data dir renders "re-sync to rebuild" instead of a 500.

**Files:** `packages/revud/src/direct/reconcile.ts`, `packages/revud/src/direct/reconcile.test.ts`,
`packages/revud/src/direct/direct-api.ts`,
`packages/revud/src/direct/local-objects-missing.test.ts` (new — the API-edge half, kept out of
`local-resync.test.ts` so M8.8.4 and M8.8.5 stay file-disjoint).

**Check:** two files.

`bun test packages/revud/src/direct/reconcile.test.ts` — the pre-flight:

- **The throw, and the lie it replaces.** Build the `forcePushFixture` store, delete one anchor blob, and
  assert `reconcileDraft` throws `ApiError` with `code === 'not_found'` and a message naming the re-sync —
  *and* that it does **not** return a report containing a `lost` result with reason `line-deleted`. The
  negative control for that absence is the file's own existing case at `:329`, which asserts a **genuine**
  `lost` / `line-deleted` with every blob present: both rows run in the gate, so the absence assertion
  cannot rot into one that passes because nothing classifies at all.
- **Binary is not missing.** The Landmine at `reconcile.ts:80` gets a permanent row: a fixture whose anchor
  blob is present with `binary: true` asserts `reconcileDraft` returns a normal report and does **not**
  throw. A pre-flight written against "did `resolveBlobLines` return null" instead of `hasBlob` is red here
  and green nowhere else.
- **The draft is untouched.** After the throw, `store.getDraft(...)` `toStrictEqual`s the pre-call value.
- **The fake store's `hasBlob` becomes reachable.** `reconcile.test.ts:93-123`'s fake declares
  `hasBlob: unexpected('hasBlob')`, which *throws* — the pre-flight turns every existing case in the file
  red until it is backed by the same `blobs` record `getBlob` reads. Wiring it to a second, independent map
  would let the pre-flight and the classifier disagree, so it must read the one record.

`bun test packages/revud/src/direct/local-objects-missing.test.ts` — the API-edge half, which today's Check
did not cover at all:

- Seed a local review, sync, then delete the `immutables` row out from under it (the manoeuvre
  `store.test.ts:251` and M8.2.3 already use) and assert the API edge answers `ApiError('not_found', …)`
  naming a re-sync — **not** a propagated `StoreUnreadableError`, asserted explicitly with
  `expect(err).not.toBeInstanceOf(StoreUnreadableError)`.
- The paired assertion that keeps the store honest: `store.getLocalSnapshot(id)` called directly **still**
  throws `StoreUnreadableError`. Mapping at the edge and softening the store are indistinguishable from the
  client, and only this pair distinguishes them at the gate.

### M8.8.5 — Deleted or renamed head/base: typed `not_found`, read-only, nothing discarded

**Do:** A head or base branch that has been deleted or renamed makes `git rev-parse --verify` exit 128,
which M8.3.2 already maps to a typed `ref_not_found` naming the ref. This unit makes that state *survivable*
rather than merely typed: the review goes read-only and **every** stored artifact stays readable. Concretely
— no code path may delete a `local_reviews` row, a `local_drafts` row, a `local_threads` row or a
`local_snapshots` row in response to a sync failure; the read methods must not consult git at all, so a
review whose branches are gone still opens, still renders its threads, and still shows its draft. Do not add
a schema column for the state (M8.2 owns v4 and this is derivable): the review is read-only exactly when its
last sync answered `ref_not_found`, which the daemon returns to the client as a typed error.

**Write first, red:** the deletion tripwire below is a guard rail — it lands before the failure path it
constrains, not after, so it encodes what the code must never do rather than what it happens to do.

**Files:** `packages/revud/src/direct/local-sync.ts`, `packages/revud/src/direct/local-resync.test.ts` (new).

**Check:** `bun test packages/revud/src/direct/local-resync.test.ts` contains:

- **The survival walk, against the fixture repo.** Create a review, sync, write a draft with three comments
  and one thread, `git branch -D <head>`, sync again → typed `ref_not_found` naming the head ref; then
  assert `getLocalDraft(...)` `toStrictEqual`s the pre-delete draft, `listLocalThreads(...)` is unchanged,
  and `getLocalSnapshot(...)` still returns the last good snapshot. Repeat for a renamed base ref
  (`git branch -m`).
- **A runtime deletion tripwire, not a grep.** Wrap the real store in a thin decorator whose every `delete*`
  and `deleteLocal*` method throws — the idiom `reconcile.test.ts:98`'s `unexpected(name)` already
  establishes — and run the whole failing-sync walk through it. A grep proves a spelling; this proves the
  path. Its negative control is permanent and in the same file: a sibling assertion calls
  `deleteLocalDraft` through the decorator directly and asserts it **does** throw, so a tripwire that was
  silently wired to nothing is red rather than green.
- The `readFileSync` source assertion that `local-sync.ts` contains no `delete` call stays as cheap
  corroboration alongside the tripwire — it is not the Check.

### M8.8.6 — Base advance under D8, and the rewrite the UI must not miscount

**Do:** Two app-side surfaces, both derivations over data that already exists — no new query, no new route.

- **Rewrite.** `useStaleness` (`queries.ts:147`) derives `newCommits` as a count *difference*, which a
  rebase leaves at zero, so a rewritten branch already falls through to the third banner branch,
  "head moved since sync" (`pr-layout.tsx:205-215`) — that copy is correct for a rewrite and needs no
  change. What is wrong is the reconcile dialog's header, which reads "N new commits"
  (`reconcile-dialog.tsx:284-287`) over a list that, after M8.8.3, contains *every* commit in the compare.
  Derive `rewritten` in the dialog with the same test the server uses —
  `!snapshot.immutable.commits.some(c => c.sha === report.draftHeadSha)` — and say "the branch was
  rewritten — N commits, all new SHAs" instead of "N new commits". No contract change: both inputs are
  already on the loaded snapshot and the report.
- **Base advance (D8).** A local review recomputes its merge base against the **live** base tip on every
  sync, so committing on the base branch alone changes `compareKey` with head untouched. The existing copy
  is already correct — `pr-layout.tsx:187-204` ("base advanced — diff changed", with the three-dot compare
  explanation) and the dialog's zero-commit state, "The compare changed without new head commits — the base
  branch moved" (`reconcile-dialog.tsx:386-391`). This unit's job is to *prove* it fires for a local review
  and to state the D8 divergence in the tooltip: a PR reads GitHub's `pull.base.sha`, which only refreshes
  on a `synchronize` event, so the two paths genuinely disagree about what changed.

Note the ordering reality: `useStaleness` needs a live list row (`usePullItem`), which for a local review
arrives with M8.5's `listPulls` merge. Until then `useStaleness` returns `null` and no banner renders —
degrade, never crash. The dialog half of this unit has no such dependency.

**Files:** `packages/app/src/components/review/reconcile-copy.ts` (new),
`packages/app/src/components/review/reconcile-copy.test.ts` (new),
`packages/app/src/components/review/reconcile-dialog.tsx`, `packages/app/src/pages/pr-layout.tsx`,
`packages/revud/src/direct/local-resync.test.ts` (the base-advance leg, after M8.8.5 creates the file).

**Check:** There is **no DOM test runner in `packages/app`** — no RTL, no jsdom, and all fourteen existing app
tests are pure-module or mock-adapter tests — so "the dialog header renders X" is not a runnable assertion.
Follow the pattern `error-copy.ts:1-12` establishes and M8.7 leans on throughout: put both derivations in
`reconcile-copy.ts` — `isRewritten(commits, draftHeadSha)` and `compareChangeLine({ draftHeadSha,
currentHeadSha, newCommits })`, which calls it — and assert them with `bun test`:

- `bun test packages/app/src/components/review/reconcile-copy.test.ts`: `isRewritten` is `true` when
  `draftHeadSha` is absent from the commit list and `false` when present (including the empty-list row,
  which must be `true` and not throw); `compareChangeLine` returns the rewrite phrasing in the first case
  and, in the second, the **byte-identical** string the dialog renders today —
  `` `${shortSha(a)} → ${shortSha(b)} · ${n} new ${n === 1 ? 'commit' : 'commits'}` `` — with the `n === 1`
  singular row asserted, because that is the branch a naive extraction drops.
- **The wiring is asserted, not assumed.** A `readFileSync(new URL('./reconcile-dialog.tsx',
  import.meta.url))` assertion in the same file: the source imports `compareChangeLine` and contains **no**
  surviving `new {' '}` / `'commit' : 'commits'` literal. That absence is controlled by the bullet above,
  which asserts the literal *is* produced by `compareChangeLine` — so the guard cannot pass because the copy
  vanished from the app entirely. This is the durable half of the rendered leg; the `?mock=1` walk recorded
  in the Log and M8.11's e2e corroborate it and never substitute for it.
- **Base advance (D8), against real git.** In `local-resync.test.ts`: sync, commit on the **base** branch
  only, sync again, and assert `headSha` is unchanged **and** `compareKey` differs from the previous sync's
  — both halves, since either alone is satisfiable by a bug (a frozen compareKey, or a compareKey that
  changes because the head moved).

### M8.8.7 — A conflict-terminated reconcile must not leave the draft's head stale

**Do:** `§8` edge case 18 — "a reconcile-apply that ends in `conflict` never updates `draft.headSha`, an
existing latent bug that makes the *next* reconcile compute new commits from a stale head. Local reviews
inherit it; **it should be fixed rather than reproduced**." No other unit in this milestone touches it, and it
sits directly on top of M8.8.3's rewrite-detection change: after M8.8.3, "the draft head is not in the current
compare" means *every* commit is reported new, so a stale `draft.headSha` no longer under-reports quietly —
it reports the whole branch as new on every subsequent reconcile, which is the loudest possible version of the
same bug.

The mechanism, verified: `draft.headSha` is written exactly once, at creation (`state/drafts.ts:210`, inside
`ensureDraft`), and nothing ever updates it. `reconcile-dialog.tsx`'s `apply()` (`:162-258`) re-anchors every
kept comment against the **freshly synced** snapshot and then submits with `expectedHeadSha:
live.currentHeadSha`. On `ok` the draft is deleted, so the staleness is invisible; on `head_moved` it re-syncs
and re-reconciles. But on a `conflict` — and on any other rejection that lands in the `catch` at `:249-256` —
the draft survives with comments anchored to the **new** head and a `headSha` recording the **old** one, and
the next `reconcileDraft` computes its commit delta from a head the comments no longer correspond to.

Fix it where the re-anchor happens: give `DraftActions` (`state/drafts.ts:153-165`) a narrow mutator that
rewrites `headSha` and `compareKey` together — they are one fact, and updating either alone re-creates the
same inconsistency on the other axis — and call it from `apply()` in the same pass that upserts the
re-anchored comments, **before** the submit, so the persisted draft is coherent no matter how the submit ends.
It must go through the ordinary debounced save path so the daemon's copy moves too (`saveDraft` rebuilds the
draft as `{ ...draft, humanId, updatedAt }`, so the client-supplied head survives and the human key does not).
Nothing here may delete or clear a draft: this unit makes a surviving draft *correct*, and "drafts survive
everything" is untouched.

`apply()` is a closure over React hooks (`queryClient`, two mutations, the toast), so "run the reconcile-apply
sequence" is not callable from `bun test` as written. Apply the doctrine's extraction rule: lift the
re-anchor pass into a pure `planReconcileApply({ draft, results, decisions, currentHeadSha, currentCompareKey,
resolveLines })` in `packages/app/src/components/review/reconcile-plan.ts`, returning
`{ updated, kept, dropped, nextHead: { headSha, compareKey } }`. `resolveLines(sha)` is an injected callback —
the same shape `reconcile.ts:78` and the dialog's own `blobShaFor`/`fetchQuery` pair already use — so the plan
is testable with a fake resolver and no query client. `apply()` then reduces to: plan → upsert/remove →
`setHead(plan.nextHead)` → `flush()` → submit, which is wiring thin enough to read at a glance.

**Files:** `packages/app/src/state/drafts.ts`,
`packages/app/src/components/review/reconcile-plan.ts` (new),
`packages/app/src/components/review/reconcile-plan.test.ts` (new),
`packages/app/src/components/review/reconcile-dialog.tsx`.

**Check:** `bun test packages/app/src/components/review/reconcile-plan.test.ts`, containing:

- **The mutator, pure.** Applying it moves `headSha` **and** `compareKey` together and leaves `comments`,
  `body`, `event` and `createdAt` untouched (`toStrictEqual` on the rest of the document). Its signature
  takes the pair, so "updating either alone" is unrepresentable rather than merely discouraged — asserted by
  a `tsc`-level row plus a runtime assertion that the returned `compareKey` ends with the returned `headSha`.
- **The plan.** Over a fixture with one keep, one drop and one accept-drift: `updated` carries the
  re-anchored lines, `kept`/`dropped` count correctly, and `nextHead` is
  `{ headSha: currentHeadSha, compareKey: currentCompareKey }` — the freshly synced pair, never the draft's.
- **The regression, permanently paired, at the API layer.** Drive the real mock (`createMockApi` from
  `@revu/app/mock`, `mockDev.reset()` in `beforeAll` — the shared-`localStorage` landmine): seed a draft
  whose `headSha` is a SHA absent from the synced snapshot's commits, then assert **both** legs in the same
  test — (i) *without* the head update, `reconcileDraft` reports the whole compare (`newCommits.length ===
  commits.length`), the pre-fix behaviour; (ii) after persisting `plan.nextHead` through the ordinary
  `saveDraft` path, a second `reconcileDraft` reports **zero** new commits. Leg (i) is what distinguishes the
  fix from a no-op, so it is an assertion in the gate rather than an observation in the Log.
- **`saveDraft` really carries the head.** Read the draft back through `getDraft` after the save and assert
  `headSha`/`compareKey` survived — `direct-api.ts:275-281` rebuilds the draft, and the whole app-side
  approach depends on the client-supplied pair surviving that rebuild.
- **Ordering, corroborated.** A `readFileSync` assertion on `reconcile-dialog.tsx` that the `setHead` call
  appears **before** `submit.mutateAsync` in the source. Source order is a weak proxy for execution order and
  is named as corroboration, not as the Check; the API-layer pair above is what proves the behaviour.

### M8.8.8 — The prune-survival suite and the control that gives it teeth

**Do:** No production code. The `Verify` section describes the acceptance test this whole ticket exists to
pass, and no unit owned writing it — so it is a unit, appended rather than folded into M8.8.2, because it
runs only once M8.8.2's pin call and M8.8.5's `local-resync.test.ts` both exist. Extend that file with the
real-git prune walk of `Verify` step 1 **and** its matched control (step 2), as a permanent pair in the gate
rather than a break-and-revert dance: the control runs the identical scenario through a runner that
intercepts and swallows the `update-ref` invocation (exit 0, no ref written), so pinning is disabled without
editing the module under test.

The two commands are not interchangeable and both are mandatory: `git gc --prune=now` alone keeps
unreachable objects for two weeks, so the walk runs
`git reflog expire --expire=now --expire-unreachable=now --all` first. A suite missing the reflog expiry is a
suite that always passes.

**Files:** `packages/revud/src/direct/local-resync.test.ts`.

**Check:** `bun test packages/revud/src/direct/local-resync.test.ts` contains both halves of the pair:

- **Survival, pinned.** After create → sync → draft with comments on three files → amend/rebase → reflog
  expire → `gc --prune=now`: `git cat-file -t` on the **stored** snapshot's `mergeBaseSha` and `headSha`
  both print `commit`, and `git cat-file -e` exits 0 for **every** SHA in the stored `immutable.blobIndex`,
  both sides. Assert the blobIndex is non-empty first, so an empty index cannot satisfy "every SHA".
- **Loss, unpinned — the control.** The identical scenario with `update-ref` swallowed asserts the specific
  red rather than "at least one assertion fails": `git cat-file -t <stored headSha>` exits **non-zero**.
  Naming the assertion matters because the reconcile leg below is *not* a reliable discriminator — the
  freshly synced compare is reachable either way, and the `blobs` table is cache-forever with a single
  `DELETE` in the whole store, so a naive rebase-then-gc-then-reconcile test passes with the feature
  entirely absent.
- **The end-to-end leg, honestly labelled.** After clearing the store's `blobs` table — which forces the
  local git tier to be the only producer, the GitHub tier being structurally absent (M8.3.6) — a re-sync +
  reconcile classifies every comment `clean` or `drifted` and **zero** as `lost` with reason
  `line-deleted`. It is the product-level assertion, and the object-survival pair above is what proves
  pinning is doing the work.

## Verify

Additional to `bun run check`:

1. **Prune survival, with teeth.** `bun test packages/revud/src/direct/local-resync.test.ts` runs against a
   real seeded repo (M8.3.8's `local-fixture-repo.ts` harness) and performs, in order: create a local review
   → sync → write a draft with comments on three files → `git commit --amend` / `git rebase` the head →
   **`git reflog expire --expire=now --expire-unreachable=now --all`** → **`git gc --prune=now`**. Both
   commands are required: `git gc` alone keeps unreachable objects for two weeks by default and a test
   without the reflog expiry proves nothing (verified — the object survived `gc --prune=now` until the
   reflog was expired). Then assert:
   - `git cat-file -t <mergeBaseSha>` and `<headSha>` of the **stored** snapshot both still print `commit`;
   - `git cat-file -e <sha>` exits 0 for **every** SHA in the stored `immutable.blobIndex` (both sides);
   - after clearing the store's `blobs` table — which forces the local git tier to be the only producer,
     because the GitHub tier is structurally absent (M8.3.6) — a re-sync + reconcile classifies every
     comment `clean` or `drifted` and **zero** as `lost` with reason `line-deleted`.
2. **Control, so the test cannot pass vacuously.** The same scenario with `update-ref` swallowed asserts a
   **named** failure — `git cat-file -t <stored headSha>` exits non-zero — and lives permanently beside the
   pinned case (M8.8.8), not as a break-and-revert. This control matters more than usual here: the store's
   `blobs` table is cache-forever with no eviction (`store.ts` has exactly one `DELETE`, in `deleteDraft`),
   so an already-provisioned blob survives any prune and a naive rebase-then-gc test passes with no pinning
   at all.
3. **Rewrite is not under-counted.** For the rebased fixture, `report.newCommits.length` equals the compare's
   full commit count, and a **live** assertion in the same test computes the old author-date subset and
   asserts it is strictly smaller — the regression this ticket exists to prevent, pinned in the gate rather
   than recorded in the Log.
4. **Nothing is discarded.** After a `git branch -D` of the head and a failed sync, the draft
   `toStrictEqual`s its pre-delete value and every thread still lists.
5. **A conflict-terminated reconcile leaves a coherent draft.** After the apply sequence ends in `conflict`,
   the surviving draft's `headSha` and `compareKey` are the ones its comments were re-anchored against, and a
   second `reconcileDraft` reports zero new commits rather than the whole compare.
6. **Ref legality against real git.** `git check-ref-format` exits 0 for every ref name `pinRefsFor`
   produces, and exits 1 for the design document's literal `<id>/<compareKey>` form.
7. **Mock parity, driven against the real mock.** The mock and revud return identical `newCommits` for the
   rewritten fixture, asserted by importing `createMockApi` from `@revu/app/mock` (not by replaying a
   transcription of the mock's body), with `conformance-reconcile.test.ts` staying green unchanged as the
   proof that the head-present branch did not move.
8. **The objects-missing answer is typed, and binary is not missing.** `reconcileDraft` throws
   `not_found` naming a re-sync when an anchor blob is absent, returns a normal report when the blob is
   present and binary, and the API edge answers the same typed error for a dangling `compareKey` while
   `getLocalSnapshot` itself still throws `StoreUnreadableError`.

## Context — verified seams

| anchor | what is there, and why this ticket attaches to it |
| --- | --- |
| `packages/shared/src/lib/anchor.ts:417` | `const newAnchorLines = sha ? resolveBlobLines(sha) : null` — the exact line where an unavailable object becomes "no content". Everything downstream is honest; the dishonesty is that `null` here is indistinguishable from a deleted line. |
| `packages/shared/src/lib/anchor.ts:283-285` | `if (newAnchorLines === null \|\| newAnchorLines.length === 0) return { kind: 'lost', reason: 'line-deleted' }` — the mass-lost mechanism. One missing blob per file is enough to lose every comment on it. |
| `packages/shared/src/lib/anchor.ts:34` | `FilePresence = 'present' \| 'deleted' \| 'renamed' \| 'added'` — there is no `objects-missing` member, which is why M8.8.4 gates *before* classification rather than adding a fifth presence. |
| `packages/shared/src/lib/anchor.ts:94` | `selectAnchorBlobSha(entry, side)` — the single definition of "which side's blob holds this comment's text". M8.8.4's pre-flight must use it, not re-derive it. |
| `packages/revud/src/direct/reconcile.ts:78-82` | `resolveBlobLines: (sha) => { const blob = store.getBlob(sha); return blob && !blob.binary ? … : null }` — the store-backed resolver. `null` for missing and `null` for binary are the same value here. |
| `packages/revud/src/direct/reconcile.ts:92-96` | `draftHeadIndex` + the `commit.author.date > draft.createdAt` fallback — the under-reporting heuristic M8.8.3 replaces, with the comment that says it "matches the mock oracle's semantics exactly". |
| `packages/revud/src/direct/reconcile.test.ts:428-440` | `test('falls back to author-date when the draft head fell out of the rewritten compare')` — the test that pins today's behaviour and must be rewritten by M8.8.3, not deleted. |
| `packages/app/src/api/mock/adapter.ts:639-643` | The mock's line-for-line identical fallback. D6 makes this the spec: it changes first, or the transports diverge and the conformance suite catches it late. |
| `packages/revud/src/direct/store.ts:417-420` | `hasBlob(sha)` — a presence probe that does no JSON parse and cannot throw. Exactly the primitive M8.8.4's pre-flight needs. |
| `packages/revud/src/direct/store.ts:422-428` | `getBlob` returns `null` for an absent SHA. There is no way to tell "never provisioned" from "provisioned then lost" at this layer. |
| `packages/revud/src/direct/store.ts:341-352` | `getSnapshot` throws `StoreUnreadableError` when the envelope references a `compareKey` with no immutable row — today's hard error for a dangling reference (edge case 15). M8.8.4 maps it, and must not soften the store itself. |
| `packages/revud/src/direct/sync.ts:422` | ``const compareKey = `${mergeBaseSha}...${headSha}` `` — the one string form of the key. The pin's ref name is a *derived* encoding of it; it must never become a second definition. |
| `packages/revud/src/direct/sync.ts:472-489` | `provisionBlobs(...)` then `mergePartial(partial, blobs.missing)` — where an unreadable object becomes a `partial` reason. This is the *real* entry point of the §5.1 degradation chain. |
| `packages/revud/src/direct/blobs.ts:125-146` | `readLocalBlob` — `git cat-file -e` / `-s` / `blob`, each non-zero exit returning `null`. With M8.3.6's optional `github`, this is the **only** producer on the local path. |
| `packages/revud/src/direct/blobs.ts:284` | `missing.push(sha)` — the terminal degradation. A SHA that lands here on a local review can never be recovered by any tier once the object is pruned. |
| `packages/revud/src/direct/direct-api.ts:257-269` | `getBlob` → `ApiError('not_found', 'Blob … is not in the local snapshot store — re-sync this pull request to fetch it.')` — the existing precedent for the typed, actionable re-sync answer M8.8.4 mirrors. |
| `packages/revud/src/direct/command-runner.ts:31-33` | `run(args: string[], opts?: { cwd?: string })` — argv array, no shell; a non-zero exit is a *result*, never a throw. Every pin command reads `code`, never stderr text. |
| `packages/revud/src/direct/context.ts:167` | `const cwd = opts.cwd ?? process.cwd()` — the repo root is never discovered today. Pin writes must run at M8.3.1's `discoverRepoRoot`, not here. |
| `packages/app/src/state/queries.ts:147-161` | `useStaleness` — `baseMoved: stale && item.pull.head.sha === snapshot.immutable.headSha` (`:156`) and `newCommits` as a count *difference*, which a rebase leaves at zero. |
| `packages/app/src/pages/pr-layout.tsx:187-204` | The "base advanced — diff changed" banner and its three-dot-compare tooltip — correct as-is for a local review under D8, which is the strongest reuse in the milestone. |
| `packages/app/src/pages/pr-layout.tsx:205-215` | The third staleness branch, "head moved since sync" — where a rewritten local branch already lands, and the reason M8.8.6 changes the dialog rather than the banner. |
| `packages/app/src/components/review/reconcile-dialog.tsx:284-287` | `{shortSha(draftHeadSha)} → {shortSha(currentHeadSha)} · {newCommits.length} new commit(s)` — the count M8.8.3 changes the meaning of, and therefore the copy M8.8.6 must fix. |
| `packages/app/src/components/review/reconcile-dialog.tsx:386-391` | `CommitList`'s empty state: "The compare changed without new head commits — the base branch moved." Already the right sentence for a D8 base advance. |
| `packages/shared/src/api/types.ts:526-532` | `ReconcileReport` — `prNumber`, `draftHeadSha`, `currentHeadSha`, `newCommits`, `results`. No field for a rewrite; it is part of the frozen contract, which is why M8.8.6 derives instead of adding one. |
| `packages/shared/src/api/types.ts:538-554` | `ApiErrorCode` — `network \| rate_limited \| not_found \| forbidden \| conflict \| broker_unreachable \| persist_failed`. There is no `objects_missing`; M8.8.4 uses `not_found` with a precise message. |
| `packages/app/src/state/drafts.ts:203-219` | `ensureDraft` — the **only** place `headSha` and `compareKey` are ever written onto a draft (`:210`), at creation. Nothing updates them afterward, which is the whole of edge case 18. |
| `packages/app/src/state/drafts.ts:153-165` | `DraftActions` — `upsertComment` / `removeComment` / `setBody` / `setEvent` / `discard` / `flush`. There is no mutator for the draft's head; M8.8.7 adds the narrow one. |
| `packages/app/src/components/review/reconcile-dialog.tsx:162-258` | `apply()` — re-anchors every kept comment against the freshly synced snapshot, then submits with `expectedHeadSha: live.currentHeadSha`. `ok` deletes the draft; `head_moved` re-syncs and re-reconciles; **every other outcome** (including the `catch` at `:249-256`) leaves a draft whose comments are anchored to the new head and whose `headSha` still records the old one. |
| `packages/revud/src/direct/direct-api.ts:275-281` | `saveDraft` rebuilds the draft as `{ ...draft, humanId, updatedAt: now() }` — a client-supplied `headSha`/`compareKey` survives to the store, a client-supplied `humanId` does not. That is why M8.8.7's head update can be an app-side mutator riding the ordinary save path. |

## Constraints that bind this ticket

- **Drafts survive everything — a draft is deleted only on confirmed submit success** (hard constraints).
  Three of this ticket's states are exactly the ones tempted to clean up: a rewritten branch, a deleted head
  ref, and an unreadable object set. None of them may delete a draft, a thread, or a snapshot row. This is
  the single most important invariant here, and edge case 14 states it as the product itself.
- **Reconcile logic is shared, not duplicated** (`anchor.ts` is pure; the server-side report must import the
  same module the UI previews with). M8.8.3's fix therefore lands in the shared path and changes the PR
  behaviour too; a local-only fork of the commit-delta rule would be the exact divergence the constraint
  forbids.
- **D6 — the mock is the spec.** Any semantic change to `reconcileDraft` lands in `adapter.ts` first; the
  conformance suite runs against the mock as oracle.
- **Two-half caching: the key is `compareKey = merge_base…head`, one string form.** The pin ref name is a
  derived encoding with a documented substitution in exactly one function; nothing may parse a compareKey
  back out of a ref name.
- **D7 — no GitHub client in the local path.** `local-pins.ts` takes a `CommandRunner` and a cwd. That it
  *could not* call GitHub is the guarantee; that it *does not* is not.
- **D8 — local reviews use the live base tip.** The merge base is recomputed on every sync, so a base
  advance changes `compareKey` with zero new head commits. This is a deliberate divergence from the PR path
  (which reads GitHub's `pull.base.sha`, refreshed only on `synchronize`) and must be stated in the UI, not
  smoothed over.
- **Frozen contract: `client.ts` + `types.ts` are read-only inputs** (AGENTS.md ownership). `ReconcileReport`,
  `AnchorResult` and `ApiErrorCode` are all in `types.ts`; this ticket must work within them or escalate (see
  Open questions), never edit them unilaterally.
- **Migration doctrine: additive only, no PK altered, no table rebuilt.** This ticket adds **no** schema —
  M8.2 owns v4 and already carries every column the local path needs. The read-only-after-branch-deletion
  state is derived, not stored.
- **Argv discipline (M8.3.1).** Every git invocation goes through `runGit` with `--end-of-options`; a local
  id and two SHAs are validated as integer / hex before they reach an argv, because nothing in the runner
  validates command arguments.
- **AGENTS.md code style.** No ticket id, phase, wave, or agent reference in any comment or docstring — the
  rewrite-detection comment blocks in `reconcile.ts` and `adapter.ts` are rewritten, so this is a live risk.

## Landmines

- **The design document's ref path cannot be created.** `refs/revu/reviews/<id>/<compareKey>` contains
  `...`; `git check-ref-format` rejects any name containing `..` (verified, exit 1). If pin failures are
  non-fatal (which M8.8.2 specifies), an implementer who copies the path verbatim gets a review that
  silently never pins and a Verify step that quietly fails only under the control case.
- **Directory/file conflict on the ref namespace.** Once `refs/revu/reviews/<id>/<key>/head` exists, git
  refuses to create a ref at `refs/revu/reviews/<id>` (verified error text in M8.8.1). Any scheme that pins
  a single commit directly at the id level and later wants two refs there is unwritable, and the failure
  arrives on a user's machine, not in CI.
- **`git gc` alone proves nothing.** Unreachable objects have a two-week default grace; the object in a
  hand-run experiment survived `git gc --prune=now` until
  `git reflog expire --expire=now --expire-unreachable=now --all` ran first. A prune-survival test without
  the reflog expiry is a test that always passes.
- **The blob store makes the naive test pass without any pinning.** `blobs` is content-addressed,
  cache-forever, and the only `DELETE` in the whole store is `deleteDraft`. A rebase that changes nothing in
  a file produces identical blob SHAs, which are already in SQLite — so "rebase, gc, reconcile, no losses"
  is true with the feature entirely absent. The control case in Verify step 2 is what gives the suite teeth;
  skip it and this ticket ships as a no-op.
- **Pinning after the diff leaves the window open.** The SHAs come from `git diff --raw`; the bytes come
  from `git cat-file` some milliseconds later. A prune in between converts the whole compare into `missing`
  blobs, and with tier 3 structurally absent there is no recovery. Order is the mitigation.
- **Changing the commit-delta rule changes the PR path.** `reconcile.test.ts:428` fails, the mock must move
  in lockstep, and the reconcile dialog's "N new commits" becomes a claim about a different set. Landing
  M8.8.3 without M8.8.6 leaves the UI saying "12 new commits" for a 12-commit rebase where nothing new
  happened.
- **`lost` / `line-deleted` is a lie the human acts on.** The dialog's only offers for a lost row are
  re-anchor and drop (`reconcile-dialog.tsx:552-557`), and re-anchor is unavailable when the blob will not
  load — so a missing object funnels a human straight to "drop" for a comment whose line never moved. Once
  dropped, the text is gone.
- **Pinned refs are real refs.** They are not fetched or pushed by any default refspec, but
  `git push --mirror` or an explicit `refs/*:refs/*` would carry them to a remote. Keeping the namespace
  under `refs/revu/` (never `refs/heads/`) is what keeps them out of every ordinary push; do not "helpfully"
  pin under a branch namespace.
- **Unbounded pin growth.** A branch rebased daily mints a fresh compareKey per sync, and every pin holds
  its objects alive forever — the retention debt of edge case 23, now with a second axis. Nothing in this
  ticket deletes a pin (deletion is M8.10.2's), so whatever bound M8.8.2 accumulates is the bound until M8.10
  lands: state it (see Open questions) rather than leaving an unbounded ref set undocumented.
- **Worktrees and bare repos.** Refs live in the common git dir, so a pin written from a linked worktree is
  visible to the main one — fine — but `git rev-parse --show-toplevel` fails in a bare repo, and pinning
  must degrade rather than throw there. `context.ts:167` still hands out a bare `process.cwd()`, so an
  implementer who reaches for the context's cwd instead of M8.3.1's discovered root will write refs into
  whichever repo the daemon happened to start in.
- **A binary blob and a missing blob are the same `null`** at `reconcile.ts:80`. M8.8.4's pre-flight must
  probe `hasBlob`, not "did `resolveBlobLines` return null" — otherwise every comment on a binary file
  triggers the objects-missing state.

## Testing exceptions

Two legs genuinely cannot carry an automated assertion in this repo. Named here rather than papered over:

- **The rendered dialog header (M8.8.6).** `packages/app` has no RTL and no jsdom, and `reconcile-dialog.tsx`
  is a hook-bound component — `renderToStaticMarkup` cannot construct it without a query client, a toast
  provider and a live report. *Compensating:* the copy itself is a pure module with exhaustive assertions
  (including the singular branch), and a source-read guard asserts the dialog imports it and retains no
  duplicate literal. The visual leg is a `?mock=1` walk recorded in the Log and M8.11's e2e.
- **`apply()`'s execution order (M8.8.7).** Same reason: `apply()` is a closure over hooks. The *decision*
  is extracted into `planReconcileApply` and fully tested, and the *outcome* — a coherent surviving draft
  and a second reconcile reporting zero new commits — is asserted at the API layer against the real mock.
  What remains untested is that the dialog calls `setHead` before `submit.mutateAsync`, corroborated only by
  a source-order assertion. That residue is deliberate and is the reason the extraction exists: everything
  load-bearing sits on the tested side of it.

Neither exception covers an assertion of absence, and neither is a substitute for a Check elsewhere in this
ticket.

## Open questions

1. **Where does "the branch was rewritten" live on the wire?** This ticket derives it client-side
   (`draftHeadSha` absent from `snapshot.immutable.commits`), which needs no contract change and no mock
   validator. The alternative is an additive optional `rewritten?: boolean` on `ReconcileReport`
   (`types.ts:526`), which is more honest about provenance but edits the frozen contract and therefore
   belongs to whoever owns `types.ts` (M8.1). Owner call; the derivation is reversible, the field is not.
2. **Should the author-date fallback change for GitHub PRs too?** This ticket says yes (shared reconcile,
   identical defect on a force-push) and rewrites `reconcile.test.ts:428` accordingly. The counter-argument
   is that it changes behaviour on the PR path for a milestone scoped to local reviews. Needs sign-off before
   M8.8.3 lands, because the test rewrite is the point of no return.
3. **Where does a failed pin surface?** It is deliberately not `Snapshot['partial']` (that means missing
   content). Options: a field on the local review row (schema — M8.2 territory), a daemon log line only, or a
   quiet inbox badge. Until answered, M8.8.2 records it on the sync result and nothing renders it.
4. ~~**What bounds the pin set before M8.10 lands?**~~ **Answered by the owner, 2026-08-19: keep every
   pin.** Pin every compareKey any stored snapshot references — correct, and unbounded only until retention
   ships. The alternative (pin the current compare, drop the previous on each sync) is bounded but lets a
   rebase strand an older draft's objects, which is the exact failure the pinning exists to prevent; it would
   also put a ref *delete* in this module, which the M8.8.1 boundary rules out. Two consequences bind this
   ticket: **M8.8.1 still exports no delete** — eviction is wholly M8.10.2's — and the ruling was given on the
   condition that **M8.10 lands in the same session as M8.8**, so the growth is bounded by the garbage
   collector arriving right behind it rather than by policy here.
5. **Should the objects-missing answer be a throw or a report?** A typed `not_found` means the human sees an
   error where they expected their comments; a fourth classification would render inline but has nowhere to
   live in the frozen `AnchorResult` union (`anchor.ts:34` / the `lost` reasons). The throw is chosen here as
   the smaller, reversible move.
6. **Edge case 15 across a genuine re-clone.** Pins are per-repository, so a fresh clone has neither the
   objects nor the refs, and a re-sync rebuilds only what the new clone can reach. Is "re-sync to rebuild"
   sufficient, or does a snapshot whose content is provably unrecoverable need a durable marker so the UI
   stops offering a re-sync that cannot help?
7. **Surface label vs. reality.** The header says `revu:revud`, but M8.8.3 must edit
   `packages/app/src/api/mock/adapter.ts` (D6 parity), and M8.8.6 and M8.8.7 are entirely app-side (the latter
   touching `state/drafts.ts` and `reconcile-dialog.tsx`, neither of which any other M8 ticket owns). Either
   the label becomes `revu:revud, revu:app` or those three units belong in a separate ticket; worth settling
   before assignment so file ownership is unambiguous.

## Log

_Append `**YYYY-MM-DD** — <what changed, files, gate result>` entries as work lands. Newest last._

**2026-08-19** — **M8.8.1 landed.** `local-pins.ts` + `local-pins.test.ts` (new), and three files the unit's
`Files` list did not anticipate: `command-runner.ts`, `local-git.ts`, `local-git.test.ts`. Gate green at
**2669 pass · 1 skip · 0 fail · 96 files** under `TZ=UTC` (baseline 2646 · 1 · 0 · 95).

**Six git facts were verified against real git 2.50.1 before a line was written**, rather than taken from the
ticket: the literal `<id>/<compareKey>` ref form is rejected (exit 1) and the substituted form accepted (exit
0); `update-ref --stdin` applies its batch atomically (one bad entry ⇒ **zero** refs written); two separate
`update-ref` calls leave a **half-write** (one ref survives the other's failure); a ref at
`refs/revu/reviews/<id>` cannot be created while children exist; and `for-each-ref` accepts
`--end-of-options`. The atomicity pair is now a permanent test — without the two-invocation leg, the batch
reads as style and a later editor may reasonably unroll it.

**The red was made meaningful rather than absent-symbol.** The module was first written with
`pinKeyForCompare` as the identity function, and the legality assertion observed red with **real git
rejecting the name** — not with "module not found". Its companion (the literal form exits 1) passed
throughout, which is what stops a substitution that does nothing from being undetectable. Restoring the
substitution turned 7 red to 0.

**An undocumented prerequisite: `CommandRunner` had no stdin.** The unit requires one
`git update-ref --stdin` invocation, and the seam could not express it — so `CommandOptions.stdin` was added
(additive and optional, so no existing fake breaks) and threaded through `GitCommand`/`runGit`. This is a
security improvement, not merely plumbing: **no object name reaches an argv slot at all**, so the values pass
git's option parser nowhere. That property is asserted, not assumed.

**Two deviations from the unit text, both deliberate.** (1) Validation is `isLocalReviewId`, not "positive
integer" — a pull request number is a positive integer, and pinning retention state under one would attach
local bookkeeping to forge-keyed data; the test names that reason. (2) `listPins` emits `--end-of-options`
before the prefix, because `isHardenedArgv` rejects a bare `refs/`-shaped operand and would have blocked the
spawn. **M8.10.2's Check specifies the argv without it and will be blocked by the same predicate** — that
ticket must route through `runGit` or its leg (a) cannot pass.

**M8.3.1's `process.cwd()` coverage guard fired**, as designed, on the new module: the scan is
`for (const m of CWD_SCANNED_MODULES)`, so enrolling `local-pins.ts` both greened the guard and added the
per-module scan — which is why the suite grew by 23 tests for a 22-test file.

**Open question 4 is answered** (owner, this session): keep every pin, conditional on M8.10 landing in the
same session. This module therefore exports **no delete**, as the unit requires; eviction is wholly
M8.10.2's.

**2026-08-19** — **M8.8.2 landed.** `local-surface.ts` + `local-surface.test.ts`, plus three
`LocalReviewSurface` doubles the compiler required to stay complete (`direct-router.test.ts` ×3,
`direct-api.test.ts` ×1). Gate green at **2675 pass · 1 skip · 0 fail · 96 files** under `TZ=UTC`.

**The ordering assertion was observed red for the predicted reason** — `firstArgvWith(sink, 'update-ref')`
returned `-1`, so the `>= 0` guard fired rather than two absent commands comparing equal. Five of the six new
assertions were red before the wiring; all six are green after.

**The seam is not where the unit text says.** The unit names `local-sync.ts`, but `resolveLocalRange` →
`readLocalSnapshotImmutable` → `provisionBlobs` are orchestrated in `local-surface.ts`'s `syncPull`
(`:594`), and the pin has to sit between the first two. `local-sync.ts` holds the readers, not the sequence,
so the ordering assertion has nothing to observe there.

**The pin outcome could not ride the sync result as the unit assumed.** `syncPull` returns `Snapshot` — a
frozen wire type shared with the hosted path — so a `pin` field on it would be a contract change. Instead
`syncLocalReview(localId): Promise<LocalSyncOutcome>` was added to the surface with the whole body, and
`syncPull` became a one-line delegate returning `.snapshot`. That keeps the contract untouched, gives
**Open question 3** a place to read from when it is answered, and is asserted: one test checks the object
`syncPull` returns carries no `pin` key.

**The repo-scoping sweep caught the new verb**, which is the guard working: `syncLocalReview` is id-keyed, so
it was added to `FOREIGN_CALLS` rather than to the exemption list, and the sweep now drives it against a
foreign id and asserts the refusal. Trusting it to inherit `syncPull`'s check would have been wrong in the
one direction that matters — the delegation runs the other way.

**`partial` is held apart from the pin by comparison, not by assertion of a rule:** a pinned run and a
pin-blind run over the same fixture are asserted to have identical `partial` and identical `immutable`, and
to differ on `pin.ok`. A future edit folding the pin outcome into `partial` turns that pair red.

**2026-08-19** — **M8.8.3 landed.** `adapter.ts` (mock, first), `reconcile.ts`, `reconcile.test.ts`,
`local.test.ts`. Gate green at **2676 pass · 1 skip · 0 fail · 96 files** under `TZ=UTC`.

**D6 was honoured literally: the mock moved first**, and its assertion was observed red against the
unmodified `adapter.ts` before either implementation changed.

**The red was stronger than the ticket predicted.** The unit describes the fallback as *under-reporting*; on
a faithful rebase fixture it reports **zero**. A rebase rewrites committer dates and preserves author dates,
so every rewritten commit keeps a date older than the draft and `commits.filter(author.date > createdAt)`
returns the empty list — "no new commits" on precisely the rewrite that moved the most work. Both new tests
pin that: the revud one asserts the old heuristic has length **0** on its fixture *and* is strictly less than
the honest answer, so the guard names the magnitude rather than only the direction.

`reconcile.test.ts:428` was **rewritten, not deleted**, and now also carries the shape-level property
(`newCommits.length === commits.length` whenever the draft head is absent) so a fixture edit cannot satisfy
it alone.

**The GitHub PR path changed too, deliberately** — `reconcile.ts` is shared, not duplicated, and a force-push
on a hosted pull request has the identical defect. `conformance-reconcile.test.ts` and the rest of the
conformance matrix are green unamended.

**Note for M8.8's parity leg:** `oracleResults` (`reconcile.test.ts:289`) replays only the mock's
*classification*, not its `newCommits` computation, so the two transports' agreement on this field is
currently asserted by two independent tests rather than by one oracle. Worth closing when the parity unit
lands.

**2026-08-19** — **M8.8.4 landed, both halves.** `reconcile.ts`, `reconcile.test.ts`, `local-surface.ts`,
`local-surface.test.ts`, `store.ts`, `store.test.ts`, and `local-objects-missing.test.ts` (new). Gate green at
**2684 pass · 1 skip · 0 fail · 97 files** under `TZ=UTC`.

**The pre-flight**, written first and observed red on three of its four cases. The fourth — a **binary** anchor
blob is present, not missing — passed before and after by construction, which is exactly its job: it is the
control that makes a pre-flight written against `resolveBlobLines` (null for a binary blob) instead of
`hasBlob` red. Side selection goes through the shared `selectAnchorBlobSha`, never re-implemented.

**An undeclared prerequisite: `ReconcileStore` had no `hasBlob`.** The shim `local-surface.ts` passes is
narrower than `DirectStore`, so the interface gained the method and the shim was wired to the **same**
content-addressed table `getBlob` reads. A second map there would let the pre-flight and the classifier
disagree, which is the divergence the pre-flight exists to prevent.

**The edge mapping covers `reconcileDraft` as well as `getSnapshot`.** Both read the snapshot, so both meet
the absence; leaving reconcile untranslated would have made it the one path still reporting a moved data
directory as a corrupt daemon.

**⚠️ The most valuable finding of the unit, and it came from an assertion the ticket did not ask for.** The
Check specifies the edge answer and the store's continued throw. Adding a fourth test — *does the advice
actually work?* — showed that **it did not**: `syncLocalReview` read `store.getLocalSnapshot` to carry the
authorship map forward, and that read is precisely the one that throws in this state. The daemon was telling
every reader to re-sync while re-syncing threw. **An actionable-sounding message that cannot be acted on is
worse than a 500**, and nothing else in the Check would have caught it.

The repair does not lose anything. `commentAuthors` lives in the **envelope**, which parses fine; only the
immutable half is gone. So the store gained `getLocalCommentAuthors(localId)` — a narrow read that cannot be
blocked by a missing immutable row — rather than catching and degrading to `{}`. Degrading would have made the
documented repair destroy the one field it cannot rebuild: nothing is stamped into a comment body and no forge
login stands behind a synthesized author, so a dropped authorship entry is unrecoverable.

**Two coverage guards fired and were satisfied properly, not silenced.** `store.test.ts`'s
`Record<LocalStoreMethod, true>` required the new method to be classified **and** actually driven by the
sweep, and the independent literal moved 17 → 18 as a deliberate act.

**A flake was diagnosed and then fixed rather than waved through.** One gate run went red on
`a successful pin is reported on the sync outcome` at **5008ms** — the hook-timeout shape. The test COUNT was
intact (2685, exactly the expected total), and the file was 66/66 green on a quiet tree. But the honest read
is that M8.8.2's six new tests each drive a **real sync against the fixture repo** and are legitimately slow,
so they were given explicit `30_000` timeouts — the idiom already used twice in that file — rather than
recorded as noise.

**Scope note — the mock was deliberately not changed.** Unlike M8.8.3, this unit's Files list omits
`adapter.ts`, and the divergence is unreachable rather than merely untested: the mock seeds its own blobs
alongside every snapshot, so it has no state in which an anchor blob is absent. Worth confirming when M8.11's
conformance leg is designed.

**2026-08-19** — **M8.8.5 landed.** `local-resync.test.ts` (new) — and **no production change was needed**,
which is the unit's real finding. Gate green at **2696 pass · 1 skip · 0 fail · 98 files** under `TZ=UTC`.

The behaviour the unit specifies was already correct: M8.3.2's typed `ref_not_found` propagates through
`rangeRefusal`, the sync throws before writing anything, and the reads consult git nowhere — so a review whose
head branch has been deleted still opens, still lists its threads, and still holds its draft. What the unit
was missing was **proof**, and the proof is now twelve assertions across a deleted head branch, a renamed base
branch, and the tripwire.

**The tripwire is a runtime decorator, and its scope had to be corrected.** Armed across the whole seeding it
went red on `submitReview` deleting the draft it had just published — the one deletion the product allows, and
not a sync failure at all. It is therefore armed **only for the failing sync**, over the same data through a
second surface, which is the rule as written rather than a broader one that happens to be false.

**Both directions were observed, not assumed.** A `deleteLocalDraft` was injected into the failure path and
the run went red **three independent ways**: the tripwire named the method, and the deleted-branch and
renamed-branch survival walks each caught the missing draft on their own. Reverted, and the file is 12/12
green. The permanent negative control stays in the file — a sibling calls a deletion through the same
decorator and asserts it throws — so a decorator wired to nothing is red rather than green.

The `readFileSync` scan is kept as corroboration and says so in its own name, with the scanner proving its
pattern fires on the construct it bans. It is not the Check.

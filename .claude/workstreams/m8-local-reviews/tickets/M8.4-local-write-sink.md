# M8.4 — Local write sink

| field | value |
| --- | --- |
| **State** | In Progress |
| **Surface** | revu:revud |
| **Kind** | Feature |
| **Depends** | M8.1 |
| **Blocks** | M8.5, M8.9 |
| **Branch** | `m8.4/local-write-sink` |
| **PR** | — |
| **Assignee** | orchestrator (lane C) |

## Goal

Deliver local implementations of the four write verbs — `submitReview`, `replyToThread`, `resolveThread`,
`addReaction` — in a module that has **no GitHub client in scope**. This is the second of the two new pieces
the whole milestone reduces to (`LOCAL_REVIEWS.md §2`): one new snapshot producer (M8.3) and one new write
sink (here). It implements **D7** (`§3`) as its spine: the absence of a GitHub client is structural, not
conditional, and is asserted by a test that fails if one is introduced. It also implements the write
semantics of `§6` — every verb returns a **complete, well-formed** value because the app's optimistic
mutations copy fields back out of the response, so a stub returning the old value makes an optimistic update
silently revert (which reads as a bug rather than an error); `head_moved` stays a 200-level value and matters
*more* locally than for a PR; and `prefixBody` stamping is skipped entirely, with the real `Human.id` carried
on `commentAuthors` instead. Verdicts stay meaningful with `canApprove` true (`§6`), so the approve gate that
exists for GitHub's self-review rule has no local counterpart.

This ticket does **not** wire dispatch — `isLocalReviewId(n)` routing lives in M8.5. It delivers the module
plus its port, tested against fakes.

## Units

Default execution order below, and it is a **test-first** order: where a unit's test must exist before the
code it constrains, the unit's **Do** says so and names the red it expects. **M8.4.1 must land first** — every
other unit imports its port type, and its test file is written *before* the module so the first run is red on
a missing import. **M8.4.6 should land second**, as a failing-then-passing guard rail, so the units that
follow are written under it rather than audited after. **M8.4.8 lands third, ahead of its number** — it is the
in-memory harness M8.4.3/.4/.5/.9 are all written against, and landing it last would mean writing three units
against three throwaway fakes. **M8.4.3, M8.4.4 and M8.4.5 are mutually independent** once .1, .2 and .8 are
in and can run in parallel on separate commits. **M8.4.7 and M8.4.9 depend on .3/.4/.5** (both consume their
return values and persisted state as fixtures).

### M8.4.1 — The module and its port, with no GitHub client in scope
**Do:** Create `local-writes.ts` exporting a `LocalWriteDeps` interface and four stub-free verb functions.
The port carries exactly: the narrow local-store slice (`getLocalSnapshot`, `putLocalSnapshot`,
`putLocalThread`, `putLocalReviewSummary`, `getLocalDraft`, `deleteLocalDraft`), `session: Session`,
`resolveHead: () => Promise<{ sha: string; commitCount: number }>`, an injected
`nextEntityId: () => number` (the durable allocator M8.2.2 owns — see M8.4.2), and `now?: () => string`.
It deliberately carries **no `github`**, **no `WriteDecorator`**, and **no `CommandRunner`**.

Two omissions are decisions, not oversights, and belong in the module docstring:
- **No `WriteDecorator`.** Its two methods are `decorateBody` (stamping — dropped by `§6` "No stamping") and
  `recordWrite` (the audit journal — dropped by `§8` edge case 25, local writes are not journaled). Both are
  inert locally, so the seam is omitted rather than injected as a double no-op.
- **No `CommandRunner`.** Head resolution arrives as an injected `resolveHead` function so the module spawns
  no subprocess at all, and so the ref-name hardening M8.3 owns (`§8` edge case 7 — a ref beginning with `-`
  is read by git as a flag) is not duplicated or half-reimplemented here.

Define the store slice as its own interface in this file, not as `Pick<DirectStore, …>`, so M8.4 is
completable against M8.1 alone.

> ⚠️ **CORRECTION, measured after M8.4.1 landed: the v4 tables do NOT satisfy this slice structurally.** This
> paragraph used to claim they would. The port names the write `putLocalReviewSummary`; M8.2.5's store method is
> `putLocalSubmittedReview`. **M8.5 therefore wires with an explicit one-line mapping, never a spread of the
> store into the deps.** The port keeps its own name deliberately — it is named for the domain concept, not the
> table, which is the whole point of a decoupled port, and M8.4.1's Check pins it. A spread that misses the
> member is a compile error, so the mapping cannot be silently forgotten. Related and already consistent:
> M8.4.3's Check (g) reads `listLocalSubmittedReviews`, which is **not** a port member and is not meant to be —
> it is a read-back on M8.4.8's fake, exactly as that unit specifies.

**Test first.** Create `local-writes.test.ts` *before* `local-writes.ts`, holding `makeLocalDeps()` — the one
typed `LocalWriteDeps` factory every test in this ticket constructs deps through — and run it: red on the
missing module. Then write the module and take it green.

**Files:** `packages/revud/src/direct/local-writes.ts` (new),
`packages/revud/src/direct/local-writes.test.ts` (new).
**Check:** `local-writes.test.ts` asserts `Object.keys(makeLocalDeps()).sort()` equals exactly
`['deleteLocalDraft', 'getLocalDraft', 'getLocalSnapshot', 'nextEntityId', 'now', 'putLocalReviewSummary',
'putLocalSnapshot', 'putLocalThread', 'resolveHead', 'session']` — the factory sets `now` explicitly (also
what makes timestamps deterministic), so the key set is exact. Because the factory is typed `LocalWriteDeps`
and is the only constructor of one in the package, a member added to the port either fails compilation in the
factory or fails this assertion — the port's shape is pinned on **every** gate, not on the day it was written.
**Negative control** (the Check asserts an absence): add `github: GithubClient` to `LocalWriteDeps` and
`github: throwingGithubClient()` to the factory, observe the key-set assertion red, revert and observe green;
record both runs in the Log. `bunx tsc -b` clean and a `grep` over the module corroborate the docstring
wording; neither is the Check.

### M8.4.2 — Local id minting: entity ids and thread ids
**Do:** Mint comment and review-summary ids from `LOCAL_ENTITY_ID_BASE` (M8.1), strictly positive and
monotonic across daemon restarts. **The high-water mark's home is settled**: M8.2.1 seeds a `meta` row and
M8.2.2 exposes `nextLocalEntityId()` as a read-and-bump inside one transaction. A `MAX(id)` scan was rejected
because comment ids live inside the stored `ReviewThread` documents rather than in a column, so the scan would
mean parsing every thread row on every mint. This module therefore holds **no persistence of its own**: it
takes the allocator as the injected `nextEntityId` on M8.4.1's port, so M8.4 stays completable against M8.1
alone (the unit tests pass a counting fake) and M8.5 wires the durable one.

Mint thread ids in the shape **M8.1.3's mock already minted** (M8.1's open question 8 fixes it, because D6
makes the mock the spec and M8.1 merges first) — unique per local review and containing no `/`, since they
travel as a single URL path segment.

Write `local-ids.test.ts` first — the band and statelessness cases are red on the missing module — then mint.

**Files:** `packages/revud/src/direct/local-ids.ts` (new), `packages/revud/src/direct/local-ids.test.ts` (new).
**Check:** `local-ids.test.ts` asserts, all durable:
- **(a) Band.** Over 1000 successive mints from a counting allocator fake: every id is `> 0`,
  `>= LOCAL_ENTITY_ID_BASE`, `Number.isSafeInteger`, strictly increasing, outside the mock's `700_000_000`
  band, and above GitHub's ~1e9–3e9 range.
- **(b) Statelessness, asserted behaviorally rather than by `grep`.** Mint three ids through allocator fake A,
  then three through a **fresh** fake B, and assert B's sequence restarts at B's own base. A module-level
  counter, a cached high-water mark, or any persistence of its own makes B continue A's sequence and this test
  red — which is what a `grep` for a store import can only guess at. **Negative control:** add a module-level
  `let seq` to `local-ids.ts` and have the minter prefer it, observe this case red, revert. Durability across
  a **daemon restart** is asserted against the real allocator in M8.2.2's suite, not simulated here; the
  import-graph half (`local-ids.ts` imports nothing but `@revu/shared`) is asserted by M8.4.6's scan.
- **(c) Thread id survives the wire.** A minted thread id contains no `/`, satisfies
  `id === decodeURIComponent(encodeURIComponent(id))`, and comes back unchanged from
  `fillPath(ROUTES.replyToThread.path, { n, threadId })` split on `/` and `decodeURIComponent`-ed — the exact
  two lines `direct-router.ts:139` performs, reproduced in the test because `matchRoute` is module-private.
  A golden thread id copied from M8.1.3's mock test is asserted to satisfy the same shape predicate; the
  mock-vs-sink equality itself is M8.11's conformance leg (see **Testing exceptions**).

### M8.4.3 — `submitLocalReview`: five phases become three
**Do:** Implement submit. The GitHub path's five phases (`writes.ts:308-447`) reduce to three:
1. **Head guard** — `resolveHead()` vs `input.expectedHeadSha`. On mismatch return
   `{ status: 'head_moved', currentHeadSha, newCommits }` as a **value**, post nothing, touch no draft.
2. ~~Approve gating~~ — dropped: `canApprove` is true locally (`§6`), so `forbidden` is unreachable. The
   `SubmitResult` variant stays (frozen contract) but nothing returns it.
3. ~~Idempotency re-check~~ and ~~the 422 path~~ — dropped as having no local analogue (`§6`): a lost network
   response and server-side re-validation are the only reasons they exist.
4. **Materialize** — one local `ReviewThread` per `PendingComment` (locally minted thread id, positive comment
   ids), plus one local `ReviewSummary` using `EVENT_STATE` (`writes.ts:57-61`) verbatim. Comment `user` is the
   sentinel `GhUser` from `§4.1` (git-config **name**, `type: 'Bot'`, empty `avatar_url`/`html_url`, **never**
   the email). Bodies are stored verbatim — no `prefixBody`. Append the new threads to the local snapshot's
   mutable half and record each new comment id in `commentAuthors` keyed to `session.human.id`. The
   `ReviewSummary` goes to `putLocalReviewSummary` **only**: per M8.1's decision D-b it is never appended to
   `snapshot.mutable.reviews`, which stays `[]` on a local review forever so §4.1's and §7's "always empty"
   holds after the first submit. `mutable.issueComments` is likewise never written.
5. **Delete the draft last, and only on confirmed success** — after the snapshot/thread/summary writes have
   returned. `§8` edge case 19: locally this is trivially satisfiable and must still be coded that way.

**Files:** `packages/revud/src/direct/local-writes.ts`, `packages/revud/src/direct/local-writes.test.ts`.
**Check:** Tests, against the M8.4.8 harness: (a) a submit of 2 pending comments produces 2 threads whose
comments each have a distinct positive id `>= LOCAL_ENTITY_ID_BASE`, and the snapshot read *after* submit
already contains them; (b) `expectedHeadSha` mismatch returns `status: 'head_moved'` and `getLocalDraft`
still returns the draft; (c) a store fake that throws on the thread write leaves the draft present (assert
`getLocalDraft !== null` after the rejection); (d) no comment body and no `ReviewSummary.body` matches
`/^\*\*.+\*\*/`; (e) `commentAuthors[id] === session.human.id` for every created comment; (f) the sentinel
`GhUser` JSON contains no `@`; (g) D-b — after a successful submit the persisted snapshot's
`mutable.reviews` and `mutable.issueComments` are both still `[]`, while `listLocalSubmittedReviews` carries
the minted summary.

### M8.4.4 — `replyToLocalThread` and `resolveLocalThread`
**Do:** Reply appends a locally-id'd `ReviewComment` to the stored thread and returns it, mirroring the mock
oracle's field derivation (`adapter.ts:379-401`): `in_reply_to_id` = the thread's first comment id,
`pull_request_review_id` carried from that first comment, `path`/`line`/`side`/`subject_type` from the thread,
`reactions` zeroed (the shape at `threads.ts:33-46`). An absent thread, or one with no comments, is a typed
`not_found`. Resolve flips `isResolved`, sets `resolvedBy` to the sentinel login (or `null` on unresolve),
persists, and returns **the whole stored thread** with every other field unchanged — `isOutdated` in
particular must be carried, not recomputed or defaulted.

**Files:** `packages/revud/src/direct/local-writes.ts`, `packages/revud/src/direct/local-writes.test.ts`.
**Check:** A test resolves a thread whose stored `isOutdated` is `true` and asserts the returned thread still
reports `isOutdated: true` and `isResolved: true`; a second asserts `resolveThread(..., false)` returns
`resolvedBy: null`; a third asserts a reply's returned `id` is positive and appears in the thread on the next
`getLocalSnapshot`; a fourth asserts an unknown `threadId` throws `ApiError` with code `not_found`.

### M8.4.5 — `addLocalReaction`
**Do:** Locate the comment by id across the local snapshot's threads, bump the rollup, persist, and return the
**new** rollup. `mutable.issueComments` is always `[]` locally (`§4.1`), so the review-vs-issue endpoint
classification at `writes.ts:560-566` collapses to a single lookup — an id found nowhere is a typed
`not_found`. Keep the mock's dedup semantic (`adapter.ts:465-472`): a repeat of the same reaction returns the
rollup unchanged. That is honest here (there is exactly one local author) and is **not** the stub `§6` warns
against — the app already reconciles an unchanged rollup silently (`state/threads.ts:330-336`).

**Files:** `packages/revud/src/direct/local-writes.ts`, `packages/revud/src/direct/local-writes.test.ts`.
**Check:** A test asserts a first reaction returns `total_count` incremented by 1 and the persisted snapshot
agrees; a repeat returns the identical rollup; an unknown comment id throws `not_found`.

### M8.4.6 — The D7 structural test
**Do:** A dedicated test file asserting the local write path cannot reach GitHub. Land it **red, before** the
units it constrains (`.2`, `.3`, `.4`, `.5`, `.8`, `.9`) — a guard written afterwards encodes what the code
does rather than what it must never do. Four assertions:
1. **Import-specifier scan** — parse only the `import … from '<spec>'` specifiers out of the local module set
   and assert none resolves to `./github-client`, `./write-decorator`, `./command-runner`, or any module whose
   basename contains `github`. Scanning specifiers rather than whole-file text is what keeps the docstring
   explaining the absence from tripping the scan — the same technique `audit-integrity.test.ts:116-125` uses
   to keep a comment naming a banned SQL construct from failing the ban.
2. **Transitive closure** — walk the relative-import graph from `local-writes.ts` and assert
   `github-client.ts` is not reachable at any depth, so a helper cannot smuggle a client in.
3. **Egress vocabulary** — assert the module body contains no `fetch(`, no `https://`, no `api.github.com`,
   and no `Bun.spawn` (the port takes `resolveHead`, so the module spawns nothing either).
4. **The scanner cannot go quietly vacuous.** Two self-assertions: the scanned file set is exactly
   `['local-ids.ts', 'local-write-fakes.ts', 'local-writes.test.ts', 'local-writes.ts']` and every one of them
   read non-empty (a renamed or mistyped path would otherwise make the whole file pass by scanning nothing);
   and the specifier extractor, run over an **inline fixture source** that imports `./github-client`, reports
   that specifier — so a regex that stops matching fails here instead of silently permitting everything.
   The test-support files are in the set deliberately: the local tests construct no GitHub client, and that
   absence is the point (`Verify` 3).

**Files:** `packages/revud/src/direct/local-write-isolation.test.ts` (new).
**Check:** `bun test packages/revud/src/direct/local-write-isolation.test.ts` green, with the four assertions
above running on every gate — assertion 4 is what makes the other three durable rather than merely present.
**Negative control, recorded:** add `import type { GithubClient } from './github-client'` to
`local-writes.ts`, run the file, observe a **red** run naming the banned specifier, then revert and observe
green; put both the failure message and the recovery in the Log. A guard rail that has never been seen to fail
is not a guard rail.

### M8.4.7 — Optimistic-path convergence, and the two bugs not inherited
**Do:** Pin the fields the app copies back out of each response, and make sure the two latent direct-mode
optimistic bugs are not inherited (`§6`):
- **Unconditional stamping in the synthetic reply** (`state/threads.ts:161`) — `syntheticReply` always calls
  `prefixBody(session.human, body)`, so the optimistic comment briefly renders `**Name** (role)` as literal
  body text before the unstamped server comment swaps in.
- **Empty-login attribution on resolve** (`state/threads.ts:292`) — the optimistic flip sets
  `resolvedBy: { login: session.brokerLogin }`, which is `''` in direct mode.

The sink's own obligation is to return values that make the optimistic path converge on the truth. **The
app-side half is M8.7's** (unit M8.7.9): both fixes live in `packages/app/src/state/threads.ts`, this ticket's
Surface is `revu:revud`, and today's unconditional stamp is wrong in *direct* mode too — so it is a small real
behavior change to existing PR reviews, not a local-only branch, and it belongs with the app tickets. This
unit owns only the response-shape contract that makes convergence possible.

**Files:** `packages/revud/src/direct/local-writes.test.ts`.
**Check:** A test asserts, field by field, that each verb's return value is fully populated for exactly the
fields the app reads back: reply → `id` (positive), `in_reply_to_id`, `path`, `line`, `side`, `subject_type`,
`reactions`, `user`, `body`; resolve → `isResolved`, `isOutdated`, `resolvedBy`; react → the whole
`ReactionRollup`; submit → a `ReviewSummary` with a positive `id`, a mapped `state`, and a `commit_id` equal
to the guarded head. Assert no returned `body` is prefix-stamped by round-tripping it through
`parsePrefixedBody` and expecting `null`.

### M8.4.8 — The in-memory local harness (lands before M8.4.3, ahead of its number)
**Do:** One shared test-support module so `.3`/`.4`/`.5`/`.9` are written against a single fake and a new port
member has exactly one place to appear. It exports `createFakeLocalStore()` — a `Map`-backed implementation of
the six store-slice methods, with a `throwOn` option naming a method that rejects, which is what drives the
persist-failure cases, and read-backs for what was written (including the submitted-summary list M8.4.3's
Check (g) reads) — plus `countingEntityIds()` (a strictly-increasing allocator fake starting at
`LOCAL_ENTITY_ID_BASE`) and `fixedHead(sha, commitCount)`. Modeled on `github-write-stubs.ts`: test-support
code, imported only by `*.test.ts`, never reachable from a live daemon. It constructs **no** GitHub client,
because the port has nowhere to put one. Real-store durability belongs to M8.2; this fake is what keeps M8.4
completable against M8.1 alone.

**Reads must return copies.** `getLocalSnapshot` returns a structural clone, never the stored object — the
whole ticket's persistence assertions ("the snapshot read *after* submit already contains them") are vacuous
if the sink mutated an object the fake still holds by reference.

**Files:** `packages/revud/src/direct/local-write-fakes.ts` (new),
`packages/revud/src/direct/local-writes.test.ts`.
**Check:** A `describe('the harness cannot make a persistence assertion pass by aliasing')` in
`local-writes.test.ts`: (a) two successive `getLocalSnapshot(id)` calls return objects that are deep-equal but
**not reference-equal**, and mutating the first does not change the second; (b) a store built with
`throwOn: 'putLocalThread'` actually rejects when that method is called. Both assertions are the reason
M8.4.3's Check (a) and (c) mean anything, and both run on every gate.

### M8.4.9 — The full local loop, and the draft-survival matrix
**Do:** Own the two acceptance behaviors no per-verb unit covers: the four verbs composing over one persisted
local review, and the draft surviving every non-success outcome. Both are stated in `Verify` and neither had a
unit; they land here as tests, not as a manual pass.

**Files:** `packages/revud/src/direct/local-writes.test.ts`.
**Check:** Two `describe`s in `local-writes.test.ts`, over the M8.4.8 harness:
- **The loop.** submit → reply → resolve → react in one test, then assert on the persisted state read back
  through the fake: exactly 1 review summary, N threads, every comment id `> 0` and `>= LOCAL_ENTITY_ID_BASE`
  and pairwise distinct, `commentAuthors` carrying an entry for **every** one of those ids keyed to
  `session.human.id`, and `getLocalDraft` returning `null`.
- **Draft survival, parameterized.** A `test.each` over the four non-success outcomes — head-moved, unknown
  thread on reply, unknown comment on react, and `throwOn: 'putLocalThread'` during submit — asserting after
  each that `getLocalDraft` is non-null and **byte-identical** to the seeded draft
  (`JSON.stringify(after) === JSON.stringify(seeded)`), so a silently rewritten draft fails as loudly as a
  deleted one. **Negative control:** move submit's `deleteLocalDraft` call above the thread writes and observe
  the head-moved and persist-failure cases go red; then have the harness's `deleteLocalDraft` fire on any verb
  and observe the reply and react cases go red too. Revert both, observe green, record all four in the Log.

## Verify

The gate (`bun run check`) is assumed, and every item below is an assertion inside it — nothing here is a
one-time observation except the explicitly-labelled negative controls (M8.4.1, M8.4.2 (b), M8.4.6, M8.4.9),
whose *durable* halves are the tests they prove can fail.

1. `bun test packages/revud/src/direct/local-writes.test.ts packages/revud/src/direct/local-ids.test.ts packages/revud/src/direct/local-write-isolation.test.ts` — green. Every unit's Check lives in one of these three files.
2. **The D7 guard rail is demonstrated red, and cannot go vacuous afterwards.** Temporarily add a
   `github-client` import to `local-writes.ts`, run the isolation test, confirm it fails and names the
   specifier, revert. Record the observed failure message in the Log entry. The standing protection against
   that scan silently degrading is M8.4.6 assertion 4 (fixed file set, non-empty reads, extractor self-test),
   which runs on every gate.
3. **A full local loop against fakes, with no GitHub client constructible** — owned by M8.4.9. One test drives
   submit → reply → resolve → react over the M8.4.8 harness and asserts the persisted end state: 1 review
   summary, N threads, every comment id positive, `>= LOCAL_ENTITY_ID_BASE` and distinct, `commentAuthors`
   covering every one of them, and no draft. Unlike every existing write test (`writes.test.ts:39` spreads
   `throwingGithubClient()`), this test constructs **no** GitHub client at all — the deps type has nowhere to
   put one, M8.4.1's key-set assertion pins that, and M8.4.6 scans the test file itself for a smuggled import.
4. **Draft survival across every non-success outcome** — owned by M8.4.9. Parameterized: head-moved, unknown
   thread on reply, unknown comment on react, and a harness that throws on the thread write; after each the
   draft is still readable and byte-identical, with the two negative controls M8.4.9 names.
5. **The port has not grown a seam.** M8.4.1's exact-key-set assertion is green, so no `github`,
   `writeDecorator` or `runner` has been added to `LocalWriteDeps` since the module was written.
6. `bunx oxlint packages/revud/src/direct/local-*.ts` clean (`noUnusedLocals`/`noUnusedParameters` are on),
   and `bunx tsc -b` clean. Both corroborate; neither is a Check.

## Context — verified seams

| anchor | what is there, and why it matters here |
| --- | --- |
| `packages/revud/src/direct/writes.ts:12` | `import type { GithubClient, ReviewCommentInput, SubmitReviewBody } from './github-client'` — the exact import the D7 test bans from the local module. |
| `packages/revud/src/direct/writes.ts:45-54` | `WriteDeps`, with `github: GithubClient` at :46 and `writeDecorator: WriteDecorator` at :52. `LocalWriteDeps` is this shape minus both. |
| `packages/revud/src/direct/writes.ts:57-61` | `EVENT_STATE` — `COMMENT→COMMENTED`, `APPROVE→APPROVED`, `REQUEST_CHANGES→CHANGES_REQUESTED`. Reusable verbatim; verdicts stay meaningful locally. |
| `packages/revud/src/direct/writes.ts:308-447` | `submitReview` end to end — the five phases the local sink reduces to three. Read the docstring at :288-307 for the phase contract. |
| `packages/revud/src/direct/writes.ts:318-331` | The head guard and `head_moved` as a returned value, with `newCommits: Math.max(0, currentCommits - priorCommits)` and the comment stating reconcile refines it. |
| `packages/revud/src/direct/writes.ts:335-345` | The approve gate. Dropped locally (`canApprove` true, `§6`) — the `forbidden` variant survives in the type but is unreachable. |
| `packages/revud/src/direct/writes.ts:405-414` | The 422 → `conflict` path, and the "NEVER discard the draft here" comment. Unreachable locally; the draft discipline it protects is not. |
| `packages/revud/src/direct/writes.ts:438-446` | `recordWrite` then `deleteDraft` — "Deleting only here is the whole invariant — nothing above this line touches the draft." The local sink keeps the ordering and drops the journal. |
| `packages/revud/src/direct/writes.ts:465-493` | `replyToThread` — thread read from the cached snapshot, `not_found` when absent or comment-less, reply addressed to the thread's first comment. |
| `packages/revud/src/direct/writes.ts:513-529` | `resolveThread` — returns the whole normalized thread; the local version returns the whole *stored* thread. |
| `packages/revud/src/direct/writes.ts:553-577` | `addReaction` — the issue-vs-review classification that collapses locally, and the read-back-the-rollup discipline. |
| `packages/revud/src/direct/threads.ts:180-202` | `normalizeReviewThread` — the authoritative field list a locally minted `ReviewThread` must fill (`isOutdated`, `startDiffSide`, `subjectType`, `resolvedBy`, …). |
| `packages/revud/src/direct/threads.ts:33-46` | `zeroReactions()` — the exact empty `ReactionRollup` a fresh local comment carries. |
| `packages/revud/src/direct/write-decorator.ts:70-84` | `createDirectWriteDecorator` — the passthrough whose `decorateBody` returns the body verbatim and whose `recordWrite` discards. Both halves are inert locally, which is why the seam is omitted rather than injected. |
| `packages/revud/src/direct/store.ts:192-195` | `getDraft` / `putDraft` / `deleteDraft` on `DirectStore` — the `(humanId, prNumber)` shape the local port mirrors as `(humanId, localId)`. |
| `packages/revud/src/direct/store.ts:462-466` | `deleteDraft` implementation, wrapped in `write('drafts', …)` so a failed delete surfaces rather than silently succeeding. The local port slice must preserve that. |
| `packages/revud/src/direct/direct-api.ts:202-210` | The `writeDeps` bundle assembled once at api construction — where M8.5 assembles the local sibling bundle. |
| `packages/revud/src/direct/direct-api.ts:315-336` | The four write methods delegating to the write core — the exact dispatch point M8.5 branches on `isLocalReviewId(n)`. Do not touch it in this ticket. |
| `packages/revud/src/direct/command-runner.ts:24-33` | `CommandRunner.run(args, opts)` — argv array, no shell. The seam a head resolver is built on; the sink takes the resolved function, not the runner. |
| `packages/revud/src/direct/github-write-stubs.ts:50-72` | `throwingGithubClient()` — how every write test today proves nothing unexpected reached GitHub. The local tests need none, and that absence is the point. |
| `packages/app/src/state/threads.ts:120-124` | `syntheticSeq = -1` — negatives are reserved for optimistic ids, which is why locally minted ids must be positive (`§8` edge case 17). |
| `packages/app/src/state/threads.ts:136-167` | `syntheticReply`, with `body: prefixBody(session.human, body)` unconditional at :161 — latent bug 1. |
| `packages/app/src/state/threads.ts:235-261` | Reply `onSuccess` — swaps the synthetic by id, then re-keys `commentAuthors` onto `comment.id` from the response. A duplicate or non-positive returned id orphans the entry. |
| `packages/app/src/state/threads.ts:288-293` | Resolve `onMutate` — `resolvedBy: { login: session.brokerLogin }`, `''` in direct mode — latent bug 2. |
| `packages/app/src/state/threads.ts:303-318` | Resolve `onSuccess` copies `isResolved`, `isOutdated` and `resolvedBy` straight out of the response. A partially built returned thread silently rewrites cached state. |
| `packages/app/src/state/threads.ts:330-336` and `:362-372` | Reaction `onSuccess` overwrites the cached rollup with the server rollup and documents that an unchanged rollup reconciles silently — so the local dedup is already handled. |
| `packages/app/src/state/drafts.ts:268-285` | `useSubmitReview` — only `status === 'ok'` drops persist state, clears the draft and invalidates the snapshot. This is the machinery that breaks if submit returns `ok` with nothing materialized. |
| `packages/shared/src/lib/identity.ts:192-209` | `isOwnComment`'s three signals. With no stamping and `botLogin === ''`, signals 2 and 3 cannot identify a local comment — `commentAuthors` (signal 1) is the only one that works. |
| `packages/shared/src/lib/identity.ts:127-129` | `prefixBody` — the function the local sink must never call. |
| `packages/shared/src/api/types.ts:357-373` | `SnapshotMutable.commentAuthors` — comment id → `Human.id`, with the explicit note that values are "never rendered into a comment body". |
| `packages/shared/src/api/types.ts:495-498` | The frozen `SubmitResult` union — `ok` / `head_moved` / `forbidden`. Unchanged by this ticket. |
| `packages/shared/src/http.ts:123-139` | `fillPath` URL-encodes path params; `direct-router.ts:139` `decodeURIComponent`s them. Thread ids are opaque to both, so a `local:`-shaped id round-trips. |
| `packages/app/src/api/mock/adapter.ts:489-596` | The mock's `submitReview` — the oracle (D6) for thread shape, id minting, `commentAuthors` population, and `deleteDraft` ordering. Match it. |
| `packages/app/src/api/mock/adapter.ts:358-416` | The mock's `replyToThread` — the field-by-field derivation the local reply mirrors (minus `prefixBody` at :396). |
| `packages/app/src/api/mock/adapter.ts:465-487` | The mock's `addReaction` dedup: "once that emoji is on the comment, another human's identical reaction changes nothing". |
| `packages/app/src/api/mock/store.ts:37` | `const ID_BASE = 700_000_000` — the mock band `LOCAL_ENTITY_ID_BASE` must stay disjoint from. |
| `packages/revud/src/collector/audit-integrity.test.ts:109-125` | The precedent for a structural source-scan test, including the "match the statement form so a doc comment naming the banned construct does not trip the scan" technique. |
| `packages/revud/src/broker/token-custody.test.ts:472-491` | The precedent for scanning module source for banned vocabulary as a custody guarantee. |

## Constraints that bind this ticket

- **`RevuApi` and the wire types are frozen** (hard-constraints). All four verbs keep their exact signatures;
  `SubmitResult` keeps all three variants even though `forbidden` becomes unreachable locally. Nothing in this
  ticket edits `packages/shared/src/api/client.ts` or `types.ts` — AGENTS.md lists both as read-only inputs.
- **`submitReview` returns `head_moved` as a 200-level VALUE, never an error** (hard-constraints; `§6`
  restates it and notes it matters more locally, because an amend or rebase moves head with no PR event to
  notice it).
- **Drafts survive everything; a draft is deleted only on confirmed submit success** (hard-constraints;
  `§8` edge case 19). Locally that success is trivially reachable, which is exactly why the ordering must be
  coded explicitly rather than assumed.
- **D7 — the local write path has no GitHub client in scope, structurally** (`§3`, `§8` edge case 24, and M8's
  exit criterion "enforced structurally … and asserted by a test that fails if one is introduced").
- **D4 / edge case 25 — no local write is journaled to `audit_log`**, and no synthetic id ever enters
  `audit_log.pr`. That is why the `WriteDecorator` is omitted rather than injected: an injected decorator
  could later be swapped for the broker one and start journaling local ids into the collector's view.
- **Email is a key, never a body** (hard-constraints; `§8` edge case 26). The synthesized `GhUser` carries the
  git-config **name**; `Human.id` (the lowercased email, `identity.ts:16-18`) appears only in
  `commentAuthors`, which the type at `types.ts:370-372` states is never rendered.
- **Locally minted ids are positive and band-disjoint** (`§8` edge case 17) — negatives are reserved for the
  app's optimistic synthetics.
- **Code style (AGENTS.md):** comments and docstrings are self-contained. Nothing in this module may reference
  M8.4, this board, a PR number, or any tracking artifact. Complete output only — no stubs, no TODOs.
- **TDD (hard-constraints):** every unit lands with its tests; `bun run check` green before the PR.

**Four questions this ticket used to carry are settled elsewhere; do not re-open them.**

- **The local `ReviewSummary` does not enter `snapshot.mutable.reviews`** — M8.1's decision D-b. It is
  persisted through `putLocalReviewSummary` and read by nothing on the render path, which is what keeps §4.1's
  and §7's "issue comments and submitted reviews are always empty" true after the first submit and keeps
  M8.7.4's threads-only Conversation tab honest. M8.4.3 writes it that way and M8.4.3's Check (g) pins it.
- **Thread id shape is M8.1's** — its open question 8. M8.1.3's mock mints the shape and M8.4.2 conforms; the
  earlier framing here ("implementer call, then mirror it back into M8.1") had the order backwards, since M8.1
  gates this ticket and D6 makes the mock the spec.
- **The two app-side optimistic fixes land in M8.7** (unit M8.7.9), not here — see M8.4.7. This ticket owns
  only the response-shape contract that lets the optimistic path converge.
- **The entity-id high-water mark lives in `meta`**, seeded by M8.2.1 and allocated by M8.2.2's
  `nextLocalEntityId()`; this module takes it as the injected `nextEntityId` and persists nothing itself. See
  M8.4.2 for why a `MAX(id)` scan was rejected.

## Landmines

- **A verb that returns anything less than the complete value silently reverts the UI.** `state/threads.ts:308-313`
  copies `isResolved`/`isOutdated`/`resolvedBy` out of the resolve response, and `:362-372` overwrites the
  cached rollup with the reaction response. A resolve that rebuilds the thread and defaults `isOutdated` to
  `false` will flip an outdated thread back to current, with no error anywhere. Return the stored object with
  only the intended fields changed.
- **Submit returning `ok` with nothing materialized is worse than an error.** `useSubmitReview`
  (`state/drafts.ts:274-283`) clears the draft and invalidates the snapshot on `ok`. If the threads were never
  written, the draft vanishes and nothing appears — exactly the "draft lost" outcome the product exists to
  prevent. Persist first, delete the draft last.
- **Without `commentAuthors`, own-comment detection fails on every local comment.** `isOwnComment`
  (`identity.ts:196-208`) tries the write log, then a `viewerLogin` match, then the smuggled name. With no
  stamping the name path returns `kind: 'github'`, and `viewerLogin` is either absent (boot relaxation) or a
  real GitHub login that never equals the sentinel. Populating `commentAuthors` is load-bearing, not
  decorative.
- **A rebase can move head with an unchanged commit count**, so `newCommits` computed as a count delta is `0`
  while `head_moved` is true. That is honest, but a surface reading `newCommits === 0` as "nothing changed"
  will mislead. The under-reporting mechanism (`§8` edge case 12 — a rebase preserves author date, so the
  fallback heuristic silently under-counts) is M8.8's to fix; do not paper over it here with a fabricated
  count.
- **Do not "fix" the reaction dedup.** Returning an unchanged rollup for a repeat reaction is the mock's
  semantic (`adapter.ts:469-472`) and the app reconciles it silently. `§6`'s warning is about a verb that
  *always* returns the old value, not about honest idempotence.
- **Do not inline `git rev-parse` here.** A ref arriving over HTTP and beginning with `-` is read by git as a
  flag (`§8` edge case 7), and nothing validates command arguments today. Head resolution is injected;
  hardening belongs to M8.3's resolver.
- **`conflict` becomes unreachable but the type stays** (`§6`). Deleting the variant would touch the frozen
  contract and break the mock's conformance leg.
- **The shared mock store leaks across test files** (memories/known-landmines.md). Any test in this ticket that
  drives `createMockApi`/`mockDev` must `mockDev.reset()` in `beforeAll`. Prefer an in-memory store fake and
  avoid the mock entirely in the unit tests — only the M8.11 conformance leg needs it.
- **A whole-file `not.toMatch(/github/i)` scan will fail on its own docstring.** Scan import specifiers and
  walk the import graph; `audit-integrity.test.ts:116-125` is the precedent for keeping the ban precise enough
  that documentation does not trip it.
- **`store.deleteDraft` throws on a failed write** (`store.ts:462-466`). If the local port's delete is the last
  step and it throws after the threads landed, the caller sees a rejection for a submit that actually
  succeeded — and a retry re-materializes duplicate threads (there is no idempotency re-check locally). Decide
  and document the ordering: the safest is threads → summary → snapshot → draft delete, with a delete failure
  surfaced as `persist_failed` and the resubmit hazard noted, mirroring the accepted duplicate-on-retry window
  documented at `writes.ts:457-463`.

## Testing exceptions

One assertion this ticket genuinely cannot make from `packages/revud`, recorded rather than faked:

- **M8.4.2 (c) — thread-id shape *equality* with the mock.** The sink and M8.1.3's mock must mint the same
  shape (D6 makes the mock the spec), but this package cannot compare them directly: `@revu/app` exports only
  `./mock` (`packages/app/package.json`), which is the whole `createMockApi` surface, and importing it drags
  in the process-wide `localStorage` shim every driving test must `mockDev.reset()` (Landmines) — an
  order-dependent dependency taken on for one string comparison. **Compensating assertions:** M8.4.2 (c) pins
  the shape locally with a predicate plus a golden id copied from the mock's own test, so a *unilateral*
  change on either side fails; and M8.11's conformance leg runs the same local-review suite over the mock and
  the direct transport, so a divergence fails there. Not covered: a change made to both the mock and the
  golden fixture in one commit — a deliberate contract move, which is the pre-merge review's job.

## Open questions

1. **What does `resolvedBy.login` carry locally?** The sentinel `GhUser.login` is the git-config name (`§4.1`),
   which reads oddly as a "login". `§6` only says resolve must "set `resolvedBy`". Using the same sentinel
   login everywhere is consistent; using the display name directly is more readable but diverges from the
   `{ login: string }` field's meaning elsewhere. **Implementer call, documented in the module.**

## Log

_Append `**YYYY-MM-DD** — <what changed, files, gate result>` entries as work lands. Newest last._

**2026-08-17 — M8.4.1 the module and its port. LANDED.**
New: `local-writes.ts`, `local-writes.test.ts`. Nothing else touched. Gate in the main tree:
**1656 pass · 1 skip · 9 fail · 85 files** (baseline 1639 · 1 · 11 · 84); `bunx tsc -b` and
`bunx oxlint --deny-warnings` clean; the app build runs clean separately (the gate short-circuits at the
designed red before reaching it). Test-first red: the test file ran before the module existed
(`Cannot find module './local-writes'`). **The isolation guard dropped 11 → 9**, and the two that greened are
exactly the closure walks from the two files created — **the maximum possible drop**, since all 9 remaining
failures name `local-ids.ts` or `local-write-fakes.ts`, which later units create.

### ⚠️ A cross-ticket contract mismatch, found by building against it

**The port's `putLocalReviewSummary` does not match the v4 store's `putLocalSubmittedReview`**, so the claim
that "M8.2's v4 tables satisfy this slice structurally" was **false**. Corrected in this ticket's M8.4.1 section:
the port keeps its own name and **M8.5 wires with an explicit one-line mapping, never a spread**. A spread that
misses the member is a compile error, so the mapping cannot be silently skipped. This is exactly the class of
defect a decoupled port is supposed to make visible, and it was invisible to every test in both tickets because
nothing has wired them together yet.

### How four "stub-free" verbs landed — a deviation, stated plainly

Signatures are final; the four bodies **refuse**. Each calls one module-private refusal that names the verb,
echoes the request, and reports — by genuinely calling `getLocalDraft` — that nothing was materialized and no
draft was deleted. The reasoning, and it is worth keeping because the alternatives were all worse:

- Every return type is a concrete value, so the only terminal expressions available are **a fabricated value** or
  **a throw**. A fabricated value is precisely this ticket's own Landmine — `useSubmitReview` clears the draft on
  `ok`, so a submit that returns `ok` with nothing materialized loses the draft and shows nothing.
- `noUnusedParameters` is on, and it was verified empirically that TS flags **every** unused parameter regardless
  of position (TS6133, no trailing exemption). A bare throw would therefore force `_`-prefixed parameters — a
  visible placeholder marker. The refusal instead reads every parameter for real diagnostic content.
- **A throw satisfies this ticket's central invariant rather than deferring it**: a thrown verb has provably not
  reached `deleteLocalDraft`. That is asserted, not assumed — each verb rejects naming itself (so a fabricated
  success is red) and each leaves the draft **byte-identical** by `JSON.stringify` comparison. That is the first
  member of M8.4.9's draft-survival matrix, landed at the earliest possible point.
- `Error`, not `ApiError`, deliberately: no error code means "this verb has no behaviour", and mapping it onto
  `unprocessable` or `persist_failed` would put a false product-level explanation in front of a user. Nothing
  dispatches here yet, so it is unreachable in practice.

**The honest judgement, as the implementer stated it: by a strict reading of "no stubs", a refusing body is a
stub.** The stub-free alternatives were to omit the four functions (fails the Do and leaves later units nothing
to fill) or to implement M8.4.3/.4/.5 inside this unit (merges four units into one). **The forcing function that
stops the refusal from shipping is M8.4.9's full-loop test**, which drives submit → reply → resolve → react and
cannot pass against a refusal. M8.4.3/.4/.5 must *replace* the refusal and delete the two rejection assertions
deliberately.

### The `Object.keys` gap — found, proved invisible to all three guards, then closed

This is the "required prop passed wrong" shape from the memories, in a new costume, and it is worth reading:

- **A REMOVED key: both halves proved separately.** Removed `deleteLocalDraft` from the **interface only** →
  `tsc` errors TS2561 but **the key-set test stays green**; the compiler is the guard for that shape, not the
  assertion. Removed from **both** interface and factory → `tsc` is **clean** and the key-set assertion goes red
  naming `- "deleteLocalDraft"`. So a coordinated removal — the shape that makes the draft-survival guarantee
  unenforceable — is caught **only** by the assertion, which is why it is a written-out literal and not derived.
- **An OPTIONAL interface member never set by the factory is invisible to everything.** Adding
  `probeOptional?: {…}` left `tsc` clean, the key-set assertion green, **and the isolation guard unchanged**
  (typed inline, naming no module, so the specifier scan had nothing to see). Making the same member *required*
  and unset is a compile error (TS2741). **Required makes a missing member a compile error and an optional one
  nothing at all.**
- **Closed with a type-level bidirectional check**: a written-out `PORT_MEMBERS` tuple plus a mutual-assignability
  assertion between `keyof LocalWriteDeps` and the tuple's union, so the **compiler** compares the interface's own
  key union against the list. That is the only way an assertion can see a member the runtime never materializes.
  Falsified three ways: optional member unlisted → TS2322; a listed name the port does not declare → TS2322;
  listed and declared but unset by the factory → runtime red naming it. **Residual, recorded in the module
  docstring:** a coordinated edit of the member list, the factory and the key-set literal in one change — a
  deliberate port move, which is the pre-merge review's job.

**The three named-seam absence tests were measured, not trusted:** injecting `github`, `writeDecorator` and
`runner` at once turned all five relevant assertions red **independently** (key set, member-list pin, and three
one-absence-per-test bans). The specified negative control also confirmed the isolation guard bites: adding a
GitHub client took it 9 → 11 fail, with the closure walks from **both** `local-writes.ts` and
`local-writes.test.ts` newly red — and constructing `throwingGithubClient()` for the control required importing
`./github-write-stubs`, which the allowlist forbids and the guard correctly flagged. Reverted cleanly, verified
by grep and by the guard returning to 9.

### Three port-shape decisions, documented in the module rather than left implicit

1. **`getLocalDraft` returns the real `ReviewDraft`, not a narrowed record.** A narrowed static type was tried
   and reverted: the draft-survival matrix compares whole drafts byte-identically, and narrowing would force
   casts in the later units.
2. **There is no draft *writer* on the port, deliberately.** A local write may read and delete a draft, never
   rewrite one — the narrowest surface that can honour delete-only-on-confirmed-success, and one that cannot
   silently mangle a draft either.
3. **The store fake in this file is minimal on purpose and says so**: reads answer from a Map seeded with one
   draft, `getLocalSnapshot` answers `null` (the honest state of an unsynced local review), and writers accept
   and discard, because nothing here materializes and **a recorder no assertion reads would be a fixture
   pretending to be a check**. The shared clone-returning harness is M8.4.8's.

**No specifier was needed that the allowlist forbids** — the module imports only `@revu/shared`; the test adds
`bun:test` and the module under test.

**2026-08-17 — M8.4.6 the D7 structural test. LANDED, DELIBERATELY RED.**
`packages/revud/src/direct/local-write-isolation.test.ts` (new, the only file). Gate in the main tree:
**1639 pass · 1 skip · 11 fail · 84 files**; `bunx tsc -b` and `bunx oxlint` clean. **The 11 failures are this
unit's designed red** and are confined to this one file — 4 in `the local write modules contain no network or
subprocess vocabulary`, 5 in `the local write path imports no GitHub client`, 2 in `the scanners cannot pass by
reading nothing`. All four scanned files (`local-writes.ts`, `local-writes.test.ts`, `local-ids.ts`,
`local-write-fakes.ts`) are created by M8.4.1 / M8.4.2 / M8.4.8; **this file goes green at M8.4.2** and was
verified to do so (see the control below). 28 assertions pass today — the whole of assertion 4's second half
plus the scope pin, which is what stops the guard from being merely aspirational.

**Anyone who "fixes" this red by making the existence assertion conditional, skipping when a file is missing,
or deriving the expected set from whichever files happen to exist, has destroyed the guard while leaving it
green.** That last variant is specifically the "two quantities that move together" shape.

**The negative control was run in four phases and all four observed.** The module under test does not exist
yet, so the four files were created as minimal placeholders, the scan run (**39 pass · 0 fail** — proof the file
*can* go green, without which its red is indistinguishable from a guard that can never pass), then broken four
ways, then every placeholder deleted. No stub left behind — this test file is the only `local-*` file in
`direct/`.
1. The specified control (`import type { GithubClient } from './github-client'` in `local-writes.ts`) → red
   naming the specifier twice: `local-writes.ts imports './github-client' — banned: ./github-client` and
   `— banned: a module whose basename contains "github"`, plus the closure test naming the resolved path.
2. **Closure isolated from the specifier scan:** an *unlisted* helper `local-write-bridge.ts` imported the
   client and `local-writes.ts` imported only the helper. The specifier scan stayed **green** (the helper is
   not in the pinned set and its name is not banned) and exactly **one** test went red — the depth walk. That
   is the proof assertion 2 catches what assertion 1 structurally cannot.
3. Egress: all four terms planted in the real module → **4 independent** fails, one per term.
4. The added vocabulary ban (below) → red on `node:child_process`.

**Both ban lists were measured rather than trusted, and one member is a dead discriminator.**
`./github-client` is **fully subsumed by the basename rule** — any specifier resolving to that sibling also has
`github` in its basename, so it can never be the unique cause of a failure. It is kept, with the subsumption
**pinned by a test** asserting the pair fires together and a comment saying it is known rather than assumed;
that way it cannot rot into a false sense of independent coverage. The other three members are each isolable.
The suspected second redundancy is **not** one: `https://` is not implied by `api.github.com` — bare
`api.github.com` trips only the host ban, `https://example.test/v1` only the scheme ban, and all four egress
members are independently falsifiable with a fixture in which only that member appears.

**One assertion added beyond the four specified, kept as a deliberate strengthening.** A ninth specifier rule,
expressed as an **allowlist** (`@revu/shared`, `@revu/shared/*`, `bun:test`) rather than another ban. The four
specified members are all *name-based*, so they let through the two evasions that matter most here: a client
published under a package name containing neither `github` nor a host (`@octokit/rest`), and a builtin that can
open a socket or spawn a process (`node:https`, `node:child_process`). The second is the important one — this
ticket's port deliberately carries **no `CommandRunner`** so the module spawns nothing, and assertion 3's
`Bun.spawn` text ban misses `node:child_process` entirely, so the decision was unguarded. Proven to fire on all
of those plus `@revu/app/mock`, whose process-global `localStorage` shim this ticket's Landmines forbid these
tests from touching. It is additive and deletable without weakening anything specified.

**Three limits recorded rather than papered over.** (1) The guard file is **not** in its own scanned set and
cannot be — it must contain `api.github.com`, `https://`, `Bun.spawn` and `fetch(` as fixture literals, and its
own imports are outside the allowlist; it constructs no client and only reads sources. (2) Egress is scoped to
the three modules, **not** `local-writes.test.ts`, because a test fixture may legitimately hold a URL-shaped
string inside a stored document; the specifier scan and the closure still cover all four files. (3) The
**dynamic-import form cannot be line-anchored**, so a comment containing a quoted `import('./x')` reads as an
import — it fails loud, never silent, and the two statement forms *are* line-anchored, which is what keeps the
write module's own docstring explaining the absence from tripping the scan.

**2026-08-17 — M8.4.8 the in-memory local harness. LANDED.**
New: `local-write-fakes.ts` (`createFakeLocalStore`, `countingEntityIds`, `fixedHead`, plus seed builders).
Extended: `local-writes.test.ts`. Gate in the main tree: **1664 pass · 1 skip · 8 fail · 85 files** (baseline
1656 · 1 · 9 · 85); `bunx tsc -b` and `bunx oxlint --deny-warnings` clean; the app build clean when run
separately. **The isolation guard dropped 9 to 8** — the one that flipped is the depth walk from
`local-write-fakes.ts`; all 8 remaining name `local-ids.ts`, which a later unit creates.

**The aliasing falsification did exactly what the unit exists for**, and the shallow-copy probe proved the
assertion pair is not redundant: with `structuredClone` swapped for a top-level shallow copy, the
**reference-inequality check stayed GREEN** while **four** cases went red — a comment body at depth 4, the
all-depths comparison including `immutable.blobIndex[path].head`, the write-side handle case, and the summary
read-back. **A "not the same object" assertion alone is satisfied by a shallow copy that still aliases every
nested thread and comment — which is exactly where the later units mutate.** Clones are applied on read **and**
on write. Full aliasing falsification: 3 red, reverted, 22 pass. The `throwOn`-ignored falsification: 1 red,
with a paired positive control (a store naming no failure accepts and records the same write) so the red cannot
come from a method that simply always throws.

**`makeLocalDeps()` was rewired through the harness, member-by-member and deliberately NOT spread** — the
harness's read-backs are not port members and a spread would change the pinned key set. All five pre-existing
assertions still pass: the key-set literal, the type-level mutual-assignability check, the four
rejects-naming-itself assertions, and the four draft-byte-identical assertions — **the last of which is now
stronger**, because drafts are handed out as copies too, so a by-reference draft can no longer be mutated
invisibly.

**Seeding surface, designed now so three later units do not bolt one on:**
`createFakeLocalStore({ snapshots, drafts, throwOn })` seeds **from the documents themselves**, so no key can
disagree with its document; seeding bypasses `throwOn`, so a persist-failure case can start from real state;
and `throwOn` is typed `keyof LocalWriteStore`, so a misspelt method is a **compile error rather than a dead
injection**. Threads are stored as rows keyed by id and recomposed into the snapshot a read returns, so
single-thread and whole-envelope writes agree and neither can double-store. Read-backs
`listLocalSubmittedReviews(localId)` and `listLocalThreads(localId)` expose what no port read does.
`reviews`/`issueComments` are deliberately **un-seedable** — a local review never has them.

### A committed source file contained a raw NUL byte, and it would have blinded the pre-merge review

`local-writes.test.ts` as landed by M8.4.1 carried a **literal NUL character** inside a draft-key template
literal, at byte offset 3371 — it renders as a space in an editor. **Verified independently in the main tree
before integrating**, by byte-scanning the committed file: `file(1)` reported it as `data`, not text.

Two consequences, and the second is the serious one:
1. `grep` classifies the file as **binary** and prints a "Binary file matches" line instead of the matching
   lines, so any grep-based source guard over it is blinded. (The isolation guard itself is safe — it uses
   `readFileSync`, not `grep`.)
2. **`git diff` prints "Binary files differ"**, so the file's contents are invisible in a diff. This ticket's PR
   is gated on a fable-tier adversarial review **of the full diff** — a file that is binary to git would have
   passed that review **unread**, with nobody noticing that it had not been read.

Fixed in this unit: the offending line was removed by the rewiring, and the fake spells the same separator as a
backslash-u escape sequence in source rather than as a raw byte. **Both files verified at 0 control bytes**
after integration, and `local-writes.test.ts` now reports as `Unicode text, UTF-8 text`. Note for anyone reading
this commit's diff: because the *previous* blob is binary, `git diff` needs `--text` on that file for this
commit only.

### Two limits recorded

1. **The isolation guard's scans are all-or-nothing, not per-file.** Its banned-specifier and egress scans die
   on the missing `local-ids.ts` before reaching `local-write-fakes.ts`, so **two of its four rules cannot yet
   see the new file**. Both were replicated out of band against the new files (specifiers
   `['@revu/shared', './local-writes']` and `['bun:test', '@revu/shared', './local-write-fakes',
   './local-writes']`, zero egress hits) — but that is a **one-time observation, not a standing assertion**,
   until `local-ids.ts` lands and the scans run to completion. Worth knowing that a single missing file
   suppresses the guard's report about every file that *does* exist.
2. **`zeroReactions()` in `./threads` could not be reused** — `threads.ts` imports `./github-client`, so the
   transitive walk would fire, and the helper is module-private anyway. The fake carries its own. That is the
   guard doing real work rather than an inconvenience: reaching into a module that transitively imports a GitHub
   client is exactly what D7 forbids, even from test support.

**2026-08-17 — M8.4.2 local id minting: entity ids and thread ids. LANDED. The lane is GREEN.**
New: `local-ids.ts` (`mintLocalEntityId`, `mintLocalThreadId`; imports **only** `@revu/shared`) and
`local-ids.test.ts` (48 tests). `local-writes.test.ts` untouched. Gate in the main tree:
**`bun run check` exit 0 at 1720 pass · 1 skip · 0 fail · 86 files** (baseline 1664 · 1 · 8 · 85) — **the first
exit 0 on this branch**, and the accounting is exact: +48 new tests, +8 from the guard's failures flipping green.
Both files at 0 control bytes. Test-first red: `Cannot find module './local-ids'`.

**The D7 isolation guard went 8 fail → 0**, and the worker proved that is a real read rather than a
pass-by-not-looking. Precise prior state: the specifier scan reached **no** file at all (`local-ids.ts` sorts
first and threw), and the egress scan reached only `local-writes.ts` — so the guard had been silent about
`local-write-fakes.ts` and `local-writes.test.ts` entirely. Two controls on the now-complete scans: a planted
`github-client` import in `local-write-fakes.ts` → 3 red naming it plus both closure walks; a planted
`https://` → 1 red naming it. The observed specifier sets are now standing assertions rather than the one-time
observation recorded under M8.4.8.

**The module is stateless by construction, and that is asserted behaviorally.** The allocator is called once per
mint and its answer returned unchanged; the module validates the band and refuses an out-of-band answer by name.
**Monotonicity is deliberately NOT checked inside the module — checking it would require remembering the previous
id, which is exactly the state the statelessness Check forbids.** Negative control: a module-scope `let seq` with
the minter preferring it → **7 red** (all three statelessness cases plus four allocator pass-through cases) —
**while the 1000-mint band sweep stayed GREEN**, which is precisely why that Check has to be behavioural rather
than a sweep.

**Thread-id shape read from the mock, not invented.** Golden `local:1:1`, copied verbatim from
`packages/app/src/api/mock/local.test.ts:922` — the only literal thread id in the mock's tests; everywhere else
they compute it. The mock mints it at `packages/app/src/api/mock/local.ts:127`. Predicate `/^local:\d+:\d+$/`
with **9 rejection fixtures** so the predicate is proved to discriminate, plus a binding equality so a
*unilateral* change on either side breaks. The uncovered case — a coordinated edit of both the mock and the
golden in one commit — is a deliberate contract move and is called out in the test's own docstring as the
pre-merge review's job.

**Check (a)'s six clauses: three can independently fail, three are arithmetic consequences.** Measured, with a
fixture table mapping mutant sequences to the exact clause set each violates: the `>= LOCAL_ENTITY_ID_BASE`
floor, `Number.isSafeInteger`, and strictly-increasing are each uniquely falsifiable; `> 0`, "outside the mock's
band" and "above the forge range" are **subsumed by the floor**, because the base exceeds all three bounds.
All six are kept — subsumption is a property of today's constants, not of the clauses — and **the three
inequalities are asserted as live tests, so lowering the floor turns the subsumption claim red rather than
leaving a stale comment.** The sweep also pins `ids.length === 1000` first: every clause is vacuously true over
an empty list.

### Finding: the no-slash rule is NOT implied by the round trip

`fillPath` percent-encodes and the router `decodeURIComponent`s, so **a slash-bearing thread id survives
`fillPath` → `matchRoute` intact.** The round-trip assertion therefore cannot stand in for the no-slash property,
which has to be asserted on the id itself. Both directions are now pinned: one test proves the encoded round trip
really would survive a slash, and one shows the actual hazard — a path assembled by **joining strings** matches
for a minted id and returns `null` for a slash-bearing one.

**Recorded limits:** durability across a real daemon restart belongs to the real allocator's suite (simulating it
against the counting fake would assert something about the fake); and the router-matcher reproduction was copied
verbatim from `packages/revud/src/direct-router.ts:129-145` but **could not be pinned against that source**,
because reading it needs `node:fs`, which the guard's allowlist forbids — compensated with three controls on the
reproduction, including the router's own documented example.

**2026-08-17 — guard correction (M8.4.6's file), found by M8.4.2 and applied by the orchestrator.**
`local-ids.test.ts` was **not** in the guard's pinned `LOCAL_MODULE_FILES` while `local-writes.test.ts` was — so
the new test file was unscanned, and nothing structurally prevented a future edit there from importing a client
or a builtin. That contradicted the guard's **own stated rationale**, which puts test-support files in the set
deliberately because "the local tests construct no GitHub client, and that absence is the point". Added to the
set and to the written-out self-check copy (which is deliberately independent of the constant, so the rename had
to be made twice). Deliberately **not** added to `EGRESS_SCANNED_FILES`, matching how the other test file is
handled: a test fixture may legitimately carry a URL-shaped string inside a stored document.
**Control run before accepting it:** a `github-client` import planted in `local-ids.test.ts` took the guard from
**40 pass · 0 fail to 38 pass · 2 fail**, naming
`local-ids.test.ts imports './github-client' — banned: ./github-client` and the basename ban, plus the depth walk
from that file; reverted to 40 pass with an empty diff. Gate after: **1721 pass · 1 skip · 0 fail · 86 files**,
exit 0.
